# 제출 안내 — 2026 기상·기후 AI 해커톤 본선

**마감** 8월 21일(금) 18:00 · 구글폼
**근거** 오리엔테이션 p.25 · 대회 가이드 `guide.weatherhackathon.kr/submit.html`

> 제출 후 **주요 기능 수정 불가**. 폰트 색상·크기·종류 등 마이너한 부분만 가능.
> 검증 중 **핵심 로직 수정·소스 누락 발견 시 탈락**.

---

## 1. 제출물 5종 — 무엇을 어디서 꺼내나

| # | 항목 | 제출 파일 | 위치 | 상태 |
|---|---|---|---|---|
| ① | 발표자료 | `발표자료_Weather24_신동준.pptx` · `발표자료_Weather24_신동준.pdf` | 저장소 루트 · `제출물/`에 사본 | 준비 완료 |
| ② | 구동 URL | `https://weather-24solar-terms.vercel.app` | — | 운영 중 |
| ③ | 소스코드 | `weather24_source.zip` | `제출물/` (git archive + 제외 pathspec) | 준비 완료 |
| ④ | 프롬프트 세션 | `weather24_sessions.zip` | `제출물/` (`prompt_sessions/` 압축) | 준비 완료 |
| ⑤ | 구동·배포 문서 | `README.md` | 저장소 루트 · `제출물/`에 사본 | 준비 완료 |

### ① 발표자료

- **주최측 템플릿 기반** — `기상기후AI해커톤_본선발표자료_템플릿.pptx`에 내용을 채웠다.
  원본은 `assets/deck/템플릿_원본.pptx`.
- **유지** 상단 헤더 장식 · 눈썹(평가 항목) 6개 · `●●` 소제목 12개 · 쪽번호 · 마스터·레이아웃 8종
  → **18개 도형이 문구·좌표까지 템플릿과 동일**(원본 재파싱 대조). 심사위원이 배점표와 연결해 듣도록 고정.
- **변경** 본문 박스 내부를 '짧은 개조식 + 그 주장을 증명하는 그림' 구조로 재구성했다
  (템플릿 8번 규칙이 디자인 자유 변경을 허용). 상세 근거는 발표자 노트로 내렸다.
- 형식 · **PPTX + PDF 둘 다 제출** · PDF **9장**, 16:9
- 구성 · 표지 / I 결과물·팀 기본정보 / 1 학습 대상 / 2 문제 정의 / 3 아이디어 / 4 체험·참여형 설계 /
  5 학습 성과와 피드백 / 6 구현 완성도 / 마무리
- 템플릿 지시대로 **가이드라인 3장(2·3·4쪽)과 별지1(12쪽)은 삭제**했다(13장 → 9장).
- 재생성 · `python scripts/build_deck.py` → PPTX. PDF 는 PowerPoint 에서 내보낸다
  (`SaveAs(..., 32)`). 한글 폰트 임베드 확인은 게이트가 한다.
- 문장은 **개조식 + 명사형** — `발표자료_템플릿_자기점검.md`에 246줄 검사 결과(종결어미 0줄)를 남겼다.
  앱 화면 문구를 큰따옴표로 인용한 29줄은 증거이므로 원문 그대로 두었다.
- 시각자료 · **앱 실물 3점**(`assets/deck/*.png` — 궤도·서울 기온 곡선·전국 16지점 지도)
  \+ **네이티브 도형 12점**(단계 스트립·난이도 계단·2×2 포지셔닝·기준선 5단 표·피드백 인용 카드 6종·지표 배지).
  도형은 템플릿 색만 쓰므로 어느 PC에서 열어도 같고, 확대해도 흐려지지 않는다.
- 배포 자산 총량 **약 243KB**(gzip 실측 합계) — 게이트가 실측과 ±5% 안인지 검사한다.
- 가이드라인 원문과 항목별 자기점검: `발표자료_템플릿_가이드라인.md` · `발표자료_템플릿_자기점검.md`

### ② 구동 URL

- 로그인·결제 없이 개방
- 크롬 **1366×768 · 1920×1080 · 2560×1440** 전부 확인 — 가로 스크롤 0, 요소 넘침 0
- **시상식(9월 3주)까지 유지 필요** — 그 전까지 Vercel 프로젝트 삭제 금지

### ③ 소스코드

- `node_modules`·빌드 산출물 제외 필요(가이드 명시)
- **`git archive` 사용** — 추적 파일만 담기므로 `.gitignore` 대상이 구조적으로 빠짐

```bash
git archive --format=zip --output=제출물/weather24_source.zip HEAD \
  ':(exclude)사무국 추가 공유 자료' ':(exclude)오리엔테이션'
```

> ⚠️ **`git archive HEAD`만으로는 부족합니다.** `사무국 추가 공유 자료/`와 `오리엔테이션/`은
> **git이 추적하는 파일**이라 그대로 담깁니다(실측 10.2MB). 위 `:(exclude)` 두 개가 반드시 필요합니다.
> `.gitignore` 대상(`.env*`·`data_collectors/output/`·`__pycache__`·`.claude/`)만 구조적으로 빠집니다.
> `submit_check.sh`가 같은 제외를 적용해 검사하므로, 게이트가 통과하면 실제 제출본도 깨끗합니다.

### ④ 프롬프트 세션

- Claude Code 실제 대화 **6개 세션**. 지시·답변 건수와 크기는 매 내보내기마다 달라지므로
  **여기에 적지 않는다** — 기준은 `prompt_sessions/README.md`의 자동 생성 표 하나뿐이다.
  (5차 감사의 교훈: 같은 숫자를 두 곳에 적으면 반드시 어긋난다)
- 도구 호출 로그·내부 추론 제외, 대화 본문만 추출
- 비밀값·**이메일**·**로컬 홈 경로** `<REDACTED>` 치환 · **세션 간 중복 턴 자동 제거**
- 제출 목적과 무관한 구간은 **구간 제외**, 제3자를 식별할 수 있는 표현은 **표현 치환** — 두 단계다.
  치환이 제외보다 먼저 돌면 제외 지문이 지워져 구간이 그대로 실린다(한 번 겪었다).
  `submit_check.sh`가 결과물을 직접 검사해 그 회귀를 잡는다
- 목차(`prompt_sessions/README.md`)는 내보내기 때 **자동 생성**되어 실물과 항상 일치
- 재생성 · `python scripts/export_sessions.py`

```bash
python scripts/pack_submission.py
```

> `scripts/pack_submission.py`가 ①③④⑤를 `제출물/`에 한 번에 모으고 SHA-256을 기록합니다.

### ⑤ 구동·배포 문서

- `README.md` — 필요 환경 · 로컬 실행 · Vercel 배포 · 환경변수 표 · 데이터 재빌드
- 외부 리소스 출처·라이선스 명시(데이터·폰트·이미지·지도 경계)
- **사용 AI 모델**과 **제출용 데이터셋** 표 포함(가이드 필수)

---

## 2. 제출 직전 — 반드시 이 순서로

```bash
bash scripts/submit_check.sh
```

전부 통과해야 제출. 하나라도 실패하면 원인 해결 전 업로드 금지.

검사 항목 · 비밀값 노출 / 제출물 5종 실물 / 세션 목록과 실물 일치 / 배포 자산 응답 / **zip 시험 생성** /
회귀 게이트 / 평가 하네스 / **문서–현실 정합성**(커밋 수 · 발표자료 장수 · 세션 목차 · 이메일 0건 · 폐기한 분류 부활 여부)

---

## 3. zip 제출 시 반드시 빼야 하는 것 — 실격 방지

`.env.local`에 **실제 OPENAI_API_KEY 값** 존재. git은 추적하지 않으나
**폴더를 통째로 압축하면 그대로 포함됨.** API 키 노출은 실격(Mo2 p.17).

제외 대상

```
.env.local  .env*        실제 API 키
.git/                    히스토리 전체
.claude/                 작업 도구 설정
제출물/                   제출용 산출물(재생성 가능)
node_modules/
data_collectors/output/  원시 CSV 약 65MB — README에 재수집 절차 있음
```

`.gitignore` 대상은 `git archive`가 구조적으로 빠집니다. 압축 후 `submit_check.sh`로 재확인.

> **사무국 배포 자료는 저장소에서 제거했습니다(2026-07-29).** `사무국 추가 공유 자료/`(PDF 4건)와
> `오리엔테이션/`(화면 캡처 9건), 그리고 대회 누리집을 전사한 `기상기후_AI_해커톤_2026_정보.md`는
> 사무국이 만든 자료이므로 사무국에 되돌려 제출하지 않습니다. 총 13파일 10.7MB가 빠졌습니다.
> `pack_submission.py`와 `submit_check.sh`의 `:(exclude)` pathspec과 위험 목록은 **그대로 둡니다** —
> 누군가 다시 넣더라도 제출본에는 들어가지 않게 하는 방어선입니다.

---

## 4. 대회 1일차 19~21시 점검 시간에 할 것

주최측 PC에서 직접 확인. **유일한 리허설 기회.**

- ☐ 배포 URL이 주최측 PC 크롬에서 정상 표시
- ☐ **`↺ 처음부터` 클릭** — 공용 PC이므로 이전 팀 기록 제거
- ☐ 폰트 확인 — Pretendard 미설치 시 맑은 고딕 대체. 레이아웃은 검증 완료, 외관만 상이
- ☐ 화면 해상도에서 차트·지도 잘림 없음
- ☐ `기기 안 빠른 점검` 작동 + 외부 AI 전송 고지·동의 체크박스 + AI 점검 1회 성공
- ☐ 교실 공유망 모의 요청 40회가 네트워크 버킷에서 차단되지 않는지 확인
- ☐ 라이트·다크 중 프로젝터 가독성이 나은 쪽으로 고정
- ☐ **스톱워치로 시연 4분 실측** — 대사만 약 3분 25초. 초과 시 대본 축소

### 학생 파일럿 — 결과가 없으면 완료 표시 금지

- ☐ `학생_파일럿_검증계획.md`에 따라 중학생 5명 + 고등학생 5명 무설명 테스트
- ☐ 핵심 코스 중앙값·90백분위, CERL 4점 이상 인원, 즉시/다음 날 전이 결과 기록
- ☐ 320·360·375·390px 실제 기기 포함, 치명적 막힘 0건 확인
- ☐ 두 명 이상이 같은 곳에서 막힌 항목을 수정하고 최소 3명에게 재검증
- ☐ 결과가 없으면 발표에서 “학습 효과 입증”, “15분 완료”라고 주장하지 않음

---

## 5. 발표 시간 감점 규정

| 초과 | 감점 |
|---|---|
| 1분 | **−9점** |
| 2분 | −15점 |
| 3분 | −21점 · **발표 중지** |

목표 **9분**. 남은 1분은 사용하지 않는 것이 기본값.

---

## 6. 심사배점표 대응 요약 (100점)

| 항목 | 배점 | 이 제출물에서 대응하는 것 |
|---|---|---|
| ① 주제 적합성 | 20 | 개발계획서의 24절기·기후 개념 구현 · 첫 화면에서 주제 즉시 인식 · **기상청 공식 지표(폭염일·열대야) 실측 제공** |
| ② 체험·참여형 설계 | 25 | **조작 4축**(기준값·날짜·비교기간·**물리량**) + 선택 2축(지역·지표) · 그래프/표가 없는 독립 예측 화면 · 미션별 필수 조작 게이트 |
| ③ 학습 효과 | 10 | 대상 중3~고2 명시 · **개념 분류 관문** · 모든 미션에서 학생 CERL 네 요소 필수 · 학생 CERL→이해 확인→모범 예시 순서 · **사전/사후 동형 문항**(처서→입동) · 예측·응답·CERL이 ‘내 기록’에 남음 |
| ④ 아이디어 독창성 | 15 | 자기 결론을 스스로 반증하게 하는 26창 · 계절 지연으로 원인 분리 · **관측(조작)과 모형(계산)을 한 앱에서 잇는 열관성 실험실** |
| ⑤ 구현 완성도 | 30 | 회귀 게이트 **총 8,360건**(데이터 8,213 + 인터페이스·문서 147, 15축) · 규칙 점검 **100/100·오탐 0** · 예측/CERL/AI동의 자동 대조 · 제작 과정 AI 활용 기록 |
