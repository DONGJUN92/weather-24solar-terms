# 제출 체크리스트 — 8/21(금) 18:00 구글폼

> 제출 후에는 **주요 기능 수정이 불가**합니다(오리엔테이션 p.25).
> 폰트 색상·크기·종류 등 마이너한 부분만 가능합니다.

---

## 0. 제출 직전 30분 — 반드시 이 순서로

```bash
python processing/verify_solar_terms.py && node eval_harness.js && node --check prototype/verify.js && bash scripts/submit_check.sh
```

네 개가 모두 통과해야 제출합니다. 하나라도 실패하면 원인을 고치기 전에는 올리지 않습니다.

---

## 1. 제출물 5종

| # | 항목 | 무엇을 올리나 | 상태 |
|---|---|---|---|
| ① | 발표자료(PPT or PDF) | `발표자료_Weather24.pdf` — 8장. **주최측 PC를 쓰므로 PDF로 내보내** 폰트 깨짐을 원천 차단 | ☐ |
| ② | 구동 가능한 URL | https://weather-24solar-terms.vercel.app/ | ☐ |
| ③ | 소스코드 | 저장소 zip **또는** GitHub 링크. zip이면 §2의 제외 목록을 반드시 적용 | ☐ |
| ④ | 주요 프롬프트 세션 | `prompt_sessions/` 안의 .md 파일들을 zip. **README가 나열한 파일이 실제로 다 있는지** 확인 | ☐ |
| ⑤ | 구동환경/배포방법 | `README.md` (§2 구동 환경·배포 방법) | ☐ |

---

## 2. zip 제출 시 반드시 빼야 하는 것 — **실격 방지**

`.env.local`에는 **실제 OPENAI_API_KEY 값**이 들어 있습니다.
git은 이 파일을 추적하지 않지만(`.gitignore`), **폴더를 통째로 압축하면 그대로 들어갑니다.**
API 키 노출은 실격입니다(사전교육 Mo2 p.17).

제외할 것:

```
.env.local          ← 실제 API 키. 절대 포함 금지
.env*               ← 변형 포함
.git/               ← 히스토리 전체(용량·불필요)
.claude/            ← 작업 도구 설정
node_modules/
data_collectors/output/   ← 원시 CSV 약 65MB. README에 재수집 절차가 있음
사무국 추가 공유 자료/     ← 사무국 배포 자료. 재배포 불필요
오리엔테이션/
```

**안전한 방법 — git이 아는 파일만 압축합니다:**

```bash
git archive --format=zip --output=../weather24_submit.zip HEAD
```

`git archive`는 추적된 파일만 담으므로 `.gitignore` 대상은 구조적으로 들어가지 않습니다.
압축 후 `scripts/submit_check.sh`로 한 번 더 확인하세요.

---

## 3. 자동 검사가 보는 것

`scripts/submit_check.sh`가 다음을 확인합니다.

- 저장소와 배포본에 API 키 패턴이 없는가
- `.env.local`이 git에 추적되지 않는가
- 제출물 5종의 실물이 존재하는가
- `prompt_sessions/README.md`가 나열한 파일이 전부 실재하는가
- 배포 URL의 핵심 자산이 200을 반환하는가
- 회귀 게이트와 평가 하네스가 통과하는가

---

## 4. 대회 1일차 19~21시 점검 시간에 할 것

주최측 PC에서 직접 확인합니다. 이 시간이 유일한 리허설 기회입니다.

- ☐ 배포 URL이 주최측 PC 크롬에서 뜨는가
- ☐ **`↺ 처음부터`를 눌러 이전 기록을 지운다** (공용 PC라 앞 팀 상태가 남는다)
- ☐ 폰트 확인 — Pretendard가 없으면 맑은 고딕으로 렌더된다. 레이아웃은 검증했으나 외관이 다르다
- ☐ 화면 해상도에서 차트·지도가 잘리지 않는가
- ☐ AI 감사 1회 호출 성공 (행사장 회선 확인)
- ☐ 라이트/다크 중 프로젝터에 잘 보이는 쪽으로 고정
- ☐ **스톱워치로 시연 4분 실측** — 대사만 이미 3분 25초다. 넘치면 대본을 줄인다

---

## 5. 발표 10분 감점 규정

| 초과 | 감점 |
|---|---|
| 1분 | **−9점** |
| 2분 | −15점 |
| 3분 | −21점 · **발표 중지** |

목표는 **9분**입니다. 남은 1분은 쓰지 않는 것이 기본값입니다.
