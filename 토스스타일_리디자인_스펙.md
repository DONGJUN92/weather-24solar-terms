# Weather24 토스 스타일 리디자인 스펙

> 작성일: 2026-07-29 · 대상: 배포본 `1d7fc32` (verify.css 994줄 77KB · base.css 112줄)
> 근거: [`레드팀_5차_카피·UIUX보고서.md`](레드팀_5차_카피·UIUX보고서.md) 검증 통과 120건
> 원칙: **verify.css 전면 재작성 금지.** 토큰을 먼저 세우고, 기존 선언이 토큰을 참조하도록 바꾼다.
> 색 값은 sRGB 상대휘도·알파합성·색약 시뮬레이션을 코드로 계산해 검증한 값이다(추정값 없음).

---

## 1. 진단 — 증상이 아니라 원인

1. **토큰이 없어서 값이 늘어났다.** `--radius:20px`은 선언돼 있지만 120개 `border-radius` 선언 중 3곳(2.5%)만 쓴다. 그래서 같은 '패널' 역할에 14·15·16·20·22px이 섞였다. 크기도 같은 방식으로 **35종**, 간격은 **px 27종 + rem 22종 = 49종**(4px 격자 밖 33종)까지 벌어졌다.

2. **레이어가 두 번 생겼다.** 21:50 리팩터가 추가한 「단계형 학습 흐름 v5」 블록(verify.css:908-994)이 `rem` 기반 스케일을 새로 만들면서, 기존 `px` 레이어와 겹쳤다. `.78rem`·`.82rem`·`.86rem`·`.88rem` 같은 값이 12.48·13.12·13.76·14.08px로 렌더돼 12~14px 구간에 10종이 몰렸다 — 인접 단계 차이가 **0.02px**까지 내려간다.

3. **위계를 색과 자간으로 만들려 했다.** 강조색 7종 + 파생 7종 = 14개 색 토큰이 의미를 나눠 갖는데, 라이트 테마에서 `--coral`과 `--red`는 **대비 1.00:1**(녹색약 ΔE 0.0)로 사실상 같은 색이면서 서로 다른 뜻에 쓰인다. `.eyebrow`는 한글 16종이 들어가는데 `ui-monospace` 스택 + `letter-spacing:.13em`이다.

4. **가장 작은 글자에 가장 중요한 정보가 있다.** 과학적 무결성 문구(출처·표본 수·"30년 기후평년 아님"·비교 기간 경고)가 전부 11.5~12.5px 계층에 몰려 본문 16px의 72~78% 크기다. 앱의 핵심 주장이 시각적으로 가장 약하다.

5. **배경이 대비를 위치의 함수로 만들었다.** `.card`에 `background` 선언이 없어 `body`의 `--glow` 라디얼과 `.sky-noise` 텍스처가 본문 글자 뒤로 들어온다. 그래서 같은 토큰이 화면 위치에 따라 AA를 통과하거나 못 한다 — 검증 자체가 불가능해진다.

6. **폴백 환경을 계산에 넣지 않았다.** `font-weight` 9종(700×51 · 800×15 · 400×5 · 600×4 · 500×3 · 750×3 · 900 · 780 · 720)을 쓰지만 정적 Pretendard는 6종, 학교 공용 PC의 Malgun Gothic은 **2종**만 렌더한다. 720·750·780은 어떤 환경에서도 독립적으로 보이지 않는다.

7. **모바일에서 조작이 화면 밖이다.** 375×667 미션 조작 화면의 수직 누적이 헤더 54 + 진행 내비 **168.8** + 단계 헤더 98.5 + 제목 35 + 지금할일 **185.5** = 553.8px이라, 슬라이더(y≈675)와 차트(y≈810)가 첫 화면에 없다. 하단 고정 CTA는 하필 그 화면에서만 무효다(`.step-actions`가 `.explore-actions` 안에 중첩돼 sticky 이동 범위 ≈0).

---

## 2. 디자인 토큰 — `base.css` 교체본

`:root` 블록을 아래로 대체한다. 기존 색 토큰 이름(`--ink`·`--muted`·`--panel`·`--line`·`--sun` 등)은 **별칭으로 남겨** verify.css 994줄을 건드리지 않고도 새 값이 적용되게 한다.

```css
/* ─────────────────────────────────────────────────────────────
   Weather24 design tokens v6
   · 무채색 9단 + 강조 1색 + 상태 3색 + 차트 4계열
   · 모든 텍스트/배경 조합의 대비비는 실측 계산값(주석의 숫자)
   · 한글에 양의 letter-spacing 금지 · 본문 행간 1.6
   · 장식 그라디언트·텍스처 없음 → 대비가 위치와 무관해진다
   ───────────────────────────────────────────────────────────── */
:root, [data-theme="light"] {
  /* 무채색 */
  --g0:#ffffff; --g50:#f9fafb; --g100:#f2f4f6; --g200:#e5e8eb; --g300:#d1d6db;
  --g400:#a8b0ba; --g500:#8b95a1; --g600:#5b6673; --g700:#3f4753; --g800:#2a313b; --g900:#15181d;

  /* 표면 3층 — 불투명만. rgba 틴트를 텍스트 뒤에 두지 않는다 */
  --page:#f2f4f6;  --card:#ffffff;  --sunken:#f2f4f6;

  /* 텍스트 (카드 위 대비비) */
  --text:      var(--g900);  /* 17.79:1 */
  --text-sub:  var(--g700);  /*  9.39:1 */
  --text-weak: var(--g600);  /*  5.84:1 */
  /* --g500(3.04:1)은 라이트에서 텍스트 사용 금지 — 구분선·비활성 아이콘만 */

  /* 강조 1색 */
  --brand:#1b64da;        /* 글자·아이콘용  카드 5.41 / 페이지 4.91 */
  --brand-solid:#1550b3;  /* 채운 버튼 면   흰 글자 7.42:1 */
  --on-brand:#ffffff;
  --brand-tint:#eaf1fd;   /* 불투명 틴트. 이 위 대비: text 15.67 / text-sub 8.27 / text-weak 5.15 / brand 4.77 / c-now 4.56 */

  /* 상태 3색 */
  --ok:#0f7a3d;    /* 5.42:1 */
  --warn:#8a5300;  /* 6.33:1 */
  --err:#c02417;   /* 5.99:1 */

  /* 컨트롤·포커스 (비텍스트 3:1) */
  --line:#e5e8eb;
  --ctl-border:#767f8b;  /* 4.05:1 */
  --focus:#1b64da;       /* 5.41:1 */

  /* 차트 4계열 — 3:1 이상 + 최악 색약 ΔE 24 (기준 15) */
  --c-past:#6b7684;  /* 4.62:1 · 파선 6-4 */
  --c-now:#c2410c;   /* 5.18:1 · 실선 2.8px */
  --c-term:#2a313b;  /* 13.12:1 · 수직 파선 2-3 + 라벨 */
  --c-thr:#1b64da;   /* 5.41:1 · 실선 2.2px + ⇅ 손잡이 + 값 배지 */

  color-scheme: light;
}

[data-theme="dark"] {
  --g0:#101317; --g50:#161a1f; --g100:#1d2229; --g200:#272d36; --g300:#3a424e;
  --g400:#5b6673; --g500:#8b95a1; --g600:#aab2bd; --g700:#c8ced6; --g800:#e2e6ea; --g900:#f7f8f9;

  --page:#101317;  --card:#161a1f;  --sunken:#1d2229;

  --text:      var(--g900);  /* 16.44:1 */
  --text-sub:  var(--g700);  /* 11.03:1 */
  --text-weak: var(--g600);  /*  8.17:1 */
  /* 다크에서는 --g500도 5.75:1로 사용 가능 */

  --brand:#7cb2ff;        /* 8.05:1 */
  --brand-solid:#1550b3;  /* 흰 글자 7.42:1 */
  --on-brand:#ffffff;
  --brand-tint:#1b2537;   /* 이 위 대비: text 14.45 / text-sub 9.70 / text-weak 7.18 / brand 7.08 / c-now 6.90 */

  --ok:#5fd08a;    /* 9.05:1 */
  --warn:#f2b74e;  /* 9.70:1 */
  --err:#ff8b80;   /* 7.70:1 */

  --line:#272d36;
  --ctl-border:#6d7885;  /* 3.89:1 */
  --focus:#7cb2ff;       /* 8.05:1 */

  --c-past:#98a2ae;  /*  6.75:1 */
  --c-now:#ff9152;   /*  7.84:1 */
  --c-term:#e2e6ea;  /* 13.93:1 */
  --c-thr:#7cb2ff;   /*  8.05:1 */

  color-scheme: dark;
}

/* ── 타이포 7단. rem 기반이라 브라우저 확대 설정이 살아 있다 ── */
:root {
  --t-display:2rem;    --lh-display:1.25;  /* 32px — 랜딩 h1 */
  --t-title:1.375rem;  --lh-title:1.4;     /* 22px — 화면 h1 */
  --t-lead:1.0625rem;  --lh-lead:1.6;      /* 17px — 도입 문단 */
  --t-body:1rem;       --lh-body:1.6;      /* 16px — 본문 */
  --t-sub:0.875rem;    --lh-sub:1.6;       /* 14px — 보조 설명 */
  --t-caption:0.8125rem; --lh-caption:1.5; /* 13px — 출처·한계 (하한) */
  --t-num:1.75rem;     --lh-num:1.15;      /* 28px — 핵심 수치 */
  --t-num-s:1.25rem;   --lh-num-s:1.2;     /* 20px — 보조 수치 */

  /* 간격 7단 (4px 격자) */
  --s1:4px; --s2:8px; --s3:12px; --s4:16px; --s5:24px; --s6:32px; --s7:48px;

  /* 반경 4단 */
  --r-sm:8px;    /* 칩·배지·세그먼트 */
  --r-md:12px;   /* 버튼·입력 */
  --r-lg:16px;   /* 패널·카드 */
  --r-full:999px;

  /* 그림자 2단 — 선 대신 층으로 나눈다 */
  --sh-1:0 1px 2px rgba(0,0,0,.06), 0 1px 3px rgba(0,0,0,.04);
  --sh-2:0 4px 16px rgba(0,0,0,.08);
}
@media (max-width:560px){
  :root { --t-display:1.75rem; --t-title:1.25rem; --t-num:1.5rem; }  /* 28 / 20 / 24px */
}

/* ── 구 토큰 별칭 — verify.css 994줄을 건드리지 않고 새 값을 적용한다 ──
   전환이 끝나면 이 블록을 지우고 사용처를 새 이름으로 바꾼다. */
:root, [data-theme="light"], [data-theme="dark"] {
  --ink:var(--text); --ink2:var(--text-sub); --ink-max:var(--text);
  --muted:var(--text-weak); --muted2:var(--text-weak); --muted3:var(--text-weak);
  --panel:var(--card); --panel2:var(--sunken); --field:var(--card);
  --sun:var(--c-term); --coral:var(--c-now); --thr:var(--c-thr);
  --sky:var(--brand); --sea:var(--brand); --green:var(--ok); --red:var(--err);
  --on-sun:var(--c-term); --on-coral:var(--c-now); --on-thr:var(--c-thr);
  --on-sky:var(--brand); --on-sea:var(--brand); --on-green:var(--ok); --on-red:var(--err);
  --ink-on-accent:var(--on-brand); --on-brand:var(--on-brand);
  --radius:var(--r-lg);
  --glow:none;                       /* 라디얼 제거 */
  --shadow:var(--sh-2);
}

/* ── 기본 요소 ── */
body{
  margin:0;
  background:var(--page);            /* --glow 합성 제거 */
  color:var(--text);
  font-family:Pretendard,"Apple SD Gothic Neo","Malgun Gothic",system-ui,sans-serif;
  font-size:var(--t-body); line-height:var(--lh-body);
  letter-spacing:0;                  /* 한글에 양의 자간 금지 */
  word-break:keep-all; overflow-wrap:break-word;
  -webkit-text-size-adjust:100%;
}
.sky-noise{display:none}             /* 텍스처 제거 */
h1{font-size:var(--t-title); line-height:var(--lh-title); font-weight:700; letter-spacing:-.02em; margin:0 0 var(--s3)}
h2{font-size:var(--t-lead);  line-height:var(--lh-lead);  font-weight:700; letter-spacing:0; margin:var(--s5) 0 var(--s2)}
h3{font-size:var(--t-body);  line-height:var(--lh-body);  font-weight:700; letter-spacing:0; margin:var(--s4) 0 var(--s2)}
p{margin:0 0 var(--s3)}
small{font-size:var(--t-caption); line-height:var(--lh-caption)}   /* max(11.5px,.86em) 삭제 */
:focus-visible{outline:3px solid var(--focus); outline-offset:2px; border-radius:var(--r-sm)}
```

**깨지는 곳 3개를 같은 커밋에서 함께 고친다.**

```css
/* 1) .card에 표면을 준다 — 지금은 background 선언이 없다 (verify.css:158) */
.card{
  background:var(--card); border:0; border-radius:var(--r-lg);
  box-shadow:var(--sh-1); padding:var(--s5); max-width:760px; margin-inline:auto;
}
@media (min-width:761px){ .card{ padding:var(--s6) } }   /* 48px → 32px. 664px 콘텐츠 폭 유지 */
@media (max-width:560px){ .card{ padding:var(--s4); border-radius:var(--r-lg) } }

/* 2) h1 역전 해소 — .intro-h만 display, 나머지 화면 제목은 --t-title */
.intro-h{font-size:var(--t-display); line-height:var(--lh-display)}
.stage-h,.hero-headline,.cerl-title{font-size:var(--t-title); line-height:var(--lh-title)}

/* 3) .eyebrow — monospace·자간 제거. 들어가는 문자열 16종이 전부 한글이다 */
.eyebrow{
  margin:0 0 var(--s2); color:var(--text-weak);
  font:700 var(--t-caption)/1.5 inherit; letter-spacing:0;
}
```

`--muted`·`--muted2`·`--muted3` 3종을 `--text-weak` 하나로 모으고, `--red`를 `--err`로 통합하면 색 토큰이 **14개 → 8개**가 된다(정보 손실 0 — 라이트에서 `--coral`/`--red`는 이미 대비 1.00:1).

---

## 3. 컴포넌트 스펙

### 3-1. 버튼 — 17종 중 12종이 44px 미달이었다

```css
/* 주 CTA */
.primary-btn,.ai-btn{
  border:0; border-radius:var(--r-md);
  min-height:48px; padding:var(--s3) var(--s5);
  background:var(--brand-solid); color:var(--on-brand);   /* 그라디언트 제거 */
  font-size:var(--t-body); font-weight:700; line-height:1.35;
  box-shadow:none; transition:background .15s;
}
.primary-btn:hover{background:var(--brand)}
/* is-muted는 disabled가 아니다 — 클릭하면 힌트가 플래시한다. 따라서 본문 4.5:1을 지켜야 한다.
   text-weak(g600) on g300은 3.99:1로 미달이므로 text-sub(g700)를 쓴다 → 6.41:1 (라이트·다크 동일). */
.primary-btn.is-muted{
  background:var(--g300); color:var(--text-sub);
  border:1px solid var(--ctl-border);   /* 면 대비가 1.46:1이라 경계선으로 형태를 세운다 */
}

/* 보조 */
.ghost-btn{
  min-height:48px; padding:var(--s3) var(--s4);
  background:transparent; color:var(--text);
  border:1px solid var(--ctl-border); border-radius:var(--r-md);
  font-size:var(--t-sub); font-weight:700;
}

/* 텍스트 버튼 — 20px 높이의 원인이었던 padding:1px 6px를 없앤다 */
.text-btn{
  min-height:44px; padding:var(--s2) var(--s3);
  background:transparent; color:var(--text-sub);
  border:0; font-size:var(--t-sub); font-weight:400;
}

/* 칩·세그먼트·프리셋 */
.chip,.seg-btn,.preset,.mtab,.lens-btn{
  min-height:40px; padding:var(--s2) var(--s3);
  border:1px solid var(--line); border-radius:var(--r-sm);
  background:var(--card); color:var(--text); font-size:var(--t-sub);
}
.chip{border-radius:var(--r-full)}
.chip.is-on,.seg-btn.is-on,.preset.is-on{
  background:var(--brand-tint); border-color:var(--brand); color:var(--text); font-weight:700;
}
@media (max-width:680px){
  .primary-btn,.ai-btn{min-height:52px; font-size:var(--t-body)}
  .ghost-btn,.text-btn{min-height:48px}
  .chip,.seg-btn,.preset,.mtab,.lens-btn{min-height:44px}
}
```

### 3-2. 하단 고정 CTA — 기획서 §4.2가 계약했지만 미구현이었다

먼저 `verify.js:2432-2433`의 중첩을 푼다. `.step-actions`가 `.explore-actions` 안에 있어 sticky 이동 범위가 0이다.

```js
/* renderExplore — before */
+ '<div class="explore-actions"><small id="touchHint"></small><div class="step-actions">…</div></div>'
/* after — .step-actions를 section.card 직계 자식으로 */
+ '<div class="explore-actions"><small id="touchHint"></small></div>'
+ '<div class="step-actions"><button class="ghost-btn" id="backOrient">← 탐구 방향</button>'
+ '<button class="primary-btn" id="toVerdict">증거 정리하기</button></div>'
```

```css
@media (max-width:680px){
  .step-actions{
    position:sticky; bottom:0; z-index:12;
    display:flex; gap:var(--s2);
    margin:var(--s4) calc(-1 * var(--s4)) 0;
    padding:var(--s3) var(--s4) calc(var(--s3) + env(safe-area-inset-bottom,0px));
    border-radius:var(--r-lg) var(--r-lg) 0 0;
    background:var(--card); box-shadow:0 -4px 16px rgba(0,0,0,.10);
  }
  .step-actions .primary-btn{flex:1 1 auto}
  .verify-stage{padding-bottom:var(--s7)}   /* 고정 바가 마지막 콘텐츠를 덮지 않게 */
}
```

`index.html:5`에 `viewport-fit=cover`가 있는데 `env(safe-area-inset-*)` 처리는 코드베이스 전체에 **0건**이었다. 위 `padding-bottom`이 그 구멍을 막는다.

### 3-3. 진행 표시 — 3중을 1개로

CERL 작성 단계에는 진행 표시가 3개(분모 5·10·4) 겹쳐 375px에서 합계 215px을 먹는다. 두 진행바가 같은 그라디언트 채움을 공유해 시각적으로 구별되지도 않는다.

```css
@media (max-width:680px){
  .verify-progress{gap:var(--s2); margin:var(--s1) 0 var(--s3); align-items:center; flex-wrap:nowrap; overflow-x:auto}
  .verify-progress .pline{display:none}       /* 연결선 5개 제거 */
  .pstep{max-width:none; flex-direction:row; gap:var(--s2)}
  .pstep small{display:none}                  /* 제목 접기 → 168.8px → 약 40px */
  .pstep.on small{display:inline; font-size:var(--t-caption)}
}
```

미션 축과 단계 축을 한 줄로 합친다.

```js
/* missionStepHeader — 분모 두 개를 한 줄로 */
'<div class="micro-progress"><b>미션 ' + (state.mi+1) + '/' + MISSIONS.length
  + ' · ' + (at+1) + '/' + seq.length + ' ' + STEP_LABELS[step] + '</b>'
  + '<span><i style="width:' + Math.round((at+1)/seq.length*100) + '%"></i></span></div>'
```

`.cerl-mini-progress`는 진행바를 버리고 **점 4개**로 바꿔 "같은 바가 두 개" 상황을 없앤다.

### 3-4. 패널 — 72개 클래스를 3종으로

`테두리 + 배경 + 반경`을 가진 패널 클래스가 72개인데, 구조가 같은 것이 중립 16개·틴트 15개다.

```css
.panel{                       /* 중립 — .readout .method .record .term-card .win-panel 등 16개 */
  background:var(--sunken); border:0; border-radius:var(--r-lg);
  padding:var(--s3) var(--s4); margin:var(--s3) 0;
}
.panel--tint{                 /* 강조 — .task .live-nums .focus-task .student-cerl 등 15개 */
  background:var(--brand-tint);
  box-shadow:inset 3px 0 0 var(--brand);   /* 좌측 액센트 바. 테두리 4방향 제거 */
}
.panel--quiet{                /* 출처·한계 */
  background:transparent; padding:var(--s2) 0;
  font-size:var(--t-caption); color:var(--text-weak);
}
```

토스식으로 **선을 줄이고 표면 층으로 나눈다**: 카드(`--card`) 안의 패널은 `--sunken`, 그 안은 여백만.

### 3-5. 과학적 무결성 문구를 최소 계층에서 꺼낸다

```css
/* verify.css:382의 죽은 선언 삭제, 713행에서 .integrity 제거 후 */
.integrity{
  margin:0 0 var(--s3); padding:var(--s2) var(--s3);
  font-size:var(--t-sub); line-height:1.7;      /* 12px → 14px */
  color:var(--text-sub);
  background:var(--sunken); border-left:3px solid var(--brand);
  border-radius:0 var(--r-md) var(--r-md) 0;
}
.integrity.is-compact{font-size:var(--t-sub); line-height:1.6}  /* font-size 선언 삭제 → 상속 */
.table-note,.win-note,.map-note,.kma-src,.cerl-tag,.method-table caption{
  font-size:var(--t-caption); line-height:var(--lh-caption); color:var(--text-weak);
}
```

**하한 13px 규칙이 실제로 성립하려면** `verify.css:757`의 `small{font-size:max(11.5px,.86em)}`을 `small{font-size:var(--t-caption)}`으로 바꿔야 한다. 그러지 않으면 `em` 상대값이 하한을 다시 깬다.

### 3-6. 핵심 수치를 크게

조작의 결과인 수치가 본문보다 작다(`.live-nums` 13.5px, 모바일 12.5px). 같은 숫자가 한 화면에서 12.5/16/17px 세 크기로 세 번 나온다.

```css
.live-nums{
  display:flex; flex-wrap:wrap; gap:var(--s2) var(--s5);
  padding:var(--s3) var(--s4); margin:0 0 var(--s3);
  background:var(--brand-tint); border-radius:var(--r-lg);
  font-size:var(--t-sub); color:var(--text-sub);
}
.live-nums b{                       /* 숫자를 크게, 단위·라벨을 작게 */
  display:block;
  font-size:var(--t-num); line-height:var(--lh-num);
  font-weight:700; color:var(--text); font-variant-numeric:tabular-nums;
}
.live-nums .v-past b{color:var(--c-past)}
.live-nums .v-now b{color:var(--c-now)}
.live-nums small{font-size:var(--t-caption); color:var(--text-weak)}
```

### 3-7. 굵기를 2종으로

`720`·`750`·`780`은 어떤 환경에서도 독립 렌더되지 않는다. Malgun Gothic 폴백에서는 2종만 나온다.

```css
/* 전수 치환: 500·600·720·750·780 → 700 · 800·900 → 700 · 400 유지 */
```

위계는 **크기 × 굵기 × 색 3단**으로만 만든다. 폴백 환경에서도 같은 위계가 유지되는 유일한 방법이다.

---

## 4. P0 — 폰에서 차트 글자를 5px로 그리는 문제

375px 새로고침 로드 실측: `viewBox="0 0 720 470"` · 렌더 `329×215` · **배율 0.457**.
SVG 텍스트가 `font-size="11"` 같은 **user unit**으로 지정돼 있어 실제 렌더가 5px가 된다.

| 지정 | 데스크톱(664px) | 폰(329px) | 대상 |
|---|---:|---:|---|
| 11 | 10.1px | **5.0px** | 월 축(1~12월), 기온 축 |
| 11.5 | 10.6px | **5.3px** | 절기 라벨, 현재 마지막더위일 |
| 10 | 9.2px | **4.6px** | ⇅ 손잡이 |
| 12 | 11.1px | **5.5px** | 기준선 값 배지 |

미션 1의 답인 `8월 30일 → 9월 12일`이 폰에서 5px로 그려진다. `W = 720`이 상수(verify.js:703)이고 `#heroSvg{width:100%;height:auto}`(verify.css:49)라 배율이 항상 곱해진다.

```js
/* verify.js:703 아래에 추가 */
var W = 720, L = 46, R = 16, TP = 20, BT = 28;

/* SVG 안의 글자를 실제 px로 고정한다.
   user unit × (렌더폭/W) = 실제 px 이므로, 원하는 px에 역배율을 곱해 둔다. */
function svgFS(px) {
  var el = $('heroSvg');
  var w = el ? el.getBoundingClientRect().width : 0;
  return (px * (w ? W / w : 1)).toFixed(1);
}
```

`drawHero()`·`lagOverlay()`의 `font-size` 전부를 치환한다. 하한을 13px로 올려 본문 하한과 맞춘다.

```js
/* before */ font-size="11"
/* after  */ font-size="' + svgFS(13) + '"
/* before */ font-size="11.5" font-weight="700"
/* after  */ font-size="' + svgFS(13) + '" font-weight="700"
/* before */ font-size="10"   (⇅ 손잡이)
/* after  */ font-size="' + svgFS(14) + '"
/* before */ font-size="12"   (기준선 값 배지)
/* after  */ font-size="' + svgFS(15) + '"
```

폰에서 배율이 2.19이므로 `svgFS(13)`은 `28.5` user unit이 되어 실제 13px로 그려진다. `viewBox` 높이가 470이라 28.5 단위 글자가 들어갈 공간은 충분하다.
리사이즈 재렌더는 이미 있다(`verify.js:3749` 디바운스 `drawHero`) — 배율이 자동으로 다시 계산된다.

**검증 방법**: 375px 새 창에서 `[...document.querySelectorAll('#heroSvg text')].map(t=>getComputedStyle(t).fontSize)`의 값에 `svg.getBoundingClientRect().width/720`을 곱해 전부 13px 이상인지 확인한다.

---

## 5. 카피 규칙 10개

| # | 규칙 | Before | After |
|---|---|---|---|
| 1 | **학습자 화면은 전부 해요체.** 예외 없음 | "맞아요. 경향에 관한 진술입니다. 다만 5년은 기후평년은 아니에요." | "맞아요. 여러 해를 모아 센 값이라 경향을 말하는 문장이에요. 다만 5년은 관측 신호일 뿐, 30년 기후평년은 아니에요." |
| 2 | **모범 문장(전문가 CERL·placeholder)만 평서체.** 인용 문형이므로 정당 | — | "나는 …라고 판단한다." (유지) |
| 3 | **2인칭 '당신' 금지.** 무주어 또는 '내/내가' | "**당신은** 자료를 보기 전 예측하고…" | "자료를 보기 전에 예측하고, 기준을 직접 조작해 확인한 뒤…" |
| 4 | **조사는 반드시 헬퍼 경유.** `eunNeun`·`eulReul`·`iGa` | `f.label + '을 '` → "근거을" | `f.label + eulReul(f.label) + ' '` → "근거를" |
| 5 | **단위는 `°C` 하나.** `℃`(U+2103) 금지 | 본문 "22℃는" + 배지 "덥다 22°C" 동시 노출 | 전부 `°C` (verify.js 34곳 · 교사_학습지 11곳) |
| 6 | **문장 60자 상한.** 넘으면 마침표로 끊는다 | "◈ 기상청 ASOS 실측 · 과거 5년(1969–1973) vs 현재 5년(2021–2025) — 관측 신호이고 30년 기후평년이 아닙니다 · 절기는 태양 위치로 정한 천문 날짜라 해마다 거의 움직이지 않습니다" (110자) | "기상청 ASOS 실측이에요. 과거 5년(1969–1973)과 현재 5년(2021–2025)을 비교해요.<br>이건 관측 신호이고, 30년 기후평년은 아니에요." |
| 7 | **문장 중간 em dash(—) 금지.** 마침표나 줄바꿈으로 | "시간이 남으면 — '표'와 '지도' 보기로…" | "시간이 남으면 '표'와 '지도'로도 확인해 보세요." |
| 8 | **내부 용어 금지** | "학생 파일럿 전 추정치" / "비교 기간 26창" / "기준표 서랍" | "걸리는 시간은 아직 재고 있어요" / "비교 기간 26가지" / "기상청 기준표 펼치기" |
| 9 | **버튼 라벨 = 동사.** `→`는 다음 단계로 넘어갈 때만. 뒤로는 `← 대상이름` 고정 | "검증 준비 완료 →" / "감사하고 완료하기 →" | "이제 검증하러 갈게요 →" / "증거 점검하고 마치기 →" |
| 10 | **같은 기능은 같은 이름.** 오프라인 점검을 4가지로 부르고 있다 | "빠른 점검 / 기기 안에서 빠른 점검 / 규칙 점검 / 기기 안 규칙 점검" | "**기기 안 점검**" 하나로 고정 |

### 즉시 치환 대상 (노출 빈도 순)

| 위치 | Before | After |
|---|---|---|
| verify.js:2647 | `f.label + '을 '` | `f.label + eulReul(f.label) + ' '` |
| verify.js:174·175 | "+14일**와** +17일로" | "제주 +14일, 강원 +17일로" |
| verify.js:2854 | "**당신은** 자료를 보기 전 예측하고, …작성했습니다." | "자료를 보기 전에 예측하고, 기준을 직접 조작해 확인한 뒤, 미션마다 주장·근거·추론·한계를 스스로 작성했어요." |
| verify.js:2226 | "봉인한 예측을 검증해 **봅시다**. …끌어 보세요. …맞출 수 **있어요**." | "봉인한 예측을 확인해 볼까요? 보라색 기준선의 ⇅ 손잡이를 잡고 위아래로 끌어 보세요. 슬라이더나 ＋− 버튼으로 1`+unit+`씩 맞춰도 돼요." (단위 보간 유지) |
| verify.js:2280 | "먼저 내 생각을 **봉인하세요**." | "먼저 내 생각을 적어 둘게요." |
| verify.js:2287 | "선택은 정답 채점이 아니라 생각의 출발점으로 **기록됩니다**." | "채점하려는 게 아니에요. 지금 생각을 출발점으로 남겨 둘게요." |
| verify.js:1917 | "…심화·자유탐구까지 55~70분 · **학생 파일럿 전 추정치** · 설치·로그인 없이 · 모바일 지원" | "미션 하나에 예측 → 조작 → 내 결론 세 단계예요. 미션 5개에 CERL 4칸씩 직접 써요. 심화까지 하면 두 차시 분량이에요. 걸리는 시간은 지금 재고 있어요. 설치·로그인 없이, 폰에서도 돼요." |
| verify.js:2725 | "**감사**하고 완료하기 →" | "증거 점검하고 마치기 →" |
| verify.js:2569 | `join(' ')` → 판정문이 한 줄로 뭉친다 | `join('\n')` 또는 `cerlHTML(v)` 재사용 |
| verify.js:3766 | `[확인] 이어서 하기 / [취소] 새로 시작(기록을 지웁니다)` | `[확인] 기록을 지우고 새로 시작하기 — 지금까지 쓴 글이 모두 사라져요 / [취소] 이어서 하기` (파괴적 동작을 확인 쪽으로) |
| verify.js:2111 | "가장 더운 날은 **왜 하지가 아닐까?**" (예측 직후 정답 단정) | "가장 더운 날은 하지일까, 그 뒤일까?" — 공개 후에만 원문 사용 |
| verify.js:1278 | "…차이가 있다면 **무엇 때문**일까요?" (앱이 무효라 선언한 인과 유도) | "지역을 네 곳 이상 바꿔 보세요. 값이 큰 지점과 작은 지점의 **공통점**을 하나로 말할 수 있나요? 말할 수 없다면 그것도 하나의 발견이에요." |

CERL placeholder는 **골격만 상태 치환**한다. 화면 수치를 placeholder에 인쇄하면 앱이 근거 문장을 대신 써 주는 셈이 되어 "내 말로 쓰기"라는 목적을 깬다.

```js
/* CERL_FIELDS를 함수로 */
function cerlFields(){ return [
  {k:'c',label:'주장',min:5, prompt:'자료를 보고 내린 판단을 한 문장으로 써요.',
   placeholder:'나는 …라고 판단한다.'},
  {k:'e',label:'근거',min:12,prompt:'지역·기간·기준과 화면에서 읽은 숫자를 넣어요.',
   placeholder:state.city + '에서 ' + state.thr + metricOf().unit + ' 기준으로 과거 … → 현재 …'},
  {k:'r',label:'추론',min:8, prompt:'그 숫자가 왜 내 주장을 뒷받침하는지 이어요.',
   placeholder:'이 차이가 뜻하는 것은 … 그래서 …'},
  {k:'l',label:'한계',min:8, prompt:'이 자료만으로 말할 수 없는 범위를 써요.',
   placeholder:'다만 이 자료만으로 전국이나 원인을 …'}
];}
```

---

## 6. 화면 재배치

### 6-1. 미션 조작 화면 (375×667) — 현재 조작 요소가 첫 화면에 없다

```
현재 (누적 553.8px 뒤에 조작이 온다)          →  개선 (조작이 첫 화면에)
┌─────────────────────────┐                   ┌─────────────────────────┐
│ 헤더 54                  │                   │ 헤더 48                  │ 48
│ 진행 내비 168.8 (2행)     │                   │ ● ● ○ ○ ○  미션1/5·4/10  │ 40  ← 라벨 접기
│ 단계헤더 98.5            │                   ├─────────────────────────┤
│ 제목 35                  │                   │ 처서의 약속은 유효할까?    │ 62  ← 22px
│ 지금할일 185.5           │                   │ 기준선을 끌어 정해요.      │ 24  ← 1문장
├── 폴드 667 ─────────────┤                   ├─────────────────────────┤
│ 라이브 수치 58            │                   │  68일    ← 28px          │ 92  ← 수치 먼저
│ 슬라이더 (y≈675)         │                   │  과거 31일 · +13일        │
│ 차트 (y≈810, 140px)      │                   ├─────────────────────────┤
│ 프리셋                   │                   │ [차트 215px · 글자 13px]  │ 215 ← 배율 보정
│ CTA (y≈1176)            │                   ├── 폴드 667 ─────────────┤
└─────────────────────────┘                   │ ⇅ 25°C  − +  22 25 28    │
                                              ├─────────────────────────┤
                                              │ [증거 정리하기]  ← 하단고정 │ 52
                                              └─────────────────────────┘
```

핵심 변경 3개: ① 진행 내비 라벨 접기(168.8 → 40px) ② `지금할일` 185.5px 블록을 한 문장으로 줄여 `orient` 화면과의 **문자열 단위 중복**을 없앰 ③ 라이브 수치를 차트 위로 올려 조작–결과가 같은 시야에 들어오게.

### 6-2. CERL 작성 — 앞 요소를 지우지 말고 쌓는다

현재는 이전 필드를 `display:none`으로 감춘다. "내 주장을 뒷받침하는지 연결하세요"라고 지시하면서 그 주장을 화면에서 지운다.

```
┌─────────────────────────────────┐
│ 미션1/5 · 6/10 내 CERL   ● ● ○ ○ │
├─────────────────────────────────┤
│ 근거 노트 (자동 펼침)             │  ← 기획서 §8.1 계약
│ 서울 · 28°C · 과거 4.8일 → 33일   │
├─────────────────────────────────┤
│ ✓ 주장  처서의 약속은 …           │  ← 읽기 전용으로 쌓기
├─────────────────────────────────┤
│ 근거                             │  ← 현재 필드만 입력 가능
│ ┌─────────────────────────────┐ │
│ │ 서울에서 28°C 기준으로 …      │ │
│ └─────────────────────────────┘ │
│                    0 / 220  ←카운터│
├─────────────────────────────────┤
│ [← 이전]        [다음 요소 →]     │
└─────────────────────────────────┘
```

```js
/* studentCerlHTML — hidden 대신 완료 요소를 쌓는다 */
+ CERL_FIELDS.slice(0, ci).filter(function(f){ return (d[f.k]||'').trim(); })
    .map(function(f){ return '<p class="cerl-done"><b>' + f.label + '</b> ' + escapeHTML(d[f.k]) + '</p>'; }).join('')
```

```css
.cerl-done{
  margin:0 0 var(--s2); padding:var(--s2) var(--s3);
  background:var(--sunken); border-radius:var(--r-md);
  font-size:var(--t-sub); color:var(--text-sub);
}
.cerl-done b{color:var(--text); margin-right:var(--s2)}
.cerl-count{
  display:block; text-align:right;
  font-size:var(--t-caption); color:var(--text-weak); font-variant-numeric:tabular-nums;
}
```

### 6-3. 완료 화면 — 학생이 제출할 기록이 한 줄로 뭉쳐 있다

`cerlText`가 `join(' ')`이라 `주장: … 근거: … 추론: … 한계: …`가 줄바꿈 없이 이어진다(한글 1,712자 · 문서 높이 3,243px). 정작 4줄로 쪼개는 `cerlHTML`은 **전문가 예시에만** 쓴다.

```
내가 쓴 판정문
┌───────────────────────────────┐
│ 처서, 약속은 유효한가            │
│ 주장  …                        │  ← cerlHTML 재사용
│ 근거  …                        │
│ 추론  …                        │
│ 한계  …                        │
└───────────────────────────────┘
학년·반·번호·이름 [        ]        ← 제출용. 이 화면에서만 쓰이고 AI로 보내지 않아요
[기록 복사] [인쇄 / PDF]
```

---

## 7. 마이그레이션 계획 — 8/21까지

각 단계가 **독립적으로 배포 가능**하고, 회귀 검증을 통과한 상태로 끝난다.

| 단계 | 범위 | 공수 | 위험 | 검증 |
|---|---|---|---|---|
| **M0** | 카피 P0/P1 치환 — 조사 헬퍼(2647), 미션3 "일와"(174·175), '당신' 5곳, `cerlText` 줄바꿈, `℃`→`°C` 45곳, 오프라인 점검 이름 통일 | 2시간 | 없음(문자열만) | `node eval_harness.js` 100/100 · `verify_solar_terms.py` 문서축 90건 · 5미션 수동 완주 |
| **M1** | 차트 SVG 배율 보정(`svgFS`) — **P0** | 1시간 | 낮음. `drawHero`/`lagOverlay`의 font-size만 | 375·390·430px에서 SVG text 실효 크기 전수 ≥13px · 데스크톱 회귀 없음 |
| **M2** | 토큰 도입 — `base.css` 교체 + 구 토큰 별칭. `--glow`/`.sky-noise` 제거, `.card` 표면 부여 | 3시간 | **중간**. 별칭으로 verify.css를 안 건드리는 것이 안전장치 | 라이트·다크 두 테마에서 대비 전수 재계산(0 위반) · 스크린샷 대조 |
| **M3** | 타이포 정규화 — 35종 → 8단. `.eyebrow` monospace·자간 제거, `small`의 `max(11.5px,…)` 제거, 무결성 문구 12→14px, 굵기 9종→2종, h1 역전 해소 | 4시간 | 중간. 줄바꿈이 바뀌어 레이아웃이 흔들릴 수 있음 | 320·360·375·390·430px 가로 넘침 0 · 13px 미만 텍스트 0건 |
| **M4** | 간격·반경 정규화 — 49종 → 7단, 15종 → 4단. `.card` 패딩 48→32/16 | 3시간 | 중간 | 동일 폭 전수 + 인쇄 미리보기 |
| **M5** | 컴포넌트 4종 — 버튼(12종의 44px 미달 해소), 하단 고정 CTA + safe-area, 진행 표시 3중→1개, `.panel` 3종 통합 | 반나절 | **높음**. `.step-actions` 중첩 풀기가 DOM 변경 | WCAG 2.2 AA 타깃 24×24 위반 0 · 375px에서 조작 요소가 첫 화면에 존재 · sticky 실동작 확인 |
| **M6** | 화면 재배치 — CERL 완료 요소 쌓기, 글자 수 카운터, 완료 화면 `cerlHTML` 재사용, 이름 칸 | 반나절 | 중간 | 5미션 완주 + 완료 기록 인쇄 대조 |
| **M7** | `@media print` 토큰 6개 보강 — 다크로 인쇄하면 흰 종이에 1.09:1이 찍힌다 | 1시간 | 없음 | 라이트·다크 각각 인쇄 미리보기에서 4.5:1 이상 |

**M0 + M1을 먼저, 따로 배포한다.** 둘은 위험이 거의 없고 P0을 즉시 없앤다. M2~M7은 스크린샷 대조가 필수이므로 리허설 여유가 있을 때만 진행한다.

> **중단 조건**: M3 이후 어느 단계에서든 (a) 320~430px 가로 넘침이 생기거나 (b) 대비 위반이 1건이라도 생기거나 (c) 5미션 완주가 막히면, 그 단계를 되돌리고 배포본을 이전 단계로 유지한다.

---

## 8. 리디자인에서 절대 버리면 안 되는 것

1. **과학적 무결성 표기 전부.** 출처·표본 수·"30년 기후평년 아님"·비교 기간 경고·잭나이프 흔들림. 크기를 **올리는** 것이지 줄이는 것이 아니다.
2. **이미 계산해 맞춘 대비 노력.** `base.css:26-28` 주석("강조색 16% 틴트 over page 기준으로 4.55:1을 맞췄다")은 실제 실측의 흔적이다. 새 토큰도 같은 방식으로 검증했다.
3. **외부 라이브러리·CDN·웹폰트 0개.** CSP `default-src 'self'`와 brotli 158KB·요청 8건은 ⑤ 실현 가능성의 최대 자산이다. 리디자인에 아이콘 폰트·CSS 프레임워크를 들이지 않는다.
4. **`word-break:keep-all`.** 한글 줄바꿈 품질의 핵심이다.
5. **비마우스 대안 4중** — 슬라이더·± 스테퍼·프리셋·표 보기. 그리고 `#heroSvg`의 `touch-action: pan-y ↔ none` 제스처 분리.
6. **기준선 드래그 히트 밴드 ±22px × 전폭.** ⇅ 글리프가 5×6px이어도 실제 타깃은 329×44px로 충족한다 — 글리프만 키우고 히트 로직은 손대지 않는다.
7. **`setStage`의 h1 포커스 이동**과 `aria-live` 3개(상호 배타적 화면에만).
8. **`prefers-reduced-motion` 처리 5곳.**
9. **`is-muted` + 상시 힌트 + 클릭 시 플래시** 패턴. 비활성 버튼보다 나은 설계다. 여기에 `aria-disabled="true"`만 더한다.
