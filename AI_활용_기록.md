# 생성형 AI 활용 기록 — Weather24

> 제출물 ④ ‘주요 프롬프트 세션’
> 심사 배점 ⑤ 구현 완성도 후반 15점 — *“기상·기후 데이터가 개발물에 실제 반영·작동하고, **생성형 AI 도구가 결과물 제작 과정에서 효과적으로 활용**되었는가”* 에 대한 근거 문서
> 사용 도구: **Claude Code** (Anthropic) · 대회 지원 팀플랜 · 2026-06 ~ 2026-08

---

## 0. 한 줄 요약

> AI를 **코드 생성기**가 아니라 **자기 결과물을 반증하는 감사자**로 썼다.
> 그 결과 AI가 만든 앱에서 AI가 **결함 200건 이상을 찾아냈고**(3라운드 44건 + 4라운드 40건 + 5라운드 65건 + 6라운드 60건),
> 그 중 “화면이 사실이 아닌 문장을 쓰고 있다”는 지적이 이 프로젝트에서 가장 큰 품질 개선을 만들었다.
>
> 4라운드에서는 한 걸음 더 갔다. **감사 도구 자체가 무엇을 못 보는지**를 감사했다 —
> 문서 검사축이 낡은 문자열 목록이라 새 오류를 구조적으로 못 잡고 있었고,
> 그 “경고 0건”이 ‘해결’ 근거로 기록돼 있었다. 방식을 뒤집자 즉시 19건이 걸렸다.
>
> 5라운드는 **직전 라운드가 감사하지 못한 변경**을 겨눴다. 4차 감사와 자기점검 보고서가 모두
> 21:50 커밋(`verify.js` 713줄, 학습 흐름 재구성)보다 **먼저** 작성돼 있었다. 그 커밋이 만든
> 회귀가 이 앱의 대표 기제를 깼다 — 단계 헤더를 하나로 통합하면서 미션의 결론 문구가
> **‘자료를 보기 전 예측’ 화면까지 올라와 사전 문항의 정답을 인쇄**하고 있었다.
> 사람이 읽어서는 알 수 없었고, `git show 1d7fc32^`로 이전 버전과 대조해야 드러났다.
> 교훈은 하나다 — **통과하는 게이트가 무엇을 검사하지 않는지 보라.**

---

## 1. 활용 단계와 프롬프트 전략

| 단계 | 기간 | AI에게 시킨 일 | 산출물 |
|---|---|---|---|
| ① 데이터 수집기 | 6월 | 8개 공개 소스별 수집 스크립트 작성, 오류·재시도·타임아웃 처리 | `data_collectors/collect_*.py` |
| ② 가공 파이프라인 | 6월 | 원시 CSV → 학습용 경량 JSON 변환 설계 | `processing/build_solar_terms.py` |
| ③ 앱 구현 | 7월 | 체험형 학습 동선(예측 → 조작 → 판정) 구현 | `prototype/verify.js` |
| ④ **AI 레드팀 감사** | 7~8월 | **자기 결과물을 공격하게 함** — 교육효과·과학무결성·완성도·심사기준·UI/UX·카피. 6차는 배포본을 브라우저에서 직접 계측(줄바꿈·겹침·대비·여백) | `레드팀_*.md` 6종 |
| ⑤ 전량 수정 | 7월 | 레드팀 지적 44건을 근거와 함께 수정 | 커밋 12c25fd ~ 64b067f |
| ⑥ 검증 자동화 | 7월 | 회귀 검증 게이트·AI 품질 평가셋 작성 | `verify_solar_terms.py`, `eval_harness.js` |
| ⑦ **4차 레드팀(다중 에이전트)** | 7월 | 6개 축을 **독립 에이전트로 병렬 감사**한 뒤 **반증 검증관**이 기본값 REFUTED로 재판정 | `레드팀_4차_종합보고서.md` |
| ⑧ 관측 → 모형으로 확장 | 7월 | ‘데이터 뷰어 아닌가’라는 자기 지적에 답하기 위해 **에너지 균형 모형** 화면 신설 | 🔬 열관성 실험실 |

### 프롬프트에서 지킨 원칙 네 가지

1. **사용자를 먼저 넣는다.**
   `"중3~고2가 처서 무렵 기온을 직접 비교하는 화면을 만들어줘"` 처럼 대상·상황·목표를 프롬프트 앞에 둔다.
   기능만 말하면 어디서나 본 듯한 페이지가 나온다.

2. **한 번에 하나만 요청한다.**
   “전부 다 만들어줘”는 디버깅 지옥이 된다. 커밋 78개가 모두 작은 단위인 이유다.

3. **AI의 답을 믿지 않고 측정한다.**
   AI가 “고쳤습니다”라고 하면 반드시 원시 데이터로 재계산하거나 브라우저에서 실측해 확인했다.
   실제로 이 과정에서 AI가 처음에 낸 주장 **11건이 틀렸음을 확인하고 기각**했다(§4).

4. **반증을 시킨다.**
   `"이 주장을 틀렸다고 증명하려고 시도하라. 불확실하면 기각 쪽으로 기울여라."`
   — 확인 편향을 막기 위해 감사 결과를 다시 반박하는 단계를 별도로 뒀다.

---

## 2. 가장 효과가 컸던 프롬프트 세션 — AI 레드팀

### 2-1. 지시한 내용 (요지)

```
현재 구현된 시스템을 레드팀으로 교육 효과, 웹앱 완성도, 평가 기준 기반으로 분석/평가하라.
단, 절대 타협하지 말고 냉정하게 분석하되, 억지로 비판하지 마라.

- 모든 결함은 (a) 파일:줄 또는 실제 화면 동작, (b) 구체적 실패 시나리오,
  (c) 심사/학습에 미치는 영향으로 서술하라.
- '더 좋았을 것'이라는 취향 의견과 '결함'을 구분하라. 결함이 아니면 보고하지 마라.
- 잘 된 부분도 명시하라. 강점을 부정하는 것도 부정확한 레드팀이다.
- 도구로 파일을 실제로 읽고 확인하라. 필요하면 데이터로 계산해 검증하라.
```

이어서 **반박 단계**를 따로 실행했다.

```
당신은 반박 담당 검증관이다. 아래 결함 주장을 '틀렸다고 증명하려' 시도하라.
- 코드가 실제로 그렇게 동작하지 않거나 이미 방어 코드가 있으면 REFUTED.
- 동작은 맞지만 심각도가 과장되었으면 PARTIAL로 낮추고 정확한 주장을 다시 써라.
- 취향/선호 류는 REFUTED로 처리하라.
- 불확실하면 REFUTED 쪽으로 기울여라 (거짓 결함을 남기는 비용이 더 크다).
```

### 2-2. 이 세션이 실제로 찾아낸 것

AI가 원시 CSV(20,992행)로 직접 재계산해 **자기가 만든 화면의 수치가 틀렸음을 증명**했다.

| 화면이 말하던 것 | 실제 관측값 | 무엇이 문제였나 |
|---|---|---|
| 서울 더위일 `33일 → 82일` | `30.8일 → 68.0일` | 15일 평활한 **곡선**에서 날을 세고 있었다. 실제 관측일수와 다르다 |
| 처서 시차 `+25일` | `+13일` | 위와 같은 원인. 게다가 다른 5년 창에서는 −1~+14일이라 **선택한 창만 이상치**였다 |
| 서울 비 온 날 `292 → 309일` (증가) | `89.2 → 82.8일` (**감소**) | 방향이 반대. 강수는 평활 곡선으로 세면 안 되는 지표였다 |
| 26℃에서 “과거엔 거의 없었는데” | 실제 **21.4일/년** | 곡선 최댓값이 25.9℃라 0일로 나왔을 뿐, **사실이 아닌 문장**이었다 |
| 고향카드 “+0.0℃ / −0.7℃” | — | 단년 차분이라 2024년생은 16지점 전부 음수 |

> **핵심**: 이 오류들은 사람 눈으로는 찾기 어려웠다. 그럴듯한 숫자였기 때문이다.
> AI에게 “원시 데이터로 다시 세어 보고 화면과 대조하라”고 시켰기 때문에 드러났다.

### 2-3. 그래서 무엇을 바꿨나

`exceed()`/`lastExceed()`가 평활 곡선 대신 **연도별 실측 통계**를 읽도록 파이프라인과 앱을 함께 고쳤다.
그리고 같은 오류가 되살아나지 못하도록 **회귀 검증 게이트**를 새로 만들었다.

```
processing/verify_solar_terms.py — 총 8,360건 (데이터 8,213 + 인터페이스·문서 147)
  A 구조   16지점·곡선 365개·임계값 그리드 완전성
  B 표본   불완결 연도(2026년 173일) 혼입 차단, 지점별 사용 연도 기록
  C 단조성 기준이 높아질 때 일수가 늘어나지 않는가
  D 실측   사전계산값을 원시 CSV에서 독립 재계산해 대조   ← 핵심
  E 방향   증감 부호가 실측과 일치하는가                 ← 부호 역전 재발 방지
  F 문서   데이터에서 뽑은 값이 5개 문서에 그 값으로 적혀 있는가  ← 4차에서 방식을 뒤집음
  G 강수   강수 강도 미션이 쓰는 전환점 값 검증
  H·H2 계절지연·극값 안정성 / I 이동창 26개
  J 절기   24개 대표 날짜를 태양황경 교차 최빈일과 대조        ← 입하·입추 정정을 잡아낸 축
  K 전지구 web_data 완결 연도·r² 재계산 대조
  L 안정성 계절 지연 잭나이프 흔들림 기록 여부, 폐기한 이분법 부활 차단
  M 폭염   폭염일·열대야 지수 존재와 값
```

> **F축을 뒤집은 이유.** 예전 F축은 낡은 문자열 7개를 블랙리스트로 갖고 있었다.
> 데이터를 다시 빌드한 뒤 문서에 남은 수치 5계열을 **하나도 잡지 못했고**, 그 “경고 0건”이
> 해결 근거로 기록됐다. 지금은 데이터에서 값을 계산해 문서에 **틀린 값이 있으면 실패**시킨다.
> 실제로 방식을 바꾸자마자 19건이 걸렸다.

---

## 3. AI로 만든 또 다른 검증 장치

### 3-1. AI 품질 평가셋 100케이스

앱 안의 AI(증거 감사관)가 학습자의 잘못된 결론을 제대로 잡는지 확인하기 위해,
**AI에게 한국어 학습자 답안 100개를 생성시키고 정답 라벨을 붙였다.**

```
카테고리 8종 — good_scoped / hold_valid / one_year_to_climate / correlation_causation /
              overgeneralize / misconception_solar / single_evidence / injection

핵심 평가 원칙: 잘 쓴 답(범위를 지킨 답)을 잘못 지적하는 '오탐'을 가장 무겁게 본다.
              좋은 데이터 리터러시를 나무라는 것은 교육적으로 해롭기 때문이다.
```

결과 **100/100, 오탐 0건** — `node eval_harness.js` 로 재현 가능 (`eval_set.json`).

> **무엇의 점수인지 분명히 합니다.** 이 수치는 LLM(`/api/ai-turn`)의 성능이 아니라 **AI가 실패해도 항상 실행되는 규칙 기반 폴백(`localAudit`)** 의 품질입니다.
> 하네스가 `verify.js`에서 그 함수를 소스째 추출해 돌리므로 배포 코드가 바뀌면 점수도 따라 바뀝니다.
> 4차 감사에서 한국어 수사의문문(“…아님?”, “…아니냐”)이 한계 진술로 오인돼 경고가 통째로 꺼지는 결함을 찾아 고쳤고(96 → 99),
> 제출 전 Self-reflection에서 주어와 서술어 사이에 수식어가 낀 문장을 추가 보정해 **99 → 100**으로 높였습니다. 두 수정 모두 잘 쓴 답을 나무라는 **오탐 0건을 유지**했습니다.

### 3-2. 502 오류의 원인 규명

배포 후 AI 감사관이 간헐적으로 502를 냈다. “가끔 그러네”로 넘기지 않고 원인을 측정으로 좁혔다.

```
프로덕션 10회 연속 요청 → 5회 실패 (50%). 실패도 성공과 같은 ~3초 → 타임아웃이 아니다.
OpenAI API 직접 호출로 재현 → status="incomplete", reason="max_output_tokens"
원인: max_output_tokens=360인데 추론 토큰이 107~346개를 소비 → JSON이 잘림 → JSON.parse 예외

수정: 출력 예산 1200으로 확대 + 잘리면 추론 없이 1회 재시도 + 원인별 오류 코드 분리
결과: 39회 연속 200, 최대 3.7초, 재시도 사용 0회
```

---

## 4. AI가 틀렸던 것 — 검증으로 걸러낸 11건

AI 감사 결과를 그대로 믿지 않고 전부 재확인했다. 다음은 **AI 주장이 틀려서 기각한** 항목이다.

| AI의 주장 | 재검증 결과 |
|---|---|
| “2026년 포함 여부가 시차의 **부호**를 바꾼다” | **틀림.** 부적절한 추정으로 나온 값. 앱 방식으로는 2022–26 ≡ 2022–25 |
| “절기 날짜 3개가 소수파” | **3차에서 1개만 사실이라 판정했으나 그 판정이 틀렸다.** 4차에서 독립 재계산한 결과 입하(5/5 31회 vs 5/6 27회)·입추(8/7 31회 vs 8/8 27회)도 소수파였다 → **정정 반영.** 처서 8/23은 51/58로 정확 |
| “기온 일수가 +38% 과장” | **하향.** 완결 연도 기준 +13% |
| “강수 결측 50~66%가 편향을 만든다” | **기각.** ASOS는 무강수일을 공란 처리 → 0으로 채우는 것이 올바른 처리 |
| “순환 평활이 연말 불연속을 만든다” | **기각.** 최저점이 1월 하순으로 물리적으로 타당 |
| “스크린리더로 데이터 접근 불가” | **기각.** `aria-live` 3곳으로 핵심 수치가 낭독됨 |
| (외 5건) | `레드팀_카피·벤치마크보고서.md` §8 참조 |

> 이 표가 이 문서에서 가장 중요할 수 있다. **AI 산출물을 검증 없이 반영하지 않았다는 증거**이기 때문이다.

---

## 5. 대회 가이드라인 준수

사전교육(7/22)에서 배운 방법을 실제로 적용한 부분이다.

| 가이드 | 적용 |
|---|---|
| “프롬프트에 사용자부터 넣는다” | 모든 기능 요청에 ‘중3~고2가 …하는’ 형태로 대상·목표를 명시 |
| “작게 요청 → 사용자 눈으로 확인 → 구체적으로 수정” | 커밋 78개가 모두 작은 단위. 각 단계마다 브라우저 실측 |
| “자주 배포할 것. 내 화면과 심사위원이 여는 URL은 다르다” | 수정마다 프로덕션에 배포하고 **배포본에서 다시 검증**(캐시 버전 v31까지) |
| “AI가 만든 코드 그대로 제출 가능” | 그대로 두지 않고 레드팀 감사 후 전량 수정본을 제출 |
| “API 키 노출 시 실격” | 추적 파일·git 전체 히스토리·배포 자산 전수 검사 결과 **0건** |

---

## 6. 원본 세션 로그

Claude Code 세션 원문은 별도 파일로 함께 제출합니다.

```
prompt_sessions/
├─ README.md                        세션 목차·집계
├─ 01_기획과_초기구현.md            데이터 파이프라인 설계 · 앱 초기 구현
├─ 02_레드팀_감사와_전량수정.md     자체 레드팀 3라운드 44건 적발 → 전량 수정
├─ 03_실측전환과_AI안정화.md        평활 곡선 → 연도별 실측 집계 전환 · AI 502 원인 규명
├─ 04_기능확장과_최종감사.md        미션5·지도·26창 확장 · 라이트 테마 · 3차 제출 감사
├─ 05_4차레드팀과_학습흐름_재구성.md 6축 다중 에이전트 감사 · 반증 검증 · 열관성 실험실 신설 · 마이크로 단계 재구성
└─ 06_5차레드팀과_우선순위_전량반영.md 13축 감사(교육·완성도·심사기준 7축 + 카피·UI/UX·CX 6축) · 축별 적대적 반증 · 우선순위 5건 전량 반영
```

> 세션 06은 **13개 축을 병렬로 감사한 뒤 축마다 별도의 반증 에이전트를 붙인** 라운드다.
> 반증 단계에서 감사자의 지적 일부가 기각됐고(예: 드래그 손잡이 크기, 비활성 CTA의 무반응),
> 반증을 하다 **더 심각한 문제 15건이 새로 나왔다**. 감사와 반증을 분리한 것이 이 라운드의 방법이다.

> **비밀값·개인정보 점검은 자동화했습니다.** `scripts/export_sessions.py`가 내보내기 단계에서
> API 키·이메일 패턴을 마스킹하고, `scripts/submit_check.sh`가 제출 직전 zip 산출물에서 다시 검사합니다.
> 세션 간 중복 턴도 같은 스크립트가 제거합니다(예전에는 03이 04의 대부분을 중복 포함해 집계가 이중 계상됐습니다).

---

## 7. 요약 — 이 프로젝트에서 AI가 한 일

1. **만들었다** — 데이터 수집기 8개, 가공 파이프라인, 학습 웹앱 전체
2. **자기 것을 공격했다** — 레드팀 6라운드로 결함 200건 이상 적발, 근거는 전부 원시 데이터 재계산 또는 배포본 DOM 실측
3. **틀린 지적은 걸러졌다** — AI 주장 11건을 사람이 검증해 기각. 5라운드에서는 **감사자와 반증자를 분리**해
   기각을 구조로 만들었다(감사 → 축별 적대적 반증 → 확증만 채택)
4. **재발을 막는 장치를 만들었다** — 총 8,360건(데이터 8,213 + 인터페이스·문서 147)의 회귀 검사 게이트, 평가셋 100케이스
5. **원인을 규명했다** — 502를 “가끔 그러는 것”으로 두지 않고 토큰 예산 문제로 특정해 해결
6. **자기 감사의 사각을 감사했다** — 5라운드에서 ‘직전 감사보다 나중에 들어온 변경’이 사각지대임을 확인하고,
   `submit_check.sh`에 **시연 문서가 코드보다 오래되면 실패**하는 게이트를 넣어 같은 사각을 닫았다

> AI로 빨리 만든 것이 아니라, **AI로 더 정직하게 만들었다**는 것이 이 프로젝트의 AI 활용이다.
