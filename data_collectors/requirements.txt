# ── 핵심 (모든 무인증 수집기 실행에 필요) ─────────────────────────────
requests>=2.31
pandas>=2.0

# ── ECMWF Open Data 실다운로드용 (무인증) ─────────────────────────────
ecmwf-opendata>=0.3.3

# ── Copernicus CDS(ERA5) 실수집용 (키 필요) ───────────────────────────
# cdsapi>=0.7.4
# xarray>=2024.0
# netcdf4>=1.6

# ── NASA GPM(IMERG) 실수집용 (Earthdata 로그인 필요) ──────────────────
# earthaccess>=0.9
# xarray>=2024.0
# h5netcdf>=1.3

# ── (선택) GRIB 파싱: ECMWF/일부 모델 격자 파싱 시 ────────────────────
# cfgrib>=0.9
# eccodes>=2.31
