# Weather24 레드팀 5차 종합보고서

> 감사일: 2026-07-29 · 대상 커밋: `1d7fc32` (배포본 = origin/main, verify.js?v=57 실측 확인)
> 이 문서는 **5차 감사 기록**이며, 4차 보고서·제출전 Self-reflection보다 **나중**입니다.
> 4차(07-28 19:01)와 Self-reflection(20:44)은 모두 21:50 리팩터(`1d7fc32`, verify.js 713줄) **이전**에 작성됐습니다.
> 방법: 7축 병렬 감사 → 축별 적대적 반증 → 확증만 종합. 감사 15 에이전트 · 도구 호출 502회.
> 반증 통과 65건 · 기각 4건 · 반증 중 신규발견 15건.

---

## 1. 심사 배점 채점 (총 72/100)

| 심사 항목 | 배점 | 채점 |
|---|---:|---:|
| ① 주제 적합성 | 20 | **15** |
| ② 체험·참여형 설계 | 25 | **17** |
| ③ 학습 효과 | 10 | **6** |
| ④ 아이디어 독창성 | 15 | **11** |
| ⑤ 실현 가능성 | 30 | **23** |
| **합계** | **100** | **72** |

### 예상 순위대

본선 15팀 중 상위권 — 예상 3~6위대(총 72/100). 대상(1위) 후보 경계선 밖. 근거: ⑤에서 이미 배포 완료 + 8,303건 검증 게이트 + 외부 의존 0의 보수적 스택은 해커톤 본선에서 흔하지 않은 자산이어서 상위권을 지탱한다. 반면 요강 예시 결과물 6종(태풍 시뮬레이터·예보관 의사결정·기압·바람 조작 맵·강수 원리·기후변화 체험·수치예보 입문)이 전부 시뮬레이터/메커니즘형인데 이 작품의 중심축은 '자료 해석 규율'이고 유일한 시뮬레이터(열관성 실험실)가 필수 동선 밖에 있어 ②·④에서 거리가 남는다. 결정적으로 공고가 무게를 둔다고 명시한 두 축(②·⑤)에서 각각 P0을 안고 있다 — 예측 봉인 누출은 심사위원이 미션 1 첫 화면에서 10초 안에 볼 수 있고, 대본 붕괴는 발표 44%를 차지하는 시연에서 발현한다. 상위 3개 우선순위를 8/21까지 처리하면 78~80으로 대상권 진입이 현실적이다.

### 항목별 채점 근거

#### ① 주제 적합성 — 15/20

기상청 ASOS 실측 1969–2026 · 16지점 × 24절기를 쓰고 '절기(천문 날짜) ≠ 기후(관측)'라는 축은 주제에 정확히 부합한다. 물리·천문 정확도가 높다 — insolation()의 전지구 연평균이 340.285 W/m²(이론 340.250, 오차 0.01%), 절기 24개가 Meeus 황경 교차로 1969~2026 전수 검산 24/24 일치. 감점 근거: (a) 학습목표 5개 중 4개(절기≠기후·자료의 범위·기준 정의·근거만큼 결론)가 데이터 리터러시·과학의 본성 목표이고 기상 메커니즘은 열관성 1개뿐이며, verify.js 전문에 기단·전선·고기압·저기압·장마·편서풍·대류·잠열이 각 0회다. 요강 예시 6종이 전부 메커니즘형인 것과 거리가 있다. (b) SCI-03/JUDGE-06 — 온실효과 슬라이더가 실물 단위 W/m²인데 이 모형의 기후민감도는 0.71℃(실제 ~3℃의 1/4)이고, 제주에서 ghg를 12까지 올리면 적합 오차가 3.12→1.68℃로 개선돼 '아주 잘 맞습니다'가 뜬다 — 기상청 심사에서 가장 잡히기 쉬운 지점. (c) EDU-03 — '자료의 범위를 밝혀라'를 가르치는 미션 4에서 앱이 지역을 '서울'로 오표기한다. 다만 JUDGE-08의 반증대로 renderTerms(1930-1971)가 자전축 23.4°·근일점 0.983AU/원일점 1.017AU·음력 354일 vs 태양년 365.24일을 실제로 가르치고 열관성 실험실이 0차원 EBM을 물리량으로 조작하게 하므로 '데이터 리터러시 수업일 뿐'이라는 프레임은 과장이며 −5를 넘기지 않는다.

#### ② 체험·참여형 설계 — 17/25

공고 명시 가중 항목. 강점이 실재한다 — 예측 봉인이 모든 진입 경로에서 코드로 강제되고(2422-2424, 2256-2264, applyHash 619-630), 미션별 조작 게이트가 과제-특이적이며(제주·강원 둘 다, 1mm+30mm↑), 비마우스 대안 4중·SVG keydown·터치 타깃 실측 수정·touch-action 제스처 분리·prefers-reduced-motion 5곳까지 접근성 구현이 촘촘하다. 감점 근거를 데모 노출도 순으로: (a) P0 — missionStepHeader(2244-2250)가 모든 단계에 goal-chip을 무조건 렌더해 예측 화면에 정답을 인쇄한다. 앱의 대표 기제가 미션 1 첫 화면에서 깨지는 것을 심사위원이 10초 안에 본다. (b) P0 — 데모 대본 2종이 존재하지 않는 미니차트·카운트업을 가리키고, 콜드 스타트에서 그래프 최초 노출까지 10클릭·8화면인데 대본은 2클릭·50초로 적었다. 심사위원이 직접 조작하는 [10–25초] 블록이 성립하지 않아 연출 자체를 잃는다. (c) P1 — labBack(3321-3324)이 진입 위치를 기억하지 않고 항상 startMission(4)를 호출해, 완료 화면에서 실험실로 갔다 돌아오면 완료 화면이 파괴되고 복구에 최소 12조작이 필요하다. 심사위원이 볼 가능성이 가장 높은 두 화면을 연결하는 동작이다. (d) 핵심 코스 59화면 중 조작 컨트롤이 있는 화면이 explore 5개뿐이다 — 반증대로 audit→explore 복귀·실험실·자유탐구가 있어 '루프 1회 종결'은 과장이지만, CERL 4화면 분할과 LENS 3화면 분할이 만든 '읽고 쓰는 화면'의 비중은 실재한다. (e) explore에서만 sticky CTA가 무효(부유 범위 ~35px 이하), 375×667에서 조작 대상이 폴드 밖, 진행률 분모가 실제 화면 수의 절반. 17/25 — 설계 사상은 상위권이나 그 사상의 대표 기제가 P0으로 깨져 있다.

#### ③ 학습 효과 — 6/10

설계 의도와 규율은 훌륭하다 — write→check→expert 순서를 코드로 강제해 재인 오염을 차단하고(2261-2263), 미션 5는 조작 없이 만들 수 없는 진짜 수행 지표(찍은 날짜와 실제 극값일의 차, wrap 처리 포함 274-277)를 산출물에 남기며, 교사 학습지에 오개념 8행 표와 6점 루브릭이 있고, 입증되지 않은 효과를 주장하지 않는 규율이 문서 전반에 강제된다. 그러나 앱 내 측정 장치 전부가 무효화 경로를 갖는다: (a) 사전/사후 기준선이 오염됐다 — goal-chip이 PRE 정답 b를 지시하고, 그 직전 renderIntroMethod(1908-1909)도 '① 절기와 기후는 어떻게 다를까'를 인쇄해 누출이 이중이다. (b) 목표 ④의 유일한 자동 검사가 글자 수 33자다 — 실행 검증에서 '가나다라마' 도배가 전 단계를 통과하고, 한 문장을 네 칸에 복붙하면 localAudit이 'ready·좋아요'를 붙인다. 그런데 완료 화면 2853-2855는 'CERL 5편 완료'와 목표 배지 4개를 무조건 발급한다. (c) 자가진단 5문항 중 3문항의 정답 어휘가 같은 화면 goal-chip에 있고, 필수 LENS(2363)가 미션 1의 결론 수치 '68일 vs 31일'을 조작 전에 인쇄한다. (d) 사후 문항이 같은 미션 안 3분 간격 오픈북이고, 직전 expert 화면이 '절기 날짜는 그대로인데'라는 정답 문형을 미리 보여 준다. (e) 산출물 무결성 버그 — selfChecks 삭제 경로(2792/1707/2511)와 완료 관문(2798-2809)이 어긋나 이미 맞힌 자가진단이 '미응답'으로 인쇄되면서 같은 화면이 '매 미션에서 직접 작성했습니다'를 단정한다. (f) 목표 ⑤가 필수 동선·완료 배지·내 기록 어디에도 없다(배지 4개). (g) 학생 데이터 0건. 6/10 — 5개 목표 중 타당하게 측정되는 것은 미션 5의 날짜 오차 하나다.

#### ④ 아이디어 독창성 — 11/15

'절기의 약속 검증소'라는 프레이밍은 국내 맥락 고유이고 신선하다. 열관성 실험실은 진짜 시뮬레이션이며 화면이 주장하는 수치가 독립 재구현으로 재현된다 — 특히 온실효과 0→12 W/m²에서 연평균은 오르지만 지연은 40일로 완전 불변(23,040 조합 전수 검사에서 변화 0건)이라는 교육적 급소가 모형에서 실제로 성립한다. 자기 불리한 값을 데이터에 남기고 화면에서 밝히는 태도(rainFlip.high=[80,-0.6], jackknife robust:false, 곡선 일수 ≠ 실측 일수의 자기 고백)는 이 규모의 작품에서 드물다. 감점 근거: (a) EDU-07 — 유일한 시뮬레이션 자산이 missionSequence에 없고 진입점 6곳 전부가 선택이며 완료 배지에서 목표 ⑤가 빠져 있다. 요강 예시 6종이 전부 시뮬레이터인데 이 앱의 시뮬레이터 부분이 기본 5분 데모 밖이다. (b) 관측 3축의 실행 형태는 꺾은선 + 임계값 슬라이더 + 워크시트이고, 결과물 내 AI는 텍스트 감사 1종이다. (c) SCI-09 — 발견 ②는 선형 모형에서 A가 위상에 영향을 줄 수 없으므로 해석적 필연인데 카드가 '확인해 줍니다'라 부르고, 한계 서랍 5개 항목에 그 사실이 없어 물리 전공 심사위원 질문에 준비된 답이 없다. 11/15.

#### ⑤ 실현 가능성 — 23/30

공고 명시 가중 항목이며 이 작품의 최대 강점 축이다 — 사실상 ES5 코드·외부 의존 0·CSP default-src 'self'·brotli 158KB·요청 8건·로그인/백엔드 없음으로 학교 환경 실패 지점이 사실상 없고, AI 실패가 학습을 끊지 않는 3중 폴백에 '정량 검증한 것은 폴백 경로'임을 스스로 명시하며, 8,303건 게이트가 문서 인용 수치까지 대조하고 배포·자산 8종 200·캐시 버스팅 갱신·localStorage 마이그레이션까지 확인된다. 그럼에도 −7: (a) 팀 자체 제출 게이트가 지금 실패다 — bash scripts/submit_check.sh 실측 exit 1('README 커밋 수 표기가 실제(47)와 2 이상 다릅니다'). 제출_체크리스트.md:70의 자기 규정상 업로드 금지 상태이고, 커밋 수는 PDF p9 39 / README 45 / 실제 47로 세 갈래다. (b) 발표·데모 문서 5건이 21:50 리팩터 이전 흐름이다 — 1d7fc32가 데모_대본.md·발표_10분_구성안.md를 건드리지 않았고(실측), 발표 10분 초과는 −9점/분이 명시돼 있으며 시연이 전체의 44%다. p5(모형) 슬라이드에는 시간 예산이 아예 없다. (c) 교사_학습지.html이 자기 산술을 어긴다 — 열거 항목 합계 69~76분인데 같은 문서 34행이 '전체 코스 예상 55~70분'이라 쓴다. 교사가 수업을 설계하며 더하면 즉시 드러난다. (d) 데이터 표시 무결성 3건 — 극한지수 표 32행 중 16행이 자기 검산 실패(실측, 최대 0.5일), 미션 4 증거 카드가 지역을 '서울'로 하드코딩, sensitivityAt이 이중 반올림으로 파이프라인의 정확한 값(9.6)보다 부정확한 +9를 인쇄. (e) 열관성 물리 코드의 게이트 커버리지가 0건(실측) — '게이트로 강제한다'는 서사가 가장 새로운 화면에서만 비어 있다. (f) 공용 기기 인수인계 구멍(askResume이 미션 1 도중 이탈을 진행으로 인정하지 않음), Self-reflection §4-4가 '아직 배포하지 않았다'로 사실과 반대, 프롬프트 세션이 01:10에서 끊겨 지금 배포된 화면 골격을 만든 대화가 제출물 ④에 없음, 파일럿 미실시. 23/30.

---

## 2. 총평

기술 기반·데이터 무결성·자기 검증 문화는 본선 15팀 중 최상위권으로 평가할 근거가 충분하다 — ES5 수준 코드에 외부 의존 0, brotli 158KB·요청 8건, 8,303건 회귀 게이트가 문서 인용 수치까지 대조하고, 일사량 전지구 평균이 이론값과 0.01% 오차로 일치하며, 자기 불리한 값(rainFlip.high=[80,-0.6], robust:false)을 데이터에 남기고 화면에서 밝힌다. 그러나 21:50 리팩터(1d7fc32, verify.js 713줄)는 감사되지 않은 상태로 배포됐고, 그 커밋이 이 작품의 두 가중 항목을 정확히 관통하는 P0 두 개를 만들었다 — missionStepHeader의 무조건 goal-chip이 미션 1·4·5의 봉인 문항 위에 정답을 인쇄해 앱의 대표 기제가 첫 화면에서 깨지고(verify.js:2244-2250 → 2280, 리팩터 이전 2199행은 '자료를 보기 전 예측'이라는 중립 라벨이었다), 같은 커밋이 데모_대본.md·발표_10분_구성안.md를 건드리지 않아 라이브 데모 대본 전체가 존재하지 않는 화면을 가리킨다(git show --name-only 1d7fc32 실측). 발표의 44%가 시연이고 10분 초과가 −9점/분인 심사에서 이것은 단일 결함이 아니라 구조적 노출이다. 형성평가는 전부 무효화 경로를 갖는다(CERL 게이트 33자, 자가진단 정답을 같은 화면 칩이 인쇄, LENS가 미션1 결론 수치 68/31을 조작 전에 인쇄). 총 72/100 — 상위권이되 대상권 경계선 밖이며, 목록의 상위 3개는 합쳐 반나절~하루 작업이고 그것만으로 78~80까지 회복 가능하다.

---

## 3. 8/21까지의 우선순위

### 우선순위 1

**예측 봉인 누출 3곳을 차단한다(총 5줄). ① missionStepHeader(prototype/verify.js:2244-2250)에 step 분기를 넣어 step==='predict'일 때 goal-chip 대신 리팩터 이전 문구 '자료를 보기 전 예측'을 렌더하고, step==='check'일 때는 '목표 ①'처럼 번호만 남긴다. ② LENS.items[2](verify.js:2363)의 '서울에서 25°C를 넘는 날이 최근 5년 평균 68일로, 1969–73년의 31일보다 많다'를 미션 1과 겹치지 않는 문장으로 교체(k:'climate' 유지). ③ renderIntroMethod(verify.js:1908-1909)의 '① 절기와 기후는 어떻게 다를까'를 내용 중립 문구로 바꾼다. 회귀 방지로 verify_solar_terms.py 학습계약 축에 'renderPrediction 출력에 m.goal 문자열이 없다' assert를 추가한다.**

- **왜**: ①은 21:50 리팩터가 만든 신규 회귀이고(1d7fc32^:2199는 중립 라벨이었다), 같은 함수 위 2272-2274행 주석이 '예측 화면에는 정답 성격을 암시하는 보조문구를 한 글자도 싣지 않는다'고 스스로 선언한 계약을 위반한다. 미션 1·4·5 세 곳에서 사전 예측이 무효화되고, 그 값이 완료 화면 성장 진술과 학생 파일럿의 사전/사후 기준선이다. 심사위원이 미션 1 첫 화면에서 10초 안에 볼 수 있는 결함이며, 데모_대본·발표_10분이 '봉인 → 검증'을 서사 축으로 쓴다. 점수당 노력 비율이 목록 전체에서 가장 크다.
- **공수**: 30분 (코드 4~5줄 + assert 1건). 데이터·로직 변경 없음.
- **예상 점수 변화**: ③ +3~4, ② +2~3 (합 +5~7)

### 우선순위 2

**데모_대본.md §1 전체와 발표_10분_구성안.md §1~§3을 현재 코드 순서로 재작성하고 스톱워치로 3회 실측한다. 실제 순서: renderIntro 텍스트 화면(1880-1896, 차트 없음) → renderIntroMethod(1898-1914) → 무자료 예측 → 개념 분류 3문장 6클릭(2299-2330) → 탐구 방향 → explore. 삭제할 서술: 데모_대본.md:13-14의 '미니차트 카운트업'·'차트 위에 +13일'(그 요소는 cbd3c6e 시점에도 없었다), :22-23의 중복 시간 구간, 발표_10분_구성안.md:49의 '핵심 2~3분 칩'(time-chip은 renderLab에만 있다), :64의 'CERL 4칸 동시 노출'. 추가할 것: §2 1:55–2:45를 1:55–3:00으로 늘리고 §1에 p5(모형) 40~50초 구간을 신설(§4 자유탐구 또는 §6 교실적용에서 회수), 자유탐구의 '2. 증거 읽기' 탭 클릭과 완료 화면 '3. 지구 맥락' 탭 클릭을 명시하고 '🌍 지구 전체는 어떨까?'를 현재 문자열 '🌍 이산화탄소와 지구 평균기온 자료 펼치기'(2877)로 교체, CERL 구간을 5단계(write→check→expert→transfer→audit)로 다시 쓴다. 재발 방지로 submit_check.sh에 '데모_대본·발표_10분이 verify.js보다 오래됐으면 실패' 게이트 1줄을 넣는다.**

- **왜**: 1d7fc32는 verify.js 713줄을 바꾸면서 이 두 문서를 건드리지 않았다(git show --name-only 실측). 발표 시연이 전체의 44%이고 10분 초과가 −9점/분(발표_10분_구성안.md:3)인 심사에서, 대본대로 진행하면 발표자가 존재하지 않는 화면을 가리키며 첫 20초를 소모하고 이후 미션 4·5·자유탐구·완료 구간에서 같은 일이 반복된다. 라이브 데모가 대본과 어긋나는 모습은 '완성된 도구'가 아니라 '방금 바꿔서 본인도 모르는 도구'로 읽혀 ⑤에 직격한다. 코드 프리즈 이후에도 수정 가능한 문서라 비용 대비 회수가 크다.
- **공수**: 반나절 (문서 재작성 3~4시간 + 스톱워치 리허설 3회 1시간 + 게이트 1줄)
- **예상 점수 변화**: ⑤ +3~5, ② +2 (합 +5~7)

### 우선순위 3

**자기 게이트를 초록으로 되돌리고 문서-현실 수치를 동기화하면서, 같은 커밋에 1줄짜리 상태 버그 3건을 넣는다. 문서: README.md:192 '45개 커밋'→47, AI_활용_기록.md:41·183 동일, scripts/build_deck.py:311 '커밋 39건'→47 및 :333 '약 210KB(실측 208KB)'→'약 212KB(7개 자산 실측 217,147B)' 후 python scripts/build_deck.py로 PDF 재생성, 교사_학습지.html:34의 '전체 코스 예상 55~70분'을 열거 합계와 일치하는 '70~80분'으로, 제출전_Self-reflection_최종보고서.md:47·12의 '아직 커밋·푸시·배포하지 않았다'를 배포 완료 사실로 교체. 코드 1줄 3건: verify.js:3321-3324 labBack이 state.labFrom(진입 시 저장)을 복원하게, verify.js:3775 progressed 판정에 missionCerl 작성 흔적과 missionStep을 포함, verify.js:2711 checkBack에 state.cerlStepById[m.id]=0 추가. 재발 방지로 submit_check.sh의 커밋 수 검사를 README 단일에서 3개 파일로 확장하고 7개 자산 gzip 합계 ±3% 검사를 추가한다.**

- **왜**: submit_check.sh 실측 exit 1이고 제출_체크리스트.md:70이 '하나라도 실패하면 업로드 금지'를 자기 규정으로 걸었다 — 검증 문화를 최대 차별점으로 내세운 팀이 자기 게이트 빨간불로 제출하는 것은 순수한 손실이다. 교사_학습지의 산술 모순은 교사·심사위원이 더하기만 하면 드러난다. labBack은 심사위원이 볼 가능성이 가장 높은 두 화면(완료 기록, 열관성 실험실)을 연결하는 동작이 완료 화면을 파괴하는 P1이고, 인트로에서 누르면 미션 1~4를 건너뛴 채 미션 5로 떨어진다. 세 코드 수정 모두 1줄이라 리스크가 낮다.
- **공수**: 2~3시간 (문서 6곳 + PDF 재생성 + 코드 3줄 + 게이트 2줄)
- **예상 점수 변화**: ⑤ +3~4, ② +1~2 (합 +4~6)

### 우선순위 4

**데이터 표시 무결성 4건을 고친다. ① studentEvidenceHTML(verify.js:2590-2597)의 rain 분기에서 리터럴 '서울' 4곳을 n.city로 바꾸고 cityOf(n.city).station을 라벨에 붙이며, 하드코딩된 1mm/50mm 행 옆에 학습자가 실제로 고른 n.thr 행을 추가한다. ② extremeTableHTML(verify.js:1213-1214)의 과거·현재·변화 3열 정밀도를 하나로 통일한다(fmtNum 고정 또는 변화 열을 표시된 두 값의 차로 계산) — 그리고 verify_solar_terms.py:422의 shown() 규약과 M축, README.md:273-275를 함께 고친다. ③ sensitivityAt(verify.js:508-521)이 이미 정수 반올림된 windows doy를 다시 차분하는 대신 파이프라인이 계산해 둔 c.sensitivity(min:4, max:17.2, long:9.6)를 쓰게 되돌린다 — 그러면 화면이 '+4~+17.2일, 30년 +9.6일'이 되어 README.md:269·데모_대본.md:54의 '+10일'과 정합한다. ④ 완료 화면 배지(verify.js:2855)에 '⑤ 관측과 모형'을 추가하고 missionSequence에 미션 5 lab 단계를 넣는다(if (m.lagMode) seq.push('lab')).**

- **왜**: ①은 지역 칩을 누른 학습자가 증거 화면(서울 89.2→82.8)과 두 화면 뒤 전문가 화면(부산 81.6→71.6)에서 서로 다른 지역·다른 숫자를 읽는 결함이고, 하필 '자료의 범위를 밝혀라'를 가르치는 미션이며 같은 화면 deepHintOf가 '지역을 바꿔 보세요'로 이 조작을 유도한다. 게다가 학습자가 조작한 기준값이 증거 카드·CERL 화면에 한 번도 나타나지 않아 '내가 정한 기준으로 내가 읽은 증거'라는 핵심 주장이 미션 4에서 끊긴다. ②는 32행 중 16행이 자기 검산에 실패해(4.2 + 18.3 ≠ 23) '숫자를 정확한 범위로 말하라'를 가르친 앱의 대표 실측 표가 암산 한 번으로 반증된다. ③은 방향이 지적과 반대다 — 문서가 옳고 화면이 이중 반올림으로 틀렸으며, 정확한 필드는 verify.js:1059에서 읽히기만 하는 죽은 변수다. ④는 요강 예시 6종이 전부 시뮬레이터인데 이 앱의 유일한 시뮬레이터가 기본 경로 밖이라는 ④ 감점의 직접 원인이다.
- **공수**: 반나절 (코드 약 40줄 + 게이트/문서 3곳)
- **예상 점수 변화**: ① +1~2, ③ +1~2, ④ +1~2, ⑤ +1~2 (합 +4~7)

### 우선순위 5

**학생_파일럿_검증계획.md의 무설명 테스트를 10명에게 실제로 실시하고, 실측 중앙값·90백분위로 verify.js:1909·README.md:39-52·교사_학습지.html:34의 시간 표기를 전부 교체한다. 동시에 CERL 필수 게이트에 최소 내용 검사를 넣는다(bindStudentCerl 2653-2657에서 localAudit의 지역·기간·기준 슬롯 누락과 프롬프트 주입만 차단, scope/causal 경고는 표시만 — 오탐이 미탐보다 무겁다는 기존 원칙 유지, 약 10줄). 미션 2~5의 CERL을 '근거 + 한계' 2필드 필수로 낮춰 자유서술 20필드를 12필드로 줄이고 미션 1만 4필드를 유지한다.**

- **왜**: ③(10점)의 유일한 증거이고, '학습 효과를 어떻게 측정했나'라는 질의에 현재 제시할 수 있는 것은 미실시 계획서와 빈칸 양식뿐이다. 시간 표기도 파일럿 없이는 방어 불가다 — 가장 짧은 미션 2조차 12화면·13회 입력·필수 서술 4필드여서 '핵심 2~3분'은 화면당 15초를 요구하고, 심사위원이 직접 해 보면 현장에서 반증된다. 실측이 30분이면 30분이라 말하는 편이 훨씬 강하다. CERL 내용 검사는 '학생이 대충 쓰면 어떻게 되나요'라는 시연 질문에 대한 유일한 방어이고, 필드 축소는 EDU-05(시간)와 JUDGE-01(화면 수)을 함께 완화한다.
- **공수**: 1~2일 (파일럿 섭외·실시·집계 + 코드 약 16줄 + 문서 3곳). 8/21까지 3주 남아 실행 가능.
- **예상 점수 변화**: ③ +2~3, ⑤ +1~2 (합 +3~5)

---

## 4. 확증 결함 30건 (심각도·데모 노출도 순)

### [P0] F01 · 학습 계약 / 체험 설계

**21:50 리팩터가 '자료 없는 예측' 화면에 정답을 인쇄했다 — 봉인 문항 위 goal-chip이 미션 1·4·5의 답을 적어 주고, LENS·인트로에도 같은 누출이 있다 (회귀)**

- **위치**: prototype/verify.js:2244-2250 (missionStepHeader) → 2280 (renderPrediction) · 2247 → 2699 (renderSelfCheckStep) · 2363 (LENS.items[2]) · 1908-1909 (renderIntroMethod) · 회귀 대조: git show 1d7fc32^:prototype/verify.js:2199
- **왜 문제인가**: missionStepHeader가 모든 단계에 `'<span class="goal-chip">' + m.goal + '</span>'`을 무조건 렌더하고 renderPrediction이 그 헤더를 그대로 붙인다. 리팩터 이전(2199행)의 예측 화면 칩은 '자료를 보기 전 예측'이라는 내용 중립 라벨이었고 m.goal은 봉인 이후 화면에만 있었다 — 이 커밋이 헤더를 통합하면서 목표 문구가 예측 화면으로 올라왔다. 실측 대조: 미션1 칩 '목표 ① 절기(천문 날짜) ≠ 기후(관측)' vs PRE 정답 b('절기 날짜는 그대로인데 … 늦게까지 이어진다'), 미션4 칩 '목표 ③④ 같은 자료, 반대 결론' vs 정답 flip('방향이 서로 다를 것이다'), 미션5 칩 '… 또 하나의 이유 — 계절 지연' vs 정답 later. 5미션 중 3개에서 사전 예측이 무효화된다. 같은 칩이 자가진단 화면(2699)에도 나와 미션 2·4·5의 정답 어휘를 인쇄하고, 필수 LENS(2363)는 미션 1의 결론 수치 '68일 vs 31일'을 조작 전에 인쇄하며(데이터 실측: 서울 exceedDays past['25']=30.8, present['25']=68 — 미션 1 기본 도시·기본 기준의 값 그대로), 직전 화면 renderIntroMethod도 '① 절기와 기후는 어떻게 다를까'를 인쇄한다. 같은 함수 위 2272-2274행 주석이 '예측 화면에는 정답 성격을 암시하는 보조문구를 한 글자도 싣지 않는다'고 스스로 선언하므로 팀 기준으로도 위반이다. state.pre는 완료 화면 성장 진술(2822-2828)과 학생 파일럿 사전/사후 비교의 기준선이며, 심사위원이 미션 1 첫 화면에서 10초 안에 볼 수 있다.
- **수정**: missionStepHeader에 step 분기 추가 — predict에서는 goal-chip 대신 '자료를 보기 전 예측'을, check에서는 '목표 ①'처럼 번호만 렌더(2~3줄). LENS.items[2].t를 미션 1과 겹치지 않는 문장으로 교체(1줄). renderIntroMethod:1908-1909의 목표 열거를 중립화. 회귀 방지로 verify_solar_terms.py 학습계약 축에 'renderPrediction 출력에 m.goal 문자열이 없다' assert 추가.

### [P0] F02 · 발표·데모 문서 / 실현 가능성

**라이브 데모·발표 문서 5건이 21:50 리팩터 이전 흐름을 가정한다 — 존재하지 않는 화면을 가리키고, 50초 슬롯에 10클릭 동선이 들어가며, 부르는 버튼 이름 하나는 이미 사라졌다**

- **위치**: 데모_대본.md:12-14·22-23·30·33-35·38·123·127 / 발표_10분_구성안.md:33·38-41·46-51·63-68·71-78·86·125 vs prototype/verify.js:1880-1896 (renderIntro) · 1898-1914 · 2236-2249 · 2299-2330 · 2622 · 2857-2860 · 2877 · 3494-3512 / git show --name-only 1d7fc32
- **왜 문제인가**: 1d7fc32가 건드린 파일은 README.md·verify_solar_terms.py·index.html·verify.css·verify.js·학습단계_분할_기획서.md 6개뿐이고 두 대본은 포함되지 않았다(실측). 불일치: (a) 대본 §1 [0–10초] '아래 미니차트에서 기준선이 스스로 훑으며 카운트업'과 '시작하기를 누르면 차트 위에 +13일' — renderIntro의 DOM은 badge·h1·버튼 4개·리드문·링크뿐이고 countUp|introSvg|miniChart grep 0건이며, introStart는 텍스트 전용 renderIntroMethod로 간다. (b) 콜드 스타트에서 그래프 최초 노출까지 실측 10클릭·8화면(intro→method→predict→lens×3→orient→explore)인데 발표_10분은 50초, 대본은 25초를 배정했다 — cbd3c6e에서는 3클릭이었으므로 리팩터가 늘린 것이다. (c) 발표 §2:49의 '핵심 2~3분 칩'은 미션 화면에 없다(time-chip은 renderLab:3260에만 존재). (d) '판정 화면의 빈 4칸'은 verify.js:2622 + verify.css:951로 한 번에 한 칸만 보인다. (e) audit까지 3개 CTA를 더 눌러야 AI 감사 UI에 닿고 그 화면은 '선택 심화'로 라벨링돼 있다. (f) 자유탐구 3탭·완료 4탭 경계를 대본이 넘고, '🌍 지구 전체는 어떨까?'는 코드에 없다(현재 2877행 '🌍 이산화탄소와 지구 평균기온 자료 펼치기'). (g) 발표_10분은 슬라이드를 10장/8장이라 세 번 적었으나 실물은 11장이고 p5(모형)에 시간 예산이 0이다 — 그런데 §4:105가 '데이터 뷰어 아닌가'의 최대 방어카드로 그 슬라이드를 지정한다. 발표_10분_구성안.md:3이 인용한 감점 규정은 1분 초과 −9점 / 2분 −15점이며 시연이 전체의 44%다.
- **수정**: 두 문서를 현재 코드 순서로 전면 재작성(반나절). 미션1 lens 6클릭을 '이 30초 관문이 절기·날씨·기후를 갈라 줍니다'로 강점화해 정직하게 보여주고, §2 1:55–2:45를 1:55–3:00으로 늘리고 §1에 p5(모형) 40~50초 구간을 신설한다(§4 자유탐구 또는 §6에서 회수). 탭 클릭 단계와 실제 버튼 라벨을 verify.js 문자열과 1:1 대조해 명시. 재발 방지로 submit_check.sh에 '두 대본이 verify.js보다 오래됐으면 실패' 게이트 1줄 추가.

### [P1] F03 · 형성평가 / 학습 효과

**CERL 통과 조건이 길이 33자뿐이다 — '가나다라마' 도배로 완료 배지 4개가 발급되고, 같은 문장 4번 복붙은 로컬 감사관까지 '좋아요'를 붙이며, 겨울 확장에서는 여름 문장이 그대로 재봉인된다**

- **위치**: prototype/verify.js:2554-2559 (CERL_FIELDS min 5/12/8/8) · 2571-2577 (cerlErrors) · 2261 (단계 가드) · 2798-2809 (완료 관문) · 2853-2855 (배지) · 3566-3660 (localAudit) · 2790-2793 (auditWinter) · 1704-1707 (season 토글)
- **왜 문제인가**: cerlErrors는 `(d[f.k]||'').trim().replace(/\s/g,'').length < f.min`만 본다 — 내용 검사가 0이고, 단계 가드와 완료 관문도 이 함수만 재사용한다. 실행 검증: ① '가나다라마' 류 165자로 5미션을 통과해 완료 화면이 'CERL 5편 완료'·'매 미션에서 주장·근거·추론·한계를 직접 작성했습니다'(2854)·목표 ①②③④ 배지 4개를 발급한다. ② 한 문장을 네 칸에 복붙하면 localAudit이 evidence_status='ready'와 '좋아요. 지역·기간·기준이 문장에 들어 있어…'를 반환한다. ③ orient 브리프를 복붙해도 'ready'다 — 필드 간 중복 검사도, 앱 자체 문장과의 대조도 없다. 규칙 감사 엔진 localAudit은 과장·범위·인과·주입을 실제로 잡는 강한 구현인데 호출 지점이 감사 화면의 보조 버튼 하나뿐이고 어떤 단계 전이도 그 결과를 요구하지 않는다. 겨울 확장 경로도 성립한다 — auditWinter(2792)와 explore의 계절 토글 season()(1707) 모두 predicts/selfChecks만 지우고 missionCerl·cerlSubmitted·missionDraft를 남기므로, 길이 조건이 이미 충족된 여름 문장이 textarea에 프리필된 채 '다음 요소 →' 4클릭으로 즉시 재봉인되고 여름 문장이 미션 5의 최종 산출물로 '내 기록'에 남는다. 심사위원이 '학생이 대충 쓰면 어떻게 되나요'를 직접 시연할 수 있는 형태다.
- **수정**: cerlErrors 또는 bindStudentCerl 마지막 분기(2653-2657)에 최소 내용 검사 3줄 추가 — (a) 공백·구두점 제거 후 네 필드 중 두 개가 동일하면 반려, (b) 근거 필드에 숫자 1개 + 지역/기준 토큰 1개 요구, (c) brief·task 문자열과 30자 이상 연속 일치하면 반려. localAudit의 슬롯 누락·주입만 필수 차단으로 승격하고 scope/causal 경고는 표시만 유지(오탐이 미탐보다 무겁다는 기존 원칙 보존, 약 10줄). auditWinter와 season()에 `delete state.missionCerl.lag; delete state.cerlSubmitted.lag; delete state.missionDraft.lag; delete state.evidenceById.lag;` 추가(각 4줄). 완료 배지를 state.selfChecks 정답 여부에 연동한다.

### [P1] F04 · 상태 관리 / 체험 설계

**열관성 실험실의 유일한 복귀 버튼이 항상 startMission(미션5)을 호출한다 — 완료 화면에서 들어갔다 나오면 완료 화면이 파괴되고, 인트로에서 들어가면 미션 1~4를 건너뛴 채 미션 5로 떨어진다**

- **위치**: prototype/verify.js:3321-3324 (labBack) · 2147-2166 (startMission) · 진입점 6곳 1894/2440/2542/2789/2922/3542 · 693-699 (진행바 칩) · 3491-3545 (renderFree에 미션 복귀 버튼 없음)
- **왜 문제인가**: labBack은 진입 위치를 저장하지 않고 무조건 `startMission(MISSIONS.indexOf('lag'))`를 호출한다(실측). startMission(4)는 visited/usedThr를 지우고 markDoy=null·lagRevealed=false·lagSeason='summer'·ti=11로 되돌리며 predicts['lag']·selfChecks['lag']를 삭제한 뒤 missionStep='predict'로 save()까지 한다. 재현: 5미션 완주 → 완료 화면 '4. 더 탐구하기' → '🔬 열관성 실험실' → '← 미션으로 돌아가기' → 완료 화면이 사라지고 미션5 '1/8 · 예측'으로 이동, phase='mission'이 저장된다. 진행바의 미완료 칩은 span이라 클릭 불가하고 renderComplete는 미션5 audit의 toFree(2788)에서만 도달하므로, 복귀에 최소 12조작이 필요하고 새로고침도 소용이 없다. 심사위원이 볼 가능성이 가장 높은 두 화면(완료 기록, 열관성 실험실)을 연결하는 동작이 앱을 뒤로 되돌린다. 같은 사고가 미션5 증거·감사 화면의 '선택 심화' 링크와 자유탐구에서도 발생하고, 인트로 진입(1894)에서는 더 나쁘다 — 미션을 하나도 하지 않은 학습자가 '미션 5 / 5'에 곧장 떨어져 미션 1~4를 건너뛴 채 학습이 시작된다. 자유탐구(3491-3545)에도 미션 복귀 버튼이 없어 실험실·자유탐구 두 곳이 공통으로 '나갈 문은 하나, 그 문이 미션5를 초기화한다' 구조다.
- **수정**: renderLab 진입 시 `if (state.phase !== 'lab') state.labFrom = { phase: state.phase, mi: state.mi, missionStep: state.missionStep };`을 저장하고, labBack에서 그것을 복원해 routeFromState()를 호출한다(3757-3765가 이미 complete/free/terms/mission을 처리). 버튼 라벨도 복귀 지점에 맞게 분기하고, load()에 labFrom 타입 검증을 추가한다. renderFree에도 같은 방식의 미션 복귀 버튼을 넣는다.

### [P1] F05 · 제출 운영 / 실현 가능성

**팀 자체 제출 게이트가 지금 실패 상태다(실측 exit 1) — 커밋 수가 제출물 3종에서 39/45/47로 세 갈래이고, 자기 규정상 업로드 금지 상태다**

- **위치**: scripts/submit_check.sh:88-92 (실행 결과 exit 1) · README.md:192 · AI_활용_기록.md:41·183 · scripts/build_deck.py:311·333 · 제출_체크리스트.md:70 · 제출전_Self-reflection_최종보고서.md:54
- **왜 문제인가**: `bash scripts/submit_check.sh` 실측 결과 §7에서 '✗ README 커밋 수 표기가 실제(47)와 2 이상 다릅니다'와 '실패 1건 — 고치기 전에는 제출하지 마세요', 종료코드 1(다른 25개 항목은 전부 통과). git rev-list --count HEAD = 47인데 발표자료 PDF p9는 '커밋 39건', README.md:192는 '45개 커밋', AI_활용_기록.md는 '커밋 45개'다. 게이트는 README만 검사하므로(:91) PDF의 39와 AI_활용_기록의 45는 애초에 검사되지 않는다. 제출_체크리스트.md:70이 '전부 통과해야 제출. 하나라도 실패하면 원인 해결 전 업로드 금지'를, 제출전_Self-reflection_최종보고서.md:54가 '발표 문서의 수치가 재현값과 다르면 제출 중단'을 자기 규정으로 걸었으므로 현재 상태는 팀 스스로 정한 금지 상태다. 여기에 자산 크기도 낡았다 — 7개 핵심 자산 gzip 실측 217,147B(212.1 KiB)인데 build_deck.py:333은 '약 210KB(실측 208KB)', Self-reflection:23은 212,742B다. 검증 문화를 최대 차별점으로 내세운 팀이 자기 게이트 빨간불로 제출하는 것은 순수한 손실이며, 심사위원이 저장소 커밋을 세면 세 숫자가 서로도 다르다는 사실이 정량 주장 전체(8,303건·100/100)의 신뢰도로 전이된다.
- **수정**: README.md:192, AI_활용_kimi록.md:41·183, build_deck.py:311을 47로, build_deck.py:333의 210KB/208KB를 '약 212KB(실측 217,147B)'로 고치고 python scripts/build_deck.py로 PDF 재생성. submit_check.sh:88-92의 커밋 수 검사를 README 단일에서 3개 파일로 확장하고(±1 허용 유지) 7개 자산 gzip 합계 ±3% 검사를 §7에 추가한 뒤 다시 돌려 exit 0을 확인한다.

### [P1] F06 · 데이터 무결성 / 학습 효과

**미션 4의 증거 카드가 지역을 '서울'로 하드코딩하고 학습자가 조작한 기준값을 아예 보여 주지 않는다 — 인접 두 화면이 서로 다른 지역·다른 숫자를 인쇄한다**

- **위치**: prototype/verify.js:2590-2597 (studentEvidenceHTML의 rain 분기) vs 203-217 (rain verdict) · 2531 (evidence 단계) · 2614 (write 단계) · 2419-2431 (renderExplore의 cityChips) · 2135 (deepHintOf)
- **왜 문제인가**: rain 분기가 리터럴 '서울'을 라벨 2곳·exceed() 인자 2곳에 박아 둔다(실측). 같은 미션의 verdict는 `rainAt(n.city, 1)`과 `cityOf(n.city).station`으로 실제 선택 지역을 쓴다. 미션 4는 lockCity:false이므로 16지역 칩이 렌더되고 bindCityChips가 state.city를 바꾼다. 데이터 실측: 서울 1mm 89.2→82.8 / 50mm 6.6→7.8, 부산 1mm 81.6→71.6 / 50mm 7.8→8.4. 재현: 부산 칩 클릭 → 프리셋 → '증거 정리하기' → 증거 화면(6/8)이 '서울 · 1mm 이상 과거 89.2일 → 현재 82.8일'을 보여 주고, 학습자는 CERL 근거 프롬프트('지역·기간·기준과 화면에서 읽은 숫자를 넣으세요')에 따라 '서울, 89.2→82.8'을 쓴다. 두 화면 뒤 전문가 비교(8/8)는 '부산(부산 관측소)에서 … 81.6일 → 71.6일'을 인쇄한다. '자료의 범위를 밝혀라'를 가르치는 미션에서 앱 자신이 지역을 오표기하고, 같은 증거 화면의 deepHintOf가 '지역을 바꿔 보고 … 같은 결론이 나오는지 확인해 보세요'로 이 조작을 유도한다. 더 심각한 것은 조작 결과가 증거에 반영되지 않는다는 점이다 — 행이 1mm/50mm 두 개로 고정돼 있어 과제문이 시키는 1→10→30→50 조작의 30mm가 증거 카드와 CERL 화면에 한 번도 나타나지 않고, 현재 기준값은 expert 단계(2213)에서만 등장한다. '내가 정한 기준으로 내가 읽은 증거'라는 앱의 핵심 주장이 미션 4에서만 끊기고, 프리셋 한 번으로 재현되므로 데모 노출도가 높다. 현 게이트는 studentEvidenceHTML의 도시·기준 인자를 검사하지 않는다.
- **수정**: rain 분기의 '서울' 4곳을 n.city로 바꾸고 cityOf(n.city).station을 라벨에 붙이며, 고정 1mm/50mm 행 옆에 `n.thr !== 1 && n.thr !== 50`일 때 현재 기준 행을 추가한다(약 6줄). 회귀 방지로 verify_solar_terms.py 학습계약 축에 'studentEvidenceHTML 내 지역명 리터럴 금지' 정적 검사를 추가한다.

### [P1] F07 · 산출물 무결성 / 학습 효과

**이미 맞힌 자가진단이 '선택 확장' 버튼 한 번으로 삭제돼 '미응답'으로 인쇄되는데, 같은 화면은 'CERL 5편 완료 · 매 미션에서 직접 작성했습니다'를 무조건 선언한다**

- **위치**: prototype/verify.js:2792 (auditWinter의 delete selfChecks) · 1707 (explore 계절 토글) · 2511 ('새 조건으로 증거 갱신') · 2769 (state.done 추가) · 2798-2809 (완료 관문) · 2845 (자가진단 기록) · 2852-2854 (완료 단정)
- **왜 문제인가**: 세 지점이 state.selfChecks를 삭제한다 — 감사 화면의 '겨울·동지에도 적용해 보기'(2792), explore의 계절 토글(1707), 증거 조건 변경 후 '새 조건으로 증거 갱신'(2511). 그런데 state.done에는 이미 'lag'가 들어 있고(2769) cerlSubmitted도 유지되므로 renderComplete 관문(2798-2809)이 cerlSubmitted와 cerlErrors만 보고 통과시킨다. 재현: 미션5 자가진단을 정답으로 마친 학생이 audit 화면에서 '겨울·동지에도 적용해 보기'를 눌렀다가 겨울 조작(markDoy·lagRevealed)을 끝내지 않고 진행 칩으로 이탈 → 완료 화면 2845행이 미션5를 '자가진단 미응답'으로 인쇄하는데, 같은 화면 2852-2854행은 'CERL 5편 완료'와 '매 미션에서 주장·근거·추론·한계를 직접 작성했습니다'를 단정한다. 즉 학생이 실제로 수행한 형성평가 기록이 선택 확장 링크 하나로 소실되고, 교사가 회수하는 유일한 산출물(기록 복사·인쇄)이 학습 사실과 어긋난다. 이것은 리팩터가 만든 삭제 경로와 완료 관문의 어긋남에서 나온 신규 조합이며 4차 레드팀·Self-reflection 모두 감사하지 않았다.
- **수정**: 세 삭제 지점을 '미션 재수행 진입'과 짝지어 처리한다 — auditWinter와 season()에서 selfChecks를 지울 때 state.done에서도 해당 미션을 제거해 완료 관문이 재수행을 요구하게 하거나(2줄), 반대로 겨울/여름 자가진단을 별도 키(selfChecks['lag:summer'] / ['lag:winter'])로 분리해 삭제 없이 공존시킨다. 후자가 기록 손실이 없어 더 낫다. 아울러 완료 화면 2852-2854의 단정문을 실제 기록과 연동한다(자가진단 미응답 미션이 있으면 '자가진단 N/5 응답'으로 정직하게 표기).

### [P2] F08 · 수업 운영 / 실현 가능성

**교사 학습지가 자기 산술을 어긴다 — 열거 항목 합계 69~76분인데 같은 문서가 '전체 코스 예상 55~70분'이라 쓰고, '미션당 핵심 2~3분'은 12화면·서술 4필드와 물리적으로 양립하지 않는다**

- **위치**: prototype/교사_학습지.html:34 (선언) vs :49·57·60·68·72·76·86·104·118 (열거) · prototype/verify.js:1909 · README.md:39-52 · 근거 코드 2236-2243 · 2554-2559 · 2644-2656
- **왜 문제인가**: 같은 문서의 항목을 더하면 사전 5 + 도입 3~5 + 활동1 (2~3)+8 + 활동2 (2~3)+6 + 활동3 (2~3)+5 + 활동4 (2~3)+6 + 활동5 (2~3)+6 + 활동6 3+7 + 정리 10 = 최소 69분 ~ 최대 76분으로, 34행이 선언한 상한 70분을 넘는다(실측 계산). 교사가 수업을 설계하며 더하면 즉시 드러나고 제출물이므로 심사위원도 검산할 수 있다. '미션당 핵심 2~3분'도 성립하지 않는다 — 가장 짧은 미션 2조차 실제 setStage 재렌더가 12화면(predict1 + orient1 + explore1 + evidence1 + write4 + check2 + expert1 + audit1)이고 필수 자유서술 4필드(공백 제외 최소 33자)에 12~13회 입력이 필요하므로 화면당 15초를 요구한다. 5미션 합계는 최소 27분(강한 스킵) ~ 45분(정독) 규모다. 심사위원이 발표 중 미션 하나를 직접 끝까지 해 보면 verify.js:1909가 인쇄한 그 수치가 눈앞에서 틀리고, '이걸 정말 1차시에 쓸 수 있는가'라는 ⑤의 핵심 질문에 팀이 답을 갖고 있지 않다는 사실이 노출된다. 완화 요소는 세 곳 모두 '학생 파일럿 전 추정치'로 스스로 유보를 달아 둔 점이다.
- **수정**: (a) 교사_학습지.html:34의 '55~70분'을 열거 합계와 일치하는 '70~80분(2차시)'으로 고친다(1줄). (b) 파일럿 실측 후 verify.js:1909·README.md:39-52·학습지 34행의 수치를 중앙값·90백분위로 교체하고, 그전에는 '미측정 추정 상한'임을 문구에 남긴다. (c) 실제 부담을 줄인다 — 미션 2~5의 CERL을 '근거 + 한계' 2필드 필수로 낮추고(cerlErrors에 required 플래그, 약 6줄) 미션 1만 4필드를 유지하면 자유서술 20필드 → 12필드, 화면 68 → 60으로 줄어 F14·F19도 함께 완화된다.

### [P2] F09 · 과학적 무결성 / 데이터 표시

**'폭염·열대야 실측' 표의 과거·현재·변화 3열이 32행 중 16행에서 산술적으로 맞지 않는다 (4.2 + 18.3 ≠ 23)**

- **위치**: prototype/verify.js:1209-1214 (extremeTableHTML) · fmtDays는 :484 · fmtNum은 :242 · README.md:273-275 · processing/verify_solar_terms.py:422 (shown 규약)
- **왜 문제인가**: fmtDays는 v>=10일 때 정수 반올림, v<10일 때 소수 1자리를 유지하는데 변화 열만 fmtNum으로 소수 1자리를 그대로 인쇄한다. 데이터를 렌더 규칙에 통과시켜 전수 재현한 결과 8지점 × 4지수 = 32행 중 정확히 16행(50%)이 (표시된 과거 + 표시된 변화) ≠ (표시된 현재)였다: 서울 폭염일 4.2 | 23 | +18.3(4.2+18.3=22.5, 오차 −0.5), 서울 열대야 3.2 | 32 | +28.3(오차 −0.5), 서울 결빙일 25 | 14 | −11.2(오차 −0.2), 인천 열대야 1.4 | 28 | +26.8, 인천 결빙일 26 | 20 | −6.3, 대구 폭염일 25 | 45 | +20.2, 대구 열대야 7.6 | 23 | +15.1. 심사위원이 기상청 기준표 서랍을 열고 23 − 4.2 = 18.8을 암산하면 표의 +18.3과 0.5일 차이가 난다. '숫자를 정확한 범위로 말하라'를 8분간 가르친 앱의 대표 실측 표가 같은 행 안에서 자기 검산을 통과하지 못한다 — 과학적 무결성을 최대 차별점으로 내세운 팀에게 가장 아픈 종류의 결함이다. 수정 범위가 한 줄이 아니라는 점을 유의하라: 이 반올림은 우발적 버그가 아니라 verify_solar_terms.py:422의 shown() 함수가 '화면이 fmtDays로 10 이상을 정수 반올림하므로 문서도 그 값을 써야 한다'며 문서 전파까지 강제하는 규약이다.
- **수정**: 세 셀의 정밀도를 하나로 통일한다 — verify.js:1213-1214에서 fmtDays 대신 fmtNum(v.past)/fmtNum(v.present)를 쓰거나, 변화 열을 표시된 두 값의 차로 계산해 인쇄한다. 그리고 같은 커밋에서 verify_solar_terms.py:422의 shown() 규약과 M축, README.md:273-275를 함께 고친 뒤, '표시된 과거 + 표시된 변화 == 표시된 현재'를 32건 assert로 추가한다(현재 이 검사가 없다).

### [P2] F10 · 과학적 무결성 / 데이터 표시

**민감도 수치가 화면 +9 vs 문서 +10으로 어긋난다 — 그런데 원인이 통상 진단과 반대다: 파이프라인은 이미 정확한 값(9.6)을 데이터에 넣었고 화면이 이중 반올림으로 틀렸으며, 정확한 필드는 읽히기만 하는 죽은 변수다**

- **위치**: prototype/verify.js:508-521 (sensitivityAt) · 523-530 (sensitivityText) · 1059 (`var sens = c.sensitivity;` — 사용되지 않음) · processing/build_solar_terms.py:162·198-201 · README.md:269 · 데모_대본.md:54
- **왜 문제인가**: 데이터를 직접 로드해 확인한 결과 solar_terms_data.js에 `서울 sensitivity = {thr:25, current:13.2, min:4, max:17.2, windows:26, longSpan:[1996,2025], longYears:30, long:9.6}`이 이미 들어 있다 — build_solar_terms.py:198-201이 반올림 전 doy 평균으로 계산한 정확한 값이다. 문서의 '+10일'은 9.6의 올바른 반올림이고, 화면의 '+9일'은 sensitivityAt이 이 필드를 무시하고 이미 정수로 반올림된 windows의 doy(build_solar_terms.py:162의 int(round(...)))를 다시 차분해 만든 이중 반올림 산물이다(base doy 242, long.last['25'] 251 → +9). 최대값도 17.2 → +17로 깎인다. 결정적으로 정확한 필드는 verify.js:1059에서 `var sens = c.sensitivity;`로 읽히기만 하고 파일 전체에서 한 번도 사용되지 않는다(grep 확인). 즉 앱은 자기 파이프라인이 계산한 값보다 부정확한 값을 화면에 인쇄하고 있다. 실패 시나리오는 실재한다 — 발표자가 대본대로 '30년으로는 +10일입니다 — 저희가 고른 창이 특별히 유리한 값이 아니라는 걸 앱이 스스로 밝힙니다'라고 말하는 순간 심사위원이 방법 서랍에서 '+9일'을 본다. 하필 '우리 숫자의 흔들림을 정직하게 공개한다'를 증명하려는 문장에서 말과 화면이 어긋난다. 게이트가 이를 잡지 못하는 이유도 확인됐다 — verify_solar_terms.py:405의 F축은 줄에 '지연/하지보다/동지보다'가 있어야 검사하는데 해당 문장은 '시차'만 쓴다.
- **수정**: 문서를 +9로 내리지 마라 — 정확도가 떨어진다. sensitivityAt(508-521)이 `c.sensitivity`의 min/max/long/longYears/longSpan을 우선 사용하고 없을 때만 현재 방식으로 폴백하게 고친다(약 6줄). 그러면 화면이 '+4~+17.2일, 30년 +9.6일'이 되어 README.md:269·데모_대본.md:54와 정합한다. verify_solar_terms.py F축에 서울 25℃ 민감도 3수치(창 최소·최대·장기창) 문서 대조 assert를 추가한다.

### [P2] F11 · 교실 운영 / 실현 가능성

**공용 기기 인수인계 가드가 '미션 1개 완료'와 '20분 유휴'를 모두 요구해서, 가장 흔한 사고(미션 1 도중 이탈)를 놓친다 — 다음 학생이 앞사람의 봉인된 예측과 CERL 초안을 물려받는다**

- **위치**: prototype/verify.js:3773-3781 (askResume) · 2630-2639 (CERL 입력마다 save) · 577 (missionStep 복원) · 600 (단일 localStorage 키) · 3739-3746 (수동 초기화만)
- **왜 문제인가**: 실측 코드: `var idle = state.savedAt && (Date.now() - state.savedAt) > 20*60*1000; var progressed = (state.done && state.done.length) || state.phase === 'complete' || state.phase === 'free'; if (!idle || !progressed) return;` — missionStep·missionCerl·cerlSubmitted를 전혀 보지 않는다. state.done은 감사 화면 도달 시에만 채워지므로(2760) 미션 1 중간에 멈춘 상태는 progressed=false다. 반면 bindStudentCerl은 textarea input마다 missionCerl·missionDraft를 저장하고 save()하며, load()가 missionStep을 화이트리스트로 복원해 부팅 시 정확히 그 마이크로 단계로 되돌린다. 재현 A(교시 간): 1교시 학생 A가 미션 1의 CERL 근거 칸까지 쓰다 반납(done=[], missionStep='write') → 3교시 학생 B가 같은 크롬북을 열면 확인창 없이 A의 문장이 채워진 CERL 화면에서 시작한다. 이 앱의 1번 학습 계약(자료 전 예측)이 B에게는 발동하지 않고, 완료 화면 '기록 복사/인쇄'로 제출하는 산출물에 A의 문장이 섞인다. 재현 B(태블릿 2인 공유): A가 미션 3까지 완료한 뒤 15분 만에 넘기면 idle=false여서 역시 묻지 않는다. 3770-3772행 주석이 '교실 공용 PC에서 앞 사람 진행을 그대로 물려받는 것이 가장 흔한 사고다'라며 막겠다고 명시한 시나리오가 정확히 그대로 발생한다. 부수 사실: 발표 PDF(build_deck.py:333)가 "'↺ 처음부터'로 공용 PC 대응"을 주장하는데 교사_학습지.html 전문에 '처음부터'·'초기화'·'공용' 세 단어가 0회여서 그 수단이 교사에게 전달되지 않는다.
- **수정**: askResume 조건을 단순화한다 — `if (!state.savedAt) return;`로 바꿔 저장된 기록이 있으면 항상 '이어서 / 새로 시작'을 묻거나, 최소한 progressed 판정에 CERL 작성 흔적과 `state.phase === 'mission' && state.missionStep && state.missionStep !== 'predict'`를 포함시킨다(1~3줄). idle 임계를 남기려면 20분 → 3분으로 낮춘다. 교사_학습지.html의 준비물·운영 안내에 '학생 교체 시 헤더 ↺ 처음부터를 누르게 한다' 한 줄을 추가하는 것이 코드 수정보다 먼저다.

### [P2] F12 · 과학 / 오개념

**온실효과 슬라이더가 실물 단위(W/m²)로 표기되지만 이 모형의 기후민감도는 실제의 1/4(2×CO₂ = 0.71℃)이고, 슬라이더를 올릴수록 적합도가 개선되는 지점이 있어 '제주는 온실효과가 강하다'는 오개념을 앱이 성공 피드백으로 강화한다**

- **위치**: prototype/verify.js:3188 (EBM.B = 5.2) · 3186-3187 (A=172의 서울 튜닝 주석) · 3225-3232 (labBestDepth가 현재 ghg로 적합 계산) · 3284-3288 (슬라이더 라벨) · 3444-3445 (판정 문구) · 3470-3475 (발견 ②) · 3476-3480 (발견 ③) · 3306-3313 (한계 서랍)
- **왜 문제인가**: B=5.2 W/m²/K는 복사냉각과 수평 열수송을 묶은 국지 감쇠계수인데 슬라이더 단위는 실제 강제력 단위 그대로다 — CO₂ 2배(3.7 W/m²)가 3.7/5.2 = 0.71℃로 IPCC 최적 추정 ~3℃의 1/4이고 슬라이더 최대 +12 W/m²에서도 2.31℃다. 더 나쁜 것은 labBestDepth가 `labState().ghg`를 그대로 써서 적합을 계산한다는 점이다(3228). EBM을 독립 재구현해 전수 확인한 결과 A=172가 서울 연평균에 맞춰 튜닝됐기 때문에 연평균이 높은 남부·연안에서 ghg가 지역 평균기온 보정용 자유 노브로 작동한다: 제주 ghg0 → 6.5m/MAE 3.12℃ → ghg6 2.10 → ghg12 7m/1.68℃로 단조 개선되어 판정문이 '제법 맞습니다'에서 '아주 잘 맞습니다'(fit<2.0)로 바뀐다. 강릉 3.16→2.46, 부산 2.35→1.89도 같은 패턴이고 서울만 정상적으로 악화한다(1.99→2.41). 실험실 최초 진입은 depth=2m이고 이때 16지점 전부 fit>=3.5이므로 3445의 '두께와 온실효과를 함께 조절해 보세요'가 항상 표시되어 이 조작을 앱이 유도한다. 바로 옆 발견 ②는 '온난화는 곡선 전체를 밀어 올린다'를 가르치므로 오개념과 결합된다. 기상청 주최 심사에서 기후 민감도 4배 과소평가와 '온실효과 = 지역 기온 피팅 파라미터'는 가장 잡히기 쉬운 물리 지적이며, 한계 서랍 5개 항목 어디에도 이 사실이 없다.
- **수정**: (a) verify.js:3228의 `labState().ghg`를 0으로 고정하고 문구를 '온실효과 0 기준에서 실측과 가장 잘 맞는 두께'로 명시한다(1줄). (b) verify.js:3445의 fit>=3.5 안내에서 '온실효과'를 제거해 두께만 조절하도록 바꾼다(제주·강릉은 어떤 두께로도 3.1℃ 아래로 내려가지 않으며 그것이 0차원 모형의 구조적 한계다). (c) 한계 서랍(3306-3312)에 두 항목 추가 — 'B에 열수송을 묶었기 때문에 이 모형의 온실효과 반응은 실제 지구보다 약 4배 작고, 이 슬라이더는 곡선이 통째로 올라간다는 방향만 보여 준다', '온실효과 값은 적합의 근거가 아니다 — 이 모형의 기준 연평균이 서울에 맞춰져 있어서 오차가 줄어드는 지역이 있다'. (d) 슬라이더 라벨에 '(모형 실험용 · 실제 강제력 크기와 다름)'을 붙인다.

### [P2] F13 · 체험 설계

**핵심 코스 59화면 중 관측 자료를 조작할 수 있는 화면이 explore 5개뿐이다 — CERL 4분할과 LENS 3분할이 '읽고 쓰는 화면'을 늘려 조작→반응 루프가 상시 상태가 아니다**

- **위치**: prototype/verify.js:2236-2243 (missionSequence) · 2554-2559 및 2618-2623 (CERL 4화면 분할) · 2299-2330 (LENS 문장별 화면) · 2426-2457 (조작 화면) · heroShell 호출부 1595·2093·2430·3502
- **왜 문제인가**: 시퀀스 실측: 미션1 = predict1 + lens3 + orient1 + explore1 + evidence1 + write4 + check1 + expert1 + transfer1 + audit1 = 15화면, 미션2~5 = 각 11화면 → 핵심 코스 59화면. heroShell(슬라이더·드래그·지역 칩·보기 전환) 호출부는 전 파일에 4곳(정의 1595, 튜토리얼 2093, explore 2430, 자유탐구 3502)뿐이므로 미션 흐름 중 조작 컨트롤이 있는 화면은 explore 5개(8.5%)다. evidence·write·check·expert·transfer·audit에는 heroShell이 아예 없다. 반증에서 확인된 완화 요소는 유효하다 — state.moved 이후에도 explore의 4개 조작 경로가 살아 있고, audit의 '기준 다시 조작하기'가 explore로 되돌리며, evidence·audit에서 열관성 실험실로 진입하고, 튜토리얼·24절기 궤도·자유탐구는 데모에 등장한다. 따라서 '루프가 미션당 1회로 종결된다'는 과장이고 8.5%는 분모 선택의 결과다. 그러나 요강 예시 6종과 참고 사이트 PhET·En-ROADS는 조작→반응이 화면의 상시 상태이고, 심사위원이 미션 1에서 기준선을 한 번 끌면 이후 14화면 동안 컨트롤이 등장하지 않는다는 체감은 실재한다 — ②(25점 가중)에서 '체험형'이 아니라 '가이드 워크시트'로 읽힐 구조적 근거가 된다.
- **수정**: (a) CERL 4화면 분할을 2단계(주장+근거 / 추론+한계)로 줄이거나 미션 2~5는 2필드 필수로 낮춘다 → 미션당 −2~3화면(F08·F15와 동일 수정). (b) evidence·check 화면에 heroShell 축소판(차트 + 슬라이더만)을 재삽입해 '숫자를 읽는 동안에도 기준을 다시 움직일 수 있게' 한다 — 이것만으로 조작 가능 화면이 5 → 15개로 는다. (c) 열관성 실험실을 미션 5의 필수 단계로 시퀀스에 넣는다(F14와 동일 수정).

### [P2] F14 · 학습 목표 / 독창성

**학습목표 ⑤(관측과 모형)가 필수 동선·내 기록·완료 배지 어디에도 없다 — 앱 자신이 완료 배지에서 ⑤를 뺐고, 유일한 시뮬레이터가 기본 5분 데모 밖에 있다**

- **위치**: prototype/verify.js:2855 (완료 배지 4개) · 2236-2242 (missionSequence에 lab 없음) · 2817-2846 (내 기록에 state.lab 없음) · renderLab 진입점 1894/2440/2542/2789/2922/3542 전부 선택 · README.md:14-21·45-53
- **왜 문제인가**: README §1은 목표를 5개로 선언하고 ⑤가 '물리 법칙 하나로 계절을 직접 계산해 보고, 모형이 맞히는 것과 맞히지 못하는 것의 경계를 말할 수 있다'이며 README:47은 실험실에 '핵심 3분'을 배정한다. 그런데 (i) missionSequence에 lab 단계가 없고, (ii) renderLab 진입점 6곳이 전부 선택이며(grep 확인), (iii) renderComplete의 '내 기록' 성장 목록이 state.lab을 한 줄도 쓰지 않고, (iv) 완료 배지(2855)가 '① 절기≠기후 ② 자료의 범위 ③ 기준 정의 ④ 근거만큼 결론' 4개뿐이다. 필수 동선(인트로 → 시작하기 → 미션 1~5 → 완료)만 따르면 실험실은 한 번도 열리지 않고 ⑤는 수행도 측정도 되지 않는다. 그런데 README가 '데이터 뷰어 아닌가'라는 예상 반론에 내놓는 답이 바로 이 실험실이고, 발표_10분_구성안 §4:105도 그 반론의 최대 방어카드로 이 화면을 지정한다. 요강 예시 6종이 전부 시뮬레이터류인데 이 앱의 유일한 시뮬레이션 자산이 기본 경로 밖이라는 것이 ④(15점) 감점의 직접 원인이다. 실험실 자체는 강하다 — 독립 재구현으로 지연 40일·온실효과 무관성이 모두 재현되므로 배치만 고치면 된다.
- **수정**: 미션 5의 missionSequence에 lab 단계를 넣는다 — `if (m.lagMode) seq.push('lab');`로 expert 뒤·audit 앞에 배치하고, renderLabStep이 renderLab을 감싸 '가장 더운 날을 하지 뒤로 밀 수 있는 값은 열용량인가 온실효과인가?' 1문항과 '← 전문가 비교 / 감사·완료 →' CTA만 추가한다(약 30줄). 완료 배지에 '⑤ 관측과 모형'을 추가하고 '내 기록'에 state.lab의 최종 depth·ghg와 도출된 지연일을 한 줄 기록한다. 이 수정은 F13(조작 밀도)과 발표 §4의 방어 논거를 동시에 강화한다.

### [P2] F15 · 학습 흐름

**CERL 4요소를 4화면으로 쪼개면서 앞 요소를 display:none으로 감췄다 — '내 주장을 뒷받침하는지 연결하세요'라고 지시하면서 그 주장을 화면에서 지우고, 기획서가 규정한 5단계 통합 검토가 미구현이다**

- **위치**: prototype/verify.js:2620-2624 (hidden 분기) · prototype/verify.css:951 · prototype/verify.js:2644-2656 (bindStudentCerl) · 2554-2559 (CERL_FIELDS 프롬프트) · 학습단계_분할_기획서.md §M-W (270-280행)
- **왜 문제인가**: studentCerlHTML이 `(fi === ci ? '' : ' hidden')`으로 현재 요소 외 3개를 감추고 verify.css:951의 `display:none !important`가 이를 확정한다. 화면에 남는 것은 관측 수치와 현재 필드 하나뿐이고 앞서 쓴 문장의 읽기 전용 요약이 없다. 그런데 CERL_FIELDS의 프롬프트는 상호 참조를 전제한다 — 추론 = '그 숫자가 왜 내 주장을 뒷받침하는지 연결하세요', 한계 = '이 자료만으로 말할 수 없는 범위를 쓰세요'. 되읽으려면 '← 이전 요소'로 renderVerdict 전체를 재렌더해야 하고 그때마다 h1로 포커스가 이동해 스크롤이 최상단으로 간다. 더 중요한 것은 bindStudentCerl이 마지막 필드에서 곧바로 cerlSubmitted=true로 넘겨 기획서 §M-W가 규정한 '5. 네 요소 전체 검토 및 봉인'이 없다는 점이다 — 학습자가 자기 4문장을 처음 한눈에 보는 시점이 전문가 답이 함께 열리는 renderExpertStep(2720)이므로 자기 점검이 아니라 모범답안 대조가 된다. CERL은 요소 간 정합성이 핵심인 프레임인데 그 정합성을 점검하는 단계가 없다. 라이브 데모에서 심사위원이 '방금 뭐라고 썼는지 안 보이네요'를 즉시 지적할 수 있다. 완화 요소: 입력값은 input마다 저장되어 소실이 없고, '근거 단계에 증거 카드 고정'은 실제로 구현돼 있다(2613).
- **수정**: hidden 필드를 지우는 대신 이미 작성한 요소를 읽기 전용 요약으로 현재 필드 위에 쌓는다 — `CERL_FIELDS.slice(0, ci).filter(...).map(f => '<p class="cerl-done"><b>'+f.label+'</b> '+escapeHTML(d[f.k])+'</p>').join('')`(약 4줄). 기획서 §M-W 5단계를 살려 cerlStepIndex가 CERL_FIELDS.length일 때 4문장 전체 + '봉인하기' 화면을 렌더한다(약 20줄). 아울러 checkBack(2711)에 `state.cerlStepById[m.id] = 0;`을 추가해 감사 화면의 editCerl(2782)과 동작을 일치시킨다(1줄).

### [P2] F16 · 검증 커버리지 / 실현 가능성

**열관성 실험실의 물리 코드 전체가 검증 게이트 0건이다 — 8,303건 assert 중 EBM·일사량·최적깊이를 검사하는 항목이 하나도 없고, 한계 서랍의 '지연 40일 · 최고 28℃'는 계산값이 아니라 문자열 리터럴이다**

- **위치**: processing/verify_solar_terms.py (A~N 15축 전체) · processing/verify_web_data.py · eval_harness.js — 대상 코드는 prototype/verify.js:3165-3505 · 하드코딩 문장은 3311
- **왜 문제인가**: 네 게이트 파일에 EBM|runEBM|labFit|insolation|rho_c|albedo|LAB_PRESETS|labBest로 grep한 결과 히트 0건이다(실측). EBM 상수(A·B·albedo·rho_c), 적분 스텝 안정성, 스핀업 수렴, 일사량 식의 전지구 평균 340 W/m² 항등식, labBestDepth의 탐색 범위, 발견 ①②③이 인쇄하는 숫자 — 어느 것도 assert되지 않는다. 반면 이 화면은 최신 커밋 1d7fc32가 대규모로 재구성한, '데이터 뷰어가 아니다'를 증명하는 유일한 계산 화면이다. 지금 거짓인 출력은 없지만(verify.js:3311의 '28℃'는 현재 27.92 → 28로 우연히 참) 실패는 전망형으로 확실하다: 본선 준비 중 EBM.B나 A, LAB_PRESETS를 한 곳만 고치면 8,303건 게이트가 전부 통과하면서 하드코딩된 문장과 실제 계산이 조용히 어긋난 채 배포된다. 팀의 작업 방식 자체가 '게이트 통과 = 안전'이라는 전제 위에 있고 본선까지 이 영역을 더 손볼 가능성이 높으므로(방금 713줄을 바꿨다) 실효 위험이다. 아울러 '검증 게이트로 강제한다'는 서사가 가장 새롭고 가장 물리적인 화면에서만 비어 있다는 점이 지적되면 서사 전체의 신뢰도가 내려간다. labBestDepth의 탐색 상한(30m)이 슬라이더 상한(60m)의 절반인 것도 같은 커버리지 공백에 숨어 있다(현재 데이터에서는 최적값이 4.5~6.5m라 미발현).
- **수정**: verify_solar_terms.py에 O축(모형) 5건을 추가한다 — (1) 일사량 면적가중 전지구 연평균 = 1361/4 ± 0.5 W/m², (2) 전진 오일러 안정 조건 dt·B/(rho_c × 최소깊이) < 1, (3) 스핀업 마지막 두 해 곡선 차 < 0.01℃, (4) 서울·5m 실행 결과가 한계 서랍 문장과 일치 — 더 나은 방법은 verify.js:3311의 리터럴을 계산값 보간으로 바꾸는 것이다, (5) labBestDepth 결과가 탐색 경계에 걸리지 않음. 원시 CSV 없이 실행 가능하고 Node로 verify.js 함수를 추출 실행하는 방식은 eval_harness.js에 전례가 있다. 함께 3225행의 탐색 상한을 슬라이더와 동기화한다(d <= 600, 결과 불변을 전수 확인함).

### [P2] F17 · 제작 과정 증빙 / 실현 가능성

**제출물 ④ 프롬프트 세션이 2026-07-28 01:10에서 끊겨 있다 — 지금 배포된 학습 흐름을 만든 마지막 6개 커밋(verify.js 1,119줄 변경)의 대화가 제출물에 없다**

- **위치**: prompt_sessions/README.md:8-16 · prompt_sessions/05_4차레드팀과_열관성실험실.md (마지막 턴 01:10) · AI_활용_기록.md:29 · 제출_체크리스트.md:44-53 · scripts/export_sessions.py
- **왜 문제인가**: 세션 5개 파일의 마지막 턴이 '[AI] 2026-07-28 01:10 — Deck is 11 pages … Now regenerating the session logs'이고, 그 이후 커밋이 6건이다(ceea6c2·3c7a906·8d19117·3243e32·cbd3c6e·1d7fc32). 그중 cbd3c6e(verify.js 406줄)와 1d7fc32(verify.js 713줄 + 학습단계_분할_기획서.md 698줄 신규)가 심사위원이 URL에서 실제로 만지는 화면 구조 — 마이크로 단계 10개, lens 게이트, 완료 4탭, 자유탐구 3탭 — 을 설계·구현했다. AI_활용_기록.md에 '마이크로'·'학습단계'·'분할' 문자열이 0건이다. 심사배점 ⑤ 후반이 '생성형 AI의 제작 과정 활용'을 보는데, 심사위원이 세션 zip의 시간축을 확인하면 로그는 7/28 01:10에서 끝나는데 앱은 그 로그에 등장하지 않는 UI로 돌아간다 — '지금 보는 결과물을 만든 과정은 제출되지 않았다'는 판정이 가능하다. 완화 요소: 어떤 제출 문서도 '로그가 전 커밋을 덮는다'고 주장하지 않고 05번 헤더가 기간을 정직하게 명시하므로 허위 주장은 없고 '증거 미제출'이다. 그리고 제출_체크리스트.md:50이 재생성 절차를 명시하고 있으므로 8/21까지 회복 가능하다.
- **수정**: `python scripts/export_sessions.py`를 다시 실행해 7/28 01:10~21:50 구간(4차 반영·실험실 신설·제출전 강화·마이크로 단계 재구성)을 06번 파일로 추가하고, prompt_sessions/README.md 표·합계와 AI_활용_기록.md §6 목차·커밋 범위를 함께 갱신한다(submit_check.sh:96-101이 목차 등재를 검사하므로 누락하면 즉시 실패). 시간이 없으면 최소한 AI_활용_기록.md에 '세션 로그 수록 범위: ~2026-07-28 01:10. 이후 마이크로 단계 재구성(1d7fc32)의 설계 산출물은 학습단계_분할_기획서.md다'를 명시해 결손을 은폐하지 않는다.

### [P2] F18 · 형성평가 / 학습 효과

**사전→사후 동형문항이 같은 미션 안 3분 간격 오픈북이다 — 직전 전문가 화면이 정답 문형 '날짜는 그대로인데'를 미리 보여 주고, 미응답 상태에서 무제한 재열람이 가능하다**

- **위치**: prototype/verify.js:48-57 (PRE) · 62-72 (POST) · 82 (전문가 CERL 추론) · 2237-2243 (expert → transfer 인접) · 2739-2757 (renderTransferStep) · 2730-2737 (postGrowthHTML)
- **왜 문제인가**: missionSequence가 expert 바로 다음에 transfer를 둔다. 직전 expert 화면이 인쇄하는 전문가 CERL 추론(82행) '절기 날짜는 그대로인데, 관측된 더위가 N일 늦게까지 이어진 것입니다'는 POST 정답 b(68행) '입동 날짜는 그대로인데, 그 무렵 관측된 기온이 예전보다 높아진 것이다'와 '날짜는 그대로인데'(9자)·'관측된'을 공유하고, POST 세 보기 중 이 패턴을 가진 것은 b뿐이다. 그리고 renderTransferStep(2755-2756)의 '← 전문가 비교' 버튼이 answered와 무관하게 항상 렌더되므로 미응답 상태에서 그 문장을 무제한 재열람할 수 있다. 실제로 측정되는 것은 9자 문구의 단기 재인이다. F01의 goal-chip 누출과 겹치면 사전 문항의 오염이 두 겹이 된다. 보기 길이 단서도 남아 있다 — 실측 PRE a24 / b(정답)53 / c7자로 정답이 2.2배이고 c는 '잘 모르겠다'라 실질 2지선다이며, 미션5 겨울 자가진단은 both(정답)65 vs 22/16자로 3.0배다. 58-61행 주석이 '보기 길이도 맞춰 가장 긴 보기가 정답이라는 단서를 없앤다'고 사후 문항에만 적용했다고 명시했는데, 그 POST조차 실측 a30 / b(정답)40 / c30으로 정답이 여전히 최장이다(1.33배). 반증에서 확인된 완화 요소: postGrowthHTML은 state.pre로 3분기해 이득을 판정하고 '내 기록'이 PRE·POST 선택 원문을 둘 다 인쇄하므로 '이득점수가 없다'는 주장은 성립하지 않는다.
- **수정**: (a) verify.js:2755행을 `answered ? '<button … id="transferBack">← 전문가 비교</button>' : ''`로 바꿔 응답 전 회귀를 막고 2756의 가드에 조건을 맞춘다(2줄). (b) POST 정답 b의 '날짜는 그대로인데'를 다른 문형으로 재작성하고('입동 날짜 자체는 해마다 거의 같고, 달라진 것은 그 무렵 기온이다') 전문가 CERL 추론(82)의 어휘를 분리한다. (c) PRE a·c와 미션5 겨울 보기 길이를 정답의 ±30% 안으로 맞춘다(데이터 정의 수정만, 로직 불변). (d) askPost를 미션1에서 떼어 마지막 미션 또는 renderComplete 첫 패널로 옮기면 '사후'가 실제로 전체 학습 뒤가 된다.

### [P2] F19 · 학습 흐름

**미션 2~5의 봉인된 예측이 미션 안에서 한 번도 결과와 대조되지 않는다 — 예측-관찰-설명 순환이 5회 중 1회만 닫히고, 예측 문구는 두 번째 화면 이후 사라진다**

- **위치**: prototype/verify.js:2339 (sealed-answer가 renderMissionOrient에만 존재) · 2696-2795 (check/expert/transfer/audit가 predicts를 읽지 않음) · 2833-2836 (내 기록) · 학습단계_분할_기획서.md §4.3 (130-139행)
- **왜 문제인가**: grep 결과 'sealed-answer'와 '내 첫 생각'은 verify.js 전체에서 2339행 단 한 곳뿐이고, state.predicts 참조 지점은 1707·2161·2171·2792·2832(초기화·접근자·삭제·완료화면)뿐이다. explore·evidence·write(4화면)·check·expert·transfer·audit 어느 렌더 함수도 예측값을 읽지 않는다. 미션 내에서 '당신은 X를 예측했고 자료는 Y였다'는 대조가 생성되는 곳은 미션 1의 transfer(2730-2737)뿐이고 그것도 사전/사후 정오 조합만 판정한다. 미션 2~5의 예측(격차 방향 / 지역 차이 / 강수 방향 뒤집힘 / 하지 대비 지연)은 응답만 저장되고 완료 화면에서 정오 판정 없이 '내 예측 「…」' 텍스트로만 인쇄된다. 재현: 미션 4에서 '방향이 서로 다를 것이다'를 고르면 탐구 방향 화면에서 '🔒 내 첫 생각'을 한 번 본 뒤 이후 8개 화면에서 자기 예측이 다시 등장하지 않고, 앱은 그 예측이 맞았는지 끝까지 말하지 않는다. POE에서 학습을 일으키는 것은 예측과 관찰의 명시적 불일치 확인인데 그 확인이 설계에서 빠졌다. 기획서 §4.3이 '봉인한 예측'을 화면이 바뀌어도 유지되는 근거 노트 항목으로 명시했으므로 팀 스펙 대비 미구현이기도 하다. 앱의 대표 기제 '예측 봉인'이 절반만 작동한다는 것이 발표 질의로 돌아올 수 있다.
- **수정**: (a) 2339행의 sealed-answer 마크업을 재사용하는 헬퍼를 만들어 explore·evidence·write 헤더 아래에 봉인 예측 한 줄을 상시 표시한다(약 6줄). (b) renderExpertStep 상단에 대조 블록을 추가한다 — `var pv = missionAsk(m).get(); … '내 첫 생각 「' + pickedText + '」 → 자료가 보여 준 것: ' + strip(v.c)` 형태로 학습자 예측과 판정 주장을 나란히 놓는다(약 12줄). 정오 채점 없이 대조만으로도 순환이 닫힌다.

### [P2] F20 · 접근성 / 문서 정합성

**라이트 테마에서 새 단계 화면의 '직접 할 일 / 지금 한 가지만' 배지가 WCAG AA 미달(4.28:1)이다 — 리팩터가 step-tag를 주황 틴트 안으로 옮긴 회귀이고, 저장소 문서는 'AA 위반 0건'이라 단언한다**

- **위치**: prototype/verify.css:658·925 · prototype/verify.js:2341·2429 · prototype/base.css:22-32 (특히 26-28 주석) · 회귀 대조 git show cbd3c6e:prototype/verify.js:2294 · 레드팀_4차_종합보고서.md:483
- **왜 문제인가**: 라이트 토큰(page #eef2f5, sun-rgb 133,81,0, green-rgb 29,106,51, green #1d6a33)으로 sRGB 상대휘도를 독립 재계산하면: sun 7%(verify.css:925 .orient-action/.focus-task) over page = (230.65, 230.73, 227.85) → green 16%(verify.css:658 .step-tag) over that = (198.39, 210.77, 199.55) → #1d6a33 대비 4.282:1. 11px/700은 large text 예외(18.66px bold) 미달이므로 AA 4.5:1 실패가 확정이다. 회귀도 확인됐다 — cbd3c6e에서는 같은 배지가 배경 없는 .hero-sub 안에 있어 4.686:1로 통과했고, .orient-action/.focus-task 컨테이너와 그 안의 step-tag 배치는 1d7fc32에서 새로 생겼다. base.css:26-28 주석이 스스로 '순수 카드 기준으로만 맞췄더니 틴트 위에서 3.74~4.28로 깨졌다'며 경고한 바로 그 함정에 새 컨테이너가 다시 걸렸다. body에 --glow 방사 그라디언트가 얹혀 카드 상단부에서는 3.85:1까지 더 내려간다. 다크는 6.554:1로 통과하므로 라이트 전용이다. 하필 이 배지가 '지금 무엇을 해야 하는가'를 지시하는 유일한 라벨이고, 발표_10분_구성안.md:30이 접근성을 강점으로 올려 두었다. 그리고 레드팀_4차_종합보고서.md:483이 '라이트·다크 양쪽 텍스트 요소 전수 계산, WCAG AA 위반 0건'이라 단언하는데 그 보고서는 21:50보다 앞선다 — 저장소에 남은 문서가 지금 사실이 아닌 접근성 주장을 하고 있어 대비 검사 도구로 반박당할 수 있다.
- **수정**: verify.css:925 뒤에 `.orient-action .step-tag, .focus-task .step-tag { background: rgba(var(--shade-rgb), .55); }`를 추가해 이중 틴트를 없앤다(1줄, verify.css:803-805가 .ro-delta에 이미 쓴 해법). 합성색이 rgb(198,211,200) → rgb(약 240,242,241)로 바뀌어 5.9:1 이상이 된다. 레드팀_4차_종합보고서.md:483에 '이 측정은 커밋 cbd3c6e 기준이며 1d7fc32 이후 재측정 필요'를 명시하거나 값을 갱신한다. 재발 방지로 verify_solar_terms.py 인터페이스 축에 '.step-tag가 틴트 배경 컨테이너 안에 있으면 실패' 정적 규칙을 넣는다.

### [P2] F21 · 모바일 체험 설계

**375×667에서 explore 화면 상단 4개 블록이 뷰포트를 거의 채워 조작 대상(슬라이더·그래프)이 폴드 밖이고, sticky CTA는 하필 그 화면에서만 작동하지 않는다**

- **위치**: prototype/verify.css:465-487·148-155·909-932·979-981·186·934 · prototype/verify.js:687-700 (renderProgress) · 2432-2434 (explore-actions 중첩) · 학습단계_분할_기획서.md:124 (§4.2)
- **왜 문제인가**: 두 결함이 같은 화면에서 겹친다. (a) renderProgress가 MISSIONS 5개의 title 전문 + '자유탐구' = pstep 6개를 모든 화면에 렌더하고 이를 접는 미디어쿼리가 없다(verify.css:465-483은 max-width만 92→68px로 줄인다). 375px에서 필요 폭이 셸 내부폭 355px을 넘어 2줄이 되고 '처서,/약속은/유효한가'가 3줄이라 진행바만 약 150~160px = 뷰포트의 23~24%다 — 정작 이 화면에서 쓰이지 않는 다른 미션들의 제목 목록이 세로 예산의 최대 단일 항목이다. 여기에 mission-step-head·h1·focus-task(4줄 지시문)를 더하면 슬라이더·그래프가 스크롤 아래로 밀린다. 기획서 §4.2가 '전체 미션 제목 목록은 펼침 메뉴로 이동한다'고 명시했으나 미구현이다. (b) verify.css:979의 sticky CTA는 ≤680px에서 .step-actions 전체에 걸리는데, renderExplore만 verify.js:2432-2433에서 .step-actions를 .explore-actions(display:flex) 안에 한 겹 더 감싼다. sticky는 자기 containing block을 벗어나지 못하므로 부유 범위가 0~35px에 불과하다 — orient·자가진단·전문가·완료는 section.card 직속이라 정상 작동한다. 결과적으로 그래프를 조작한 뒤 '증거 정리하기 →'를 찾으려면 약 400px을 더 스크롤해야 하고, 다음 화면들에서는 CTA가 따라다니므로 '어떤 화면에서는 있고 어떤 화면에서는 없다'는 불일치를 겪는다. 요강이 모바일 권장을 명시하고 심사위원이 자기 폰으로 열어 볼 시나리오에서 가장 먼저 부딪히는 지점이다. 단, 이 두 결함 모두 학습을 끊지는 않고 스크롤로 도달 가능하며, ≤680px는 주최측 PC 시연의 주 경로가 아니다.
- **수정**: (a) verify.css의 ≤560px 블록에 `.verify-progress { flex-wrap: nowrap; overflow-x: auto; gap: 6px; } .pstep small { display: none; } .verify-progress .pstep { max-width: none; }`을 추가해 라벨 없는 원형 인디케이터 한 줄(약 34px)로 줄인다 — 약 120~130px 회수. title 속성과 aria-label을 pstep에 남겨 완료 미션 재방문(data-goto)을 유지한다. (b) verify.js:2432-2433의 마크업을 뒤집어 .step-actions를 카드 직속 자식으로 올린다(#touchHint는 id로 접근하므로 그대로 동작). 이때 verify.css:410-412의 인쇄 숨김 목록에 `.step-actions, .panel-tabs`를 함께 추가해 인쇄 회귀(완료 화면 인쇄물 첫 줄에 탭 4개가 찍히는 문제)까지 같은 커밋에서 막는다.

### [P2] F22 · 자기보고 정확성 / 실현 가능성

**제출전_Self-reflection_최종보고서가 '아직 배포하지 않았다'고 적어 사실과 반대이고, §3 '재현한 검증 결과' 표는 21:50 리팩터 이전 측정인데 시점을 밝히지 않는다 — 그런데 다른 보고서들이 이 문서를 '최신 기준'으로 지목한다**

- **위치**: 제출전_Self-reflection_최종보고서.md:47 (§4-4) · :12 (§1) · :26-40 (§3 표, 특히 :37·38·39) · 레드팀_4차_종합보고서.md:3-4 · processing/verify_solar_terms.py:404 (DOCS 목록에 이 문서 없음)
- **왜 문제인가**: §4-4는 '현재 수정본은 아직 원격 저장소에 커밋·푸시·배포하지 않았다. 배포 뒤 CSP, /api/ai-turn, 캐시 무효화, 모바일 실기기 스모크 테스트를 별도로 통과해야 한다'고 적었으나, git rev-parse HEAD와 origin/main이 모두 1d7fc32이고 submit_check.sh §2·§4에서 배포 URL·자산 8종이 전부 200이다. §1도 같은 취지다. 문서 mtime은 20:44(cbd3c6e) 시점이므로 §3 표의 ':37 실제 브라우저 핵심 동선', ':38 반응형 실측 320~420px 넘침 0', ':39 콘솔 경고·오류 0'은 그 이후 verify.js 713줄을 바꾼 리팩터를 포함하지 않는데 표에 측정 시각·대상 커밋 표기가 없다. 그리고 이 문서는 회귀 게이트의 문서 대조 대상이 아니다(verify_solar_terms.py:404 DOCS에 없음)이므로 낡음이 자동으로 잡히지 않는다. 문제는 레드팀_4차_종합보고서.md:3-4가 '현재 상태와 최종 재검증 결과는 제출전_Self-reflection_최종보고서.md를 기준으로 읽으세요'라고 지목한다는 점이다 — 심사위원이 그 유도를 따르면 '아직 배포 안 함'을 읽고 URL을 열어 정상 배포본을 본다. 다행히 방향은 과소 주장(과대 주장이 아님)이고, 반증에서 확인된 대로 1d7fc32의 verify.css diff에는 @media 2건이 실제로 추가돼 새 컴포넌트(.micro-progress·.step-actions·.evidence-change-grid·.panel-tabs)를 좁은 화면용으로 처리하고 있어 반응형 코드 자체는 대응돼 있다 — 측정을 다시 하지 않은 것이 남은 문제다.
- **수정**: §4-4와 §1을 사실로 교체 — '커밋 1d7fc32를 origin/main에 푸시하고 Vercel 배포를 완료했다. 자산 8종 200, 콘솔 오류 0 확인. 남은 것은 주최측 PC와 모바일 실기기 스모크 테스트(1일차 19~21시)다.' §3 표에 '측정 커밋' 열을 추가하고 :37·38·39를 1d7fc32 기준으로 재측정한 뒤 값을 다시 적는다(재측정 전에는 '대상 커밋 cbd3c6e'로 명시). submit_check.sh §7에 'Self-reflection의 mtime이 HEAD 커밋 시각 이후인가' 검사를 넣어 같은 시점 역전을 막는다.

### [P2] F23 · 학습 흐름 / 안내 정합성

**증거 화면의 '선택 심화' 안내가 그 화면에 존재하지 않는 조작(지역 칩·표·지도 보기)을 가리킨다 — 리팩터가 안내문만 옮기고 컨트롤은 두고 왔다**

- **위치**: prototype/verify.js:2133-2144 (deepHintOf) → 2534 (renderEvidence의 유일한 호출) · 컨트롤 원산지 1599-1614 (heroShell) · heroShell 호출부 2093·2430·3502 · 2527-2541 (renderEvidence의 setStage) · 학습단계_분할_기획서.md §M-E
- **왜 문제인가**: deepHintOf는 '지역을 바꿔 보고', '절기를 바꿔 보고', "'표'와 '지도' 보기로"를 조립해 '선택 심화 시간이 남으면 — … 같은 결론이 나오는지 확인해 보세요'를 만든다. grep 결과 유일한 호출 지점이 renderEvidence:2534인데, renderEvidence의 setStage에는 heroShell이 없다 — #cityChips·#viewChart/#viewMap/#viewTable·기준 슬라이더가 DOM에 생성되지 않고 bindViewTools()·bindCityChips()도 호출되지 않는다(2539행은 renderReadouts·updateKmaRef·updateMethod·updateWindow만 호출). 미션 1(lockCity·lockTerm 모두 true)에서도 안내문은 "'표'와 '지도' 보기로"를 출력한다. 학습자나 심사위원이 화면 전체를 훑어도 그 버튼이 없고, 실제 버튼은 직전 화면에 있는데 안내문은 되돌아가라고 말하지 않는다. 미션 5만 예외적으로 인라인 #deepLab이 실제 바인딩되어(2542) 동작한다 — 같은 문단에서 한 지시는 작동하고 세 지시는 죽어 있다. 미션 4에서는 여기에 '지역을 바꿔 보고'가 더해져 F06의 잘못된 지역 표기까지 유도한다. 아픈 점은 2130-2132행 주석이 이전 레드팀이 바로 같은 부류('실제로 없는 것을 가리키는 고정 안내')를 P1으로 고쳤다고 적어 두었는데 화면 분할이 같은 결함을 다시 만들었다는 것이다. 기획서 §M-E가 이 화면의 보조 동작으로 명시한 '표로 정확한 값 보기 / 16지점 지도 보기'도 둘 다 미구현이다. 다만 명시적 선택 영역이고 잘못된 수치를 만들지 않는다.
- **수정**: 택 1. (a) deepHintOf 호출을 renderEvidence:2534에서 renderExplore의 explore-actions 위로 옮긴다(1줄 이동) — 안내와 컨트롤이 같은 화면에 놓인다. (b) 증거 화면에 남기려면 문구를 화면 사실에 맞춘다 — '← 다시 조작하기를 눌러 지역·보기를 바꿔 보세요'로 명시하고, 기획서 §M-E가 약속한 '표로 정확한 값 보기' 버튼(setView('table') + 되돌아가기)을 실제로 추가한다. (a)가 1줄이므로 8/21 전에는 (a)를 권한다.

### [P2] F24 · 상태 관리 / 학습 계약

**'이전 조건 복원'이 예측 봉인을 함께 복원하지 않아, 겨울에 답한 예측이 여름 문항의 답으로 표시되고 산출물에도 남는다**

- **위치**: prototype/verify.js:2483-2491 (restoreEvidenceConfig) · 2453-2461 (evidenceConfig 스냅샷) · 1703-1714 (season) · 2790-2793 (auditWinter) · 2333-2339 (sealed-answer) · 2832-2835 (내 기록)
- **왜 문제인가**: evidenceConfig 스냅샷에 예측 답이 없고 restoreEvidenceConfig는 city/ti/metric/thr/thr0/moved/touched/lagSeason/markDoy/lagRevealed만 되돌리며 state.predicts에 손대지 않는다. 그런데 계절 전환 두 경로(season() 1707, auditWinter 2792)는 모두 `delete state.predicts['lag']`로 봉인을 풀고 새 계절 문항을 다시 묻는다. 선택지 값이 여름·겨울 모두 same/later/unknown으로 동일해(255-268) 값 검증으로도 걸리지 않는다. 재현: 미션5 여름 완료(예측 'later') → 감사 화면 '겨울·동지에도 적용해 보기' → 겨울 예측 'same' → 1월 20일 찍고 비교 → '증거 정리하기' → 조건 불일치 감지 화면 → '이전 조건 복원'. 결과: lagSeason='summer'·ti=11·markDoy=212로 되돌아가지만 predicts['lag']는 겨울 답 'same'으로 남아, 방향 화면의 sealed-answer가 여름 선택지 텍스트를 매핑해 '🔒 내 첫 생각: 하지가 가장 덥다'를 표시한다 — 학습자가 한 번도 고르지 않은 답이다. 노출 지점이 두 곳이다: 완료 화면 '내 기록'(2832-2835)도 같은 missionAsk로 '내 예측 「…」'을 찍으므로 '기록 복사·인쇄' 산출물에 고르지 않은 답이 남는다. '자료를 보기 전 예측을 봉인한다'가 이 앱의 핵심 교육 장치이고 완료 화면의 성장 기록이 그 값을 근거로 삼는다. 좁은 경로이지만 전 과정이 결정적이고, 조작 화면의 여름/겨울 토글(1715-1716)이라는 1급 컨트롤로도 같은 상태에 도달한다.
- **수정**: 예측 답을 증거 스냅샷에 포함시켜 함께 복원한다 — (1) evidenceConfig에 `predict: m.lagMode ? (state.predicts[m.id] != null ? state.predicts[m.id] : null) : null` 추가(sameEvidenceConfig 비교에는 넣지 말 것 — 예측은 조건이 아니라 부속 기록이다). (2) restoreEvidenceConfig 마지막에 `if (m.lagMode) { if (cfg.predict != null) state.predicts[m.id] = cfg.predict; else delete state.predicts[m.id]; }` 추가. 복원 후 예측이 비면 renderMissionFlow의 2256 가드가 예측 화면으로 되돌리므로 추가 분기가 불필요하다(총 3줄).

### [P2] F25 · AI 감사관 / 학습 효과

**로컬 감사관이 '이산화탄소'라는 단어만으로 인과 단정 오탐을 낸다 — 앱 스스로 보여 준 상관을 학생이 올바르게 인용하면 반려하고, 같은 함수 주석이 '오탐은 미탐보다 무겁다'고 선언한 원칙을 스스로 어긴다**

- **위치**: prototype/verify.js:3583 (causalWord 정규식) · 3570-3572 (hasLimitation) · 3568-3578 (원칙 주석) · 3125-3131 (완료 화면 CO₂ 패널) · eval_set.json C029
- **왜 문제인가**: 3583행의 causalWord 정규식에서 '이산화탄소'와 'co2'가 서술어 조건 없이 단독 트리거다. 발췌 실행으로 오탐을 재현했다 — '한계: 이산화탄소 농도도 같은 기간 늘어난 것은 관측 사실이지만 이 화면 자료는 서울 한 지점 5년이다' → evidence_status='revise', flags=['causal'], 경고 '관측된 변화의 「원인」을 단정하고 있어요.' 반면 CO₂를 언급하지 않은 같은 구조의 답은 'ready'다. 한편 완료 화면의 '지구 맥락' 패널(3125-3131)은 '이산화탄소와 전지구 기온이 함께 오르는 모습'을 앱이 직접 보여 주므로, 학생이 그것을 본 뒤 상관만 보고하고 범위 한계를 밝힌 모범 답안을 쓰면 감사관이 반려하고 '함께 변했다는 것과 원인이라는 것은 어떻게 다를까요?'를 붙인다 — 학생은 'CO₂를 언급하면 틀린 것'이라고 학습하고, 이는 앱이 가르치려는 상관/인과 구분과 정반대다. eval_set.json 100건 중 CO₂ 케이스는 C029 하나뿐이고 그것은 진짜 인과 단정이라 'eval 100/100·오탐 0'이 이 케이스를 보증하지 않는다. 심사위원이 CO₂를 언급하는 문장으로 감사관을 시연하면 즉시 노출된다. 완화 요소: 감사 대상은 4필드 전체이고 부정·유보 서술이 조금이라도 들어가면 통과하며(실측: '한계: 원인은 이 자료로 알 수 없다' → ready), localAudit은 진행을 차단하지 않는 선택 버튼이다.
- **수정**: 3583행에서 `이산화탄소|co2|화석연료|탄소\s*배출|인간\s*활동`을 단독 트리거에서 빼고 `(이산화탄소|co2|화석연료|탄소\s*배출|인간\s*활동)[^.!?]{0,12}(때문|탓|원인|초래|야기|증거)` 형태로 묶는다(1줄). eval_set.json에 상관-only + 한계 명시 케이스 2건(status:'ready')과 상관-only 무한계 케이스 1건을 추가해 회귀를 고정한다.

### [P2] F26 · 과학적 서술 정확성

**미션 4의 강수 부호가 비단조인데 판정문 주장은 1mm·50mm로 하드코딩돼 있다 — 서울 80mm에서는 큰비가 오히려 줄었는데(−0.6일) 같은 화면이 '큰비가 늘었다'를 단정한다**

- **위치**: prototype/verify.js:203-217 (verdict의 flipped 판정) · 195 (과제문) · 36 (METRICS.precip.grid=[1,80]) · 511·523-529 (sensitivityText는 temp 전용) · 995-1000
- **왜 문제인가**: solar_terms_data.js를 직접 파싱해 서울 precip 전 구간 차이를 재계산한 결과 부호가 5회 뒤집힌다: 1mm −6.4, 7mm +0.2, 19mm −0.4, 30mm +3.4, 50mm +1.2, 61mm −0.2, 69mm −0.6, 73~80mm 전부 음수(80mm는 3.0 → 2.4 = −0.6). 데이터 자체에 rainFlip.high=[80,−0.6]으로 이 역전이 기록되어 있다(정직함은 강점이지만 화면이 쓰지 않는다). verdict는 `lo=rainAt(city,1), hi=rainAt(city,50); flipped = lo.d<0 && hi.d>0`으로 임계값을 하드코딩하므로 학습자가 80mm까지 올려도 주장 문장이 '「비가 줄었다」와 「큰비가 늘었다」가 동시에 참일 수 있습니다'로 고정된다. 슬라이더 상한이 80mm라 끝까지 끌면 반드시 이 구간에 도달한다. 증거 문장은 정직하게 '지금 고른 80mm 기준으로는 3일 → 2.4일(−0.6일)'을 보여 주므로 같은 화면 안에서 증거와 주장이 어긋난다. 서울 80mm 초과일은 연 3.0일 = 5년 15건이라 −0.6일/년은 3건 차이인데, sensitivityText는 metric!=='temp'면 null이어서 강수에는 5년 창 민감도 문장이 붙지 않는다. 기상 전문 심사위원이 '그러면 80mm에서는 반대인데요? 5년 15건 표본으로 방향을 말할 수 있습니까?'라고 물을 수 있고, 과제문이 '방향이 어디서 뒤집히는지'로 단일 전환점을 전제하는데 실제 데이터는 5회 뒤집힌다. 완화 요소: 증거 카드가 sampleText('과거 5년 vs 현재 5년 · 30년 기후평년 아님')를 항상 표시하고, 과제문이 지정한 프리셋 경로(1→10→30→50) 안에서는 전환이 1회뿐이다. 전국 16지점 합계는 50mm +13%·80mm +11%로 방향이 견고하므로 결함은 서울 단일 지점 서술에 국한된다.
- **수정**: (a) verdict에 현재 임계값 분기를 추가한다 — `if (n.thr >= 60 && cur.d < 0)`일 때 주장 뒤에 '단, 지금 고른 N mm에서는 방향이 다시 뒤집힙니다 — 이 기준의 5년 표본은 M건뿐이라 부호가 흔들립니다'를 덧붙인다. rainFlip.high가 이미 데이터에 있어 계산 없이 읽으면 된다(약 4줄). (b) 과제문(195)의 '방향이 어디서 뒤집히는지'를 '방향이 뒤집히는 구간이 몇 군데인지, 표본이 적어지는 높은 기준에서 왜 부호가 흔들리는지'로 바꾼다. (c) sensitivityText에 강수 분기를 추가한다(windows 데이터가 이미 존재).

### [P3] F27 · 실험실 피드백 정합성

**'가장 잘 맞는 값으로' 버튼이 실험실의 목표(계절 지연 재현)를 16지점 중 10곳에서 망가뜨린다 — 버튼을 누르면 바로 위 '지연 맞추기' 카드가 실패 색으로 바뀐다**

- **위치**: prototype/verify.js:3225-3232 (labBestDepth) · 3446-3452 ('지연 맞추기' 판정) · 3476-3480 (발견 ③) · 3436-3439 (labNums)
- **왜 문제인가**: labBestDepth는 365일 MAE(labFit)만 최소화하는데, 이 모형에서 연평균은 깊이와 무관하므로 MAE는 평균 편차·진폭에 지배되고 위상(지연)은 거의 반영되지 않는다. EBM을 독립 재구현해 16지점 전수 계산한 결과 최적깊이의 모형 지연이 실측과 |차이|>=3인 지점이 10곳이다 — 부산 49 vs 42(7일), 경북 46 vs 40, 제주 49 vs 44, 전남 46 vs 42, 강릉 43 vs 39, 인천·광주·충남·전북 43 vs 40, 강원 37 vs 40. 제주가 특히 극적이다: 깊이 5.5m에서 모형 지연이 실측과 정확히 44일로 일치하는데(fit 3.21) 버튼은 fit이 0.09℃ 낮은 6.5m(fit 3.12, 지연 49일)로 학습자를 옮긴다. 3446-3452의 lagGap<=2 조건 때문에 버튼 클릭 직후 '지연 맞추기' 카드가 v-none(실패 색)으로 바뀌므로, 화면 상단이 선언한 '목표 ① 계절 지연을 만들 수 있는 메커니즘 시험'과 앱이 제공한 '정답 버튼'이 정면 충돌한다. 조작→피드백 루프가 서로 반대 방향의 성공 신호를 주면 학습자는 무엇을 최적화하는지 알 수 없다. 심각도를 P3로 두는 이유: labNums(3436-3439)가 '모형의 지연 49일 · 부산 실측 지연 42일'을 나란히 인쇄하고 안내문이 방향을 주므로 감소 방향은 도출 가능하며, 발견 ③이 판정 기준인 평균 오차(℃)를 같은 문장에 인쇄해 라벨과 목적함수가 일치하고, 데모 대본 경로(서울)는 gap 0이라 스크립트 데모에서는 발현하지 않는다. 지적 중 '11지점'은 대구(|−1|)를 잘못 포함한 것으로 정확히는 10지점이다.
- **수정**: 목적함수를 학습 목표와 짝지어 두 값을 함께 보여 준다 — 실측 hotLag과 모형 lag의 차를 최소화하는 깊이를 '지연이 가장 잘 맞는 두께'로, MAE 최적값을 '곡선 전체가 가장 잘 맞는 두께'로 이름을 나누고 왜 다른지 한 문장으로 설명한다('하나의 숫자로 두 목표를 동시에 만족시킬 수 없다'는 것 자체가 모형 사용 능력의 핵심 교재다). 최소 수정 대안은 labFit을 평균 제거 후 MAE로 바꿔 위상 성분의 비중을 키우는 것이다.

### [P3] F28 · 과학 문안 / 투명성

**미션 5 판정문의 '실제로 가장 더운 날은 7월 31일(29.4°C)'이 5년·15일 평활 평년값임을 밝히지 않고, 조정 모수 A=172를 서울에 맞춰 튜닝한 사실이 한계 서랍에서 빠져 있다**

- **위치**: prototype/verify.js:290-292 (미션5 verdict의 e) · processing/build_solar_terms.py:270-273 (hotT 출처) · prototype/verify.js:3185-3188 (A 튜닝 주석) · 3308-3313 (한계 서랍: B·알베도·S0는 값까지 공개, A만 누락)
- **왜 문제인가**: 두 문안 공백이 같은 성격이다. (a) hotT는 완결 5년 일자별 평균에 15일 원형 이동평균을 걸어 만든 보기용 평년 곡선의 최고값(서울 29.4℃)인데, 판정 문장에 '평년'·'5년 평균'·'평활' 중 어떤 한정어도 없다. 원시 CSV 재계산 결과 서울 2021–2025 일평균기온 최댓값은 32.8℃(2025-07-27)로 3.4℃ 높다. 학습자가 CERL에 '서울에서 가장 더운 날 기온은 29.4도'라고 적으면 감사관이 이 값의 성격을 검사하지 않으므로 통과한다. 완화 요소는 실질적이다 — '실제로'는 온도가 아니라 날짜를 하지와 대비하는 부사이고, 29.4℃는 같은 화면 곡선(범례 '실측 평년(2021–2025)')의 실제 꼭대기 값이라 화면과 문장이 일치하며, 방법 서랍 1091-1093이 '이 화면의 곡선·일수는 일 평균값이고 폭염일은 일 최고기온으로 따로 셌다'고 이미 구분해 두어 폭염일 표와 모순이 아니다. (b) 한계 서랍은 B(5.2 W/m²/K)·알베도(0.3)·태양상수(1361)를 값까지 밝히면서 유일한 조정 모수 A는 값도 출처도 튜닝 사실도 인쇄하지 않는다. 코드 주석(3186-3187)은 'A는 서울(37.57°N)·두께 5m에서 연평균 12.5℃·지연 40일이 나오도록 맞췄다'고 적었고 재현값(mean 12.43℃, lag 40일)이 그 목표를 그대로 재생한다. 교사나 심사위원이 'A는 어디서 온 값입니까'라고 물으면 화면에 답이 없다. 다만 '순환 검증을 예측 성공으로 제시한다'는 판정은 성립하지 않는다 — 3311행이 스스로 '원인을 확정한 결과가 아니라 단순한 모형이 큰 그림을 재현할 수 있는지 본 적합 사례'라고 명시한다. README §7 약속8('모든 화면에 출처·계산 방법·한계')의 반례라는 점만 남는다.
- **수정**: (a) verify.js:291의 문장에 한정어를 넣는다 — '실제로 가장 더운 날은 7월 31일 무렵입니다(5년 평년곡선의 최고점 29.4°C — 특정 해의 하루 기온이 아니고, 15일 평활로 하루 단위 요동을 지운 값입니다)'. 참고값으로 실측 일평균 최고(32.8℃)를 병기하면 평활의 뜻이 조작 없이 전달된다. (b) 한계 서랍 3311행에 'A = 172 W/m²는 서울(37.6°N)·5m에서 연평균 12.5℃·지연 40일이 나오도록 맞춘 값입니다. 따라서 서울과 잘 맞는 것은 적합 결과이고, 검증에 해당하는 것은 A를 고치지 않은 채 나머지 15지점에서도 지연이 39~49일 범위에 들어온다는 점입니다'를 추가한다.

### [P3] F29 · 코드 위생 / 유지보수

**리팩터가 실행 불가능한 개념 렌즈 사본 35줄과 쓰기 전용 전역, 무동작 바인딩, 무동작 스키마 클램프 4줄을 남겼다 — 학습 흐름이 코드에 두 버전으로 존재한다**

- **위치**: prototype/verify.js:2367-2401 (showLensGate, 호출자 0) · 2368 (predictOverlay — 정의 없음) · 2145·2150·2278·2371·2395·2425 (overlayOpen 쓰기 전용) · 2440 (deepLab 무동작 바인딩) · 562-568 (Object.assign 별칭으로 클램프 4줄 무동작)
- **왜 문제인가**: showLensGate는 첫 줄에서 `$('predictOverlay')`를 읽고 없으면 즉시 반환하는데, predictOverlay는 index.html에 존재하지 않고 grep 결과 verify.js:2368이 유일한 언급이며 호출자가 0개다(실측). 리팩터 전에는 renderExplore가 그 오버레이를 렌더하고 showLensGate를 호출했으나 이 커밋이 렌더와 호출을 모두 제거하고 함수 35줄만 남겼다. overlayOpen은 5회 대입되지만 읽는 조건문이 없고, renderExplore:2440의 `if ($('deepLab'))`도 무동작이다(deepLab을 만드는 deepHintOf는 renderEvidence에만 삽입). 별도로 562행 `var o = Object.assign(base, s)`는 base를 변형해 그 자체를 반환하므로 563-568의 city/metric/ti/thr 클램프 4줄이 자기 대입이 되어 완전한 무동작이다 — 563행 주석 '저장된 값이 현재 스키마를 벗어나면 기본값으로 되돌린다'가 사실과 다르다(발췌 실행으로 확인: thr='abc', ti=99, city=null, metric='zzz'가 모두 정정 없이 통과). 런타임 실패는 0이다. 실패는 유지보수 시점에 발생한다 — LENS를 4문항으로 늘리려는 사람이 사장 사본(2373-2388)을 실제 흐름으로 오인해 그쪽만 고치면 화면은 변하지 않는데 리뷰상으로는 수정된 것으로 보인다. 실제 흐름은 renderLensStep(2299-2329)이다. 클램프 쪽도 본선 전 데이터 핫픽스로 CITIES 목록이나 D.terms 길이를 한 번이라도 손대면 이전 사용자 전원이 흰 화면이 되는 구조다. 8/21까지 학습 흐름을 손댈 가능성이 높으므로 정리 가치가 있다.
- **수정**: showLensGate(2367-2401) 전체와 overlayOpen 선언·대입 6곳, renderExplore:2440의 무동작 바인딩을 삭제하고 verify.css:195의 고아 .predict-overlay 규칙도 정리한다. 562행을 `var o = Object.assign({}, base, s);`로 바꿔 별칭을 끊고(한 글자 수준) `if (!Array.isArray(o.done)) o.done = [];`을 추가한다(renderProgress:689의 state.done.indexOf가 배열이 아닌 값에서 즉시 예외). 삭제 후 `node --check prototype/verify.js`와 `node eval_harness.js`로 회귀를 확인한다.

### [P3] F30 · 접근성 / 시연 안정성

**24절기 입문의 5개 마이크로 패널만 포커스 이동이 없고, 4단계에서 '다음 →'을 누르면 그 버튼이 hidden이 되어 포커스가 body로 떨어진다 — 게다가 termIntroStep이 저장되어 리허설 후 시연 첫 동작이 불가능해질 수 있다**

- **위치**: prototype/verify.js:1956-1970 (syncTermStep) · 1962 (termNext.hidden) · 1953 (orbitStart) · 553·578 (termIntroStep 저장·복원) · 2943-2948 (bindPanelTabs의 정상 처리) · prototype/verify.css:919·969 (미정의 @keyframes rise) · 발표_10분_구성안.md:41
- **왜 문제인가**: renderComplete·renderLab·renderFree의 패널 전환은 bindPanelTabs를 쓰고 거기에는 `active.setAttribute('tabindex','-1'); active.focus(...)`가 있다. 반면 renderTerms는 자체 syncTermStep을 돌리는데 hidden 토글·라벨·바 폭·save만 하고 포커스를 옮기지 않는다 — 비대칭이 사실이다. 더 확실한 결함이 1962행이다: 4단계에서 '다음 →'을 누르면 방금 누른 그 버튼이 hidden이 되어 브라우저가 포커스를 문서로 되돌리므로, 이후 Tab이 헤더부터 다시 시작해 바로 옆 '이제 검증하러 가기 →'(1953)에 도달하려면 페이지 전체를 다시 훑어야 한다. 시각 신호도 하나 죽어 있다 — verify.css:919·969가 참조하는 `@keyframes rise`가 verify.css·base.css 어디에도 정의되지 않아(정의된 것은 cardIn·flashRing·poIn·reveal 4개) .micro-panel 5곳과 .learning-panel 11곳의 등장 전환이 조용히 사라졌다. 시연 리스크도 있다: state.termIntroStep이 localStorage에 저장되고 renderTerms 진입 시 초기화하지 않으므로, 5/5까지 본 뒤 소개로 돌아가 '🌍 24절기가 뭐예요?'를 다시 누르면 궤도 패널이 hidden인 채 마지막 패널이 뜬다 — 발표_10분_구성안.md:41이 이 화면의 '궤도에서 처서 클릭'으로 라이브 시연을 시작하므로 리허설 직후 '↺ 처음부터'를 빠뜨리면 시연 첫 동작이 불가능해진다. 그 25초 슬롯에 '다음 →' 3~4클릭이 숨어 있다는 점도 F02와 함께 반영해야 한다. 주의: aria-live 누락을 결함으로 보지 마라 — 기획서 §11(602행)이 '결과 읽기 aria-live는 한 곳만 유지한다'고 명시했으므로 현재 상태가 규격 준수다.
- **수정**: ① syncTermStep 마지막에 bindPanelTabs와 같은 처리를 넣는다 — `var act = stage.querySelector('[data-term-panel="'+state.termIntroStep+'"]'); if (act) { act.setAttribute('tabindex','-1'); act.focus({preventScroll:true}); }`(3줄). h2가 없는 패널0·1에는 sr-only h2를 추가한다(renderVerdict:2669과 같은 패턴). ② 1962행에서 termNext를 hidden 처리하는 대신 disabled로 두거나, hidden 직전에 orbitStart로 포커스를 옮긴다(1줄). ③ verify.css에 `@keyframes rise { from { opacity:0; transform: translateY(8px); } to { opacity:1; transform:none; } }`을 추가한다 — 이 한 블록으로 16곳의 전환 신호가 한꺼번에 살아난다. ④ renderTerms 진입 시 소개에서 온 경우 termIntroStep을 0으로 초기화한다(1줄) — 시연 안정성 확보.

---

## 5. 과거 레드팀·Self-reflection이 놓친 것

4차 레드팀(19:01)과 Self-reflection(20:44)은 둘 다 21:50 리팩터(1d7fc32, verify.js 713줄)보다 먼저 작성됐고, 그 결과 이 작품 최대 규모의 변경이 감사되지 않은 채 배포됐다. 놓친 것 일곱 가지. (1) 예측 봉인 누출이 이 커밋의 신규 회귀다 — missionStepHeader가 goal-chip을 모든 단계에 무조건 렌더하면서 목표 문구가 예측 화면으로 올라왔고, git show 1d7fc32^:prototype/verify.js:2199 대조 결과 이전 버전은 '자료를 보기 전 예측'이라는 내용 중립 라벨이었다. 앱의 대표 기제가 첫 화면에서 깨진 것을 아무도 보지 않았다. 누출 채널도 하나가 아니다 — 같은 칩이 자가진단 화면에도 나오고, 직전 renderIntroMethod(1908-1909)가 '① 절기와 기후는 어떻게 다를까'를 인쇄하며, 필수 LENS(2363)가 미션 1의 결론 수치 68/31을 조작 전에 인쇄한다. goal-chip만 되돌려도 사전 문항은 완전히 봉인되지 않는다. (2) 문서-코드 시차가 구조적이다 — 이 커밋은 verify.js 713줄을 바꾸면서 데모_대본.md·발표_10분_구성안.md를 건드리지 않았고(git show --name-only 실측), 어떤 게이트도 '대본이 코드보다 오래됐다'를 검사하지 않는다. 그리고 데모_대본의 '미니차트 카운트업'은 1d7fc32뿐 아니라 cbd3c6e 시점에도 이미 없었으므로 두 세대에 걸쳐 방치된 문서다 — '리팩터 후 갱신'이 아니라 전면 재작성이 필요하다. 더 나쁜 것은 레드팀_4차_종합보고서가 '최신 상태는 Self-reflection을 기준으로 읽으라'고 유도하는데 그 문서 §4-4가 '아직 배포하지 않았다'로 사실과 반대이고 §3 표가 리팩터 이전 측정이라는 점이다. (3) SCI-04의 원인이 통상 진단과 반대다 — 데이터를 직접 로드해 확인한 결과 파이프라인은 이미 정확한 값을 넣어 두었다(`서울 sensitivity = {min:4, max:17.2, long:9.6, longYears:30}`). 문서의 '+10일'은 9.6의 올바른 반올림이고, 화면의 '+9일'은 sensitivityAt(508-521)이 이미 정수 반올림된 windows doy를 다시 차분한 이중 반올림 산물이다. 결정적으로 정확한 필드는 verify.js:1059에서 `var sens = c.sensitivity;`로 읽히기만 하고 파일 전체에서 한 번도 사용되지 않는 죽은 변수다 — 즉 올바른 수정은 문서를 +9로 내리는 것이 아니라 코드가 자기 파이프라인의 값을 쓰게 되돌리는 것이며, 문서를 내리면 정확도가 오히려 떨어진다. (4) 팀 자체 게이트가 지금 빨간불인데 어떤 보고서도 그것을 현재 상태로 기록하지 않았다 — submit_check.sh 실측 exit 1, 제출_체크리스트.md:70의 자기 규정상 업로드 금지 상태다. (5) 산출물 무결성 버그가 리팩터가 만든 신규 조합이다 — selfChecks 삭제 경로(2792/1707/2511)와 완료 관문(2798-2809)이 어긋나 이미 맞힌 자가진단이 '미응답'으로 인쇄되면서 같은 화면이 'CERL 5편 완료 · 매 미션에서 직접 작성했습니다'를 단정한다. 교사가 회수하는 유일한 산출물이 학습 사실과 어긋난다. 같은 성격으로 여름 문장의 겨울 재봉인 경로도 감사 화면의 '선택 확장' 버튼뿐 아니라 explore의 여름/겨울 토글(1707)이라는 1급 컨트롤에서도 열린다 — 접근성이 기존 서술보다 높다. (6) 접근성 회귀와 문서의 충돌 — 레드팀_4차_종합보고서.md:483이 '라이트·다크 전수 계산, WCAG AA 위반 0건'이라 단언하는데, 이 커밋이 step-tag를 주황 틴트 컨테이너 안으로 옮겨 4.686 → 4.282로 내렸다(독립 재계산). base.css:26-28 주석이 스스로 경고한 함정에 다시 걸린 것이고, 저장소에 남은 문서가 지금 사실이 아닌 접근성 주장을 하고 있다. (7) 리팩터가 학습 흐름을 코드에 두 버전으로 남겼다 — showLensGate 35줄이 호출자 0개이고 그 함수가 읽는 predictOverlay는 저장소 어디에도 정의가 없다(grep 실측). LENS를 손대려는 사람이 사장 사본을 실제 흐름으로 오인할 위험이 8/21까지의 작업 기간에 실재한다. 마지막으로 방법론적 교훈: 통과하는 게이트가 무엇을 검사하지 않는지를 보라. 8,303건 assert에 열관성 실험실의 물리 코드가 0건이고(4개 게이트 파일 grep 히트 0), 극한지수 표의 자기 검산이 0건이며(32행 중 16행 실패), studentEvidenceHTML의 지역·기준 인자가 0건이고, 문서 F축은 '시차'만 쓴 줄을 스킵한다.

---

## 6. 반증 과정에서 새로 발견된 것 (15건)

- **(EDU-FLOW / EDU-01)** 봉인 누출 채널이 하나 더 있다: 예측 화면 직전 화면인 renderIntroMethod(verify.js:1908-1909)가 'intro-lead'에서 '절기·날씨·기후를 구분하는 힘을 기릅니다'를, 접힘 패널 '이번 학습에서 기를 힘 5가지'에서 '① 절기와 기후는 어떻게 다를까'를 인쇄한 뒤 introMission→startMission(0)→renderPrediction으로 곧바로 PRE_QUESTION을 낸다. 즉 goal-chip만 되돌려도 사전 문항은 완전히 봉인되지 않는다 — 사전/사후 기준선을 쓰려면 인트로 문구까지 중립화해야 한다.
- **(EDU-FLOW / EDU-06)** 동일 결함이 더 중심 경로에도 있다: bindLagControls 내부 season()(verify.js:1704-1707)이 auditWinter와 똑같이 predicts/selfChecks만 지우고 missionCerl·cerlSubmitted는 남긴다. 즉 여름 문장이 겨울 산출물로 재봉인되는 경로는 감사 화면의 '선택 확장' 버튼뿐 아니라 미션 5 조작 화면의 여름/겨울 토글에서도 열린다 — 지적이 서술한 것보다 접근성이 높다.
- **(EDU-FLOW / EDU-12)** 주석이 '보기 길이도 맞췄다'고 선언한 POST 문항조차 완전히 맞지 않았다 — 실측 a30 / b(정답)40 / c30으로 정답이 여전히 최장(1.33배)이다(verify.js:62-70). 즉 팀이 대책을 적용했다고 문서화한 지점에서도 길이 단서가 남아 있다.
- **(EDU-ASSESS / EDU-08)** 더 심각한 산출물 무결성 버그: 이미 응답한 자가진단이 지워져 '미응답'으로 인쇄된다. verify.js:2792(감사 화면의 '겨울·동지에도 적용해 보기')와 1707(explore의 계절 전환 버튼)이 `delete state.selfChecks['lag']`를, 2511(증거 조건 변경 후 '새 조건으로 증거 갱신')이 `delete state.selfChecks[m.id]`를 실행한다. 재현: 미션5 자가진단까지 정답으로 마친 학생이 audit 화면에서 '겨울·동지에도 적용해 보기'를 눌렀다가 겨울 조작(markDoy·lagRevealed)을 끝내지 않고 진행 칩으로 이탈 → state.done에는 'lag'가 이미 들어 있고(2769) cerlSubmitted도 유지되므로 renderComplete 관문(2798-2809)을 통과한다 → 내 기록 2845행이 미션5를 '자가진단 미응답'으로 인쇄하는데, 같은 화면 2852-2854행은 'CERL 5편 완료 · 매 미션에서 … 직접 작성했습니다'를 무조건 선언한다. 즉 학생이 실제로 맞힌 형성평가 기록이 선택 확장 버튼 한 번으로 소실되고, 교사가 회수하는 산출물이 학습 사실과 어긋난다.
- **(APP-STATE / APP-01)** 같은 labBack이 인트로 진입(1894)에서는 더 나쁘게 작동한다 — 미션을 하나도 하지 않은 학습자가 소개 화면에서 '🔬 열관성 실험실' → '← 미션으로 돌아가기'를 누르면 startMission(4)로 곧장 '미션 5 / 5'에 떨어져 미션1~4를 건너뛴 채 학습이 시작되고 phase='mission'이 저장된다. 또 renderFree(3491-3545)에는 미션으로 돌아가는 버튼이 아예 없어(자유탐구 → 미션 복귀는 done 칩 또는 homeLink뿐), 실험실·자유탐구 두 곳이 공통으로 '나갈 문은 하나, 그 문이 미션5를 초기화한다' 구조다.
- **(APP-STATE / APP-07)** 같은 결함의 '실제로 도달 가능한' 변종이 있고 이쪽이 더 심각하다(P2). 앱이 해시를 절대 쓰지 않으므로(pushState 없음) 진입 URL의 해시는 그 탭에 영구히 남고, applyHash는 부팅마다 다시 적용된다. 그런데 자유탐구 3503-3505의 '🔗 이 화면 링크 복사'는 '선생님이 배부할 때 쓰세요'라고 안내하며 #c=…&m=…&t=…&s=…&v=free 링크를 만든다 — 앱이 스스로 배부를 권하는 기능이다. 이 링크로 들어온 학생이 homeLink → 시작하기(1913행 introMission → startMission(0))로 미션1에 진입해 진행하다 F5를 누르면, applyHash가 phase='free'와 c/m/t/s를 다시 덮어쓰고 save()하므로 매 새로고침마다 자유탐구로 튕긴다. renderFree(3491-3545)에는 미션 복귀 버튼이 없고 done이 없으면 진행바 칩도 span이라, 탈출 경로는 homeLink → startMission(0)뿐이어서 moved/touched·visited가 다시 초기화된다(기준선 재드래그 필요). 모바일 탭 복원까지 고려하면 교실에서 재현 가능성이 높다.
- **(APP-UX / APP-UX-01)** 데모_대본.md의 '미니차트 카운트업'은 1d7fc32뿐 아니라 cbd3c6e 시점에도 이미 없었다(git show cbd3c6e:verify.js:1866-1888의 renderIntro에도 차트 없음, intro-seal 3단 도식만). 즉 두 세대에 걸쳐 방치된 문서이므로 '리팩터 후 갱신'이 아니라 대본 전면 재작성이 필요하다.
- **(APP-UX / APP-UX-03)** 레드팀_4차_종합보고서.md:483이 '라이트·다크 양쪽 텍스트 요소 전수 계산, WCAG AA 위반 0건'이라고 단언하는데 이 보고서는 1d7fc32(21:50)보다 앞선다. 즉 저장소에 남은 문서가 지금 사실이 아닌 접근성 주장을 하고 있다 — 질의응답에서 대비 검사 도구로 반박당할 수 있는 문서 리스크.
- **(APP-UX / APP-UX-04)** 같은 함수에 더 확실한 키보드 결함이 있다. verify.js:1962 `$('termNext').hidden = state.termIntroStep === labels.length - 1;` — 사용자가 4단계에서 '다음 →'을 누르면 방금 누른 그 버튼이 hidden이 되어 포커스가 body로 떨어진다(포커스된 요소를 숨기면 브라우저가 포커스를 문서로 되돌림). 이후 Tab이 문서 맨 위(헤더 브랜드·사용법)부터 다시 시작하므로 바로 옆의 '이제 검증하러 가기 →'(verify.js:1953 orbitStart)에 도달하려면 페이지 전체를 다시 훑어야 한다. 지적된 '포커스가 다음 버튼에 남는다'보다 나쁜 상태다. 부수적으로 state.termIntroStep이 localStorage에 저장(verify.js:553·578)되고 renderTerms 진입 시 초기화하지 않으므로, 5/5까지 본 뒤 소개로 돌아가 '🌍 24절기가 뭐예요?'를 다시 누르면 궤도 패널이 hidden인 채 마지막 패널이 뜬다 — 발표_10분_구성안.md:41이 이 화면에서 '궤도에서 처서 클릭'으로 시연을 시작하므로 리허설 직후 ↺ 처음부터를 빠뜨리면 시연 첫 동작이 불가능해진다.
- **(SCI / SCI-01)** 게이트가 이 반올림 규약을 문서로까지 전파를 강제한다(verify_solar_terms.py:422-436의 shown() 함수). 즉 '표시값 22.5→23'을 문서도 반드시 23으로 적게 만들어, 원본 소수값이 문서 어디에도 남지 않는다. 변화 열 소수를 살릴지, 현재 열 소수를 살릴지 결정하면 게이트의 shown()도 함께 고쳐야 한다.
- **(SCI / SCI-04)** 더 심각한 발견: 파이프라인은 이미 정확한 값을 계산해 데이터에 넣어 두었다. solar_terms_data.js의 서울 sensitivity = {min:4, max:17.2, long:9.6, longYears:30}(build_solar_terms.py:198-201, 반올림 전 doy 평균으로 계산). 문서의 '+10일'은 이 9.6의 올바른 반올림이고, 화면의 '+9일'은 verify.js:508-521 sensitivityAt이 이미 정수로 반올림된 windows의 doy(build_solar_terms.py:162 int(round(...)))를 다시 차분해 만든 이중 반올림 산물이다. 최대값도 17.2 → 17로 깎인다. 결정적으로 정확한 필드는 verify.js:1059에서 'var sens = c.sensitivity;'로 읽히기만 하고 파일 전체에서 한 번도 사용되지 않는 죽은 변수다(grep '\bsens\b' 히트 1건). 즉 앱은 자기 파이프라인이 계산한 값보다 부정확한 값을 화면에 인쇄하고 있고, 올바른 수정은 문서를 고치는 것이 아니라 sensitivityAt이 c.sensitivity를 쓰도록 되돌리는 것이다.
- **(JUDGE / JUDGE-05)** 발표 PDF/scripts/build_deck.py:333의 운영 조건 카드가 "· '↺ 처음부터'로 공용 PC 대응"이라고 주장하는데, 그 버튼을 실제로 누르라고 지시하는 곳이 교실 문서에 없다 — prototype/교사_학습지.html 전문에 '처음부터'·'초기화'·'공용' 세 단어가 0회다(grep 결과 무). 즉 '공용 PC 대응'의 유일한 수단이 교사에게 전달되지 않는다. 학습지 준비물/운영 안내에 '학생 교체 시 헤더 ↺ 처음부터를 누르게 한다' 한 줄을 넣는 것이 코드 수정보다 먼저다.
- **(JUDGE / JUDGE-07)** 더 심각한 것을 찾았다 — 미션 4의 증거 화면이 학습자가 조작한 값을 아예 보여 주지 않는다. studentEvidenceHTML은 m.id==='rain'일 때 행을 ['서울·1mm 이상', '서울·50mm 이상'] 두 개로 하드코딩한다(verify.js:2590-2594). 이 함수는 evidence 단계(2531)와 write 단계(2614) 양쪽의 '내가 읽은 증거' 카드다. 따라서 학생이 프리셋 30mm를 눌러 조작한 뒤 '증거 정리하기 →'를 눌러도, 증거 카드와 CERL 작성 화면에는 30mm가 한 번도 나타나지 않고 1mm/50mm만 나온다. 현재 기준값은 expert 단계의 전문가 예시 문장(2213)에서만 등장한다. '내가 정한 기준으로 내가 읽은 증거'라는 이 앱의 핵심 주장이 미션 4에서만 끊기고, 과제문이 시키는 1→10→30→50 조작의 결과가 증거 카드에 반영되지 않는다. 이것이 JUDGE-07의 비단조 문제보다 재현이 쉽고(프리셋 한 번) 데모에서 더 잘 드러난다.
- **(JUDGE / JUDGE-09)** 대신 실제 심사 대상 문서에서 미이행 항목을 찾았다. 개발계획서_Weather24.md:65가 IBTrACS 태풍 경로를 '태풍 시뮬 모듈'로, :101이 사전 개발(7.23~8.7) 작업으로 '폭염·태풍 확장 모듈'을, :171이 체험·참여형 근거로 '타임랩스 재생'과 '퀴즈'를 열거한다. 그러나 verify.js 전문에 '태풍' 0회, '타임랩스' 0회, '재생' 0회다(폭염은 기준표 서랍으로 구현됨). 즉 심사위원이 되돌아볼 수 있는 문서에서 검증 가능한 미이행은 태풍 모듈과 타임랩스 두 개이며, 이 둘은 README의 '미사용' 표에도 상태가 적혀 있지 않다. 대응은 기능 구현이 아니라 README에 '계획 대비 범위 조정' 한 줄(태풍·타임랩스는 학습 초점 집중을 위해 제외)을 남기는 것이다.
- **(DOC / DOC-02)** 발표_10분_구성안.md:38-41의 '1:30–1:55(25초) 24절기 입문' 슬롯도 같은 누락이 있다. renderTerms는 5패널 단계형이고(verify.js:1957 labels ['1/5'…'5/5']), 슬롯이 요구하는 '오개념 교정 한 줄(거리 3.4% vs 자전축 23.4°)'은 4번째 패널(:1944-1946, data-term-panel=3)에 있어 '다음 →'을 3회 눌러야 도달한다. '이제 검증하러 가기' 버튼도 5/5에서만 노출된다(:1963). 즉 25초 슬롯에 최소 4회 추가 클릭이 숨어 있다.

---

## 7. 기각된 지적 (되살리지 말 것)

- **EDU-09** LENS 정답키가 5년 통계를 '기후'로 채점하는데 앱의 모든 화면 배너는 '5년은 30년 기후평년이 아니다'를 반복한다 — 배너를 내재화한 학생이 오답 처리되고 기록에 남는다
  - 기각 사유: 실패 시나리오가 화면에서 성립하지 않는다. 분류 버튼은 층 이름만 주는 것이 아니라 앱의 정의를 함께 인쇄한다 — LENS.kinds(verify.js:2356-2358) '날씨 / 하루·한때의 관측', '기후 / 여러 해에 걸친 경향'이 renderLensStep(2269-2271)의 `<b>kd.t</b><small>kd.s</small>`로 버튼 안에 그대로 렌더된다. item c는 '최근 5년 평균 68일 / 1969–73년 31일'로 명시적 다년 통계이므로, 학생이 클릭하는 버튼에 인쇄된 정의만 읽으면 '날씨(하루·한때의 관측)'는 배제된다. 배너의 규범('5년은 30년 기후평년이 아니다')은 기후평년의 산출 기간에 관한 진술이고 절기/날씨/기후 층 분류와 다른 층위이며, item c의 why(2363)가 답한 직후 '여러 해를 모아 센 값이라 경향에 관한 진술입니다. 다만 5년은 관측 신호이지 30년 기후평년은 아니에요'로 그 구분을 즉시 명시한다. '배너를 내재화한 학생이 벌점을 받는다'는 버튼에 적힌 정의를 무시한다는 가정이 필요해 재현 불가 → DROP.
- **APP-UX-07** 한 화면에 서로 다른 단계 카운터 4종이 동시에 뜬다 — 사용법 다이얼로그 '다섯 단계', 상단 진행바 6칩, '미션 1 / 5', '1/10 · 예측'
  - 기각 사유: 전제가 무너진다. (1) '다섯 단계'는 리팩터 이전 흐름의 잔재가 아니라 1d7fc32에서 의도적으로 새로 쓴 문구다 — git show 1d7fc32 -- prototype/index.html: '이 앱은<br />세 단계로 움직입니다.'(3항목)를 '한 화면에 한 가지씩,<br />다섯 단계로 검증합니다.'(5항목)로 같은 커밋에서 교체했고, 5항목은 predict/orient/explore/evidence+write/check+expert에 정확히 대응하는 개념 묶음이다. 즉 stale copy가 아니라 설계된 요약이다. (2) '미션 1 / 5'와 '1/10 · 예측'의 병기는 위반이 아니라 기획서 요구사항이다 — 학습단계_분할_기획서.md:123 '상단에는 미션 2/5 · 3/8과 현재 단계명만 유지한다'가 두 카운터 병기를 명시적으로 지시했고 verify.js:2246-2249가 그대로 구현했다. (3) 근거로 든 §3.2(학습단계_분할_기획서.md:96) '같은 수치를 제목·그래프·카드·문단에 중복 노출하지 않음'은 문맥상 관측 데이터 값(일수·날짜)에 대한 규칙이며(같은 목록의 92 '즉시 비교할 핵심 값: 최대 3개' 참조) 진행률 지시자와 무관하다. (4) '한 화면에 4종 동시'도 사실이 아니다 — 사용법은 <dialog class="modal" id="guideDialog">(index.html:46)로 열리는 모달이라 미션 화면과 동시에 읽히지 않고, 닫은 뒤 남는 것은 상위(미션 5개)·중위(미션 내 단계)·하위(CERL 4요소)의 정상적 3단 중첩 진행률이다. (5) 실패 시나리오가 '심사위원이 이런 질문을 할 수 있다'는 가정뿐이고 잘못된 결과를 낳는 입력·조작이 없다 → 취향 지적으로 DROP.
- **JUDGE-09** 서류심사를 통과시킨 개발계획서(20개 사건 · AI 보조관 5종 · 태풍·위성·재분석 비교)와 실제 산출물(5미션 · AI 1종 · 3개 데이터셋 미사용) 사이의 이행 격차가 크다
  - 기각 사유: 실패 시나리오의 전제가 사실이 아니다. 지적이 인용한 개발계획서(고도화).md는 서류심사에 제출된 문서가 아니다 — 접수 마감 2026.7.3, 서류심사 7.6~7.16(기상기후_AI_해커톤_2026_정보.md:105-106)인데, 이 파일은 2026-07-22 커밋 bcc2510('feat: build weather24 investigation experience')에서 처음 추가됐다(git log --diff-filter=A). 즉 본선 진출 확정 이후에 작성된 내부 확장 구상이며, '이 계획서로 15팀을 뽑았다'는 서술은 성립하지 않는다. 실제 심사 대상이었던 개발계획서_Weather24.md에는 20개 사건 파일도, AI 보조관 5종도, C-04 태풍 경보 판단도 존재하지 않는다(해당 문서에서 'C-04'·'보조관'·'반대신문관' grep 히트 0). ERA5·GPM·IBTrACS 미사용은 README.md:134-136이 표에 명시해 이미 처리된다. 남는 것은 '미구현 내부 구상 문서가 제출 저장소에 함께 들어 있다'는 정리 문제뿐이다.
- **DOC-11** 발표_10분_구성안:30의 '45분 수업'이 앱·README·교사 학습지가 일치시켜 둔 '핵심 15~20분 / 전체 55~70분(2차시)'과 충돌한다
  - 기각 사유: 핵심 근거가 사실이 아니다. (1) "'45분'은 발표_10분_구성안.md:30 단 한 곳"이 틀렸다 — grep 결과 개발계획서(고도화).md:230·413·460·485·513에 5건, 레드팀_카피·벤치마크보고서.md:393·491에 2건이 있다. 즉 지적이 전제한 '유일한 이탈 문구'라는 그림이 무너진다. (2) 실물 슬라이드에 45분 주장이 없다 — 발표자료 p10(교실 적용) 텍스트 추출 결과 '교사가 받는 것 / 학생이 남기는 것 / 운영 조건 / 접근성'뿐이고 시간 표기가 아예 없다. :30은 그 슬라이드에 담을 키워드 메모다. (3) 논리적 충돌도 성립하지 않는다 — README.md:39-42와 prototype/교사_학습지.html:34는 '핵심 코스 예상 15~20분 / 전체 55~70분(2차시)'이고, 15~20분 핵심 코스는 45분 1차시에 들어간다. '한 차시에 됩니까?'에 '핵심 코스는 됩니다, 전체는 2차시입니다'가 정답이며 앱·학습지가 그대로 그렇게 적고 있어 답변이 뒤집힐 근거가 없다. 취향·해석 수준의 지적이므로 DROP.

---

## 8. 확인된 강점 (12건)

- 【기술 기반 — 15팀 중 최상위권일 가능성이 높다】 verify.js 3,792줄 전체에 화살표 함수 0건·옵셔널 체이닝 0건·replaceAll/flatMap 0건(사실상 ES5), CSS에 :has() 0·@container 0·color-mix 0·dvh 0. 외부 JS 라이브러리·CDN·폰트 0개이고 vercel.json의 CSP가 default-src 'self'로 이를 강제한다. 첫 로드 실측 brotli 158KB / gzip 215KB / 요청 8건(index.html이 참조하지 않는 1.75MB thumbnail.png는 로드되지 않음). 로그인·설치·백엔드 DB 없음. 40명 교실 구형 크롬북에서 실패할 지점이 사실상 없다 — ⑤(30점)의 최대 자산이며 라이브 데모 리스크가 구조적으로 낮다.

- 【예측 봉인이 문구가 아니라 코드로 강제된다】 prototype/verify.js:2422-2424 renderExplore가 missionAsked(m) 거짓이면 무조건 예측 화면으로 되돌리고, 2256-2264의 5중 하향 가드가 evidence/write/check/expert/transfer/audit를 선행조건 미충족 시 앞 단계로 강제 복귀시킨다. applyHash(619-630)는 딥링크로 들어와도 touched=false·moved=false·markDoy=null까지 초기화한다. renderReadouts(936-940)는 예측 전 판독 카드를 '🔒 예측 봉인 후 공개'로 잠근다. `#v=m0&p=audit`·`#v=m4&p=audit` 등 우회 딥링크를 추적했으나 통과 경로를 찾지 못했다. 선택지 보조필드(o.s)를 의도적으로 렌더하지 않는 조치(2285행 주석)는 문항작성 수준의 배려다.

- 【일사량 천문 계산이 교육용 근사 수준을 훨씬 넘는다】 verify.js:3177 insolation()을 node로 발췌 실행해 위도 1° 격자에 cos(lat) 면적가중 전지구 연평균을 구하면 340.285 W/m²(이론값 S0/4 = 340.250, 오차 0.01%). 적도 연평균 415.6·하지 90°N 524.0 W/m²로 문헌값과 일치. Spencer(1971) 적위 근사와 일출각의 극야/백야 분기(x<=-1→π, x>=1→0)가 정확하다. 절기 24개 대표 날짜도 Meeus 겉보기 황경 교차(장동·광행차·ΔT 보정 + KST)로 1969~2026 전수 검산해 24/24 일치.

- 【열관성 실험실은 진짜 시뮬레이션이고 화면 주장이 재현된다】 verify.js:3164-3216의 0차원 EBM(C·dT/dt = Q(1−α) − (A+B·T), 40년 스핀업)을 독립 재구현: 서울 5m → 지연 40일·최고 27.9℃(화면 표기 40일·28℃와 일치), 깊이 0.5/2/5/15/50m → 3/17/40/72/88일 단조 증가. 온실효과 0→12 W/m²에서 연평균은 12.4→14.7℃로 오르지만 지연은 40→40일로 완전 불변 — 화면 발견 ②('온실효과는 지연을 바꾸지 않는다', 3470-3473)의 교육적 급소가 모형에서 실제로 성립한다. 전진 오일러 안정 조건도 하한 0.5m에서 9배 여유(dt·B/C = 0.215)로 발산·진동이 없다.

- 【과학적 무결성이 수사가 아니라 데이터·코드·게이트에 박혀 있다】 solar_terms_data.js에 자기 불리한 값이 숨겨지지 않고 저장돼 있다 — 서울 rainFlip.high = [80, -0.6](80mm에서는 큰비가 오히려 줄었다), seasonalLag에 jackknife 결과(jackMaxSpan, robust:false, outliers:['제주','강릉','전남']). extremes는 비교 기간이 다르다는 사실(1969–73 5년 vs 2022–25 4년)을 caption에서 스스로 밝힌다(verify.js:1217-1221). verify.js:1113 monthTable()은 '곡선에서 센 날수'가 '실측 연평균 일수'와 다르다는 것을 :1130에서 자기 고백하고 교재로 전환한다. verify_solar_terms.py 15축 8,303건(데이터 8,213 + 문서 90)이 문서 인용 수치까지 대조하며, 재실행 결과 그대로 통과한다.

- 【AI 실패가 학습을 끊지 않는 구조가 설계·검증 양쪽에서 완결됐다】 api/ai-turn.js:250-296 — 1차 추론 low(16초) → 잘리면 추론 끄고 재시도(9초) → 실패 시 원인 코드(timeout/network/truncated/upstream_nnn) 구분 반환. 기본 버튼은 외부 전송이 없는 기기 내 규칙 점검이고 외부 AI는 전송 문구 동의 체크박스 뒤에만 활성. 정량 검증한 것이 AI 경로가 아니라 폴백 경로(localAudit 100케이스 100/100·오탐 0)라는 사실을 README §4에 스스로 명시 — 과장 없이 '죽어도 되는 쪽'을 정확히 밝힌 드문 태도다. 키는 환경변수만 사용, store:false.

- 【접근성 구현이 실측 기반이다】 setStage(1869-1877)가 화면 전환마다 h1로 포커스를 옮기고, 시각적 h1이 없는 CERL 단계에도 sr-only h1을 만들어 뒀다(2669). aria-live는 파일 전체에 3개뿐이고 상호 배타적 화면에만 있어 중복 낭독이 없다(1604-1606 주석에 근거 명시). SVG 기준선 드래그의 비마우스 대안이 4중(슬라이더·±스테퍼·프리셋·표 보기)이고 aria-valuetext가 매 조작마다 실제 수치로 갱신된다(770-776). SVG 커스텀 컨트롤에 keydown이 실제로 붙어 있다(1554, 2037). verify.css:663-666은 터치 타깃을 '측정값: 사용법 48×20 · 처음부터 73×20'으로 실측한 뒤 32×32로 고친 흔적이다. #heroSvg의 touch-action을 pan-y ↔ none으로 제스처 단위 분리(verify.css:44-50 + verify.js:1675-1688).

- 【자기 반증 장치가 지금 이 순간에도 작동한다】 bash scripts/submit_check.sh를 실제 실행하면 7개 영역 중 6개(비밀값 0건·제출물 5종·배포 자산 8종 200·zip 청결·회귀 게이트 8,303건·평가 100/100)를 통과하고, '문서-현실 정합성'에서 README 커밋 수 표기(45)가 실제(47)와 다르다는 것을 스스로 검출해 exit 1을 반환한다(실측 REAL_EXIT=1). 자기 결과물의 문서 수치를 CI로 검사하는 팀은 15팀 중 소수일 것이며 그 자체가 ⑤의 가점 요인이다.

- 【입증되지 않은 학습 효과를 주장하지 않는 규율】 학생_파일럿_검증계획.md:3 '상태: 아직 실시하지 않음', :106 '빈칸이 남아 있으면 학생 검증 완료를 주장하지 않는다'. 제출전_Self-reflection_최종보고서.md:44·51이 '학습 효과 입증이라고 쓰면 제출 중단'을 자기 규정으로 걸었다. README.md:41-43·verify.js:1909가 시간을 모두 '학생 파일럿 전 추정치'로 한정한다. 레드팀 보고서 6종 전부가 첫머리에 staleness 배너와 최신 문서 포인터를 달았고, 레드팀_해결확인.md:3-11은 자기 '해결' 판정 4건이 거짓이었음을 표로 정정한다.

- 【교사가 실제로 들고 들어갈 수 있는 수업 자료가 실물로 있다】 prototype/교사_학습지.html 176줄 — 대상(중3~고2)·준비물·2코스 시간표·활동 1~6 지도안·오개념 8행 표(1월 초 0.983AU vs 7월 1.017AU, 자전축 23.4°, '해안이라서'의 반증으로 인천 11일·포항 10일)·6점 루브릭(:164-171)·2022 개정 성취기준 연결. 미션 5 지도안에는 '계절 지연은 과거 서울 41일, 현재 40일로 사실상 불변 → 원인 분리가 이 활동의 핵심'이라는 교사용 해설까지 들어 있다(:96).

- 【미션별 조작 게이트가 과제-특이적이다 — '슬라이더 한 번'으로 열리지 않는다】 missionDone(verify.js:2176-2188): region은 state.visited['제주']와 ['강원'] 둘 다, rain은 usedThr['1']과 30 이상 기준 사용을, canJudge는 lagMode에서 markDoy와 lagRevealed를 함께 요구한다. 미충족 시 missionTodo(2193-2205)가 '제주·강원 칩을 눌러 두 지역을 모두 비교해 보세요' 같은 구체 지시를 낸다. 증거 스냅샷 무효화도 6축(city/ti/metric/thr/lagSeason/markDoy) 전부를 비교해(2474-2478) 조건이 바뀌면 이전 글과 새 숫자를 조용히 섞지 않고 '이전 조건 복원 / 새 조건으로 갱신'을 명시적으로 묻는다.

- 【용어 수준 관리가 실제로 되어 있다】 '열관성'은 verify.js:293에서 '땅과 바다가 데워지는 데 시간이 걸립니다(열관성)'로 정의와 동시 도입. '잭나이프'는 코드 주석(429·433)에만 있고 화면은 '5년 중 한 해를 빼고 다시 계산해도 최대 N일밖에 움직이지 않습니다'로 용어 없이 개념만 전달(297). '0차원'은 3307행에서 '한 지점을 열을 머금는 물통 하나로 봅니다'로 즉시 환언. CERL은 인트로 2번째 화면(1906)에서 '주장·근거·추론·한계'로 전개된 뒤에만 약어로 쓰인다.
