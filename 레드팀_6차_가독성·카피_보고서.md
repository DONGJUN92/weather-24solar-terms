# 레드팀 6차 — 가독성·시인성·카피 정밀 감사

**대상** Weather24 (배포본 `https://weather-24solar-terms.vercel.app/`, verify.js?v=58 / verify.css?v=48)
**감사일** 2026-08-03 · 제출 마감까지 18일
**방법** 배포본을 브라우저에서 직접 구동하고 DOM을 계측했다. 모든 수치는 실측이며, 재현 조건을 함께 적었다.
**측정 뷰포트** 1440×900 · 1366×768 · 863×640 · 390×844 · 375×812
**심사 배점** ① 주제 적합성 20 ② 체험·참여형 25 ③ 학습 효과 10 ④ 독창성 15 ⑤ 실현 가능성 30

> **먼저 밝혀 둘 것** — 이 앱은 이미 상위권 완성도다. 계측으로 확인한 강점은 다음과 같고,
> 아래 지적은 그 위에 남은 마감의 문제다.
> - 가로 스크롤 0 (1440·1366·863·390·375 전부, `documentElement.scrollWidth − clientWidth = 0`)
> - 대비비 WCAG AA 실패 **2건뿐**(다크 1 / 라이트 2). 알파 합성까지 계산한 결과다.
> - 슬라이더에 `aria-label` + **`aria-valuetext`가 결과값까지 읽어 준다** (`"덥다 기준 25°C · 더위일 과거 31일, 현재 68일"`). 이 수준으로 만든 교육 웹앱은 드물다.
> - `word-break: keep-all; overflow-wrap: break-word`가 body 전역(base.css:44), `line-height: 1.72` 기본값
> - AI 감사관 실제 동작 확인 — 과대주장 문장을 넣자 200 OK / 3.65초 / 지역 확대·인과 단정을 정확히 지적
> - 첫 로드 자산 844KB 전량 자체 호스팅, 외부 CDN 0건, thumbnail.png(1.7MB)는 로드되지 않음

**감사 범위와 근거의 출처**

| 절 | 근거 |
|---|---|
| §1 §2 §3 §4 §7 §8 §9 | **배포본 직접 계측 + 소스 직접 확인.** Range API 줄 분해, `getBBox()` 대 `viewBox` 비교, 요소 쌍별 교차 검사, 알파 합성 대비비 계산, `performance` 리소스 집계, `/api/ai-turn` 실호출 |
| §5 §6 §11 | 소스·문서 전수 감사 후 **적대적 재검증**을 거침. 과장으로 판정된 논거는 뺐다(예: '검증소=기관 사칭'은 기각, 브랜드 '약속'은 P0→부분인정으로 하향) |
| 미수록 | 없음. 다만 §2·§4·§7·§8·§9는 보조 감사가 중단되어 **전부 직접 계측으로 대체**했다. 소스 정독으로만 나올 수 있는 항목(예: 미사용 CSS, 미도달 분기)은 이번 회차에서 다루지 못했다 |

**검증 과정에서 기각한 것** — 억지 지적을 넣지 않기 위해 다음은 확인 후 제외했다.
`검증소`가 기관 사칭으로 읽힌다(근거 없음) · `유효하다`가 중고생에게 어렵다(유효기간·유효슛 등 일상어) · 하드코딩 색상 29개(전부 `@media print` 안, 정당) · 음수 자간(대형 표제 관용 범위) · 범례 칩 줄바꿈(스와치 `<i>`를 별개 줄로 오인한 계측 오류) · `renderAuditStep`의 닫는 태그 파손(배포본 확인 결과 0건, 검색 도구 표시 이스케이프였음)

---

## 0. 최우선 7건 (모두 수정 비용 30분 이하)

| # | 증상 | 위치 | 실측 근거 | 배점 영향 |
|---|---|---|---|---|
| **1** | 궤도에서 **절기 3개(추분·백로·한로)가 잘려 안 보임** | `drawOrbit()` viewBox | 콘텐츠 bbox `y=−15.2`, viewBox 상단 0, `overflow:hidden` | ①③ |
| **2** | 막대그래프 라벨이 막대에 **가려짐** | `drawDrift()` verify.js:2093 | 라벨 폭 111.6 유닛 > 라벨 열 92 유닛 | ①⑤ |
| **3** | 열관성 실험실 차트 **주석 2개 충돌**(기본 설정 포함) | `renderLab` 조건 조작 탭 | 0.5m 174px / 2m 146px / **5m(기본) 100px** 겹침 | ②④⑤ |
| **4** | 지도(16지점) 뷰 **지점명·값 라벨 전부 겹침** | 지도 뷰 SVG | 16지점 × 2라벨 상호 충돌 24×3px 등 | ②⑤ |
| **5** | 히어로 리드 등 **고아줄 16곳** | 전역 | 마지막 줄이 첫 줄의 6~37% | ①③ |
| **6** | 1366×768에서 **주 CTA가 화면 밖** | 궤도 화면·자유탐구 | CTA top=890 / 1429 (vh=768) | ⑤ (라이브 데모) |
| **7** | 세로 여백 스케일 부재 — 섹션 경계 **0~2px** | 조작 화면 전역 | 한 화면에 여백값 20종, 최대 리듬 20px | ①②⑤ |

**시각 결함과 별개로, 제출 전에 반드시 닫아야 하는 것 3건** (상세 §11)

| | 증상 | 근거 |
|---|---|---|
| **A** | 폐기된 `/archive.html`이 라이브에 살아 있고, **본 앱의 외부 전송 동의를 우회해** 학생 글을 OpenAI로 보낸다 | URL 직접 접속 확인 · `app.js:350` |
| **B** | `submit_check.sh`의 정규식 2개가 백슬래시를 잃어 **검사하려던 것을 못 잡으면서 항상 ✓를 찍는다** | `submit_check.sh:149, 156` |
| **C** | 게이트가 **"배포본 == 제출 소스"를 검증하지 않는다** — 가장 무거운 실격 사유가 자동 검사 밖 | `submit_check.sh` §4 |

---

## 1. 첨부하신 스크린샷 3장 — 원인 규명

### 1-1. 사진 ① 히어로 리드의 고아줄 — "도구입니다."만 혼자 남는다

| 항목 | 값 |
|---|---|
| 요소 | `p.intro-lead.intro-lead-compact` (verify.js:1918) |
| 문구 | "절기의 약속을 실제 관측 자료로 직접 검증하고, 내 근거로 결론을 만드는 기후 학습 도구입니다." (53자) |
| 실측(1440px) | 2줄 — **[562px, 78px]**, 마지막 줄 비율 **0.14** |
| CSS | `.intro-lead-compact { max-width: 620px }` (verify.css:913), `font-size:15px`, `line-height:1.7` |

**원인** 620px 상자에 15px 한글 → 한 줄 약 41자. 53자 문장이 47자 + 6자로 갈린다.
그리고 **`text-wrap: pretty`가 이 코드베이스 전체에 단 한 번도 쓰이지 않았다**(verify.css·base.css 전수 검색 0건).
`word-break: keep-all`은 이미 켜져 있지만, keep-all은 *어절 중간에서 끊기는 것*만 막을 뿐 *마지막 어절만 남는 것*은 막지 못한다.

**처방 — 두 가지를 같이 해야 한다**

① CSS 한 줄 (전역 효과, 가장 큰 지렛대)
```css
/* base.css — 본문 전역 */
p, li, dd, .sub, .why-p, .orient-brief, .cerl-intro, .integrity, small { text-wrap: pretty; }
/* 제목은 두 줄 균형 */
h1, h2, h3, .stage-h, .intro-h, .hero-headline, .why-h { text-wrap: balance; }
```
`text-wrap: pretty`는 Chrome 117+/Safari 17.5+에서 동작하고, 미지원 브라우저는 현재와 동일하게 렌더된다(무해한 점진적 향상). **본 보고서 §2의 고아줄 16건 중 12건이 이 한 줄로 사라진다.**

② 문장 자체를 41자 이하로 (히어로는 CSS에만 맡기지 말 것)

| | 문안 | 자수 |
|---|---|---|
| 현재 | 절기의 약속을 실제 관측 자료로 직접 검증하고, 내 근거로 결론을 만드는 기후 학습 도구입니다. | 53 |
| **A안 (권장)** | 기상청 실측으로 절기가 지금도 맞는지 직접 확인하는 학습 도구입니다. | 36 |
| B안 | ‘덥다’의 기준을 내가 정하고, 우리 지역 실측으로 절기를 확인합니다. | 36 |
| C안 (2문장) | “처서가 지나면 더위가 그친다” — 정말 그런지 실측으로 확인합니다.<br>기준은 내가 정하고, 결론도 내 근거로 씁니다. | 32 / 26 |

---

### 1-2. 사진 ② 위쪽 빨간 원 — **절기 라벨 3개가 잘려 있다** (P0)

이건 여백 문제가 아니라 **잘림**이다. 사용자가 "적도"로 읽으신 글자는 위가 반쯤 잘린 **「백로」**다.

```
#orbitSvg  viewBox="0 0 640 460"   CSS: overflow: hidden
실측 getBBox → { x: 12.5, y: −15.2, w: 626.6, h: 469.5 }
                        ↑ 콘텐츠가 viewBox 위쪽으로 15.2 유닛 삐져나가 잘린다
```

| 라벨 | bbox y | 높이 | 잘린 양(유닛) | 잘린 양(1440px 화면) | 결과 |
|---|---|---|---|---|---|
| **추분** | −15.2 | 14 | 15.2 | 19.5px | **전부 잘림 — 화면에 없음** |
| **백로** | −7.6 | 14 | 7.6 | 9.7px | 54% 잘림 |
| **한로** | −7.6 | 14 | 7.6 | 9.7px | 54% 잘림 |

유저 좌표 기준이라 **뷰포트 폭과 무관하게 항상 잘린다**(1440·863·390 전부 재현).
"24절기를 전부 보여 주는 것"이 유일한 목적인 화면에서 24개 중 3개가 안 보이고,
그중 하나는 **춘분·추분·하지·동지 4대 절기 가운데 하나인 추분**이다. 기상·과학 심사위원이 가장 먼저 눈으로 세어 볼 지점이다.

**처방 (1속성)**
```js
// verify.js:1970
- <svg id="orbitSvg" viewBox="0 0 640 460" …>
+ <svg id="orbitSvg" viewBox="0 -24 640 508" …>
```
상단 24유닛 + 하단 24유닛 여유. 하단 여유는 1-3의 캡션 충돌도 같이 해결한다.
(비율이 조금 작아지는 것이 싫다면 `drawOrbit()`의 원 중심 cy를 +24, 캡션 y를 +48 하는 방식도 동일한 결과다.)

---

### 1-3. 사진 ② 아래쪽 빨간 원 — 캡션이 절기 라벨과 겹치고, 모바일에서 6px로 줄어든다

| 항목 | 값 |
|---|---|
| 요소 | `<text y="452">비스듬히 내려다본 모식도입니다 — 거리 차이가 3.4%뿐이라 궤도를 거의 원으로 그렸습니다(실제 비율 아님).</text>` |
| 충돌 상대 | 절기 라벨 **춘분** (bbox 하단 441.3) |
| 실측 겹침 | 1440px: 28×2px · 863px: 28×2px · **390px: 24×3px (면적비 17%)** |
| 모바일 글자 크기 | 캡션 `font-size:11`(유저 단위) × 축척 0.578 = **화면상 6.4px** |

**원인 두 가지가 겹쳤다.**
(a) viewBox 높이 460인데 캡션이 y=452 — 하단 여유가 8유닛뿐이고 궤도 하단 라벨은 441까지 온다.
(b) 캡션이 SVG 안에 있어 **축척과 함께 축소된다.** 절기 라벨은 `@media` 규칙으로 모바일에서 11→21유닛으로 키워 놨는데, 캡션·"태양"·"북극"·"1월 초 · 가장 가까움(0.983 AU)"·"7월 초 · 가장 멂(1.017 AU)"는 그 규칙에서 빠져 있다. 그래서 모바일에서 **이 네 종류만 6~7px로 줄어든다.**

**처방 (권장: 캡션을 SVG 밖으로)**
```js
// verify.js drawOrbit() — 캡션 <text> 를 SVG에서 제거하고
// renderTerms()의 .orbit-wrap 바로 아래에 HTML 캡션으로 둔다
+ '<p class="fig-caption">비스듬히 내려다본 모식도입니다. 거리 차이가 3.4%뿐이라 궤도를 거의 원으로 그렸습니다(실제 비율 아님).</p>'
```
```css
.fig-caption{margin:8px auto 0;max-width:52ch;font-size:12px;line-height:1.7;
             color:var(--muted2);text-align:center;text-wrap:pretty}
```
이러면 (a) 겹침이 구조적으로 불가능해지고 (b) 모바일에서도 12px를 유지하며 (c) 두 줄로 자연스럽게 접힌다.
**부수 처방** — `@media`에서 절기 라벨만 키우는 규칙에 `태양`·`북극`·AU 라벨도 함께 넣어야 한다. 지금은 모바일에서 이 셋만 6.6px다.

---

### 1-4. 사진 ③ — 「태양년 (지구 한 바퀴)」가 막대에 가려진다 (P0)

원인이 코드에 그대로 있다.

```js
// verify.js:2095-2103  drawDrift()
var W = 640, H = 118, L = 92, R = 18, maxD = 400;   // ← L = 라벨 열 폭 92 유닛
g += '<text x="0" y="…" font-size="11.5" …>' + r[0] + '</text>'   // 라벨을 먼저 그리고
   + '<rect x="' + L + '" …/>'                                     // 막대를 그 위에 덮는다
```

| 라벨 | 실측 폭(getBBox) | 라벨 열 | 결과 |
|---|---|---|---|
| **태양년 (지구 한 바퀴)** | **111.6 유닛** | 92 | **19.6유닛(1440px에서 25px) 초과 → 막대가 덮음** |
| 음력 12달 | 51.7 유닛 | 92 | 안전 |

막대 `opacity:0.8`이라 글자가 비쳐 보여서 "지저분하게 가려진" 상태가 된다. 사진과 정확히 일치한다.
`<rect>`가 `<text>` 뒤에 그려지므로 **z축상 항상 막대가 이긴다.**

**처방 (권장 조합)**
```js
- var W = 640, H = 118, L = 92, R = 18, maxD = 400;
+ var W = 640, H = 118, L = 108, R = 18, maxD = 400;
…
- g += '<text x="0" y="' + (y + 14) + '" font-size="11.5" fill="var(--muted2)">' + r[0] + '</text>'
+ g += '<text x="' + (L - 10) + '" y="' + (y + 14) + '" text-anchor="end" font-size="11.5" fill="var(--muted2)">' + r[0] + '</text>'
…
- var rows = [['태양년 (지구 한 바퀴)', 365.24, …], ['음력 12달', 354.37, …]];
+ var rows = [['태양년', 365.24, …], ['음력 12달', 354.37, …]];
```
- `text-anchor="end"` 우측 정렬은 막대 차트의 표준 관례이고, 두 라벨의 오른쪽 끝이 맞아 읽기도 좋아진다.
- "(지구 한 바퀴)"는 바로 위 본문(`why-p`)이 이미 "지구가 태양을 한 바퀴 도는 데는 365.24일"이라고 설명하므로 중복이다. 빼도 정보 손실이 없다.
- 세 라벨 모두 남기고 싶다면 `L = 150`으로만 바꿔도 해결된다(막대가 짧아지는 대신 안전).

**같은 함수의 부수 지적** — `<text y="107" fill="var(--on-coral)">3년이면 약 한 달</text>`은 coral 사각형(y=10~68) **밖**에 그려진다. `--on-coral`은 coral 배경 위에 쓰라고 만든 색인데 카드 배경 위에 쓰이고 있다. 색 하나만 `var(--coral)`로 바꾸면 된다.

---

## 2. 고아줄(orphan) 전수 — 측정값과 개선 문안

판정 기준: 요소를 Range API로 줄 단위 분해해 **마지막 줄 폭 / 가장 긴 줄 폭 < 0.40**이면 고아줄로 집계했다.

### 2-1. 전수 표 (1440×900 기준, 표시 없으면 데스크톱)

| # | 화면 | 셀렉터 | 줄 폭(px) | 비율 | 끝에 남는 말 |
|---|---|---|---|---|---|
| O-01 | 소개 | `p.intro-lead-compact` | [562, **78**] | 0.14 | 도구입니다. |
| O-02 | 24절기 3/5 | `p.why-p` | [754, **136**] | 0.18 | 365.24일이 걸립니다. |
| O-03 | 24절기 3/5 | `p.why-p` | [747, 251] | 0.34 | “망종에 모내기”처럼요. |
| O-04 | **24절기 4/5** | `p.why-p` | [760, **45**] | **0.06** | 3.4%입니다. ← 최악 |
| O-05 | 24절기 5/5 | `p.why-p` | [771, 126] | 0.16 | 수십 년에 걸쳐 변합니다. |
| O-06 | 예측 봉인 | `p.predict-contract` | [630, **59**] | 0.09 | 고르세요. |
| O-07 | 예측 봉인 | `p.po-q` (발문 20px) | [607, 183] | 0.30 | 가장 정확히 설명한 것은? |
| O-08 | 탐구 방향 | `p.orient-brief` | [734, 256] | 0.35 | 유효한지 검증하세요. |
| O-09 | 조작 화면 | `p`(task 16px) | [774, 178] | 0.23 | 그치는 날’을 비교하세요. |
| O-10 | 조작 화면 | `p.integrity.is-compact` | [773, 240] | 0.31 | 거의 움직이지 않습니다 |
| O-11 | CERL | `p.cerl-intro` | [754, 116] | 0.15 | 그대로 저장됩니다. |
| O-12 | 실험실 3탭 | `b` | [377, **67**] | 0.18 | 확인하는 일 |
| O-13 | 조작 화면 **390px** | `small`(touchHint) | [348, 350, **39**] | 0.11 | 있어요. |
| O-14 | 조작 화면 **390px** | `p.integrity` | [314,320,326,**49**] | 0.15 | 않습니다 |
| O-15 | CERL **375px** | `b` "주장 → 근거 → 추론 → 한계" | [145, **27**] | 0.19 | 한계 |
| O-16 | CERL **375px** | `span` 기간 배지 | [320, **23**] | 0.07 | 아님 |

### 2-2. 구조적 원인 3가지

**① `text-wrap: pretty` 미사용 (0건)** — §1-1 참조. O-01·03·05·06·07·08·09·10·11·12·13·14가 이 한 줄로 개선된다.

**② `.why-p`에 `max-width`가 없다** — O-02~O-05의 직접 원인.
```css
/* verify.css:526 — 현재 */
.why-p { margin: 10px 0 0; font-size: 14px; line-height: 1.8; color: var(--muted); }
```
`.why-box`가 카드 폭을 그대로 물려주어 실측 **754~771px**. 14px 한글이면 **한 줄 55자**다.
한국어 본문의 권장 행장은 **35~45자(약 500~630px @14px)**. 55자는 눈이 다음 줄 시작점을 잃는 길이다.
```css
+ .why-p { max-width: 46ch; text-wrap: pretty; }
```
`ch` 단위는 폰트에 따라 흔들리므로, 확실히 하려면 `max-width: 620px`.

**③ 문장이 길다** — 화면 문자열 973개 중 **45자 이상이 185개**, 최장 201자.
한국어 화면 문장은 **한 문장 45자 이하 / 한 문단 3문장 이하**가 기준선이다.

### 2-3. 45자 초과 문장 — 쪼개기 제안 (대표 12건)

> 표기: **현재** → **제안**. 자수는 공백 포함.

| # | 위치 | 현재 (자수) | 제안 |
|---|---|---|---|
| S-01 | verify.js:1918 소개 리드 | 절기의 약속을 실제 관측 자료로 직접 검증하고, 내 근거로 결론을 만드는 기후 학습 도구입니다. (53) | 기상청 실측으로 절기가 지금도 맞는지 직접 확인하는 학습 도구입니다. (36) |
| S-02 | verify.js:1980 (4/5 패널) | 실제로 지구는 가장 추운 1월 초에 태양과 가장 가깝고(0.983 AU), 7월 초에 가장 멉니다(1.017 AU). 거리 차이는 약 3.4%입니다. (77) | 지구가 태양에 가장 가까운 때는 **1월 초**(0.983 AU)입니다.<br>가장 먼 때는 7월 초(1.017 AU)이고, 차이는 3.4%뿐입니다. (32/34) |
| S-03 | verify.js:1981 | 계절을 만드는 핵심은 거리가 아니라 자전축이 23.4° 기울어져 있다는 사실입니다. 축은 늘 같은 방향을 가리켜 북반구가 태양 쪽으로 기울 때 여름, 반대일 때 겨울이 됩니다. (98) | 계절을 만드는 것은 거리가 아니라 **23.4° 기울어진 자전축**입니다.<br>축이 늘 같은 곳을 가리키기 때문에, 북반구가 태양 쪽으로 기울면 여름, 반대면 겨울입니다. (36/44) |
| S-04 | verify.js:1976 | 옛날 달력은 달의 모양을 기준으로 삼았어요(음력). 그런데 달 12번이 도는 데는 354일, 지구가 태양을 한 바퀴 도는 데는 365.24일이 걸립니다. (79) | 옛날 달력은 **달의 모양**을 기준으로 삼았어요(음력).<br>그런데 달이 12번 도는 데는 354일, 지구가 태양을 한 바퀴 도는 데는 365.24일이 걸립니다. (28/48) |
| S-05 | verify.js:1978 | 그래서 태양의 위치로 1년을 24칸으로 나눈 24절기를 함께 썼습니다. 절기는 날씨 예보가 아니라 농사 일정표였어요 — “곡우에 못자리”, “망종에 모내기”처럼요. (91) | 그래서 **태양의 위치**로 1년을 24칸 나눈 24절기를 함께 썼습니다.<br>절기는 날씨 예보가 아니라 **농사 일정표**였어요. “곡우에 못자리”, “망종에 모내기”처럼요. (37/48) |
| S-06 | verify.js:1984 (5/5) | 절기 날짜는 태양의 위치로 정해져 해마다 거의 그대로입니다. 하지만 그 무렵의 실제 날씨는 해마다, 지역마다 다르고 수십 년에 걸쳐 변합니다. (76) | 절기 날짜는 태양의 위치로 정해져 **해마다 거의 그대로**입니다.<br>반면 그 무렵의 실제 날씨는 해마다, 지역마다 다릅니다.<br>그리고 수십 년에 걸쳐 **변합니다.** (33/28/18) |
| S-07 | verify.js:78 미션1 브리프 | 처서(8/23)는 “더위가 그침”을 뜻합니다. 서울의 과거(1969–1973)와 현재(2021–2025)를 비교해, 이 약속이 아직 유효한지 검증하세요. (78) | 처서(8/23)는 “더위가 그침”을 뜻합니다.<br>서울의 과거(1969–1973)와 지금(2021–2025)을 비교해, 정말 그런지 확인해 보세요. (24/47) |
| S-08 | verify.js:79 미션1 과제 | 그래프의 보라색 ‘덥다 기준선’을 위아래로 끌거나 아래 슬라이더·프리셋으로 정한 뒤, 과거와 현재의 ‘더위가 그치는 날’을 비교하세요. (76) | 보라색 **‘덥다’ 기준선**을 위아래로 끌어 기준을 정하세요.<br>그다음 과거와 지금의 ‘더위가 그치는 날’을 비교합니다. (31/32) |
| S-09 | integrity 문단 | ◈ 기상청 ASOS 실측 · 과거 5년(1969–1973) vs 현재 5년(2021–2025) — 관측 신호이고 30년 기후평년이 아닙니다 · 절기는 태양 위치로 정한 천문 날짜라 해마다 거의 움직이지 않습니다 (100) | 세 조각을 `·`로 잇지 말고 **줄바꿈 3개의 목록**으로. 가운뎃점 나열은 4개 이상이면 읽히지 않는다.<br>`◈ 기상청 ASOS 실측 · 과거 1969–1973 vs 현재 2021–2025`<br>`관측 신호입니다 — 30년 기후평년이 아닙니다`<br>`절기는 태양 위치로 정한 날짜라 해마다 거의 그대로입니다` |
| S-10 | verify.js:2335 예측 계약문 | 다음 화면에서 그래프를 조작해 이 생각을 검증합니다. 지금은 정답을 맞히려 하지 말고, 현재 생각을 고르세요. (57) | 다음 화면에서 직접 확인합니다.<br>지금은 맞히려 하지 말고, **지금 생각**을 그대로 고르세요. (17/32) |
| S-11 | verify.js:2606 CERL 안내 | 전문가 문장을 보기 전에 주장 → 근거 → 추론 → 한계 순서로 씁니다. 이 미션은 네 요소 모두 필수예요. 이전 단계의 내용은 그대로 저장됩니다. (74) | 예시를 보기 전에 **주장 → 근거 → 추론 → 한계** 순서로 씁니다.<br>이 미션은 네 가지 모두 필수예요. 쓴 내용은 자동 저장됩니다. (38/32) |
| S-12 | 실험실 "여기서 배우는 것" | 기후를 이해한다는 것은 자료를 보는 일만이 아니라, 가장 단순한 법칙 하나로 자연을 다시 만들어 보고 어디까지 맞는지 확인하는 일입니다. …(201자 문단) | 문단을 3문장으로. 첫 문장을 **"기후를 이해하려면 자료를 보는 것만으로는 부족합니다."**(28)로 짧게 끊고 시작. |

> 45자 초과 185건 전체 목록은 `_ko_long.txt`(자동 추출)에 있다. 위 12건은 화면 노출 빈도·심사 동선 기준 상위다.

### 2-4. 하드코딩 `<br>` 전수 — 11곳, 그중 진짜 위험은 1곳

`<br>`은 특정 폭에서만 옳고 나머지 폭에서 전부 틀리므로 전수 조사했다. **결과는 예상보다 훨씬 깨끗하다.**

| # | 위치 | 형태 | 판정 |
|---|---|---|---|
| **3** | **verify.js:1913** | `<h1 class="intro-h">24절기의 약속은<br>아직 유효할까?</h1>` | **문제 있음 — 아래 상세** |
| 1 | verify.js:1450 | 판정문 + `<small>` 보조문 사이 | 문단 구분 용도. 의미상 `<p>`가 맞지만 렌더는 정상 |
| 2 | verify.js:1559 | 지도 캡션의 조건줄 + 판정줄 | 동일 |
| 4–10 | verify.js:2980, 2985, 2986, 2987, 3006, 3012, 3017 | `<li><b>라벨</b><br>값</li>` | 라벨/값 쌍. 의미상 `<dl><dt>/<dd>`가 맞지만 반응형 위험 없음 |
| 11 | verify.js:3661 | 실험실 결론의 문장 구분 | 동일 |

**#3만 실제 결함이다 — 반응형이 아니라 텍스트 추출 때문이다.**

```
DOM textContent  →  "24절기의 약속은아직 유효할까?"
                                  ↑ 공백이 없다
```
`<br>`은 시각적 줄바꿈일 뿐 **어절 경계가 아니다.** 그래서
- 스크린리더가 "…약속은아직…"을 한 덩어리로 읽을 수 있고,
- 복사·붙여넣기와 검색엔진·소셜 스크레이퍼가 붙은 문자열을 가져간다.

(같은 마크업이 `archive.html`에도 있어 그 페이지 `<h1>`도 동일하게 붙는다.)

**처방** `<br>`을 없애고 CSS에 맡긴다 — 반응형까지 같이 해결된다.
```js
- '<h1 class="intro-h">24절기의 약속은<br>아직 유효할까?</h1>'
+ '<h1 class="intro-h">24절기, 지금도 맞을까?</h1>'      // 브랜드 교체안 적용 시
```
```css
.intro-h{ text-wrap: balance; max-width: 12ch; margin-inline: auto; }
```
`balance`는 두 줄의 길이를 스스로 맞춘다. 지금은 260px + 235px인데 균형 배치가 더 안정적이고, 어떤 폭에서도 어색한 줄바꿈이 안 나온다.
(최소 수정만 원하면 `약속은<br>아직` → `약속은 <br>아직` 처럼 **공백을 넣는 것만으로도** 텍스트 추출 문제는 사라진다.)

### 2-5. 줄간격 1.5 미만 셀렉터 전수

| 셀렉터 | line-height | font-size | 판정 |
|---|---|---|---|
| `.pstep small` (진행 표시 라벨) | **1.20** | 10.5px | **미달** — 5개 중 4개가 2줄이 되는 라벨이다 |
| `.intro-h` | 1.22 | clamp(26–40px) | 제목이므로 적정 |
| `.hero-headline` / `.modal h2` | 1.30 | clamp(21–27px) | 제목, 적정 |
| `.stage-h` / `.learning-panel > h2` | 1.35 | clamp(20–26px) | 제목, 적정 |
| `h1,h2,h3,.hero-headline`(모바일 분기) | 1.38 | — | 제목, 적정 |
| `.lens-btn small` | **1.40** | 11px | 보조 설명문 → 1.55 권장 |
| `.seal-step span` | **1.45** | 10.5px | 소개 3단계 설명 → 1.6 권장 |
| `.cerl-title` | 1.45 | clamp(17–21px) | 제목, 적정 |
| `.integrity.is-compact` | **1.45** | 0.76rem | **미달** — 모바일에서 4줄이 되는 문단이다 → 1.65 |
| `base.css` 1.45 | — | — | 확인 필요(어느 셀렉터인지) |

**정정하자면, 제목의 1.2~1.45는 옳다.** 고칠 것은 **본문·설명문 4개**(`.pstep small`, `.lens-btn small`, `.seal-step span`, `.integrity.is-compact`)뿐이다.

### 2-6. 이 항목들은 문제가 없다 (확인함)

- **음수 자간** 7곳, 최대 `-.02em`(`.tc-name` 21px = −0.42px). 한글 대형 표제에서 관용 범위이고 본문에는 쓰이지 않았다.
- **하드코딩 색상값 29개 전부가 `@media print` 블록 안**이다. 게다가 인쇄용으로 **테마 토큰 전체를 재정의**한다(`--ink`·`--panel`·`--coral`… 흑백 종이 기준). "다크 테마에서 인쇄하면 흰 종이에 흰 글자가 나오던" 문제를 이미 고친 흔적이다. 학생이 자기 CERL 기록을 종이로 낼 수 있다.
- **`prefers-reduced-motion`이 base.css:82에서 전역(`*, *::before, *::after`)으로 모든 transition·animation을 끈다.** 추가로 verify.css에 3개 개별 대응. 전정 장애 대응이 완비되어 있다.
- **`:focus-visible` 규칙 16개**(verify.css 15 + base.css 1). 키보드 포커스 표시가 실제로 설계되어 있다.

### 2-7. 고아줄이 구조적으로 생기는 어미 패턴

한국어 서술문은 **`~입니다 / ~합니다 / ~됩니다`**로 끝나므로, 문장이 한 줄을 6~10자 초과하면 **어미 한 덩어리만 다음 줄로 떨어진다**. 위 16건 중 11건이 이 패턴이다.

세 층위로 막는다.
1. **CSS** `text-wrap: pretty` — 브라우저가 마지막 줄에 최소 2어절을 남기도록 재분배
2. **행장** 본문 `max-width: 42~46ch` (14~15px 기준 약 560~620px)
3. **문장** 45자 초과 금지. 초과하면 무조건 두 문장으로.

---

## 3. 겹침·잘림 전수 (7건)

| # | 화면 | 충돌 | 실측 | 조건 | 심각도 |
|---|---|---|---|---|---|
| X-01 | 24절기 궤도 | 추분·백로·한로 라벨이 viewBox 밖 | y=−15.2 / −7.6 | **항상** | **P0** |
| X-02 | 24절기 궤도 | 캡션 × 춘분 | 28×2px (390px: 24×3px) | **항상** | P1 |
| X-03 | 24절기 3/5 | 「태양년 (지구 한 바퀴)」 × 막대 | 19.6유닛 ≈ 25px | **항상** | **P0** |
| X-04 | 열관성 실험실 | 「하지 6/21 · 햇빛이 가장 강한 날」 × 「모형의 가장 더운 날 …」 | 0.5m **174×16px** / 2m 146×16 / **5m(기본) 100×8** | 깊이 ≤5m | **P0** |
| X-05 | 지도(16지점) | 지점명 × 값 라벨 상호 충돌 | 서울×▲37일 24×3 / ▲37일×인천 10×12 (16지점 전부) | **항상** | P1 |
| X-06 | CERL **375px** | sticky `.step-actions` 2개가 상하 겹침 | 「다음 요소 →」 764–802 × 「← 증거 다시 보기」 782–802 | ≤375px | P1 |
| X-07 | 궤도 **390px** | 「북극」 × 「백로」 | 6×3px | ≤390px | P2 |

### X-04 상세 — 열관성 실험실 (발표 슬라이드의 대표 화면)

프리셋을 하나씩 눌러 가며 두 주석 라벨의 화면 좌표를 잰 결과다.

| 프리셋 | 모형의 가장 더운 날 | 「하지 6/21 …」와의 겹침 |
|---|---|---|
| 메마른 땅 0.5m | 6월 24일 | **174 × 16 px** |
| 내륙 2m | 7월 8일 | **146 × 16 px** |
| **한반도 5m (기본값)** | 7월 31일 | **100 × 8 px** |
| 연안 15m | — | 없음 |
| 대양 50m | — | 없음 |

**기본 상태에서 이미 겹치고, 학생이 슬라이더를 왼쪽으로 끌수록 더 심해진다.** 이 화면은 발표자료 5장(모형)의 근거 화면이고 `#v=lab` 딥링크로 시연하도록 대본에 적혀 있다.

**처방** 두 주석의 y를 분리하고, 가까워지면 위/아래로 자동 배치한다.
```js
// 두 라벨의 x 중심 거리가 (labelW/2 + otherW/2 + 8) 미만이면 한 쪽을 아래 단으로 내린다
var stack = Math.abs(xHot - xSolstice) < 150;
// 하지 라벨은 곡선 위(y-10), 모형 라벨은 곡선 아래(y+18)로 고정 분리해도 충분하다
```
가장 싼 해법은 **두 라벨을 처음부터 위/아래 다른 단에 두는 것**이다(하지는 상단, 모형은 하단). 겹침 조건 자체가 사라진다.

### X-05 상세 — 지도 뷰

16개 지점 각각이 `지점명` + `▲NN일` 두 줄인데 두 줄이 3px씩 겹치고, 인접 지점끼리도(예: 「▲37일」 × 「인천」 10×12px) 겹친다. `지도 16지점`은 미션 3(지역 범위)의 핵심 근거 화면이다.

**처방** 지점명과 값을 하나의 `<text>` 안에 `<tspan dy="1.15em">`으로 묶고, 라벨 앵커를 지점 위치에서 반경 방향으로 밀어내는 간단한 충돌 회피(수도권처럼 밀집한 곳은 leader line)를 넣는다. 최소 수정으로는 값 라벨의 `dy`를 3→14로 키우는 것만으로 자체 겹침 3px는 사라진다.

### X-06 상세 — 모바일 sticky 바 중첩

```
.step-actions              position: sticky, y 754–811, x 23,  w 329
.step-actions-sub          position: sticky, y 772–811, x  6,  w 363   ← 둘이 동시에 하단에 붙는다
```
서로 다른 부모에 있는 두 sticky 요소가 같은 하단 위치에 붙어 **서로를 덮는다**. 또한 스크롤 컨테이너에 `padding-bottom`이 없어 `.cerl-form` 상단 39px이 바 뒤로 들어간다.

**처방**
```css
@media (max-width: 480px){
  .step-actions{ position: static; }            /* 보조 액션은 스티키 해제 */
  .step-actions:not(.step-actions-sub){ position: sticky; bottom: 0; }
  .cerl-step-card{ padding-bottom: 76px; }      /* 스티키 바 높이만큼 확보 */
}
```

---

## 4. 여백·간격 — "너무 붙어 있다"의 정체

### 4-1. 측정 결과: 스케일이 없다

미션 조작 화면 **한 장**에서 쓰인 서로 다른 여백 값은 **20종**이다.

```
8px(39회)  6px(19)  4px(14)  2px(12)  1px(10)  7px(9)  16px(5)  13px(5)  10px(5)
9px(4)  12px(2)  3px(2)  10.4px(2)  18.4px  9.6px  8.8px  14px  12.8px  20px  −2px
```

- 최빈값이 **8·6·4·2·1px** — 즉 화면 대부분의 분리가 **한 자릿수 픽셀**이다.
- 가장 큰 세로 리듬이 **20px**. 24·32·40px 계층이 아예 없다.
- **−2px**가 존재한다(브랜드 워드마크와 서브타이틀이 겹친다).

정보 계층이 넷(섹션 → 카드 → 블록 → 라벨/값)인데 여백은 1~8px 한 구간에 몰려 있으니, **모든 것이 같은 거리로 보인다**. 이것이 "가려지고 가독성이 떨어진다"의 실체다.

### 4-2. 붙어 있는 지점 목록 (실측 간격)

| 위치 | 앞 요소 | 뒤 요소 | 실측 간격 |
|---|---|---|---|
| 헤더 | `<strong>WEATHER24</strong>` | `<small>절기의 약속 검증소</small>` | **−2px (겹침)** |
| 조작 화면 | `.chart-card` | `.integrity`(출처·한계 문단) | **0px** |
| 조작 화면 | `.view-tools`(그래프/지도/표) | 차트 SVG | **0px** |
| 조작 화면 | 차트 SVG | `.range-row`(기준 슬라이더) | **0px** |
| 조작 화면 | `.range-row` | `.presets`(프리셋 버튼) | **0px** |
| 조작 화면 | `.presets` | `.chart-legend` | **2px** |
| 프리셋 버튼 내부 | `<b>22°C</b>` | `<small>선선한 여름날</small>` | **1px** |
| 절기 카드 | `<small>태양 황경</small>` | `<b>150°</b>` | **3px** |
| 절기 카드 | `<b>150°</b>` | `<em>15° 간격 · 날씨와 무관…</em>` | **3px** |
| 실험실 결과 | `.ro-v` 값 | 설명문 | **3px** |
| 실험실 2탭 | 차트 SVG | `유효 열용량 깊이` 슬라이더 | **0px** |
| 24절기 3/5 | `해마다 10.9일 어긋남` | `3년이면 약 한 달` | **2.9px** |
| CERL 375px | `dt`(서울 · 덥다 25°C) | `dd`(기준 이상 더위일 …) | **2px** |

### 4-3. 처방 — 8pt 스케일 도입

```css
:root{
  --sp-0:2px;  --sp-1:4px;  --sp-2:8px;  --sp-3:12px;
  --sp-4:16px; --sp-5:24px; --sp-6:32px; --sp-7:48px;
}
```

| 계층 | 지금 | 바꿀 값 | 적용 대상 |
|---|---|---|---|
| 카드 ↔ 카드 | 0~18px | **`--sp-5` 24px** | `.card + .card`, `.why-box`, `.micro-panel` |
| 카드 내부 블록 ↔ 블록 | 0~8px | **`--sp-4` 16px** | 차트 ↔ 슬라이더, 슬라이더 ↔ 프리셋, 차트카드 ↔ integrity |
| 제목 ↔ 본문 | 4~8px | **`--sp-2` 8px** | `.why-h + .why-p`, `.stage-h + .sub` |
| 관련 항목 묶음 | 1~3px | **`--sp-1` 4px** | 프리셋 `<b>`↔`<small>`, `dt`↔`dd`, `.ro-v` 값↔설명 |
| 인라인 칩 내부 | 1~2px | 2px 유지 | 배지·범례 |

**우선 고칠 4곳 (가장 크게 체감된다)**

```css
/* ① 차트 카드와 출처·한계 문단을 떼어 낸다 — 지금 0px */
.integrity{ margin-top: var(--sp-4); }

/* ② 차트와 조작부를 떼어 낸다 — 지금 0px, 지금은 그래프가 슬라이더에 얹혀 보인다 */
.range-row{ margin-top: var(--sp-4); padding-top: 0; }
.presets  { margin-top: var(--sp-3); }
.chart-legend{ margin-top: var(--sp-3); padding-top: var(--sp-2); }

/* ③ 프리셋 버튼 안 두 줄 — 지금 1px */
.presets b { display:block; margin-bottom: var(--sp-1); }

/* ④ 워드마크 겹침 — 지금 −2px */
.brand strong{ line-height:1.15; }
.brand small { display:block; margin-top: 2px; line-height:1.3; }
```

### 4-4. 줄간격 점검 (한국어 기준 1.6~1.8)

| 셀렉터 | font-size | line-height | 비율 | 판정 |
|---|---|---|---|---|
| body 전역 | — | 1.72 | 1.72 | 양호 |
| `.why-p` | 14px | 25.2px | **1.80** | 양호 |
| `.intro-lead-compact` | 15px | 25.5px | 1.70 | 양호 |
| `.orient-brief` | 16.8px | 29.4px | 1.75 | 양호 |
| `p`(task) | 16px | 26.4px | 1.65 | 양호 |
| `.integrity` | 12.16px | 17.63px | **1.45** | **낮다** → 1.65 (20px) 권장 |
| `.cerl-intro` | 13.5px | 23.6px | 1.75 | 양호 |
| 진행 표시 `small` | 11.5px | 13.8px | **1.20** | **너무 낮다** → 2줄 라벨이 붙는다. 1.35 권장 |
| `.chart-legend span` | 11.5px | 19.78px | 1.72 | 양호 |

**줄간격은 전반적으로 잘 잡혀 있고, 문제는 두 곳뿐이다.**
- `.integrity` 1.45 — 화면에서 가장 조밀한 4줄짜리 문단인데 줄간격이 가장 좁다(모바일에서 4줄).
- 상단 진행 표시 `small` 1.20 — "처서, 약속은 유효한가"처럼 2줄이 되는 라벨이 5개 중 4개인데(실측 높이 28px = 두 줄) 줄 사이가 붙는다.

### 4-5. 1366×768 — 주 CTA가 화면 밖에 있다

라이브 데모 표준 해상도(주최측 PC·프로젝터)에서 잰 값이다.

| 화면 | 뷰포트 높이 | 주 CTA 위치 | 결과 |
|---|---|---|---|
| **24절기 궤도** | 768 | `다음 →` **top = 890** | **122px 아래 — 스크롤해야 보인다** |
| **자유탐구 1탭** | 768 | `이 조건의 증거 읽기 →` **top = 1429** | **661px 아래 — 두 화면 스크롤** |
| 개념 렌즈 | 768 | `다음 문장 →` top = 473 | 정상 |
| 열관성 실험실 | 768 | `조건을 바꿔 보기 →` top = 531 | 정상 |

궤도 화면은 SVG가 592px를 차지해(하단 y=872) 그 아래 버튼이 밀린다.
**처방** `#orbitSvg`에 `max-height: min(52vh, 520px)`를 걸고 `.orbit-wrap`을 `display:grid; place-items:center`로. 세로가 짧은 화면에서 궤도가 알아서 작아지고 CTA가 접히지 않는다.

> 제출_체크리스트.md는 "1366×768 · 1920×1080 · 2560×1440 전부 확인 — 가로 스크롤 0, 요소 넘침 0"이라 적었다. **가로**는 실제로 0이 맞다(재확인함). 위 항목은 **세로 폴드**의 문제라 그 점검에 걸리지 않았다.

---

## 5. 어색한 한국어 표현 — 전량 리스트업

> 아래 지적은 **적대적 재검증**을 거쳤다. 과장으로 판정된 논거는 뺐고, 코드로 확인된 것만 남겼다.

### 5-1. 브랜드 「절기의 약속 검증소」

**사실관계부터 정리한다.**
- `검증소`는 사전 등재어가 아니다. 한국어 `-소(所)`는 실재 기관·시설에만 붙는다(연구소·발전소·보건소·시험소·계량검정소). **「검증소」는 그 목록에 없는 조어다.** — 사용자 지적이 맞다.
- 하필 이 앱에는 `관측소`가 화면마다 나온다(`서울 관측소`, `춘천 관측소`). 같은 `-所` 계열이 나란히 놓여 **`검증소`도 실재 기관명처럼 보인다.**
- 소개 리드는 스스로를 **"기후 학습 도구입니다"**라고 정의한다. 브랜드는 *장소*, 자기 정의는 *도구* — 범주가 어긋난다.
- `약속`은 코드상 **민간 속담의 은유**로 정의되어 있다(verify.js:1936 `“처서가 지나면 더위가 그친다” 같은 절기의 약속`). 따라서 "과학적으로 틀렸다"고까지는 할 수 없다. 다만 **의인화 어투**인 것은 사실이고, 미션 제목 5개 중 이것만 문어체다.

**미션 제목 5개의 어투가 갈린다 — 이게 더 확실한 문제다.**

| | 현재 | 어투 |
|---|---|---|
| 1 | 처서, 약속은 **유효한가** | 명사종결 문어체 |
| 2 | 여름은 며칠**일까** | 구어 의문형 |
| 3 | 우리 지역만 **그럴까** | 구어 의문형 |
| 4 | 비는 줄었을까 **늘었을까** | 구어 의문형 |
| 5 | 가장 더운 날은 **언제일까** | 구어 의문형 |

1번만 톤이 다르다. 진행 표시줄에 다섯 개가 나란히 뜨므로 바로 눈에 띈다.

**브랜드 대안 10안**

| 순위 | 안 | 뉘앙스 | 심사위원 인상 | 중고생 이해도 | 리스크 |
|---|---|---|---|---|---|
| **1 (권장)** | **절기, 아직 맞을까** | 의문형·의인화 0·절기를 '기준'으로 둠 | 주제+활동이 6음절, 발표에서 소리 내기 좋음 | 최상 | 고유명사성 약함 → 워드마크 WEATHER24가 그 역할을 하므로 무해 |
| 2 | 절기, 지금도 맞나 | 더 담백 | 무난 | 최상 | 1안보다 후킹 약함 |
| 3 | 내 기준으로 보는 24절기 | 체험성 전면화(②25점 직결) | "내가 정한다"가 즉시 전달 | 상 | 12자 — 서브타이틀엔 길다 |
| 4 | 관측으로 확인하는 24절기 | 중립·정확 | 안전하나 밋밋 | 상 | 차별점 없음 |
| 5 | 움직이지 않는 절기, 움직이는 기후 | 앱 핵심 논지 그대로(verify.js:1984 재활용) | 인상 강함 | 상 | 17자 — 브랜드보다 **발표 슬로건**에 적합 |
| 6 | 절기 검증 노트 | 산출물 은유, `-소`보다 자연 | 학습 도구다움 | 상 | '노트'가 정적 인상 |
| 7 | 처서는 아직 처서일까 | 강한 후킹 | 기억에 남음 | 최상 | 미션1에 종속 — 5개 미션을 못 덮음 |
| 8 | 절기 검증소 (약속만 제거) | 최소 변경 | 변화 체감 적음 | 상 | `검증소` 조어 잔존 — **비권장** |
| 9 | 24절기 검증 실험실 | 과학다움 | 좋음 | 상 | 앱 안에 이미 '열관성 실험실'이 있어 명칭 충돌 — **비권장** |
| 10 | 절기 vs 기후 | 짧고 대비 선명 | 강렬 | 상 | 영문 vs — 학습지·공문에 부적합 |

**1안 적용 시 일괄 치환**
```
index.html:8   <title>Weather24 | 절기의 약속 검증소</title>   → Weather24 | 절기, 아직 맞을까
index.html:16  og:title                                        → Weather24 | 절기, 아직 맞을까
index.html:17  og:description "기준선 하나로, 내 지역에서 절기의 약속을 실측으로 검증하는 기후 레슨"
                                                               → 기준은 내가 정하고, 우리 지역 실측으로 절기를 확인하는 기후 수업
index.html:30  <small>절기의 약속 검증소</small>                 → <small>절기, 아직 맞을까</small>
index.html:30  aria-label "Weather24 절기의 약속 검증소 — 소개로" → Weather24 절기, 아직 맞을까 — 소개로
verify.js:1913 24절기의 약속은<br>아직 유효할까?                  → 24절기, 지금도<br>맞을까?
verify.js:76   title: '처서, 약속은 유효한가'                     → '처서, 지금도 맞나'
verify.js:2146 chuseo: '처서의 약속은 아직 유효할까?'             → '처서가 지나면 정말 더위가 그칠까?'
verify.js:2621 placeholder '나는 이 절기의 약속이 …라고 판단한다.' → '나는 …라고 판단합니다.'   ← 한다체/합쇼체 혼용도 함께 해소
교사_학습지.html:4,32,175 동일 치환
```

### 5-2. 「관측 자료 조작」 — 소개 화면 2단계 (우선 교체 권장)

```js
// verify.js:1934
<div class="seal-step"><small>2 · 다음</small><b>관측 자료 조작</b><span>기준·지역·기간</span></div>
// verify.js:1932 aria-label
"1단계 자료 없이 예측, 2단계 관측 자료 조작, 3단계 내 CERL 작성"
```

- **양쪽 해석이 다 성립한다.** 과학 교육과정에는 `조작변인`이 표준 용어이므로 교사·심사위원은 操作으로 읽을 가능성이 높다. 반면 **"자료 조작"이라는 명사구 자체**는 일반 한국어에서 造作(날조)로 읽히는 쪽이 훨씬 흔하다("연구 자료 조작 의혹").
- 이 문구가 놓인 자리가 문제다 — **기상청 주최 대회의 첫 화면, 큰 글자(`<b>`), 스크린리더 라벨.** 데이터 무결성을 이 프로젝트의 최대 강점으로 내세우는 마당에, 해석이 갈리는 단어를 첫 화면에 둘 이유가 없다.
- **교체 비용이 사실상 0이다.** 애매하면 바꾸는 쪽이 맞다.

```
verify.js:1934  <b>관측 자료 조작</b><span>기준·지역·기간</span>
             →  <b>관측 자료 직접 다루기</b><span>기준·지역·기간 바꿔 보기</span>
verify.js:1932  aria-label "… 2단계 관측 자료 조작 …" → "… 2단계 관측 자료를 직접 다루기 …"
verify.js:2268  explore: '직접 조작'        → '직접 해 보기'
verify.js:2335  그래프를 조작해 이 생각을 검증합니다 → 그래프를 직접 움직여 이 생각을 확인합니다
verify.js:2395  직접 조작하기 →             → 직접 움직여 보기 →
verify.js:2604  ← 다시 조작하기             → ← 다시 해 보기
verify.js:2936  기준 다시 조작하기           → 기준 다시 맞춰 보기
verify.js:3441  2. 조건 조작                → 2. 조건 바꾸기
verify.js:3478  ← 조건 조작                 → ← 조건 바꾸기
index.html:51   질문과 조작 방법을 확인합니다 → 질문과 무엇을 움직일지 확인합니다
```
(`조작`은 verify.js에 40회 등장한다. 주석을 뺀 화면 노출분만 위 10곳이다.)

### 5-3. 화면에 없는 UI 용어로 조작을 지시한다 — 체험 점수 직결

미션 3·4의 **판정 게이트가 이 조작으로 열린다.** 그런데 여는 방법을 화면에 없는 단어로 안내한다.

| 용어 | 등장 | 화면에 그 글자가 있나 | 문제 |
|---|---|---|---|
| **칩** | verify.js 11회 (117, 153, 2232 등) | **없음** — 실제 라벨은 `지역 (지역마다 대표 관측소 1곳)` | 중고생에게 '칩'은 과자/반도체. **미션3 게이트가 이 조작으로 열린다** |
| **프리셋** | verify.js 11회 + index.html 1회 | **없음** — `.presets`에 `aria-label`만 있고 가시 라벨 없음 | **미션4 게이트(1mm + 30mm↑)가 이 조작으로 열린다** |
| **서랍** | verify.js 7회 (33, 1014, 1111) | **없음** — 실제 요소는 `<details><summary>📋 기상청은 어떤 기준을 쓸까?</summary>` | 특히 verify.js:33은 **비활성 버튼의 보조문구**라 두 번 막힌다 |
| **창** (window 직역) | verify.js 40회 (1387, 1436, 1450 등) | 조작 라벨은 `비교 기간 (‘현재’로 쓸 5년을 직접 골라 보세요)`로 잘 돼 있는데 판정문만 '창' | 데모 대본이 시연 동작으로 지정한 패널이라 심사위원이 실제로 읽는다 |
| **차트** | verify.js:2118 | 화면 라벨은 '그래프' | 같은 것을 두 이름으로 |

**처방 — 화면 라벨을 먼저 만들고, 지시문은 그 라벨을 인용한다.**
```js
// ① 프리셋에 가시 라벨을 붙인다 (verify.js:1653 앞)
+ '<span class="picker-label">자주 쓰는 기준</span>'

// ② 지시문 치환
verify.js:117  <b>지역 칩을 눌러 내 지역으로도 바꿔 볼 수 있어요.</b>
            →  <b>위쪽 지역 단추를 눌러 우리 지역으로도 바꿔 볼 수 있어요.</b>
verify.js:153  제주 칩과 강원 칩을 번갈아 눌러, …
            →  <b>제주</b>와 <b>강원</b>을 번갈아 눌러, …
verify.js:2232 '<b>'+need.join('·')+'</b> 칩을 눌러 두 지역을 모두 비교해 보세요.'
            →  '위쪽 <b>지역</b>에서 <b>'+need.join('·')+'</b>을(를) 눌러 두 지역을 모두 비교해 보세요.'   // 조사는 기존 eulReul() 사용
verify.js:197  아래 프리셋을 1mm → 10mm → 30mm → 50mm 순서로 눌러, …
            →  아래 <b>자주 쓰는 기준</b>에서 1mm → 10mm → 30mm → 50mm를 차례로 눌러, …
verify.js:2236 <b>1mm</b> 프리셋으로 …          → 먼저 <b>1mm</b>를 눌러 …
verify.js:2237 <b>30mm 또는 50mm</b> 프리셋으로 … → 이번엔 <b>30mm</b>나 <b>50mm</b>를 눌러 …
verify.js:33   낮 최고기온 기준 — 기준표 서랍에서 실측 확인
            →  낮 최고기온 기준 — 아래 ‘기상청은 어떤 기준을 쓸까?’에서 확인
verify.js:1014 실측은 <b>기상청 기준표</b> 서랍에 있어요
            →  실측은 아래 <b>‘기상청은 어떤 기준을 쓸까?’</b>를 펼치면 볼 수 있어요
verify.js:1436 N개 창 가운데 과거보다 적은 창은 …  → N개 구간 가운데 과거보다 적은 구간은 …
verify.js:1387 ← 5년 창의 시작 연도 →            → ← 고른 5년의 첫 해 →
verify.js:1450 창마다 값은 …                     → 구간마다 값은 …
index.html:52  슬라이더·＋−·프리셋으로            → 슬라이더, ＋− 단추, ‘자주 쓰는 기준’으로
```

### 5-4. 같은 것을 여러 이름으로 부른다 (용어 통일)

| 대상 | 현재 이름들 | 통일안 |
|---|---|---|
| 학습자 산출물 | CERL / 판정문 / 결론 | **내 결론(CERL)** — 첫 등장에서 한 번만 병기, 이후 `내 결론` |
| 오프라인 점검 | 규칙 점검 / 기기 안 빠른 점검 / 빠른 점검 / 증거 감사관(규칙 점검) / 로컬 점검 | **빠른 점검** |
| 열용량 슬라이더 | 유효 열용량 깊이 / 열을 머금는 층의 두께 / 깊이 / 두께 / 유효 깊이 | **열을 머금는 두께** (첫 등장에 `(유효 열용량)` 병기) |
| 기준 초과 일수 | 기준 이상 일수 / 기준초과 / 기준을 넘은 날 / 기준 이상 더위일 / 마지막 기준초과일 | **기준 넘은 날 수** / **마지막으로 기준을 넘은 날** |
| 예시 문장 | 전문가 예시 / 전문가 CERL / 전문가 문장 / 전문가 비교 | **모범 예시** (앱이 만든 템플릿이므로 '전문가'는 과한 표현) |
| 접힌 상세 | 서랍 / 펼치기 / 전체 보기 / 추가 관측값 확인 | **펼쳐 보기** |
| 온도 단위 | `°C` 49회 / `℃` 34회 (한 화면에 동시 노출: 프리셋 `폭염 33°C` ↔ 기준표 `일 최고기온 33℃ 이상`) | **`°C`로 통일** |

### 5-5. 그 밖의 어색한 표현 (문구 → 제안)

| # | 위치 | 현재 | 제안 | 사유 |
|---|---|---|---|---|
| C-01 | verify.js:1992 | `4/5 · 계절 오개념 확인` | `4/5 · 계절은 왜 생길까` | '오개념'은 교사·연구자 메타 용어. 게다가 다음 패널 발문(`여름은 태양에 가까워서 더울까?`)의 답을 라벨이 미리 흘린다 |
| C-02 | verify.js:1992 | `3/5 · 절기가 필요했던 이유` | `3/5 · 왜 절기가 필요했나` | 위와 톤 통일(의문형) |
| C-03 | verify.js:2881 | `감사하고 완료하기 →` | `내 결론 점검하고 마치기 →` | 중고생은 監査를 感謝로 읽는다. 최종 CTA라 특히 위험 |
| C-04 | verify.js:2269, 2926 | `증거 감사관` | `근거 점검` 또는 `내 결론 점검` | 위와 동일 |
| C-05 | verify.js:2845 | `설명하기 전에, 이해를 한 번 꺼내 보세요.` | `설명을 보기 전에, 스스로 한 번 답해 보세요.` | retrieval practice 직역. '이해를 꺼내다'는 한국어에 없는 연어 |
| C-06 | verify.js:2339 | `선택은 정답 채점이 아니라 생각의 출발점으로 기록됩니다.` | `채점하지 않아요. 지금 생각을 그대로 기록해 둡니다.` | 불필요한 피동 + 번역투 |
| C-07 | verify.js:32 | `여름의 통상 기준` | `여름에 흔히 쓰는 기준` | '통상'은 법률·무역 문어체 |
| C-08 | verify.js:1087 | `완결 5년` / `불완결 연도` | `자료가 다 있는 5년` / `아직 안 끝난 해` | '완결'은 소설·드라마 어휘 |
| C-09 | verify.js:959, 1676 | `극값일` / aria-label `가장 극값일 것 같은 날짜` | `가장 더운(추운) 날` / `가장 더울 것 같은 날짜` | 통계 용어. aria-label은 비문 |
| C-10 | verify.js:90 등 13회 | `시차` (날짜 차이) | `늦어진 날수` 또는 `차이` | 한국어 '시차'는 time zone / jet lag. 미션5 '계절 지연'과도 충돌 |
| C-11 | verify.js:148 | `반증해 보세요` | `정말 그런지 다른 지역으로 확인해 보세요` | 과학철학 용어이고, 실제 동작은 반증이 아니라 비교 |
| C-12 | verify.js:2415 | `30초 분류 — 같은 주제인데 층이 다릅니다` | `30초 분류 — 같은 주제인데 종류가 다릅니다` | layer 직역 '층' |
| C-13 | verify.js:2120 | `진짜 임무` | `첫 미션 시작` | 앱 전체가 '미션'인데 여기만 '임무'이고, 앞 과정을 '가짜'로 만든다 |
| C-14 | verify.js:1914 | `✦ 가이드로 먼저 해볼게요` | `✦ 가이드로 먼저 해 보기` | 옆 버튼 3개(`24절기가 뭐예요?`·`열관성 실험실`)와 화자·어투가 다르다 |
| C-15 | verify.js:2267 | 단계 라벨 `증거 확보` / `개념 준비` | `증거 읽기` / `개념 정리` | 수사·군사 어휘 + 뜻이 모호한 명사구 |
| C-16 | verify.js:2269 | `자가진단` | `이해 확인` | 의료 어휘. 실제로는 문항 1개 |
| C-17 | verify.js:3027 | `3. 지구 맥락` | `3. 지구 전체와 비교` | 한자 명사 2연발 + global context 직역 |
| C-18 | verify.js:3413~ | `대양 50m / 깊은 혼합층` | `먼바다 50m / 물이 깊게 섞임` | 해양학 용어를 설명 없이 |
| C-19 | verify.js:1642 | `기준 구간 확대` | `기준선 주변 확대` | '기준 구간'이라는 대상이 화면에 없다 |
| C-20 | verify.js:1251 | `있어서예요` | `있기 때문이에요` | 연결어미 + 체언 종결 '예요' — 비문 |
| C-21 | verify.js:1544 | 지도 낭독 `서울 서울관측소` | `서울 · 서울 관측소` | 중복 + 띄어쓰기 누락 |
| C-22 | verify.js:2956, 3038 | `내 고향 기후 카드` | `내가 태어난 해 기후 카드` | 화면 동작(출생연도 ±2년)과 '고향'이 다르다 |
| C-23 | verify.js:1450 | `숫자는 흔들리고 방향은 남는 것 — 이 차이를 구별하는 게` | `숫자는 흔들려도 방향은 남습니다. 이 둘을 구별하는 것이` | 명사구와 지시어가 호응하지 않는 비문 |
| C-24 | verify.js:2924 ↔ 3020 | `미션을 완료했습니다.`(합쇼체) ↔ `검증을 마쳤어요.`(해요체) | 완료 화면 전체를 **합쇼체**로 | 같은 화면에서 어투가 갈린다 |
| C-25 | verify.js:2621~2625 | placeholder `나는 이 절기의 약속이 …라고 판단한다.` | `나는 …라고 판단합니다.` | 앱 전체가 합쇼체인데 여기만 한다체 |
| C-26 | index.html:48~55 | 사용법 모달 `다섯 단계로 검증합니다` | 실제 미션 진행 표시는 `1/10`, `1/8` | 약속한 단계 수와 화면이 다르다 |
| C-27 | index.html:30 ↔ 38 | `↺ 처음부터`(기록 삭제) ↔ 브랜드 클릭(소개 이동, `confirm()`) | 브랜드 클릭 → 확인창 없이 소개로 / `↺` → `기록 지우고 새로 시작` | 둘 다 '처음'을 말하는데 결과가 다르다 |
| C-28 | verify.js:5, 1520, 3212 | 자료 로드 실패 문구 3종의 어투·구조가 전부 다름 | 하나의 문형으로 통일 | 하나는 '주세요'가 두 번 |
| C-29 | verify.js:2255~2262 | 한 안내 덩어리에 `~봅시다 / ~보세요 / ~있어요 / ~됩니다` 혼재 | `~보세요`로 통일 | |
| C-30 | verify.js:921, 1670, 1729 | 같은 동작을 `찍으세요 / 찍어 보세요 / 찍어 주세요` 3가지로, 둘이 동시에 표시 | `찍어 보세요`로 통일 | |
| C-31 | verify.js:1934, 2927, 3712 | `과장 · 범위 · 인과` / `범위 · 과장 · 인과` / `기준·지역·기간` — 순서와 가운뎃점 공백이 화면마다 다름 | 순서 고정 + 공백 규칙 하나로 | |
| C-32 | 미션 2·3 브리프 | `‘여름’을 “일평균 …”` — 홑따옴표(‘ ’)와 겹따옴표(“ ”)가 한 문장에 혼재 | 인용은 겹따옴표, 강조·정의어는 홑따옴표로 규칙화 | |
| C-33 | api/ai-turn.js:42, 248 | `수사실`(폐기된 탐정 은유), 오류 문구 `증거 카드 2장…` | `자유탐구` / `증거 두 가지…` | UI에 '증거 카드'라는 말이 없다 |
| C-34 | index.html:61 | `CERL 주장·근거·추론·한계` | `CERL — 주장·근거·추론·한계. (과학교육의 표준 틀 CER에 ‘한계(Limitation)’를 더한 우리 확장입니다.)` | 표준은 CER이다. **숨기면 감점, 밝히면 독창성 근거**가 된다 |

**은유 정리** — 화면에 공존하는 은유가 7계열이다: `검증소` · `감사관` · `증거/판정` · `미션` · `봉인` · `실험실` · `약속`.
중고생 1차시에 7개 세계관은 많다. **`증거·판정` + `실험실` 두 계열만 남기고 나머지는 평이한 말로** 정리하는 것을 권한다.
(`봉인`은 30회 등장하는데, 실제로는 수정 가능한 CERL에까지 쓰여 문구가 사실과 다르다 → `기록해 두기`)

---

## 6. 교육 효과 — 냉정한 재평가

**결론부터** 예측 봉인의 *봉인* 쪽은 본선 상위권 수준으로 단단하다. 문제는 **검증 쪽 절반이 비어 있다**는 것과, **형성평가 문항의 작성 품질**이다.

### 6-1. 확인된 강점 (억지로 깎지 않는다)

- 예측 봉인이 **5중 가드로 코드 강제**된다 — `renderExplore`(2489) · `renderMissionFlow`(2306) · `applyHash`(641-651) · `renderReadouts`(958) · `updateTable`(1164). 5차에서 지적된 goal-chip·LENS·인트로 목표문 누출은 실제로 수정됐다.
- 판정 게이트가 `state.moved`로 잠겨 있어 **슬라이더를 만지지 않으면 진행 자체가 안 된다**(직접 확인함). '조작 없이 클릭만으로 통과'가 구조적으로 불가능하다.
- CERL 게이트에 길이 검사 + **문장 중복 검사**(`cerlContentError`)가 있어 같은 문장 복붙을 잡는다.
- 미션 5개가 각각 다른 과학적 대상(기온 시차 / 기준 정의 / 지역 범위 / 강수 강도 분포 / 계절 지연)이라 **같은 게임의 반복이 아니다**. 개발계획서가 스스로 경계한 실패를 실제로 피했다.
- 열관성 실험실은 **필터가 아니라 물리량(유효 열용량·온실효과)을 조작**한다. 주최가 "원리·인과 학습엔 조작형 시뮬레이터"라고 못박은 지점을 정면으로 맞췄다.

### 6-2. P1 — 예측-검증(POE) 루프의 'E'가 없다

| | 내용 |
|---|---|
| **증상** | 미션 2~4의 봉인 예측이 **앱 어느 화면에서도 정오 대조되지 않는다** |
| **근거** | `state.predicts`를 읽는 렌더 지점은 `renderMissionOrient`의 한 줄(verify.js:2391)뿐. explore·evidence·write(4화면)·check·expert·audit 어느 함수도 예측을 읽지 않는다. 완료 화면(2995)은 `내 예측 “…”` 텍스트만 인쇄하고 맞았는지 말하지 않는다 |
| **왜 문제인가** | POE에서 학습을 일으키는 것은 예측과 관찰의 **명시적 불일치 확인**이다. 지금은 8개 화면을 지나도록 자기 예측이 다시 등장하지 않는다. 앱의 대표 기제가 절반만 작동한다 |
| **배점** | ② −2 / ③ −1 |
| **처방** | `renderExpertStep`(2864) 최상단에 봉인 해제 블록을 넣는다.<br>`predictVerdict(m, n)`을 두어 미션별로 매핑 — rain은 `lo.d<0 && hi.d>0 ? 'flip':'same'`, region은 `Math.abs(A.drift−B.drift)>=5 ? 'diff':'same'`, summer는 25℃·28℃ 격차를 직접 계산.<br>`🔓 봉인 해제 — 내가 예측한 것은 “격차가 커진다”, 자료가 말한 것은 “기준을 올리면 두 시기 모두 0에 가까워져 격차가 줄어든다”입니다(25°C: 12.4→34.6일 = +22.2 / 28°C: 1.2→7.8일 = +6.6). 어긋난 지점이 오늘 배운 것입니다.`<br>맞았을 때는 `예측과 자료가 같은 방향입니다. 다만 **얼마나**까지 맞혔는지 숫자로 확인해 보세요.`로 분기 |

### 6-3. P1 — 미션 2의 예측은 필수 경로에서 **검증이 불가능하다**

```js
// 예측 발문 (verify.js:114)
'기준을 25°C에서 28°C로 올리면, 과거와 현재의 <b>격차</b>(현재 − 과거)는 어떻게 될까?'
// 그런데 게이트는 (2221) summer에 대해 return true; → canJudge는 state.moved만 본다 (2225)
// 판정문 3분기(119-136) 어디에도 '격차'라는 말이 없다
```
24→25로 **한 칸만 옮겨도 판정이 열리고**, 학생은 28°C를 한 번도 보지 않은 채 미션을 끝낼 수 있다.

**처방**
```js
// missionDone에 summer 분기 추가
if (m.id === 'summer') {
  var u = state.usedThr && state.usedThr[m.id];
  return !!(u && Object.keys(u).some(k => Number(k) <= 25)
             && Object.keys(u).some(k => Number(k) >= 28));
}
// missionTodo 문구
'<b>28°C 이상</b>으로도 한 번 올려 보세요 — 예측한 <b>격차</b>가 어떻게 되는지가 이 미션의 질문입니다.'
// task문(117)도 '지역 칩'만 안내하지 말고 두 기준 비교를 지시
```

### 6-4. P1 — 자가진단 5문항 전부에 문항작성 결함이 있다

문항을 하나씩 검토한 결과다.

| 미션 | 결함 | 근거 |
|---|---|---|
| 1 처서 | **목표 불일치** — 미션 목표는 ①(절기≠기후)인데 문항은 ③(기준 정의)을 묻는다 | goal(75) vs selfCheck(101) |
| 2 여름 | **직전 화면 어휘 누출** — 정답 `기준 온도와 비교 기간`의 두 단어가 CERL 근거 발문(2622 `지역·기간·기준과 …`)에 그대로 인쇄됨. 오답 둘 다 절대어 `만` 포함, 정답만 명사구 | 138-142 / 2622 |
| 3 지역 | **브리프가 답을 지시** — `…반증해 보세요`(148), CERL 한계 발문 `…말할 수 없는 범위를 쓰세요`(2624)가 정답 술어를 미리 인쇄 | 148 / 182-186 / 2624 |
| 4 강수 | **실질 2지선다** — 오답 `둘 중 하나는 틀렸다`와 `함께 성립할 수 없다`가 **같은 명제**. 추측 정답률 33%가 아니라 50% | 223 |
| 5 지연 | **정답이 2~3배 길다** — 겨울 정답 63자 vs 오답 15·20자. POST_QUESTION에는 `보기 길이도 맞춰 …단서를 없앤다`(주석 60-61)고 원칙을 써 놓고 정작 적용 안 함 | 58-61 / 326-332 / 342-347 |

**대체 문항안 (미션 1·4 예시)**

```js
// 미션 1 — 목표①을 실제로 묻는다
q: '내년 처서는 8월 23일 무렵입니다. 앞으로 10년 동안 더위가 더 길어진다면, 처서 날짜는 어떻게 될까?'
options: [{v:'move', t:'더위에 맞춰 뒤로 밀린다'},
          {v:'fixed', t:'8월 23일 무렵 그대로다'},      // 정답
          {v:'gone',  t:'절기 자체가 없어진다'}]
explain: '절기는 태양의 황경으로 정한 <b>천문 날짜</b>라 기온과 무관하게 해마다 거의 그대로입니다.
          달라진 것은 <b>그 무렵 관측된 더위</b>예요 — 방금 여러분이 기준선으로 센 그 숫자입니다.'

// 미션 4 — 오답 하나를 진짜 매력적 오답으로
options: [{v:'ok',    t:'세는 기준이 다르면 둘 다 맞을 수 있다'},   // 정답
          {v:'wrong', t:'둘 중 하나는 잘못된 보도다'},
          {v:'total', t:'비의 총량이 늘었다면 두 말이 다 맞는다'}]  // 실측이 정확히 반증
```
`total`은 "총강수량이 늘면 빈도와 강도가 함께 는다"는 전형적 오개념이고, 서울 실측(1mm 89→83일 **감소**, 50mm 6.6→7.8일 **증가**)이 그것을 정면 반박하므로 해설이 학생 자신의 데이터로 이어진다.

### 6-5. P1 — 형성평가 9문항 전부 "1회 응답 → 즉시 정답 고지"

개발계획서(고도화).md:498이 **"피해야 할 실패"로 못박은 설계**를 코드가 그대로 하고 있다.

> 개발계획서: *"오답은 정답 고지가 아니라 필요한 자료 범위로 되돌린다"* / 피해야 할 실패: *"완료 배지로 정답 여부를 가리는 것"*

```js
// verify.js:2830-2831  onSelfCheck
state.selfChecks[m.id] = { picked: btn.dataset.v, correct: right }; save(); renderSelfCheckStep(m);
// 재렌더에서 (2849) 모든 버튼에 disabled + is-right 클래스 + sc.explain 전문 인쇄
```
`그래프에서 다시 확인` 버튼(2853)은 오답일 때만 뜨는데, **그 시점엔 이미 정답과 해설이 화면에 있다.** 학습 행동이 아니라 확인 사살이다.
심사위원이 개발계획서와 화면을 나란히 놓으면 바로 드러난다.

**처방** — 1차 오답은 정답을 공개하지 않고 되돌린다.
```js
var tries = (state.selfCheckTries[m.id] || 0) + 1;
state.selfCheckTries[m.id] = tries;
if (!right && tries === 1) {          // 정답 하이라이트·해설 없이, 모든 보기 활성 유지
  state.selfCheckHint = sc.retryHint; save(); renderSelfCheckStep(m); return;
}
state.selfChecks[m.id] = { picked: btn.dataset.v, correct: right, tries: tries };
```
각 문항에 `retryHint`를 추가한다. 예(미션4): `'1mm와 50mm를 다시 눌러, 두 숫자가 <b>같은 방향</b>으로 움직였는지 보고 오세요.'`
완료 화면 기록도 `정답(1차) / 정답(2차) / 미해결` 3분기로 남기면 **교사가 난이도를 읽을 수 있는 산출물**이 된다(심사 방어에도 유리).

### 6-6. 그 밖의 P1 (요약)

| # | 지적 | 근거 |
|---|---|---|
| E-06 | **개념 렌즈 3문항이 전부 키워드 매칭** — `어제`↔`하루`, `한 해 평균…에서…로`↔`여러 해 경향`. 목표①이 잡으려는 '절기↔기후 혼동' 문항이 **0개** | 2417-2431 |
| E-07 | **전이 문항이 미션 1에만** (`askPost`는 chuseo 단독, 105). 목표②③⑤와 미션 2~5는 전이 측정 0건인데 완료 화면은 목표 배지 4개를 **무조건 발급**(3023) | 105 / 2275 / 3023 |
| E-08 | 전이 문항 정답이 **두 화면 앞 예시 문장과 같은 문형** — 판정문 `절기 날짜는 그대로인데, 관측된 더위가 …`(84)가 `<details open>`(2869)으로 펼쳐진 채 있고, 전이 정답은 `입동 날짜는 그대로인데, 그 무렵 관측된 기온이 …`(66). 문형 대조만으로 풀린다 | 84 / 2869 / 66 |
| E-09 | **CERL 게이트가 미션 2~5에서 정규화 20자면 통과**하고, **한계 칸에는 내용 검사가 전무** | 2620-2625 |
| E-10 | **앱의 유일한 오개념 검출기 `localAudit`이 필수 경로에 없다** — "핵심 학습은 끝났습니다"(2925)라고 선언한 화면의 **선택 버튼 뒤**에 있어, 학생이 쓴 문장의 오개념은 필수 경로에서 한 번도 검사되지 않는다 | 2925-2926 |
| E-11 | **실험실은 진입만으로 `labSeen=true`** — 조작 0회에도 목표⑤ 배지와 "내가 고른 값" 기록이 기본값으로 발급된다 | 3431 / 3010 |
| E-12 | 실험실 '결과 해석' 탭이 **학생이 조작하기 전에 세 실험의 결과와 결론을 이미 계산해 인쇄**한다. 교사 학습지 활동6의 빈칸 3개가 화면에 답으로 떠 있다 | 3646-3671 |
| E-13 | **소요시간 표기 충돌** — 핵심 코스가 59화면인데 교사_학습지의 활동별 '핵심 2~3분'을 합하면 13~18분. 앱 본문은 "69~76분(두 차시)". 45분 1차시를 계획한 교사는 반드시 파탄한다 | 교사_학습지.html:34,60,68… |
| E-14 | 자유탐구 **'1. 질문 만들기' 탭에 질문을 적을 칸도 스캐폴드도 없다** (스캐폴드는 다음 탭에 있다) | 3686-3695 |
| E-15 | 자유탐구 판정문(`freeDraft`)이 완료 화면 '내 기록'에 **포함되지 않는다** — 교사가 회수할 산출물이 없다 | 3016-3017 |
| E-16 | 진행 표시 분모가 실제 화면 수와 다르다 — `write`가 4화면인데 1단계로 계산. 학생은 "60%"에서 남은 상호작용의 절반 이상을 앞에 두고 있다 | 2271-2278 |

### 6-7. "학생이 끝내고 나서 말로 설명할 수 있게 되는 것"

| 개발계획서 학습목표 | 실제 도달 | 판정 |
|---|---|---|
| ① 24절기·날씨·기후 구분 | 절기가 천문 날짜임은 반복 노출. 다만 **타당한 형성평가가 사실상 없다**(6-4, 6-6 E-06) | 부분 |
| ② 자료의 범위(지역·기간·지표) 읽기 | 미션 3에서 직접 비교하고, integrity 문단이 매 화면에 범위를 밝힌다 | **달성** |
| ③ 모호한 말을 임계값으로 정의 | 기준선 조작이 게이트라 반드시 경험한다 | **달성** |
| ④ 근거만큼만 결론 쓰기 | CERL을 쓰긴 하지만 **한계 칸에 내용 검사가 없고**, 오개념 검출기가 필수 경로 밖이다 | **미달** |
| ⑤ 물리 법칙으로 계절 재현 | 실험실이 실제로 물리량을 조작한다. 다만 **진입만으로 배지 발급**(E-11) | 부분 |

---

## 7. 과학적 무결성 — 직접 검산했다

주최가 기상청이다. 이 항목은 숫자를 하나씩 다시 계산해서 확인했다.

### 7-1. 검산 결과 — 전부 맞다

| 화면의 수치 | 검산 | 판정 |
|---|---|---|
| 태양년 **365.24일** | 회귀년 365.24219일 | ✔ |
| 음력 12달 **354.37일** | 삭망월 29.530589 × 12 = 354.367 | ✔ |
| **해마다 10.9일 어긋남** | 365.24 − 354.37 = 10.87 → 10.9 | ✔ |
| 근일점 **0.983 AU** (1월 초) | `sunDistance(4)` = 1.00014 − 0.01671·cos(360.49°) − 0.00014·cos(2g) = **0.98329** | ✔ |
| 원일점 **1.017 AU** (7월 초) | `sunDistance(185)` = **1.01671** | ✔ |
| 거리 차이 **3.4%** | (1.01671 − 0.98329) / 1.0 = 3.34% | ✔ |
| 자전축 기울기 **23.4°** | 현재 23.44° | ✔ |
| 처서 태양황경 **150°** | `termLongitude(15) = (15×15 + 285) % 360 = 150` (동지 270 · 소한 285 기준) | ✔ |
| 태양상수 **1361 W/m²** | 최신 관측 1360.8 ± 0.5 | ✔ |

거리 계산식은 **USNO/Meeus 저정밀 태양 위치 근사**를 그대로 쓴다. 일사량은 **Spencer(1971) 푸리에 근사**로 태양 적위와 지구–태양 거리를 구한다 — 둘 다 실제로 인용 가능한 표준 근사이고, 화면에 출처가 적혀 있다.

### 7-2. 열관성 실험실의 모형 — 정직하다

```
C · dT/dt = Q(날짜, 위도) − (A + B·T)
```
0차원 에너지수지 모형(Budyko–Sellers 형)의 표준형이다. 그리고 **앱이 스스로 단순화를 밝힌다**:

> "나가는 열을 직선으로 근사했습니다(A + B×기온). 실제 복사는 T⁴에 비례하고, 대기의 열 수송도 함께 일어납니다. 두 가지를 감쇠 계수 B 하나로 묶었습니다."
> "이 모형의 '깊이'는 실제 수심이 아니라 땅·바다·대기·혼합 같은 효과를 한 값에 모은 **유효 열용량 매개변수**입니다."
> "값이 클수록 지연이 길어진다는 가설은 보여 주지만, 이것만으로 '바닷가라서 늦다'고 단정할 수는 없습니다."

**모형과 관측을 혼동시키지 않는 이 태도가 이 앱에서 가장 강한 부분이다.** 기상 전문가 심사위원이 가장 높게 살 지점이라고 본다.

### 7-3. 계절의 원인 — 최악의 오개념을 정면으로 다룬다

"여름은 태양에 가까워서 더울까?" 패널이 **1월 초에 가장 가깝고 7월 초에 가장 멀다**는 수치를 먼저 보여 준 뒤 자전축으로 넘어간다. 오개념을 강화하지 않고 반증 자료로 쓰는 정석 구성이다.

다만 **궤도 그림 자체가 위험 요소를 하나 안고 있다.** 캡션이 "거리 차이가 3.4%뿐이라 궤도를 거의 원으로 그렸습니다(실제 비율 아님)"라고 정확히 밝히고 있어 지금은 방어되어 있지만, **§1-3의 처방대로 캡션을 SVG 밖 HTML로 빼면 모바일에서도 이 문장이 읽힌다**(현재는 6.4px라 사실상 안 읽힌다). 즉 이 캡션은 미관 문제가 아니라 **과학 무결성 장치**다.

### 7-4. 표본 — 정직하게 다루고 있으나, 방어 화면이 서랍 안에 있다

| 항목 | 현황 |
|---|---|
| 비교 기간 | 과거 **1969–1973(5년)** vs 현재 **2021–2025(5년)** |
| 고지 | `.integrity` 문단이 **모든 조작 화면에** "관측 신호이고 30년 기후평년이 아닙니다"를 인쇄 |
| 기상청 기준표 | 기간이 다름(2022–2025)을 **스스로 밝히고** "나란히 놓을 수 없습니다"라고 적음 |
| 변동성 방어 | **26개 이동 5년 구간** 패널이 있어 "5년이 우연 아니냐"에 실측으로 답한다 |

**우려 1 — "왜 30년이 아니라 5년인가?"**
자료는 1969–2026까지 있으므로 30년 평년 산출이 가능하다. 기상 전문가는 반드시 묻는다.
현재의 답(26개 구간 전부 같은 방향)은 **이미 앱 안에 있는데 접힌 상세 안에 있다.**
→ **처방**: 이 패널을 데모 대본의 필수 시연 항목으로 올리고, 조작 화면 `.integrity` 문단에 한 줄 추가.
`5년이 우연인지 궁금하다면 → '5년 구간을 옮겨 보기'에서 26개 구간을 모두 확인할 수 있어요.`

**우려 2 — 관측 불연속(관측소 이전·장비 교체)에 대한 언급이 0건이다**
`도시화`는 2곳에서 언급된다(감사 한계 목록, 실험실 주의문). 하지만 `관측소 이전`·`장비 교체`·`균질화(homogenization)`는 코드·문서 전체에 **0회**다.
1969년과 2025년 사이 서울 관측소를 포함해 다수 지점에 이전·환경 변화가 있었다. 지금의 결론(방향)은 그것으로 뒤집히지 않지만, **기상청 심사위원이 물었을 때 "생각해 보지 않았다"가 되면 안 된다.**
→ **처방**: `.integrity`의 한계 목록에 한 줄.
`한 지점의 기록은 관측소 이전·주변 환경 변화의 영향도 함께 담고 있습니다. 이 화면은 그 영향을 걸러 내지 않습니다.`
(문장 하나로 "알고 있고 범위를 정확히 말한다"가 성립한다. 이 앱의 다른 모든 한계 진술과 톤이 같다.)

**우려 3 — 강수 기준의 단위 차이**
"호우특보는 3시간·12시간 강우량으로 정하므로 이 화면의 일강수량과는 기준이 다릅니다"라고 **이미 정확히 밝히고 있다.** 별도 조치 불필요.

### 7-5. 요약 — 과학은 이 프로젝트의 강점이다

검산한 9개 수치 전부 정확, 모형의 한계를 앱 스스로 서술, 오개념을 반증 자료로 사용, 지점 대표성(제주=제주 관측소, 강원=춘천 관측소)까지 화면에 명시.
**남은 것은 §7-4의 한 줄짜리 보강 두 개와, 그 방어 장치들이 접힌 상세에 숨어 있다는 배치 문제뿐이다.**

---

## 8. UX · 접근성 · 완성도 (실측)

### 8-1. 접근성 — 이 항목은 강점이다

직접 계측한 결과:

| 항목 | 결과 |
|---|---|
| 슬라이더 | `<input type=range>` 네이티브 + `aria-label` + **`aria-valuetext`에 결과값까지** — `"덥다 기준 25°C · 더위일 과거 31일, 현재 68일"`. 키보드 조작 가능(tabIndex 0) |
| 라이브 리전 | `p.live-nums[aria-live="polite"]` — 기준선을 바꾸면 숫자 변화가 낭독된다 |
| 그래프 | `<svg role="img" aria-label="서울 기온 연간 곡선. 과거 1969–1973(회색 점선)과 현재 2021–2025(빨간 실선), 하지 6/21 고정 세로선, 덥다 기준 25°…">` — 값까지 서술 |
| 문서 | `<html lang="ko">`, 화면당 `h1` 1개, `h1.sr-only`로 스크린리더용 제목 제공 |
| 테마 | 자동/밝게/어둡게 3단, `role="radiogroup"` + `aria-checked` 동기화 + 좌우 방향키 지원 |
| 대비비 (알파 합성 계산) | **다크 실패 1건 / 라이트 실패 2건**. 이 규모의 앱에서 매우 좋은 수치다 |

### 8-2. 대비비 실패 2종 (전부)

| 테마 | 요소 | 실측 | 필요 | 진단 |
|---|---|---|---|---|
| **다크** | `button.primary-btn.is-muted` "증거 정리하기 →" | **1.01 : 1** | 4.5 | 전경 `rgb(32,20,58)` / 배경 `rgb(8,28,45)` — **글자가 사실상 보이지 않는다** |
| 라이트 | 같은 요소 | 1.13 : 1 | 4.5 | 흰 글자 / `rgb(238,242,245)` |
| 라이트 | `span.step-tag` "지금 한 가지만" | 4.28 : 1 | 4.5 | 경계선상 |

`is-muted`는 `disabled` 속성이 아니라 **클릭 가능한 버튼**이다(누르면 힌트를 띄운다). WCAG의 비활성 컨트롤 예외에 해당하지 않고, 무엇보다 **다크 모드에서 학생이 "여기 버튼이 있다"는 사실 자체를 볼 수 없다.**

**처방**
```css
.primary-btn.is-muted{
  color: var(--muted);            /* 배경 대비 4.5:1 이상인 회색 */
  background: var(--panel2);
  border: 1px dashed var(--line); /* '아직 못 누른다'를 색이 아니라 테두리로 */
  opacity: 1;                     /* opacity로 죽이지 않는다 */
}
.step-tag{ color: var(--sea-ink); } /* 라이트에서 4.5:1 이상 확보 */
```

### 8-3. 터치 타깃 (권장 44×44, 최소 40×40)

390px 실측:

| 요소 | 크기 | 판정 |
|---|---|---|
| `button.theme-btn` (◐ 자동 / ☀ 밝게 / ☾ 어둡게) | **32 × 30** | 미달 |
| `button.text-btn#resetBtn` (↺ 처음부터) | **32 × 32** | 미달 |
| `button.text-btn#openGuide` (사용법) | 44 × 32 | 세로 미달 |
| `a.brand` | 115 × 28 | 세로 미달 |
| `button.ghost-btn` (← 탐구 방향 / ← 증거 다시 보기) | **86 × 20** / 116 × 20 | **크게 미달** |
| 소개 화면 `ghost-btn` (🌍 24절기가 뭐예요? 등) | 137 × **20** | **크게 미달** |
| `button.seg-btn` (표) | 33 × 34 | 미달 |

주최가 "모바일 환경에서도 작동하는 것이 권장"이라고 명시했다. 특히 **소개 화면의 세 부가 버튼이 20px**이라, 첫 화면에서 학생이 '24절기가 뭐예요?'를 누르려다 빗맞는다.

**처방**
```css
.ghost-btn, .text-btn, .theme-btn, .seg-btn{
  min-height: 44px;                 /* 시각적 높이는 padding으로 유지하고 히트영역만 확대 */
  display: inline-flex; align-items: center;
}
/* 또는 시각 높이를 유지하려면 */
.ghost-btn{ position: relative; }
.ghost-btn::after{ content:''; position:absolute; inset:-12px -8px; }
```

### 8-4. 브라우저 뒤로가기가 없다

`location.hash`는 **읽기만** 하고(applyHash, 631) 앱이 스스로 쓰지 않는다. `popstate`/`pushState` 사용 0건, `hashchange` 리스너 1개뿐.
→ **59화면짜리 레슨을 진행하다 뒤로가기를 누르면 앱 밖으로 나간다.** 심사위원이 화면을 되짚어 보려 할 때 가장 먼저 하는 동작이다.

**처방(저비용)** 화면 전환마다 `history.replaceState(null,'', stateHash())`로 주소만 동기화하고(이미 `stateHash()`가 있다), 미션 단계 전환에서만 `pushState`를 쓴 뒤 `popstate`에서 `applyHash(); routeFromState();`를 호출한다. 앱 구조를 건드리지 않는다.

### 8-5. 확인 대화상자가 네이티브 `confirm()`이다

```js
verify.js:3925  if (confirm('처음(소개)으로 돌아갈까요? 진행 기록은 유지됩니다.')) renderIntro();
verify.js:3929  if (confirm('기록을 지우고 처음부터 시작할까요?\n(공용 컴퓨터에서 …)'))
```
브랜드 로고 클릭에도 확인창이 뜬다. 라이브 데모에서 OS 기본 팝업이 뜨면 완성도 인상이 깎이고, 진행 흐름도 끊긴다.
**처방** 로고 클릭은 확인 없이 소개로(기록은 어차피 유지된다). `↺ 처음부터`만 앱 내부 `<dialog>`로 확인받는다(이미 `.modal` 스타일이 있다).

### 8-6. 딥링크 `p=` 파라미터가 사실상 죽어 있다

`applyHash`는 `state.missionStep = q.p`를 세우지만 같은 함수에서 `state.touched = false; state.moved = false;`(647)를 하므로, `#v=m0&p=check`·`p=expert`·`p=transfer`·`p=audit` 전부 **조작 화면(explore)으로 되돌아간다**(5개 모두 직접 확인).
발표_10분_구성안.md가 쓰는 **미션 단위 링크(`#v=m0`·`#v=m3`·`#v=m4`·`#v=lab`·`#v=free`)는 정상 동작하므로 데모에는 문제가 없다.** 다만 코드에 죽은 파라미터가 남아 있다.

### 8-7. 성능 — 문제없음

첫 로드 자산 **decoded 844KB / 리소스 6개 / 외부 도메인 0**.
`solar_terms_data.js 404KB` + `verify.js 297KB` + `verify.css 76KB`가 대부분이고 전부 gzip 전송된다. `thumbnail.png`(1.7MB)는 **로드되지 않는다**(파비콘을 32/180으로 분리한 조치가 유효하다).
지구 맥락 데이터도 `/web_data/*.json`을 같은 오리진에서 받는다(17KB·14KB·1KB, 60~80ms). **외부 API 의존이 0이므로 현장 네트워크가 느려도 앱은 완주한다.**

### 8-8. AI 감사관 — 실제로 동작한다 (검증함)

과대주장 문장을 실제로 넣어 확인했다.

```
POST /api/ai-turn   →  200 OK, 3,650ms
draft: "서울에서 25도 기준으로 더위가 그치는 날이 늦어졌으니,
        우리나라 전체 기후가 기후변화 때문에 완전히 바뀌었다고 결론지을 수 있다."
응답:
  evidence_status    : "revise"
  overclaim_warning  : "서울 자료만으로 우리나라 전체를 말할 수 없고,
                        ‘기후변화 때문에 완전히 바뀌었다’는 원인·정도 판단도 증거에 없다."
  socratic_question  : "이 결론이 서울 5년 비교만으로 가능한가, 아니면 다른 지역 자료도 더 봐야 할까?"
  next_action        : "state_limitation"
  feedback           : "서울의 25°C 기준 더위일수와 마지막 기준초과일 변화만 말하고,
                        지역 확대·원인 단정은 빼세요."
```
**지역 확대와 인과 단정을 정확히 둘 다 잡았고, 구조화 출력 스키마도 지켜졌다.** 이건 발표에서 자신 있게 라이브로 보여도 되는 수준이다.

**배치는 오히려 좋다** — 소스를 다시 확인한 결과, AI 감사관은 자유탐구에만 있는 것이 아니라 **미션 5개 각각의 마지막 화면(`renderAuditStep`, verify.js:2919)에 전부 들어 있다.**

```js
// verify.js  missionSequence()
var seq = ['predict'];
if (m.id === 'chuseo') seq.push('lens');
seq = seq.concat(['orient','explore','evidence','write','check','expert']);
if (m.askPost) seq.push('transfer');
seq.push('audit');        // ← 모든 미션의 마지막이 감사 화면이다
```
그 화면에 `#aiConsent` 체크박스 + `#askAudit` 버튼 + `doAudit()`이 모두 있다(2931-2945). 발표_10분_구성안.md의 `4:15–4:35 · AI 해커톤의 AI` 블록도 이 화면을 가리킨다. **즉 "AI가 마지막 선택 화면에 묻혀 있다"는 우려는 사실이 아니다.**

**남는 진짜 문제는 라벨이다.** 그 화면은 이렇게 말한다.

> `<h1>미션을 완료했습니다.</h1>`
> `<p>핵심 학습은 끝났습니다. 내 CERL을 점검하거나 바로 다음 미션으로 갈 수 있습니다.</p>`
> `<p class="eyebrow">✦ 증거 감사관 · 선택 심화</p>`

**"핵심 학습은 끝났습니다" + "선택 심화"** 두 문구가 AI 감사관을 *건너뛰어도 되는 것*으로 규정한다. 그 결과
- 학생 대부분은 누르지 않고 `다음 미션 →`으로 넘어가고,
- §6-6 E-10의 지적(오개념 검출기가 필수 경로에 없다)이 여기서 발생하며,
- 심사위원이 직접 만질 때도 이 버튼을 지나칠 수 있다.

**처방** — 기능 이동이 아니라 **문구와 순서만** 바꾼다.
```
"핵심 학습은 끝났습니다. 내 CERL을 점검하거나 바로 다음 미션으로 갈 수 있습니다."
→ "마지막으로 내가 쓴 결론을 점검합니다. 범위·과장·인과 세 가지만 봅니다."

"✦ 증거 감사관 · 선택 심화"  →  "✦ 내 결론 점검"
```
그리고 `기기 안에서 빠른 점검`(외부 전송 없음)을 **화면 진입 시 자동 1회 실행**한다. 지금은 버튼을 눌러야만 돌아가는데, 이 검사는 오프라인·무료·즉시이므로 자동화해도 비용이 0이다. 그러면 학생 문장의 오개념이 **필수 경로에서 반드시 한 번 검사**된다(E-10 해소). 외부 AI 요청만 지금처럼 동의 뒤 선택으로 남긴다.

### 8-9. 그 밖

- **상태 복원** — `localStorage`로 진행이 보존되고, 오래 방치된 세션은 `askResume()`이 이어할지 묻는다. 공용 PC 시나리오까지 고려한 설계다. (강점)
- **표 접근성** — 표 보기의 `<table>`에 `<caption>`이 있다. `<th scope>` 지정 여부는 추가 확인 필요.
- **스킵 링크 없음** — 헤더 5개 버튼을 매 화면 탭으로 지나야 본문에 닿는다. `<a class="skip-link" href="#stage">본문으로 건너뛰기</a>` 한 줄이면 된다.

---

## 9. 심사 배점 기반 채점

> **⚠ 이 절의 초판은 배점표를 잘못 썼다.** 아래 표는 **예선(서류) 배점** — ⑤가 「실현 가능성」 —
> 을 기준으로 매긴 것이다. **본 대회 배점표는 ⑤가 「구현 완성도」이고, 그 안에 「AI 도구·기술이
> 결과물 제작 과정에서 효과적으로 적용되었는가」 15점이 별도로 있다.** 또한 본 대회에는
> **발표 10분 초과 감점(1분 −9 / 2분 −15 / 3분 −21·발표 중지)** 이 있다.
> 본 대회 기준의 항목별 채점은 **§12**에 새로 작성했다. 아래 표는 예선 기준 기록으로만 남긴다.

| 항목(예선 기준) | 배점 | 현재 추정 | 잃고 있는 지점 | 위 처방 적용 후 |
|---|---|---|---|---|
| ① 주제 적합성 | 20 | **17** | 브랜드·카피가 첫 3초 인상을 깎음(§5). 「관측 자료 조작」(§5-2) | **19** |
| ② 체험·참여형 | 25 | **20** | 예측 검증 루프 미완성(§6-2), 화면에 없는 용어로 조작 지시(§5-3), 겹침·잘림(§3), 1366×768 CTA 폴드(§4-5) | **23** |
| ③ 학습 효과 | 10 | **6.5** | 형성평가 9문항 전부 즉시 정답 고지(§6-5), 자가진단 5문항 전부 문항 결함(§6-4), 목표④ 미달(§6-7) | **8.5** |
| ④ 독창성 | 15 | **12** | 24절기 × 실측 검증 × 물리 모형은 확실히 독창적. 다만 네이밍 첫인상과 "PhET/기후스트라이프와 뭐가 다른가"에 대한 한 문장 답이 화면에 없음 | **13** |
| ⑤ 실현 가능성 | 30 | **26** | 이미 배포·동작·외부 의존 0·AI 실동작이라 기본이 높다. 감점은 **시각적 마감**(§1·§3·§4)이 "덜 다듬어졌다"는 인상을 주는 부분 | **29** |
| **합계** | **100** | **≈ 81.5** | | **≈ 92.5** |

### 9-1. 항목별 근거

**① 주제 적합성 17/20** — 기상청 ASOS 실측 1969–2026, 16지점 × 24절기. 소재가 한국 고유이고 교육 목표가 문서화되어 있다. 감점은 순전히 **첫 화면 언어**다. 심사위원이 URL을 열고 3초 안에 읽는 것이 브랜드·히어로·3단계 다이어그램인데, 그 세 곳이 각각 어색한 조어(`검증소`) / 문어체(`유효할까`) / 해석이 갈리는 단어(`관측 자료 조작`)를 쓰고 있다.

**② 체험·참여형 20/25** — **판정 게이트가 조작을 강제한다**(슬라이더를 만지지 않으면 진행 불가, 직접 확인). 미션 5개가 각기 다른 조작이고, 실험실은 필터가 아니라 물리량을 조작한다. 이 설계는 배점 최상위 항목을 정면으로 겨냥한 것이 맞다.
감점 요인은 셋: (a) 예측을 봉인만 하고 대조하지 않아 대표 기제가 절반만 작동, (b) 게이트를 여는 방법을 **화면에 없는 단어**(칩·프리셋)로 안내해 심사위원이 직접 만질 때 막힌다, (c) 시연 화면 4곳에 겹침·잘림.

**③ 학습 효과 6.5/10** — 가장 크게 잃고 있는 항목이다(비율로 35% 손실).
개발계획서가 **스스로 "피해야 할 실패"로 명시한 것**(정답 즉시 고지)을 코드가 하고 있고, 자가진단 5문항이 전부 문항작성 결함을 갖고 있다. "학습 효과를 어떻게 측정했나"라는 질문에 제시할 수 있는 타당한 지표가 사실상 전이 1문항인데 그마저 오픈북 재인이다.
**다만 수정 난도가 가장 낮다** — 문항 텍스트 교체 + 1차 오답 처리 분기 하나. §6-4·6-5의 처방으로 8.5까지 회복 가능하다고 본다.

**④ 독창성 12/15** — "기존에 있는 것 아닌가"에 대한 방어:

| 유사 서비스 | 겹치는 부분 | Weather24만의 것 |
|---|---|---|
| PhET Greenhouse Effect | 조작형 물리 시뮬레이터 | **실측 관측과 모형을 같은 앱에서 대조**한다 |
| En-ROADS | 슬라이더 시나리오 | 정책이 아니라 **학습자가 '덥다'의 정의 자체를 정한다** |
| Climate Stripes / Our World in Data | 장기 기온 시각화 | 보는 것이 아니라 **기준을 정해 결론을 만든다** |
| NASA Climate Time Machine | 과거–현재 비교 | **24절기라는 한국 고유의 문화적 기준선**과 대조 |
| 기상자료개방포털 | 같은 ASOS 원자료 | 원자료를 **학습 과제로 재구성**하고 결론까지 쓰게 한다 |

핵심 차별점은 **"절기(움직이지 않는 천문 날짜) vs 기후(움직이는 관측)"를 나란히 놓은 것**과 **학습자가 임계값을 정하면 결론이 바뀌는 것을 몸으로 겪게 한 것**이다. 그런데 이 한 문장이 **화면 어디에도 없다.** verify.js:1984의 "이 앱은 움직이지 않는 절기와 움직이는 기후를 나란히 놓고 비교합니다"가 24절기 입문 5/5 패널 안쪽에 묻혀 있다.
→ **처방**: 이 문장을 소개 화면으로 끌어올린다(브랜드 대안 5안 「움직이지 않는 절기, 움직이는 기후」를 발표 슬로건으로 쓰는 것도 같은 효과).

**⑤ 실현 가능성 26/30** — 배점이 가장 크고, 기본기가 가장 좋다.
로그인 없이 즉시 동작 · 외부 API 의존 0 · 첫 로드 844KB · 데이터 전량 자체 호스팅 · AI 엔드포인트 실동작(3.6초) + 실패 시 로컬 규칙 점검 폴백 · 제출물 5종 준비 완료 · `submit_check.sh` 게이트까지 있다.
감점은 **"완성도(polish)" 인상**이다. 심사위원이 화면에서 잘린 라벨과 겹친 글자를 보면 "기능은 되는데 마감이 덜 됐다"로 읽는다. §1·§3·§4가 전부 이 항목으로 되돌아온다.

### 9-2. 심사위원이 던질 가장 아픈 질문 8개

| # | 질문 | 지금 답할 수 있나 |
|---|---|---|
| 1 | "5년 대 5년으로 기후를 말할 수 있나?" | **가능** — 26개 이동 구간 패널이 이미 답이다. 다만 **접힌 상세 안**이라 시연 대본에 넣어야 한다 |
| 2 | "관측소 이전이나 도시화 영향은?" | **부분** — 도시화는 2곳 언급, 관측소 이전은 0건. §7-4 한 줄 추가 필요 |
| 3 | "학습 효과를 어떻게 측정했나?" | **취약** — 타당한 지표가 전이 1문항이고 그마저 재인이다. §6-4·6-5 처방 필요 |
| 4 | "AI는 어디에 쓰였나? 없어도 되는 것 아닌가?" | **가능** — 미션 5개 각각의 마지막 화면에 있고 실동작을 확인했다. 다만 화면이 스스로 "선택 심화"라 부르므로 §8-8의 문구 수정 필요 |
| 5 | "PhET·기후스트라이프와 뭐가 다른가?" | **답은 있으나 화면에 없다** — §9-1 ④ 참조 |
| 6 | "45분 수업에 정말 들어가나?" | **취약** — 핵심 코스 59화면인데 교사 학습지 활동 시간 합계는 13~18분, 앱 본문은 69~76분. 세 숫자가 서로 다르다 |
| 7 | "모형이 실제 관측과 맞는 게 우연 아닌가?" | **가능** — 앱이 먼저 "원인을 확정한 결과가 아니라 적합 사례"라고 밝힌다. 모범 답변 |
| 8 | "학생이 아무 말이나 써도 통과하나?" | **부분** — 길이·중복 검사는 있으나 미션 2~5는 20자면 통과하고 한계 칸에 내용 검사가 없다 |

---

## 10. 실행 우선순위

제출 마감(8/21 18:00)까지 18일. 아래는 **비용 대비 효과 순**이다.

### 즉시 (합계 2~3시간, 위험도 0 — 전부 문자열·CSS)

| # | 작업 | 파일 | 효과 |
|---|---|---|---|
| 1 | 궤도 `viewBox="0 0 640 460"` → `"0 -24 640 508"` | verify.js:1970 | **잘린 절기 3개(추분·백로·한로) 복구** + 캡션 충돌 해소 |
| 2 | `drawDrift()` 라벨 `text-anchor="end"` + `L=108` + "태양년" 단축 | verify.js:2095-2103 | 막대에 가린 라벨 복구 |
| 3 | 실험실 두 주석을 곡선 위/아래 다른 단으로 고정 분리 | `renderLab` | 기본 상태 겹침 해소 |
| 4 | `text-wrap: pretty` (본문) / `balance` (제목) 전역 추가 | base.css | **고아줄 16건 중 12건 해소** |
| 5 | `.why-p { max-width: 620px }` | verify.css:526 | 궤도 3~5/5 패널 고아줄 해소 + 행장 55자→44자 |
| 6 | 8pt 여백 스케일 + 우선 4곳(§4-3) | verify.css | "붙어 있다"는 인상 해소 |
| 7 | `.primary-btn.is-muted` 대비 수정 | verify.css | 다크 모드에서 안 보이던 버튼 |
| 8 | 「관측 자료 조작」 → 「관측 자료 직접 다루기」 외 9곳 | verify.js, index.html | 데이터 윤리 오해 소지 제거 |
| 9 | 지도 값 라벨 `dy` 3 → 14 | 지도 렌더 | 16지점 자체 겹침 해소 |
| 10 | `.integrity` line-height 1.45 → 1.65, 진행 표시 `small` 1.20 → 1.35 | verify.css | 가장 조밀한 두 곳 |

### 1일 이내 (효과 큼, 위험 낮음)

| # | 작업 | 효과 |
|---|---|---|
| 11 | **브랜드 교체** 「절기, 아직 맞을까」 + 히어로/미션1 제목 톤 통일 | ① 첫인상. 치환 지점 12곳 |
| 12 | 화면에 없는 UI 용어 제거 — `칩`·`프리셋`·`서랍`·`창` (§5-3) + 프리셋에 가시 라벨 추가 | ② 조작 성공률 직결 |
| 13 | 자가진단 5문항 교체 (§6-4) | ③ 형성평가 타당도 |
| 14 | 1차 오답 시 정답 비공개 + `retryHint` (§6-5) | ③ 개발계획서 자기 위반 해소 |
| 15 | 45자 초과 화면 문장 12건 리라이팅 (§2-3) | 가독성 전반 |
| 16 | 터치 타깃 44px 확보 (§8-3) | ⑤ 모바일 권장 요건 |
| 17 | 1366×768 궤도 CTA 폴드 해소 — `#orbitSvg { max-height: min(52vh, 520px) }` | ⑤ 라이브 데모 |

### 2~3일 (설계 수정 — 중간점검 전에 끝낼 것)

| # | 작업 | 효과 |
|---|---|---|
| 18 | **예측 봉인 해제 블록** — `renderExpertStep`에 예측 vs 자료 대조 (§6-2) | ② 대표 기제 완성. **단일 항목 최대 효과** |
| 19 | 미션 2 게이트에 25°C/28°C 두 기준 요구 (§6-3) | ② 예측이 검증 가능해진다 |
| 20 | `localAudit`(오개념 검출기)을 CERL 제출 시 필수 경로에 태움 | ③ 목표④ 미달 해소 |
| 21 | 개념 렌즈에 '절기↔기후 혼동' 문항 2개 추가 | ③ 목표① 측정 도구 확보 |
| 22 | 전이 확인 3문항을 완료 화면에 추가 | ③ "학습 효과를 어떻게 측정했나" 방어 |
| 23 | 실험실 `labSeen`을 **조작 1회 이상**으로 변경 | ③ 목표⑤ 배지 신뢰도 |
| 24 | 브라우저 뒤로가기(`pushState`/`popstate`) | ⑤ |

### 제출 리스크 — 마감 전 반드시 (§11)

| # | 작업 | 왜 지금인가 |
|---|---|---|
| **A** | **`/archive.html`·`app.js`·`style.css` 저장소·배포에서 제거** (또는 vercel.json rewrite로 차단) | 폐기된 프로토타입이 라이브에 노출되어 있고, **본 앱의 외부 전송 동의 설계를 우회해** 학생 글을 OpenAI로 보낸다. 본 앱은 이 셋을 한 줄도 참조하지 않는다 |
| **B** | **`submit_check.sh` 정규식 2개 수정 + 일부러 위반값을 넣어 ✗가 뜨는지 확인** | 게이트가 검사하려던 것을 못 잡으면서 **항상 ✓를 찍는다**. 검사기 자체가 검증된 적이 없다 |
| **C** | 게이트에 **"배포본 == 제출 소스" 해시 대조** 추가 | 가장 무거운 실격 사유(핵심 로직 수정·소스 누락)가 자동 검사에서 빠져 있다 |
| **D** | AI 요청에 클라이언트 타임아웃(예: 12초) + 진행 표시 | 지금은 `maxDuration: 30`까지 버튼이 잠긴다. 라이브 데모에서 30초 정지는 치명적 |
| **E** | 감사 화면 문구 수정 + `기기 안 빠른 점검` 자동 1회 실행 (§8-8) | "핵심 학습은 끝났습니다 / 선택 심화"가 **AI와 오개념 검사를 건너뛰어도 되는 것**으로 규정한다. 문구 2줄 + 자동 호출 1줄 |

### 문서·발표 (코드 무관)

| # | 작업 |
|---|---|
| 25 | **소요 시간 숫자 하나로 통일** — 앱 본문 69~76분 / 교사 학습지 13~18분 / 45분 1차시 주장이 서로 다르다. 학생 파일럿으로 실측한 뒤 한 곳(교사 학습지)에서만 관리 |
| 26 | `.integrity`에 관측 불연속 한 줄 추가 (§7-4) |
| 27 | 발표 대본에 **26개 이동 구간 패널**과 **AI 감사관 실연**을 필수 항목으로 못박기 |
| 28 | "PhET/기후스트라이프와 무엇이 다른가"를 소개 화면 한 문장으로 (§9-1 ④) |

---

## 11. 코드 견고성 · 제출물 리스크

### 11-1. 먼저 확인된 것 (이 축은 대체로 단단하다)

- **XSS 경로 0건** — `innerHTML` 사용처 59곳을 훑었고, 학습자 자유입력(CERL·판정문·`canDo`)과 AI 응답이 화면으로 되돌아가는 경로는 **전부 `escapeHTML()`(verify.js:3753)을 거친다.**
- **CSP가 제대로 잠겨 있다** — `script-src 'self'; connect-src 'self'; object-src 'none'; frame-ancestors 'none'` (vercel.json). 인라인·외부 스크립트가 모두 차단된다.
- `.env.local`은 **git 미추적**(`.gitignore`의 `.env.*`).
- 라이브 콘솔 에러 **0건**.
- AI 엔드포인트는 프롬프트 인젝션 판정문에 대해서도 정상 저항.

### 11-2. P1 — `/archive.html`이 라이브에 살아 있다 (직접 확인)

`https://weather-24solar-terms.vercel.app/archive.html` — 본 앱에서 링크되지 않지만 **URL로 바로 열린다.** 직접 열어 확인한 상태:

| 항목 | archive.html | index.html(본 앱) |
|---|---|---|
| AI 호출 | `app.js:350` → `POST /api/ai-turn` | `verify.js:3845` |
| **외부 전송 동의 체크박스** | **없음** | 있음 (`#aiConsent`, 체크 전에는 요청 차단) |
| `X-Learning-Session` 헤더 | **없음** (세션 레이트리밋 우회) | 있음 |
| `data-theme` | **`null`** (theme-init.js 미로드) → 라이트 사용자에게 다크 전용 화면 | 정상 |
| 파비콘 | **`thumbnail.png` 1.71MB** | `favicon-32.png` |
| 데이터 캐시 키 | `solar_terms_data.js?v=6` | `?v=11` → **같은 404KB 파일을 두 번 받는다** |
| 용어 | `20개 사건 대시보드` · `수사 노트` · `증거 카드` (폐기된 탐정 은유) | 현행 용어 |
| 히어로 | `24절기의 약속은아직 유효할까?` (`<br>` 때문에 붙어 보임) | 동일 |

**심사 관점의 문제**
1. 심사위원이 URL을 만지다 이 페이지를 발견하면 **폐기된 프로토타입이 라이브로 노출된 상태**로 보인다.
2. 본 앱이 공들여 만든 **외부 전송 동의 설계가 이 경로로 우회된다.** 개인정보 설계의 일관성이 깨진다.
3. 제출 소스에 포함되므로 코드 검토에서도 보인다.

**처방 (셋 중 하나, 전부 저비용)**
- (A, 권장) `prototype/archive.html`·`app.js`·`style.css`를 저장소에서 제거. 본 앱은 이 셋을 한 줄도 참조하지 않는다.
- (B) `vercel.json` rewrites에 `{"source":"/archive.html","destination":"/prototype/index.html"}`를 **`/(.*)` 규칙보다 앞에** 추가.
- (C) 남겨야 한다면 최소한 ① `askCoach` 제거 ② `theme-init.js` 추가 ③ 파비콘 교체 ④ `?v=` 동기화.

> 5차에서 "앱 파일은 한 바이트도 바뀌지 않았다"며 사무국 자료를 정리한 커밋이 있다. 같은 기준을 적용하면 archive.html도 정리 대상이다.

### 11-3. P1 — `submit_check.sh`의 정규식 두 개가 백슬래시를 잃었다 — **항상 ✓를 찍는다**

```bash
# scripts/submit_check.sh:149  — 로컬 홈 경로(윈도우) 검사
grep -rqE "([A-Za-z]:\+Users\+|/(home|Users)/)[A-Za-z0-9._-]+" prompt_sessions/*.md
#                      ↑ \+ 는 ERE에서 '리터럴 +'다. 즉 "C:+Users+"를 찾는다 — 절대 없다.
#                        실제 경로 C:\Users\... 는 이 패턴에 걸리지 않는다.

# scripts/submit_check.sh:156  — COASTAL 이분법 회귀 검사
grep -qE '^s*var COASTALs*=' prototype/verify.js
#          ↑ \s 가 s 로 붕괴. 들여쓴 선언(실제 코드 형태)은 ^s* 에 걸리지 않는다.
```

커밋 `6c5ab4f`("세션 로그의 로컬 홈 경로 마스킹 — 제출 직전 최종 감사에서 발견")가 고친 문제의 **회귀 방어선이 사실상 비어 있는데, 게이트는 `ok "세션 로그 로컬 경로 마스킹 확인"`을 출력한다.** 게이트를 신뢰하고 제출하면 회귀를 놓친다.

**처방**
```bash
grep -rqE '([A-Za-z]:\\+Users\\+|/(home|Users)/)[A-Za-z0-9._-]+' prompt_sessions/*.md
grep -qE '^[[:space:]]*var[[:space:]]+COASTAL[[:space:]]*=' prototype/verify.js
```
(작은따옴표로 감싸고 `\\`로 이스케이프하거나, `[[:space:]]`처럼 백슬래시가 필요 없는 클래스를 쓴다.)
**게이트를 고친 뒤 반드시 "일부러 위반 문자열을 넣어 ✗가 뜨는지" 한 번 확인할 것.** 지금 상태는 검사기 자체가 검증된 적이 없다는 뜻이다.

### 11-4. 그 밖 (요약)

| # | 지적 | 위치 |
|---|---|---|
| R-01 | 게이트가 **"배포본 == 제출 소스"를 검증하지 않는다** — 가장 무거운 실격 사유(핵심 로직 수정·소스 누락)가 자동 검사에서 빠져 있다 | submit_check.sh §4 |
| R-02 | AI 감사 요청에 **클라이언트 타임아웃이 없다** — `vercel.json`의 `maxDuration: 30`까지 버튼이 잠긴다. 서버는 16초/9초 타임아웃을 걸어 놨는데 클라이언트는 안 걸었다 | verify.js:3841-3862 |
| R-03 | **프롬프트 인젝션 탐지 규칙이 AI 성공 경로에서 실행되지 않는다** — `doAudit`이 성공 시 `localAudit`을 호출하지 않는다. AI_품질_평가.md의 "이중 방어" 서술과 코드가 다르다 | verify.js:3836-3862 |
| R-04 | `api/ai-turn.js`의 **coach 모드 전체가 죽은 경로** — 본 앱은 audit만 호출한다. 소스 검토에서 미사용 코드로 보인다 | api/ai-turn.js:34-44, 100-117 |
| R-05 | 레이트리밋 `Map`이 만료 키를 제거하지 않는다(웜 인스턴스에서 무한 증가), 본문 검증보다 먼저 실행돼 **400으로 거절될 요청도 학생의 6회 한도를 깎는다** | api/ai-turn.js:24-25, 230 |
| R-06 | AI 엔드포인트에 **Origin/Referer 확인이 없다** — 제3자 페이지가 `no-cors` POST로 OpenAI 크레딧을 태울 수 있다 | api/ai-turn.js:228-248 |
| R-07 | `load()`가 `state.done`을 **타입 검증하지 않는다** — 손상된 저장값이면 진행 표시줄이 통째로 사라지고 조용히 실패한다 | verify.js:566-621 |
| R-08 | `autoDemo`의 250ms `setTimeout`이 `stopTimers` 대상이 아니다 — 화면을 빨리 벗어나면 `state.thr`가 미션 기준값+4로 남는다 | verify.js:2522, 2469-2481 |
| R-09 | `loadGlobal`의 재진입 가드가 두 번째 호출의 콜백을 소리 없이 버린다 | verify.js:3196-3213 |
| R-10 | `copyLink`·`copyRec` 실패 폴백이 `prompt()`를 쓴다 — 차단 환경에서 아무 일도 안 일어난다 | verify.js:663-668 |
| R-11 | 게이트가 `verify.js` 구문 검사만 하고 `api/ai-turn.js`·`app.js`·`theme-init.js`는 검사하지 않는다 | submit_check.sh:87 |
| R-12 | `lagChange()`가 `seasonalLag` 결측을 한쪽에서만 방어 — 같은 함수 안 가드가 비대칭 | verify.js:414-426 |
| R-13 | verify.js:3865의 테마 주석이 "인라인 스크립트"라고 적혀 있으나 실제로는 외부 파일(`theme-init.js`) — CSP 설명과 어긋난다 | verify.js:3865-3868 |

---

---

## 12. 본 대회 배점표 기준 채점 (제출 가부 판정)

**근거 배점표** 본 대회 심사 배점표 10개 세부 항목 · 합계 100점 · 발표 10분 초과 감점 별도
**채점 원칙** 배포본(`https://weather-24solar-terms.vercel.app/`)을 직접 구동해 계측한 것과, 제출물 5종의 실제 파일만 근거로 삼는다.

### 12-0. 제출 가부

**결론 — 지금은 제출 가능. 단, 이 점검을 시작한 시점에는 불가였다.**

발견한 차단 사유와 조치:

| # | 발견 | 근거 | 조치 |
|---|---|---|---|
| **1** | 제출물 5종이 **7/29 생성본**으로, 8/3 6차 수정을 담고 있지 않았다 | `제출물/weather24_source.zip` 안 `verify.js` = `34b476c8` ↔ 현재 `c1d62cb1` | `pack_submission.py` 재실행 |
| **2** | 그 zip에 **삭제한 `archive.html`·`app.js`·`style.css`가 그대로** 있었다 | zip 목록 확인 | 재포장 후 4개 전부 0건 |
| **3** | 발표자료 PDF가 **구 브랜드**(절기의 약속 검증소) 슬라이드였다 | `build_deck.py` 3곳 + 용어 5곳 | 문구 교체 후 11장 재생성 |
| **4** | 데모 대본·README에 **구 용어**(자가진단·전문가 예시·프리셋·관측 자료 조작·시차) | grep 대조 | 현재 화면 문구로 통일 |
| **5** | 발표 러닝타임 표기가 **두 곳에서 어긋남**(9분 00초 / 8분 45초) | `발표_10분_구성안.md` L14 ↔ L21 | 8분 45초 하나로 |

**조치 후 3자 해시 일치 확인** — 배포본 = HEAD = 제출 zip

```
index.html    46e20028c04e   base.css   9048b5b68ae6   verify.css  155c6198945d
verify.js     c1d62cb19c6f   theme-init.js 4814cf8510a2
```

> zip #2가 특히 중요하다. `archive.html`은 본 앱의 외부 전송 동의를 우회해 학생 글을 OpenAI로 보내던 페이지다. 그 zip을 그대로 냈으면 페이지를 지운 조치 자체가 무의미해졌다.

### 12-1. 항목별 채점

| 항목 | 세부 | 배점 | 채점 | 근거 (전부 실측) |
|---|---|---|---|---|
| **① 주제 적합성** | 개발계획서 제시 개념이 다루어졌는가 | 10 | **9** | 예선 제출 계획서 §9의 5개 개념 대조 — ①기후평년/계절 길이 ✓(`기후평년` 9회 명시 + 미션 2) ②기온 상승·극값·계절 이동 ✓(`폭염` 16회 + 미션 1·2·5) ③24절기의 과학 ✓(태양황경 15° + 궤도 화면) ④습도·강수 변화 ✓(습도 지표 + `큰비` 8회) ⑤(확장)CO₂–기온 ✓(지구 맥락) / 태풍·위성 ✗. 미구현 2건은 **계획서가 스스로 "(확장 개념)"으로 괄호 표시**했고 README가 ERA5·GPM·IBTrACS를 *현재 배포본 미사용*으로 명시 |
| | 체험하며 주제를 명확히 인식하는가 | 10 | **9.5** | 히어로가 곧 주제(`24절기, 지금도 맞을까?`) · 리드 2줄이 "실측으로 확인" + "움직이지 않는 절기 vs 움직이는 기후" · 매 화면 integrity 문단이 자료 범위 고지 |
| **② 체험·참여형** | 조작선택이 실제 작동하고 학습 개념과 연계 | 15 | **14** | 기준 변경 4경로(슬라이더·＋−·자주 쓰는 기준·드래그) **전부 즉시 반응 실측** · 지역 16 × 절기 24 × 지표 3 × 비교구간 26 · **판정 게이트가 조작을 강제**(`state.moved` 없이 진행 불가) · 미션 2는 25·28°C 두 기준, 미션 3은 제주·강원 둘 다, 미션 4는 1mm+30mm↑를 요구 |
| | 변수 탐구 가능 + 조작 직관 + 개념 해석 | 10 | **9** | 열관성 실험실이 **필터가 아니라 물리량**(유효 열용량·온실효과)을 조작 · 픽커 5개 전부 가시 라벨 실측(`지역`·`절기`·`지표`·`자주 쓰는 기준`·`비교 기간`) · `aria-valuetext`가 결과값까지 낭독 · 자유탐구 질문 조립기가 "무엇이 답할 수 있는 질문인가"를 조작으로 가르침. 감점: 미션 1이 16화면으로 길다 |
| **③ 학습 효과** | 학습 대상 명확 + 난이도 적합 | 5 | **4** | 중3~고2 명시(계획서·교사 학습지) · 어휘를 대상 수준으로 정리(자가진단→이해 확인, 전문가→모범 예시) · 교사용 학습지에 수업 흐름·활동지·오개념 표·루브릭. 감점: **실제 소요 시간 미측정**(앱이 정직하게 밝힘), 앱 화면에 대상 학년 표기 없음 |
| | 학습 성과 구체적 + **잘못된 조작·선택에 학습적 피드백** | 5 | **5** | 성과: 완료 화면 '내 기록'에 예측·이해 확인·전이 3문항·CERL 5편·실험실 값·자유탐구 질문/결론. 피드백 6종 — ①이해 확인 1차 오답 시 **정답 비공개 + retryHint로 자료로 되돌림** ②예측 봉인 해제 대조 ③실험실 예측 오답 두 방향 대조 ④CERL 근거 단위 없음/한계 범위어 없음 차단 ⑤**극단 기준 조작 시 그 자리에서 비교 불가 안내** ⑥중복 문장·발문 베끼기 차단 |
| **④ 아이디어 독창성** | 차별화 포인트가 구체적으로 구현 | 8 | **7** | 24절기(한국 고유 문화 기준선) × 실측 검증 × 물리 모형 3중 결합 · "움직이지 않는 절기 vs 움직이는 기후"를 히어로에 명시 · 학습자가 임계값을 정의하면 결론이 바뀐다(PhET·En-ROADS·Climate Stripes와 다른 축). 감점: 개별 요소는 기존에 있고 **결합이** 독창 |
| | 새로운 학습 경험과 인터랙션 | 7 | **6.5** | 예측 봉인 → 조작 → 봉인 해제 대조 루프 · 조건이 질문 문장을 만드는 질문 조립기 · 실험실 예측-공개 · 앱이 스스로 "비교할 수 없다"고 말하는 정직한 실패 상태 |
| **⑤ 구현 완성도** | URL 안정 구동 + 계획한 기능·체험 실제 작동 | 15 | **14.5** | 라이브 **11화면 × 3뷰포트(1366/390/1920) 무결점** — 고아줄 0·겹침 0·잘림 0·24px 미달 타깃 0·가로 넘침 0·콘솔 에러 0 · 로그인 없음 · 외부 도메인 0 · 6리소스/134KB · 폐기 경로 4개 404 · 배포본=HEAD=제출 zip 3자 일치 |
| | 기상·기후 데이터 실제 반영 + **AI 도구가 제작 과정에 효과적 적용** | 15 | **14.5** | 데이터: 기상청 ASOS 1969–2026 · 16지점 × 24절기 × 3지표, 조작 시 즉시 재계산 실측 · 회귀 검증 **8,360건**(데이터 8,213 + 문서 147) 자동 통과 · 과학 수치 9개 검산 통과. AI(제품 안): `/api/ai-turn` 실동작 — 과대주장 문장에 **200 OK / 4.8초 / "서울 자료만으로 우리나라 전체를 말할 수 없고, '기후변화 때문에'라는 원인도 단정할 수 없다"** + 실패 시 로컬 규칙 폴백. AI(제작 과정): 프롬프트 세션 원문 6건 · `AI_활용_기록.md` · `AI_품질_평가.md` · eval 100문항 **100/100** · 6차에 걸친 AI 레드팀 |
| **합계** | | **100** | **≈ 93** | 감점 전 |

### 12-2. 감점 리스크 — 이 하나가 가장 크다

발표 10분 초과: **1분 −9점 / 2분 −15점 / 3분 −21점·발표 중지.**

−9점은 위 표에서 ③ 학습 효과(10점) 전체에 맞먹는다. 현재 설계는 **8분 45초**(구성 합계)로 10분까지 1분 15초 여유가 있으나, **아직 스톱워치 리허설 기록이 없다.** 이것이 지금 남은 최대 리스크이고, 코드로 해결할 수 없는 유일한 항목이다.

권고: 리허설 3회, 매회 스톱워치. 9분 30초를 넘으면 슬라이드가 아니라 **시연 항목**을 줄인다(발표 구성안 §2의 4분 20초 시연 블록이 가장 압축 가능).

### 12-3. 점수를 더 올리려면 (배점 대비 효과 순)

| 대상 | 지금 | 남은 근거 | 필요한 일 |
|---|---|---|---|
| ③ 학습 대상·난이도 (5점 중 4) | 4 | 소요 시간이 실측이 아니라 설계값 | **학생 파일럿 1회**(중3~고2 5명, 스톱워치). 앱 수정 아님 |
| ① 계획서 개념 (10점 중 9) | 9 | 태풍·위성이 미구현(계획서가 "확장"으로 표시) | 발표에서 "확장 데이터는 수집했으나 학습 초점을 흐리지 않기 위해 배포본에서 뺐다"를 **한 문장으로 선제 공개** |
| ② 조작 직관 (10점 중 9) | 9 | 미션 1이 16화면 | 렌즈를 4→3으로 더 줄이면 15화면. 다만 판별 문항이 줄어 ③이 깎일 수 있어 **파일럿 결과를 보고 판단** |
| ④ 독창성 (15점 중 13.5) | 13.5 | 결합의 독창성이 발표에 얼마나 전달되는가에 달림 | 코드가 아니라 **발표 프레이밍** 문제 |

---

## 부록 A. 사용한 계측 방법 (재현용)

브라우저 콘솔에서 실행해 같은 수치를 다시 얻을 수 있다.

**고아줄 탐지** — Range API로 요소를 줄 단위로 분해하고 `마지막 줄 폭 / 최대 줄 폭 < 0.40`을 집계
```js
const r = document.createRange(); r.selectNodeContents(el);
const lines = [...r.getClientRects()] /* top으로 그룹화 */;
```

**SVG 잘림 탐지** — `getBBox()`가 `viewBox` 범위를 벗어나는지 비교
```js
const vb = svg.getAttribute('viewBox').split(' ').map(Number), bb = svg.getBBox();
// bb.y < vb[1] 이면 상단 잘림, bb.y+bb.height > vb[1]+vb[3] 이면 하단 잘림
```

**겹침 탐지** — 텍스트 리프 노드의 `getBoundingClientRect()` 쌍별 교차(조상·형제 인라인 제외)

**대비비** — 배경의 알파를 조상까지 **합성**한 뒤 WCAG 상대휘도로 계산.
(합성하지 않으면 `rgba(10,95,162,0.09)` 같은 반투명 배경을 불투명으로 오인해 거짓 실패가 무더기로 나온다 — 첫 측정에서 21건이 나왔으나 합성 후 실제 실패는 2건이었다.)

**여백 스케일** — `main *` 전체의 `marginTop/Bottom`·`paddingTop/Bottom` 값을 빈도 집계

**화면 문자열 전수** — verify.js에서 주석 제거 후 문자열 리터럴 추출 → 한글 포함분 973건
(`_ko_strings.txt`, 45자 초과 185건은 `_ko_long.txt`)

