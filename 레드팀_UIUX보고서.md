# Weather24 UI / UX / CX 레드팀 보고서

> **📍 문서 상태 — 3차 감사 기록(원문 보존).** 이 문서의 지적은 대부분 이미 반영됐고,
> 일부 수치는 그 뒤 데이터 재빌드로 바뀌었습니다. **현재 값으로 읽지 마세요.**
> 최신 상태와 정정 내역: [`레드팀_4차_종합보고서.md`](레드팀_4차_종합보고서.md)


> 대상: 배포본 https://weather-24solar-terms.vercel.app/ (`prototype/index.html` + `verify.js` 657줄 + `verify.css` 19KB + `style.css` 49KB)
> 기준일 2026-07-26 · 현장 데모까지 26일
> **방법: 전량 1차 실측.** 배포 사이트를 375×667 / 430×932 / 1280×800 뷰포트에서 직접 계측(getBoundingClientRect·getComputedStyle·합성 PointerEvent 드래그·performance API)하고, `verify.css`·`style.css`·`verify.js`를 대조했다. 추정치는 '계산'으로 명시했다.
> 1차 레드팀(데이터·과학·제품)은 [레드팀_보고서.md](레드팀_보고서.md) 참조. 본 보고서는 **UI/UX/CX만** 다룬다.

---

## 0. 한 화면 요약

| | |
|---|---|
| **근본 원인 하나** | `bounds()`(verify.js:115–119)가 y축을 **연중 최저~최고(서울 −6.2 ~ +32.0°C = 38.3°C)** 로 잡는데, 드래그 가능한 임계값은 **20~32°C(12°C)** 뿐이다. 제품의 유일한 조작이 y축의 **31%** 안에 갇혔다 |
| **모바일 정밀도** | 375px에서 히어로 SVG 높이 **152px** → **1°C = 3.4 CSS px**. 실측 드래그: **8px 이동 = 2°C 점프.** 손가락 접촉폭(~40px)이 전체 12°C를 훑는다 |
| **원인–결과 비공존** | 숫자가 바뀌는 `#readouts`가 **도착 시 화면 밖** — 375px에서 229px 아래, **1280×800에서도 60px 아래**. 데모 대본의 핵심 문구는 "숫자가 0초로 뒤집힌다" |
| **예측 봉인이 안 보인다** | `position: static`(모달 아님). 1280×800에서 top=1051 / bottom=1397, 뷰포트 800 → **가시 영역 −251px = 전혀 보이지 않음.** 포커스도 이동하지 않음 |
| **헤더가 모든 폰에서 깨진다** | 375px: `↺ 처음부터`가 **25×100px = 글자 6줄 세로 쌓임**, `사용법` 25×61. 430px(최대 폰)에서도 2줄. **원인은 `데이터 보관실 →` 링크의 `white-space:nowrap`** — 그 링크를 숨기니 즉시 79×22 / 51×22로 정상 복귀(실증) |
| **성능·명암비는 결함 아님** | 첫 로딩 **92.7KB(gzip)**, DOMContentLoaded **135ms**, `drawHero()` 0.66ms, 375px 가로 오버플로 0, 콘솔 에러 0, 명암비 5.58~15.62 전부 AA 통과 |
| **한 줄 판정** | 데이터 결함(1차 보고)이 "앱이 틀린 말을 한다"였다면, UX 결함은 **"앱이 자기가 가르치려는 인과를 사용자에게 보여주지 못한다"**다. 조작과 결과가 같은 화면에 없고, 조작 자체가 폰에서 정밀하지 않다 |

---

## 1. 지켜야 할 UX 강점 (실측 확인 · 수정 중 깨뜨리지 말 것)

| # | 강점 | 실측 근거 |
|---|---|---|
| 1 | **성능이 진짜 빠르다** | 첫 로딩 전송 92.7KB(gzip), DCL **135ms**, `solar_terms_data.js` 192KB→53KB 압축. `drawHero()` 1회 **0.66ms** — 드래그 60fps 예산(16ms)의 4% |
| 2 | **모바일 드래그가 실제로 작동한다** | `#heroSvg { touch-action: none }`(verify.css:44) — 세로 드래그가 페이지 스크롤에 먹히지 않는다. 이걸 놓친 제품이 훨씬 많다 |
| 3 | **범례 기호가 실제 선 스타일과 정확히 일치** | `.lg-past`는 `repeating-linear-gradient`로 점선, `.lg-now` 실선, `.lg-term` 점선, `.lg-thr` 실선(verify.css:40–43) ↔ 차트의 `stroke-dasharray`와 1:1. **색에만 의존하지 않아 색약 사용자도 구분 가능** |
| 4 | **명암비가 전 구간 AA 통과** | 본문 8.83~15.62, muted 5.58, 시작 버튼 #152435 on #ffbe58 ≈ 9.1. 다크 테마에서 흔한 실패를 피했다 |
| 5 | **포커스 링이 전역으로 정의됨** | `button:focus-visible, select:focus-visible, textarea:focus-visible { outline: 3px solid var(--sun); outline-offset: 3px }`(style.css:2) |
| 6 | **reduce-motion CSS 블록 존재** | style.css:5에서 `transition/animation: none !important` |
| 7 | **가로 오버플로 0 · 탭 타깃 24px 이상** | 375px `scrollWidth == clientWidth`. 측정한 포커스 가능 요소 중 24×24 미만 **0건**. 지역 칩은 약 84×48px |
| 8 | **칩 스트립이 모바일 관례를 지킨다** | `overflow-x:auto` + `scroll-snap-type: x proximity` + `-webkit-overflow-scrolling: touch`(verify.css:20) |
| 9 | **판정 화면은 한 화면에 들어간다** | 1280×800에서 전체 1.0 스크린, CERL 판정문 top=228, 자가진단 top=492 — **둘 다 폴드 위** |
| 10 | **교사 학습지에 인쇄 스타일이 있다** | `교사_학습지.html:28 @media print` — 실제로 배포 가능한 수업 자료 |
| 11 | **평가 하니스가 정확히 재현된다** | `node eval_harness.js` 재실행 → 96/100, 실패 4건(C017·C023·C026·C094) 동일. 검증 가능한 주장 |

---

## 2. P0 — 데모 전 반드시

### UX-1 [CRITICAL] 조작 기하학: y축이 시그니처 제스처를 압살한다

**근거**: `verify.js:115–119`
```js
function bounds(city, metric) {
  var all = series('past',...).concat(series('present',...));
  var lo = Math.min(...), hi = Math.max(...), pad = (hi - lo) * 0.08;
  return { lo: lo - pad, hi: hi + pad };   // 서울: -6.24 ~ +32.04  (38.3°C)
}
```
`verify.js:18`의 드래그 범위는 `{ lo: 20, hi: max(28, min(34, round(b.hi))) }` = **20~32°C(12°C)** 뿐이다. 즉 **y축의 31%만 조작 가능**하고 나머지 69%(겨울~봄)는 드래그 불가 영역이다.

**실측 정밀도**

| 뷰포트 | SVG 높이 | **1°C당 픽셀** | 전체 밴드(20~32) | 시차 계산되는 구간(20~25) |
|---|---|---|---|---|
| **375×667** (가장 흔한 폰) | **152px** | **3.4px** | **40.8px** | **17px** |
| 430×932 (최대 폰) | ~166px | ~3.7px | ~44px | ~18px |
| 1280×800 (심사 노트북) | 377px | 8.45px | 101.5px | 42.3px |

**실측 드래그 테스트** (합성 PointerEvent): 25°C 지점에서 포인터를 **8px 내리니 23°C**로 이동. 즉 **8px = 2°C**. 손가락 접촉폭 약 40px는 전체 12°C 범위에 해당한다.

**사용자 시나리오**: 학생이 "28도로 맞춰 볼까" 하고 선을 잡는다. 손가락을 조금 움직이면 24°C, 조금 더 움직이면 30°C가 된다. 정확히 28을 짚으려면 3px 정밀도가 필요하다. 결국 학생은 **의도한 값을 고르는 것을 포기하고 아무 값에서 손을 뗀다.** 그런데 이 앱의 3번 학습목표는 *"'덥다'를 몇 도로 정할지는 **내가** 정한다"*다 — **의도적 값 선택이 불가능하면 목표 ③이 성립하지 않는다.**

**심사 영향**: ②체험·참여형 25점의 핵심. 심사위원이 자기 폰으로 조작해 "값이 튄다"를 경험하면, 이 제품의 유일한 조작이 미완성으로 읽힌다.

#### 해법 3안 비교

| 안 | 방법 | 정밀도 이득 | 학습적 대가 | 비용 |
|---|---|---|---|---|
| **A. y축 범위 잘라내기** | `bounds()`를 `[8, 32]`(24°C)로 클립 | 38.3→24°C = **1.6×** (375px에서 3.4→5.4px/°C) | 겨울 곡선이 잘려 **연간 계절 곡선의 전체 형태**를 잃는다. '절기 vs 기후' 서사에서 겨울 절기(동지·소한)를 다룰 수 없음 | 낮음 (1~2h) |
| **B. 별도 임계값 트랙** | 차트 옆에 전체 높이 세로 슬라이더 | 플롯 전체 높이 사용 = **3.2×** | 곡선 위 직접조작(제품의 시그니처)을 잃는다. "선을 끌어 내가 정한다"의 물리적 은유가 약해짐 | 중간 (4~6h) |
| **C. 반응형 viewBox + 완만한 클립 + 프리셋** | ① 모바일에서 viewBox를 `720×340`→`720×560`으로 (렌더 높이 152→**270px**) ② y축 `[8,32]` 클립 ③ 의미 있는 프리셋 버튼(`25°C` `열대야 25°C` `폭염 33°C` `직접입력`) 병기 | 1.64×(높이) × 1.6×(클립) = **약 3×** (375px에서 3.4→**10.3px/°C**, 현재 데스크톱 수준) | 겨울 일부 손실은 A와 동일하나, **프리셋이 오히려 학습 가치를 추가**한다(기상청 실제 기준을 이름으로 배움) | 중간 (4~5h) |

> **권고: C안.** 계산 근거 — 375px에서 카드 내부 가용폭 약 347px. viewBox가 720×560이면 렌더 높이 = 347 × 560/720 = **270px**, 플롯 512 units, y스팬 24°C → 21.3 units/°C, 스케일 0.482 → **10.3 px/°C**. 이는 현재 데스크톱(8.45)보다 좋다.
> 프리셋 버튼은 1차 보고서의 **RC-M(폭염·열대야를 꺼내놓고 검증 불가)** 을 동시에 해소한다 — 프리셋에 `폭염 33°C(최고기온 기준, 현재 데이터 미지원)`를 비활성 상태로 두면 "우리가 왜 일평균을 쓰는가"가 UI로 설명된다.
> **주의**: C안은 차트를 118px 더 키우므로 UX-2(비공존)를 **악화**시킨다. 반드시 UX-2와 **함께** 처리할 것.

---

### UX-2 [CRITICAL] 원인과 결과가 같은 화면에 없다

**실측 (도착 시점, 스크롤 0)**

| 뷰포트 | 차트 | `#readouts`(숫자) | 판정 버튼 | 판정 |
|---|---|---|---|---|
| 375×667 | 455~607 | **697~896** | 965 | readouts가 폴드 아래 **229px** |
| **1280×800** | 327~704 | **767~860** | 985 | **데스크톱에서도 60px 아래** |

정확히 말하면: 차트top~readouts바닥 블록은 375에서 **441px**, 1280에서 **533px**이므로 **스크롤하면 공존은 가능하다.** 문제는 ① 도착 시 비공존 ② 앱이 스크롤을 유도하지 않음 ③ 조작을 시작한 위치에서는 결과가 안 보임.

**부분 완화 요소(정직 고지)**: 차트 **내부**에는 즉시 피드백이 있다 — 채워지는 면적(verify.js:154), `덥다 25°C` pill(verify.js:160–161), 과거/현재 마지막초과일 점 2개(verify.js:162–163). 즉 **정성 피드백은 보이고 정량 피드백만 화면 밖**이다.

**사용자 시나리오**: 심사위원이 폰으로 선을 끌어올린다. 색 면적이 줄어드는 건 보인다. 그런데 발표자가 말한 "과거 33일 → 현재 82일"은 어디에도 없다. 스크롤을 내리면 숫자가 보이지만 **그때는 차트가 화면 위로 사라져** 어떤 조작이 그 숫자를 만들었는지 연결되지 않는다.

**심사 영향**: 데모 대본이 두 번(12행·17행) "숫자가 0초로 뒤집힌다"를 핵심 문구로 쓴다. 그 장면이 폰에서 성립하지 않는다.

**수정 (권장)**: `#readouts`의 2개 숫자를 **차트 안 또는 차트 바로 위**로 올린다. 자유탐구에는 이미 `#freeCerl`(liveSentence, verify.js:181–186)이 있으나 **미션 화면에는 없다** — 같은 컴포넌트를 미션 화면 차트 상단에 재사용하면 신규 개발이 거의 없다. 또는 SVG 우상단에 `과거 33일 → 현재 82일` 텍스트 2줄을 그린다(`drawHero()` 안에서 5줄).
**작업량 2~3h. UX-1(C안)과 반드시 같은 작업에서.**

---

### UX-3 [CRITICAL] 예측 봉인이 화면 밖에서 열린다

**근거**: `verify.js:397–402` `showPredictOverlay()`. 클래스명은 `predict-overlay`인데 **computed `position: static`** — 오버레이가 아니라 카드 맨 아래 인라인 블록이다.

**실측 (1280×800, scrollY=0)**: top=**1051**, bottom=**1397**, 높이 347px → **가시 영역 −251px = 전혀 보이지 않음.**
- 첫 등장 시 `scrollIntoView` **없음**
- 포커스 이동 **없음** (`document.activeElement` = `BODY`)
- `role="dialog"` 아님, 포커스 트랩 없음, Esc 처리 없음

**사용자 시나리오**: 학생이 선을 끈다 → 앱이 보이지 않는 질문 뒤에 게이트를 세운다 → `이 결과로 판정하기`는 회색으로 남고 힌트는 "예측을 봉인하면 판정할 수 있어요"(verify.js:394) → **학생은 무엇을 어디서 해야 하는지 모른다.**

**복구 경로는 있다(정직 고지)**: `verify.js:429`에서 게이트된 버튼을 누르면 `scrollIntoView({behavior:'smooth', block:'center'})`가 호출된다. 즉 **한 번 막힌 뒤에야** 질문이 보인다.

**심사 영향**: 데모 대본 20행이 [25–40초] 하이라이트로 *"첫 드래그 직후 뜬 예측 봉인 오버레이를 가리키며"* 를 지시한다. **무대에서 가리킬 것이 없다.**

**수정**: 등장 시 `scrollIntoView` 1줄 + 첫 선택지로 포커스 이동 1줄. 또는 진짜 모달로 승격(`position: fixed` + backdrop + `role="dialog"` + 포커스 트랩). **최소안 0.5h, 모달화 2h.**

---

### UX-4 [HIGH] 헤더가 모든 폰에서 깨진다 — 수정까지 실증 완료

**실측**

| 뷰포트 | `↺ 처음부터` | `사용법` | 상태 |
|---|---|---|---|
| **375×667** | **25 × 100px** | 25 × 61px | **글자 6줄 세로 쌓임** |
| **430×932** (최대 폰) | 60 × 41px | 40 × 41px | 2줄 |
| **430 + 아카이브 링크 숨김** | **79 × 22px** | **51 × 22px** | **정상 1줄** |

**원인**: `.verify-top`은 `display:flex; justify-content:space-between`(verify.css:5). `.ghost-link`에는 **`white-space: nowrap`**(verify.css:7)이 있어 `데이터 보관실 →`가 122px를 사수하는데, `.text-btn`에는 nowrap이 없다. 375px에서 가용폭 347px − 브랜드 143 − 갭 − 아카이브 122 = 두 버튼에 남는 폭이 부족해 **한 글자 단위로 줄바꿈**된다. `verify.css`의 유일한 모바일 미디어쿼리(234–238행)는 `readouts` 1열화·패딩·CERL 폰트만 다루고 **헤더를 전혀 건드리지 않는다.**

**심사 영향**: 심사위원이 폰으로 열면 가장 먼저 보이는 영역이 깨져 있다. 첫인상 손실이 즉각적이고, 원인이 **1차 보고서에서 이미 제거를 권고한 그 링크**다.

**수정**: `index.html:27`의 아카이브 링크 제거(1차 보고서 RC-P와 동일 조치) + 안전망으로 `.text-btn { white-space: nowrap }`. **작업량 15분. 비용 대비 효과 최상.**

---

### UX-5 [HIGH] 진입 CTA가 폴드 아래 137px

**실측 375×667 인트로**: `시작하기 →` top=**804** (뷰포트 667) → **137px 아래**. `가이드로 먼저 해볼게요` top=816. 인트로 전체 1.49 스크린.
반면 **인트로 미니차트는 378~499로 잘 보인다** — 즉 임팩트는 보이는데 **들어갈 문이 안 보인다.**

**사용자 시나리오**: 심사위원이 폰으로 QR/URL을 열고 애니메이션을 본다. 다음에 무엇을 눌러야 하는지 화면에 없다. 스크롤을 내려야 비로소 버튼이 나온다.

**수정**: 인트로 요소 순서 재배치(목표 배지 4개·설명 문단을 CTA 아래로) 또는 모바일에서 CTA를 `position: sticky; bottom: 12px`로 고정. **작업량 1h.**

---

### UX-6 [HIGH] 되돌아갈 수 없다 — 내비게이션 부재

**근거**
- 진행 표시(`#progress`)는 `renderProgress()`(verify.js:124–131)가 만드는 **4개의 비대화형 `<span>`**. 실측: `tags: ["SPAN","SPAN","SPAN","SPAN"]`, `clickable: false`. **완료한 미션으로 돌아갈 수 없다.**
- 유일한 이탈 경로: `homeLink`(confirm 후 인트로, verify.js:649)와 `resetBtn`(**파괴적** — localStorage 삭제 + reload, verify.js:650).
- `state.phase`에 `verdict`/`complete`가 없다(verify.js:654–656). **판정 화면이나 고향카드 화면에서 새로고침하면 산출물이 사라지고** 미션 조작 화면으로 되돌아간다.
- 딥링크는 `?demo` 하나뿐(verify.js:652). 특정 미션·지역·임계값으로 진입하거나 결과를 URL로 공유할 수 없다.

**사용자 시나리오**: ① 교실에서 학생이 미션2를 하다가 미션1의 숫자를 학습지에 옮겨 적으려 한다 → 돌아갈 방법이 없다. `↺ 처음부터`를 누르면 전부 날아간다. ② 부스에서 심사위원이 고향카드를 만든 뒤 실수로 새로고침 → 카드 소실.

**수정**: `.pstep`을 완료된 단계에 한해 `<button>`으로 전환 + `startMission(i)` 연결(1h). `phase`에 `verdict`/`complete` 추가(1.5h). **작업량 2.5h.**

---

## 3. P1 — 점수 상승 효과가 큰 것

### UX-7 [MEDIUM] 타이포 스케일이 학습용으로 작다

**실측 375px 인트로**: 가시 텍스트 요소 32개 중 **15개가 12.5px 이하, 4개가 10.5px**. 분포 — 10.5px×4, 11.5px×1, 12px×5, 12.5px×6, 13px×8, 14px×**1**, 15px×5, 18px×1, 26px×1.

10.5px에 담긴 것은 **진행 표시 미션명** 하나다(`.pstep small`, verify.css:78) — 4개 미션 라벨.

> **정정 (3라운드 반박 반영)**: 초판에서 *"자료의 한계를 알리는 문장이 화면에서 가장 작은 글자"*라고 썼으나 **틀렸다.** 직접 확인 결과 무결성 문구 `.integrity`는 **12px**(verify.css:63), 스파크라인 캡션 `.spark-cap`도 **12px**(verify.css:173)이다. 10.5px은 진행 표시 미션명뿐이다. 정확한 진술은 이것이다 — **자료의 한계 문구가 12px `#8ba0a8` 회색으로 카드 최하단에 있고, 판정문(`.cerl` 16px)의 1/1.33 크기다.** 한계 고백이 결론보다 작고 흐린 것은 여전히 결함이지만, "가장 작은 글자"는 사실이 아니다.

명암비는 문제가 아니다(10.5px에서도 5.58로 AA 통과). **크기와 위계가 문제다.**

**수정**: 모바일 최소 본문 12px → 13px, `.pstep small` 10.5 → 11.5px, `.integrity` 10.5 → 12px. 스케일 단계를 9개에서 5~6개로 정리. **작업량 1.5h.**

---

### UX-8 [MEDIUM] 접근성 — 잘된 부분과 격차

**통과 확인**: `:focus-visible` 전역 정의(style.css:2) · 범례 기호가 색 외 구분 제공 · 24×24 미만 타깃 0건 · 375px 리플로 정상 · reduce-motion CSS 블록 · `touch-action` 처리.

**격차**

| 항목 | 실측 근거 | 영향 |
|---|---|---|
| **링크·슬라이더에 포커스 링 없음** | style.css:2의 선택자는 `button, select, textarea`만. **`a`와 `input[type=range]` 제외** | `데이터 보관실`·`교사용 학습지` 링크와 **임계값 슬라이더**(키보드 유일 경로)가 포커스 표시 없음. WCAG 2.4.7 실패 |
| **화면 전환 시 포커스 미이동** | 전환 후 `document.activeElement` = `BODY` (실측). `verify.js`에 `tabindex` 처리 **0건** | 키보드 사용자는 Tab을 헤더부터 다시 눌러야 새 화면 컨트롤에 도달 |
| **aria-live 3중 중첩** | `#stage`에 `aria-live="polite"`(index.html:32) + `#readouts` + `#thrOut`. `#stage` 텍스트 실측 **371~377자** | 화면 전환마다 스크린리더가 **화면 전체를 낭독**. WCAG 4.1.3 취지 위반(상태 메시지가 아니라 페이지 전체) |
| **차트 데이터의 텍스트 대안 없음** | `heroSvg` aria-label은 "과거와 현재의 하루 관측 곡선, 고정된 절기 세로선, 드래그 가능한 기준선"(verify.js:240) — **차트 종류만 설명, 값 없음** | 스크린리더 사용자는 `#readouts`의 요약 2개만 얻고 곡선·시차를 탐색할 수 없다. 표 보기 토글이 없다. WCAG 1.1.1 |
| **JS 애니메이션이 reduce-motion 무시** | `verify.js`에 `matchMedia` **0건**. `introPreview` `setInterval(70ms×18)`(verify.js:337), `autoDemo` `setInterval(70ms×16)`(verify.js:407)이 그대로 실행 | CSS 블록은 JS 타이머를 막을 수 없다. WCAG 2.3.3(AAA)·2.2.2 |
| **다크 전용 · 인쇄 불가** | `prefers-color-scheme` 규칙 **0건**, 메인 앱에 `@media print` **0건**(교사 학습지에만 있음) | 밝은 교실·프로젝터 대비 저하. **학생이 자기 판정 결과를 인쇄해 제출할 수 없다** |

**공교육 조달 관점 (냉정한 평가)**: 한국 웹 접근성 품질인증(KWCAG 2.2) 기준으로 현재 상태는 **인증 취득 불가**다 — 차트 데이터의 텍스트 대안 부재와 링크 포커스 표시 부재가 각각 필수 항목이다. 다만 이는 **해커톤 심사 항목이 아니며**, 교육청 정식 도입 단계에서 필요한 작업이다. 8/21까지는 **저비용 3건만** 처리하는 것이 합리적: 포커스 링 선택자 확장(10분), `matchMedia` 게이트(30분), `#stage` aria-live 제거 후 `#thrOut`로 축소(20분). **작업량 1h.**

---

### UX-9 [MEDIUM] 어포던스: 끌 수 있다는 신호가 없다

**근거**: `drawHero()`(verify.js:159–161)가 그리는 것은 선 1개 + 96×20px pill뿐. **그립 핸들이 없다.** `cursor: ns-resize`(verify.css:44)는 데스크톱 전용 신호다.

발견 경로는 셋뿐이고 전부 약하다:
1. `autoDemo` — 조건이 `!demoPlayed && state.mi === 0 && state.pre == null && !state.touched`(verify.js:432). **미션1 첫 방문에만** 실행. 미션2·3·자유탐구에는 없고, 리허설 후에는 미션1에서도 안 나온다.
2. 하단 range 라벨 텍스트 `'덥다' 기준(하루 평균기온)을 위아래로 끌어 보세요`(verify.js:241) — 차트에서 시선이 먼 위치.
3. 튜토리얼(`가이드로 먼저 해볼게요`) — 선택 경로.

**추가 결함 — 히트 영역이 SVG 전체**: `bindThreshold()`(verify.js:271–273)가 `svg.addEventListener('pointerdown', ...)`로 **SVG 전체**에 걸려 있고, 핸들러는 즉시 `setY(e.clientY)`를 호출한다. 즉 **차트 어디를 눌러도 임계값이 그 y로 점프한다.**
시나리오: 학생이 8월 봉우리를 자세히 보려고 곡선을 탭한다 → 임계값이 28°C로 튀고 → 화면 숫자가 전부 바뀌고 → (1차 보고서 확인) "과거엔 거의 없었는데"라는 허위 문장이 뜬다. **탐색 의도의 탭이 파괴적 조작이 된다.**

**수정**: 임계값 선에 원형 그립(r=7) 렌더 + 우측 pill에 `⇅` 아이콘(30분). `pointerdown` 히트 판정을 임계값 선 ±16px로 제한하고 그 밖은 무시(1h). **작업량 1.5h.**

---

### UX-10 [MEDIUM] 선택한 칩이 화면 밖에 있다

**근거**: `bindTermStrip()`은 선택 항목을 스크롤해 보여준다 — `verify.js:260` `if (on && on.scrollIntoView) on.scrollIntoView({inline:'center'})`. 그런데 **`bindCityChips()`(verify.js:246–250)에는 이 처리가 없다.**

시나리오: 자유탐구에서 16개 지역 칩이 가로 스크롤로 놓인다. `강원`(마지막)이 선택된 상태로 화면에 들어오면 칩 스트립은 왼쪽(`서울`)을 보여주므로 **어느 지역을 보고 있는지 화면에 표시되지 않는다.** 차트 어디에도 지역명이 없다(자유탐구의 `#freeCerl` 문장에만 있음).

**수정**: `bindCityChips`/`bindCompareChips`에 `scrollIntoView` 1줄씩. 추가로 차트 좌상단에 현재 `지역 · 절기 · 지표`를 항상 렌더. **작업량 1h.**

---

### UX-11 [MEDIUM] '도' 칩 라벨이 지리 오개념을 만든다

**근거**: `data_collectors/collect_kma_allyears.py:18–27`의 지점 매핑을 확인했다. 16개 칩 중 **8개는 광역 이름인데 실제로는 단일 관측소**다.

| 칩 라벨 | 실제 관측소 | 지점번호 | 문제 |
|---|---|---|---|
| 경기 | **수원** | 119 | |
| 강원 | **춘천** | 101 | 내륙. 강릉(별도 칩)과 기후대가 전혀 다름 |
| 충북 | 청주 | 131 | |
| 충남 | 서산 | 129 | |
| 전북 | 전주 | 146 | |
| 전남 | **목포** | 165 | **해안** |
| 경북 | **포항** | 138 | **해안** (lon 129.38) — 학생은 내륙 대구권을 떠올린다 |
| 경남 | 진주 | 192 | |

칩에는 `도`라고만 표기된다(verify.js:248). 개발계획서 §8.2가 스스로 요구한 *"지역이 실제 관측소인지 대표 지점인지 표시"* 위반이며, **미션3이 "지역마다 다르다"를 가르치는데 그 '강원'은 춘천 한 곳**이다. 학생이 "우리 도는 이렇구나"로 일반화하면 앱이 막으려는 과잉일반화를 앱이 유발한다.

**수정**: 칩 `<small>`을 `도` → **`춘천`** 처럼 실제 관측소명으로 교체(30분) + 무결성 한 줄에 `관측소 1곳의 기록입니다` 명시(30분). 이 수정은 1차 보고서 RC-J와 동일 작업. **작업량 1h.**

---

### UX-12 [MEDIUM] 자유탐구가 방향 없는 조합 공간

16지역 × 24절기 × 3지표 × 13임계값 ≈ **1만5천 조합**인데 프리셋·추천·저장·즐겨찾기가 없다. 안내 문구는 *"내 관심 주제를 직접 검증하세요"*(verify.js:555) 한 줄이다. 3개 미션이 끝난 학습자가 다음에 무엇을 볼지 결정할 근거가 화면에 없다.

**수정**: 프리셋 카드 4~6장(`내 고향의 여름`, `장마는 언제 왔나`, `밤이 더 더워졌나(로드맵)`, `가장 추운 절기는 정말 대한인가`)을 자유탐구 상단에 배치. 각 카드는 지역·절기·지표·임계값을 한 번에 설정. **작업량 2.5h. ②체험형·④독창성에 직접 기여.**

---

## 4. P2 — 여유 시

| 항목 | 근거 | 시간 |
|---|---|---|
| **죽은 CSS 정리** | `verify-mode` 페이지가 `style.css` **49KB**를 로드하지만 대부분이 폐기된 `entry-mode`·대시보드용(`entry-experience`, `orbit-ring`, `prompt-chips`, `interactive-coach`, `investigation-shell`, `case-rail`, `evidence-rail`…). gzip 12KB라 **성능 영향은 없다** — 26일 크런치 중 오류 유발 위험이 이유 | 1.5h |
| **차트 정보 밀도** | y축 라벨 3개(하단/중간/상단)에 **단위 표기 없음**, x축 4개(1·4·7·10월), 격자선 3개(verify.js:151–152). '마지막 초과일'을 눈으로 읽어야 하는 과제에 눈금이 부족 | 1.5h |
| **점 마커 식별성** | 과거/현재 마지막초과일 마커가 r=4 / r=5.5px(verify.js:162–163). 375px에서 차트 높이 152px 기준 상대적으로 작다 | 0.5h |
| **범례 aria-hidden** | `chart-legend`가 `aria-hidden="true"`(verify.js:233) — 시각 사용자만 선 종류를 안다 | 0.5h |
| **인쇄 스타일** | 메인 앱에 `@media print` 없음. 학생 판정 결과 제출 경로 | 1h |

---

## 5. 심사위원이 자기 폰으로 열었을 때 — 초 단위 (실측 기반, 375×667)

| 시각 | 실제로 일어나는 일 | 근거 |
|---|---|---|
| 0.0s | 페이지 로드 완료 | DCL 135ms, 92.7KB |
| 0.1s | 헤더 우측에 **`↺ 처음부터`가 25px 폭 6줄 세로 글자**로 표시 | 실측 25×100 |
| 0.1–1.4s | 인트로 미니차트가 기준선을 스스로 훑으며 `과거 33일 → 현재 82일` 카운트업 (**보인다**, top 378~499) | verify.js:337, 70ms×18=1.26s |
| 1.4s | **다음에 누를 것이 화면에 없다.** `시작하기 →`는 137px 아래 | 실측 top=804 |
| ~3s | 스크롤 1회 → `시작하기 →` 발견, 탭 | |
| 3.5s | 미션1 진입. 차트 top=455 — **화면 하단 절반**. 상단은 헤드라인·미션 배지·과제문 | 실측 |
| 4.0–5.1s | `autoDemo`가 기준선을 훑는다(첫 방문 한정) | verify.js:432, 70ms×16=1.12s |
| ~6s | 선을 잡고 끈다. **8px 움직이니 2°C 점프.** 원하는 값에 못 맞춤 | 실측 드래그 |
| ~7s | 색 면적이 변하는 건 보인다. **"과거 33일 → 현재 82일"은 폴드 아래 229px에 있어 안 보인다** | 실측 |
| ~8s | 손을 뗀다. 예측 봉인 오버레이가 **폴드 아래 251px에서 열린다 — 화면에 아무 변화 없음** | 실측 −251px |
| ~10s | `이 결과로 판정하기`를 찾아 스크롤(top=965) → 회색 버튼 + "예측을 봉인하면 판정할 수 있어요" | verify.js:394 |
| ~12s | 버튼을 누른다 → **이때 비로소** 오버레이가 `scrollIntoView`로 화면에 나타난다 | verify.js:429 |
| ~15s | 예측 선택 → 다시 판정 버튼 → 판정 화면. **여기서부터는 좋다** — 1화면에 CERL + 자가진단 | 실측 |

**요약**: 60초 중 **첫 15초가 전부 "찾기"에 쓰인다.** 스크롤 3회, 실패한 클릭 1회. 그리고 정작 조작의 정량 결과는 한 번도 조작과 함께 보이지 않는다.

---

## 6. '글로벌 최고'를 위한 UX 최소선과 현재 격차

교육용 인터랙티브의 사실상 표준(PhET, En-ROADS, OWID, NOAA Climate.gov)에서 예외 없이 지켜지는 패턴 기준.

| # | 최소선 | 현재 | 격차 | 해당 항목 |
|---|---|---|---|---|
| 1 | **조작기와 결과가 항상 한 화면** | 도착 시 비공존(375: 229px / 1280: 60px 아래) | **미달** | UX-2 |
| 2 | **조작 정밀도가 의도한 값을 고를 수 있는 수준** | 폰에서 1°C = 3.4px, 8px = 2°C | **미달** | UX-1 |
| 3 | **값 직접 입력 또는 의미 있는 프리셋 병기** | 없음(드래그 + 129px 슬라이더뿐) | **미달** | UX-1 C안 |
| 4 | **기본값 복귀 / 되돌리기** | 없음. `다시 조작`은 `state.thr`을 유지 | **미달** | UX-9 |
| 5 | **차트의 표 보기 · 출처 서랍**(OWID는 Chart/Table/Sources가 기본) | 무결성 1줄만, 표 없음 | **미달** | UX-8, RC-J |
| 6 | **상태 딥링크 · 공유** | `?demo` 하나. 결과 공유는 PNG뿐 | 부분 | UX-6 |
| 7 | 반응형에서 차트 높이 유지 | `height:auto`로 aspect 고정 → 폰에서 152px | **미달** | UX-1 C안 |
| 8 | 완료 단계 재방문 | 진행 표시가 비대화형 span | **미달** | UX-6 |
| 9 | 색 외 구분 | **충족** (선 종류 + 범례 일치) | 충족 | 강점 3 |
| 10 | 로딩 성능 | **충족** (92.7KB, 135ms) | 충족 | 강점 1 |

**Weather24가 이들보다 나은 점 (정직하게)**
1. **예측 봉인이 코드로 강제된다** — PhET·En-ROADS에는 선개념 인출 게이트가 없다. 조작 이력과 예측 응답 둘 다 없으면 다음 단계로 못 간다(verify.js:427–431).
2. **오개념 교정 언어가 UI·판정문·AI·교사자료에서 축자 정렬** — 교사 학습지의 '되돌리는 질문' 4개가 `localAudit`의 소크라테스 질문 4개와 거의 동일하고 플래그와 1:1 대응한다. 이 정도의 정렬은 드물다.
3. **교사 수업 자료가 제품과 함께 배포된다** — 인쇄 가능한 학습지 + 루브릭 + 오개념표. PhET은 별도 사이트, En-ROADS는 별도 워크숍 자료다.

---

## 7. 1차 레드팀 P0와의 충돌 검사

| UX 작업 | 데이터 P0와 충돌? | 순서 |
|---|---|---|
| UX-4 헤더(15분) | **없음** — 아카이브 링크 제거는 RC-P와 **동일 작업** | 즉시. 가장 먼저 |
| UX-3 오버레이(0.5h) | 없음 | 즉시, 병행 |
| UX-5 인트로 CTA(1h) | 없음 | 병행 |
| UX-6 내비게이션(2.5h) | 없음 | 병행 |
| UX-1 조작 기하학(4~5h) | **간접 충돌** — RC-A(평활→실측 초과일수)가 임계값별 값을 바꾸므로, 프리셋 버튼의 라벨·기본값은 **RC-A 이후** 확정 | RC-A 후 |
| UX-2 숫자 co-visibility(2~3h) | **직접 충돌** — RC-A가 `#readouts` 라벨·값을 바꾼다. 같은 컴포넌트를 두 번 만지지 않도록 **RC-A와 한 작업으로 묶어라** | RC-A와 동시 |
| UX-11 관측소명(1h) | **동일 작업** — RC-J와 합쳐라 | RC-J와 동시 |
| UX-7 타이포(1.5h) / UX-8 a11y 3건(1h) / UX-9 어포던스(1.5h) / UX-10 칩(1h) / UX-12 프리셋(2.5h) | 없음 | P1 구간 |

**UX P0 합계 약 11~13h** (그중 UX-1·UX-2 7~8h는 데이터 P0의 RC-A 작업에 흡수 가능) · **UX P1 합계 약 8.5h**

**권고 배치**: 7/27 첫날에 **UX-4(15분) → UX-3(0.5h) → UX-5(1h) → UX-6(2.5h)** 을 먼저 끝내라. 데이터 P0와 완전히 독립이고, 합쳐 4시간이며, **심사위원이 폰으로 열었을 때의 첫 15초를 그날 안에 고칠 수 있다.**

---

## 7-B. 추가 계측 — archive.html 모바일 · 320px (2차 보완)

### archive.html은 메인 앱보다 반응형이 잘 되어 있다 (375×667 실측)

| 항목 | archive.html | 메인 앱 |
|---|---|---|
| 적용되는 미디어쿼리 | **5개** (max-width 1100 / 820 / 760 / 700×3) | **1개** (verify.css:234, 3줄) |
| 헤더 깨짐 | **없음** — `← 검증소` 56×20, `수사 규칙` 69×22 | **`↺ 처음부터` 25×100** |
| 가로 오버플로 | 없음 | 없음 |
| 3열 셸(205\|1fr\|250) | flex 세로 스택으로 정상 붕괴, 레일 각 351px | — |
| 지도+차트 2열 | 1열로 정상 붕괴, 지도 SVG 295×230 | — |
| 전체 높이 | **4.75 스크린** | 1.6 스크린 |

**이것이 UX-4의 진단을 확정한다**: archive의 헤더는 액션이 2개뿐이고 `nowrap` 링크가 없어 멀쩡하다. 메인 앱 헤더가 깨지는 원인은 **세 번째 항목인 `데이터 보관실 →` 링크 하나**다.

**archive의 고유 결함**: `+ 현재 결과를 증거로 저장` 버튼과 `증거 보드`가 **624px = 0.93 스크린** 떨어져 있다. 증거를 저장해도 그것이 보드에 꽂히는 장면을 볼 수 없다 — 메인 앱 UX-2와 **같은 패턴의 비공존**이다.

### 두 제품의 용어·순서 불일치 (실측 인용)

| | 메인 앱 | archive.html |
|---|---|---|
| 브랜드 부제 | 절기의 약속 **검증소** | 데이터 보관실 · 24절기 기후 **수사국** |
| H1 | 24절기의 약속은 아직 유효할까? | **동일** |
| 단계 어휘 | 미션 / 판정 / 자유탐구 / 예측 봉인 / AI 증거 감사관 | 사건 파일(20) / 증거 보드 / 수사 규칙 / 수사 노트 / 판정 초안 |
| **예측 시점** | 첫 조작 **직후** 봉인 (verify.js:383 주석: *"예측은 텍스트 게이트가 아니라 첫 조작 직후 오버레이로 봉인한다"*) | **"데이터를 보기 전, 예측을 선택하세요."** (step 01) |

공존하는 은유가 **7개**다: 검증소 / 수사국 / 보관실 / 사건 / 증거 / 감사관 / 미션.

가장 심각한 것은 마지막 행이다. **두 화면이 서로 반대되는 학습 순서를 가르친다.** 심사위원이 "왜 데이터를 먼저 보여주고 예측을 나중에 받습니까?"라고 물은 뒤 헤더의 링크를 눌러 "데이터를 보기 전, 예측을 선택하세요"를 보면, 방금 들은 설계 근거가 즉흥적으로 들린다. 링크 제거(UX-4)가 이 문제까지 함께 닫는다.

### 320px는 신뢰성 있게 측정하지 못했다

브라우저 패널의 최소 CSS 폭이 약 367px여서 `innerWidth: 367` vs `clientWidth: 320`으로 값이 어긋났고, 유일하게 넘친 요소는 `position:fixed; inset:0`인 장식 레이어 `.sky-noise`(의도된 동작)였다. **측정 실패로 기록한다.**

계산으로는: 320px에서 셸 패딩 28px를 뺀 292px 안에 브랜드 143 + 아카이브 링크 122(nowrap) + 버튼 최소 25×2 + 갭 약 38 ≈ **353px**가 필요하므로 부족하고, `body { overflow-x: hidden }`(375px에서 실측) 때문에 스크롤 대신 **오른쪽이 잘린다**. 다만 이는 계산이며, 320px는 iPhone SE 1세대급 희귀 폭이므로 우선순위가 낮다. **375·430px 실측으로 이미 확정된 UX-4를 보강하는 정도로만 취급하라.**

---

## 8. 가장 냉정한 UX 판단

1차 레드팀의 결론이 *"과학적 무결성을 대표 주장으로 내세운 팀이 자기 화면에서 과대주장을 한다"* 였다면, UX의 결론은 이것이다.

**이 제품은 "조작하면 즉시 반응한다"를 유일한 교육 논거로 삼는데, 그 반응을 조작과 같은 화면에 두지 않았고 조작 자체를 폰에서 정밀하게 만들지 않았다.** 데모 대본이 두 번 강조한 "숫자가 0초로 뒤집힌다"는 문장은 **1280×800 노트북에서도 60px 부족해 성립하지 않는다.** 폰에서는 229px 부족하고, 1°C를 고르려면 3.4px 정밀도가 필요하며, 예측 봉인 질문은 화면 밖 251px에서 조용히 열린다. 헤더의 `↺ 처음부터`는 25px 폭에 글자 6줄로 쌓여 있다.

이 결함들의 성격이 중요하다. **디자인 역량 부족이 아니라, 데스크톱 넓은 화면에서 만들고 폰에서 끝까지 통과해 보지 않은 결과**다. 근거: 명암비는 전 구간 AA를 통과하고, 범례 기호는 선 스타일과 정확히 일치하며, `touch-action: none`처럼 놓치기 쉬운 처리를 해냈고, `:focus-visible`이 전역으로 정의돼 있고, 성능은 92.7KB·135ms다. **디테일을 아는 팀이 만들었다.** 그런데 `verify.css`의 모바일 미디어쿼리는 **단 하나(234–238행), 3줄**이다.

그래서 이 부분은 26일 안에 가장 확실하게 회복되는 영역이다. UX-4는 15분, UX-3은 30분, UX-5는 1시간이고, 셋을 합치면 심사위원 첫 15초가 완전히 달라진다. 진짜 판단이 필요한 것은 **UX-1(y축 기하학)** 하나이며, 그것은 취향이 아니라 계산 문제다 — 375px에서 `viewBox`를 `720×560`으로 바꾸고 y축을 `[8,32]`로 클립하면 **3.4 → 10.3 px/°C**로 3배가 되어 현재 데스크톱보다 좋아진다. 이 계산이 맞다면 결정은 어렵지 않다.

**남은 위험은 하나다.** UX-2(숫자를 차트 옆으로)와 RC-A(평활→실측 초과일수)는 **같은 컴포넌트(`#readouts`)를 건드린다.** 이 둘을 따로 진행하면 8월 중순에 라벨과 값이 갈라진다. 반드시 한 작업으로 묶어라.
