# Weather24 — 레드팀 4차 종합 보고서

> **📍 문서 상태 — 4차 감사 당시 기록(원문 보존).** 제출 전 수정으로 일부 구조·수치가 달라졌습니다.
> 현재 상태와 최종 재검증 결과는 [`제출전_Self-reflection_최종보고서.md`](제출전_Self-reflection_최종보고서.md)를 기준으로 읽으세요.

> 작성 2026-07-27 · 대상 커밋 `6e57b3c` · 배포본 <https://weather-24solar-terms.vercel.app/>
> 범위: 교육 효과 · 웹앱 완성도 · 본선 심사기준(100점) 3축
> 원칙: **타협하지 않는다. 그러나 근거 없이 비판하지도 않는다.**

---

## 0. 이 보고서의 검증 방식 — 무엇을 어떻게 확인했는가

앞선 레드팀 3라운드(44건)와 다른 점은 **'해결'이라 기록된 것을 믿지 않고 코드를 다시 열었다**는 것이다.

| 방법 | 내용 |
|---|---|
| 6축 독립 감사 | 학습설계 · 교실운영·접근성 · 과학무결성 · 코드완성도 · 심사채점 · 제출요건 |
| 반증 검증 | 축별 검증관이 **기본값 REFUTED**로 재판정. 2건 완전 기각 + 하위 주장 다수 기각 |
| 배포본 실조작 | 5미션 완주 · 딥링크 · 375px 모바일 · `/api/ai-turn` 실호출 · 명도대비 계산 · 네트워크 계측 |
| 독립 재계산 | 계절지연 **잭나이프**(원시 CSV 재빌드) · 절기 **태양황경 전수계산**(Meeus, 1969–2026) · 고향카드 · 문서 수치 대조 |

**검증 상태 표기**
- ✅ **재현 확인** — 코드/데이터/화면에서 직접 재현했다
- 🔬 **독립 재계산** — 별도 구현으로 다시 계산해 같은 결론을 얻었다
- ⚠️ **코드 판독** — 코드로는 명백하나 실행 재현은 하지 않았다

> `과학무결성` 축은 반증 검증관이 API 오류로 사망했다. 그 축의 12건 중 8건은 **본 보고서 작성자가 직접 재검증**했고(아래 표기), 4건은 코드 판독 수준임을 명시한다.

---

## 1. 총평

**아키텍처와 교육 설계는 수상권이다. 지금 점수를 깎는 것의 8할은 코드가 아니라 문서·문구·상태 초기화라는 값싼 층이다.**

이 작품의 최대 강점은 "주장하지 않고 측정한다"는 규율이며, 그것은 **코드 안에서 실제로 작동한다** — 예측 봉인은 렌더 분기로 강제되고, 일수는 곡선이 아니라 실측에서 세며, 화면의 물리 설명문이 데이터로 무너지면 빌드가 깨지는 assert까지 있다. 자체 레드팀의 '해결' 주장 대부분은 실제로 해결돼 있었다.

문제는 그 규율이 **자기 숫자에서만 무너져 있다**는 것이다. 데이터를 재빌드한 뒤(커밋 `d280dc0`) 인쇄물·대본·README를 갱신하지 않아 24곳 이상이 낡은 값으로 남았고, 그것을 막겠다고 만든 게이트의 문서 검사축은 고정 문자열 블랙리스트라 **하나도 잡지 못했다**. 더 아픈 것은 그 "경고 0건"이 `레드팀_해결확인.md`의 '해결' 근거 칸에 적혀 있다는 점이다. 심사위원이 가장 싸게 검증할 수 있는 지점(`python processing/verify_solar_terms.py` 한 줄, zip 목차 대조, 공개 저장소 커밋 수)이 정확히 그 무너진 지점과 겹친다.

과학적으로는 하나가 남았다. 미션 5의 **여름** 결론(하지 +40일, 전국 39~44일)은 잭나이프에서 흔들리지 않는 견고한 사실이다. 그런데 같은 미션의 **겨울** 결론("바다가 천천히 식으니 해안이 늦다")은 5년 중 한 해만 빼면 격차가 11일 → 2일로 사라지고, 화면이 '해안'이라 부른 7지점 중 인천(11일)·포항(10일)은 내륙 분포 안에 그대로 들어간다. 이 미션의 자기 선언은 *"흔들리는 숫자와 남는 방향을 구별하는 것이 핵심"*인데, 같은 판정문의 추론절이 흔들리는 숫자로 인과를 단정한다.

**정리:** P0 5건 + P1 상위 6건에 약 12시간을 쓰면 76점대에서 88점대로 올라간다. 남은 결함은 대부분 30분~1.5시간짜리 국소 누락이고, 구조 재설계가 필요한 것은 24절기 궤도 그림 하나뿐이다.

---

## 2. 심사 5항목 예상 점수

| 항목 | 배점 | 현재 | 반영 후 | 현재 점수를 만드는 결손 |
|---|---|---|---|---|
| ① 주제 적합성 | 20 | **16** | 18.5 | 24절기 궤도 그림의 물리 180° 반전 · 절기 대표날짜 2개가 자기 산출규칙 위반 · "자료에 최저·최고기온이 없다"가 사실과 다름 |
| ② 체험·참여형 | 25 | **20** | 23 | 대본에 박힌 조작 3건이 실행 불가·불편 · 5미션 전부에 없는 컨트롤을 가리키는 안내문 · 봉인 누출 |
| ③ 학습 효과 | 10 | **6** | 8.5 | 사전=사후 동일 문항 + 정답이 같은 화면에 축자 인쇄 · 자가진단 4/5의 답이 바로 위 CERL에 있음 · 학습 증거가 산출물에 안 남음 · 학생 파일럿 0회 |
| ④ 독창성 | 15 | **12.5** | 13.5 | 26창·판정 3층·감사관 절제는 경쟁 우위. 감점은 CERL 자기일관성 훼손뿐 |
| ⑤ 실현 가능성 | 30 | **22** | 27 | 제출물 목차 불일치 + 미완 TODO · 문서 수치 4계열 · README 검증 명령이 zip에서 죽음 · 게이트 F축 무력 · 세션 로그 97% 중복 |
| **합계** | 100 | **76.5** | **90.5** | 상향폭 +14의 대부분이 문서·문구·상태 층 |

> 내부 우선순위 정렬용 추정이다. 대외 문서·발표에 쓰지 말 것.

---

## 3. P0 — 제출·시연 전 반드시

### P0-1. 문서 수치 4계열이 화면과 다르다 — 무대에서 말과 스크린이 충돌한다 ✅🔬
**비용 3시간 · 24곳 이상**

화면은 옳다. **문서만 틀렸다.** 데이터 재빌드 후 갱신 누락이다.

| 계열 | 문서 표기 | 실제 화면·데이터 | 위치 |
|---|---|---|---|
| 여름 지연 범위 | 39~42일 | **39~44일** (강릉 39 ~ 제주 44) | 교사_학습지:91 · 데모_대본:100 · 발표_10분:60 |
| 서울 과거 지연 | 「과거(1969–73)에도 **33일**」 | **41일** | 교사_학습지:93 · 데모_대본:101 · 발표_10분:61 |
| 16지점 평균 변화 | +0.9일 | **−0.8일** (41.4 → 40.6, 부호 반대) | 교사_학습지:93 |
| 겨울 지연 범위 | 2~43일 | **10~42일** | 교사_학습지:96 · 데모_대본:103 |
| 해안/내륙 | 25일 / 7일 | **22일 / 11일** | 교사_학습지:97 · 발표_10분:81 |
| 서울 28℃ 초과일 | 34.8일 | **32.6일** | 데모_대본:19 |
| 게이트 검사 건수 | 4,810 | **4,852** (직접 실행 확인) | README:100·214·243 · AI_활용_기록:93·195 · 데모_대본:61 · 발표_10분:93·108 — **8곳** |
| 커밋 수 | 32 | **41** (`git rev-list --count HEAD`) | README:173 · AI_활용_기록:35·162 |
| 발표자료 장수 | 8장 | **10장** | 발표_10분:120 (제출_체크리스트:23은 10장으로 정확) |
| 96/100 귀속 | `/api/ai-turn` 절 아래 | 실제 측정 대상은 **규칙 기반 fallback(`localAudit`)** | README:182-183 · AI_활용_기록:120 · 발표_10분:94 |

**왜 P0인가.** 8/21~22 천안 라이브 데모에서 발표자가 대본대로 *"과거에도 33일이었고"*라고 말하는 순간, 투사된 화면의 판독 카드에는 **「과거(1969–1973)에도 41일이었습니다」**가 떠 있다(실측 확인). 그것도 대본이 *"여기가 이 미션의 핵심입니다"*라고 표시한 30초 구간이다. 게다가 제출 PDF는 4,852로 **최신값이 맞아 있어**, README와 PDF가 서로 다른 숫자를 말한다.

**조치**
1. 위 표대로 일괄 교체
2. `assert` → `검사(ck 호출)` 표현 정정, AI_활용_기록:93의 7축을 **9축**으로 정렬
3. **96/100은 약화가 아니라 강화로 다시 쓴다** — *"정량 검증한 것은 AI가 죽어도 반드시 동작하는 경로의 품질입니다"*
4. 대본 규칙을 **"숫자를 외우지 않고 화면 값을 읽는다"**로 변경
5. `build_deck.py`가 커밋 수를 git에서 읽어 렌더

---

### P0-2. 게이트의 문서 검사축이 무력하고, 그 "경고 0건"이 '해결' 근거로 적혀 있다 ✅
**비용 4시간 · `processing/verify_solar_terms.py:262-281` · `scripts/submit_check.sh`**

팀의 최대 차별점이 "스스로 반증하는 게이트"인데, **P0-1의 모든 오류를 통과시킨 원인이 이 게이트다.**

- `:269` 검사 대상은 `교사_학습지.html`·`데모_대본.md` **둘뿐** — `발표_10분_구성안.md`·`README.md`·`AI_활용_기록.md`는 검사 밖
- `:271` stale 리터럴은 `("과거 33일","현재 82일","+25일","25일 늦","2022–26"…)` **7개 화이트리스트**. 문서 실제 표기는 「과거(1969–73)에도 33일이었고」라 문자열이 어긋나고, `39~42일`·`+0.9일`·`2~43일`·`평균 25일`은 목록에 없다 → **실행 결과 경고 0**
- 게이트는 같은 실행에서 `계절 지연(현재·16지점): 여름 39~44일 · 겨울 10~42일`을 **직접 출력하면서도** 그 값과 문서를 대조하지 않는다
- `레드팀_해결확인.md:80·81`이 이 "경고 0"을 **'해결' 근거 칼럼에 기재** — 방법론 심사에서 가장 아픈 종류의 발견이다
- `submit_check.sh` §3은 `prompt_sessions/README.md`만 대조해 P0-3을 통과시키고, §5 zip 검사 bad 조건에 `.claude/`가 없다

**조치**
1. **F축을 블랙리스트 → 화이트리스트 대조로 반전.** 데이터에서 값을 뽑아 문서에 그 값이 실제로 등장하는지 검사. **불일치는 warn이 아니라 fail**
2. 검사 대상에 `발표_10분_구성안.md`·`README.md`·`AI_활용_기록.md` 추가
3. 게이트 출력 검사 건수를 파싱해 문서와 대조하는 assert 추가
4. `submit_check.sh`: §5 bad에 `.claude/` 추가 + `git rm --cached .claude/launch.json` / §6 숫자 대조 / 세션 파일 간 공통 턴 비율 30% 초과 fail + 이메일 정규식
5. `레드팀_해결확인.md:42·80·81`의 '해결' 근거 칼럼 정정

> **순서 주의**: P0-1을 먼저 하고 P0-2를 빼면 다음 편집에서 다시 썩는다. 반드시 함께 닫을 것.

---

### P0-3. 제출물 ④ `prompt_sessions/` 정합성 ✅
**비용 2시간**

1. **목차 불일치 7/7 + 미완 TODO.** `AI_활용_기록.md:175-181`이 열거한 세션 파일 7개(`01_데이터파이프라인.md` 등)와 실물(`01_기획과_초기구현.md` 외 4개)이 **일치 0건**. 같은 문단 `:184`에 `> **제출 전 확인**: … API 키·개인정보가 없는지 검색해 확인할 것.`이 그대로 남아 있다. zip을 푼 심사위원이 가장 먼저·가장 싸게 하는 검증이 목차 대조다.
2. **제출 목적과 무관한 구간.** `01_기획과_초기구현.md:1767`·`1820-1868`에 제출 목적과 무관하고 **제3자를 식별할 수 있는 서술**이 있다. 이 제출물은 사무국이 보관·회람하므로 내보내기 단계에서 제외한다. 또한 세션 02~04는 모두 "본선 진출이 확정되어"로 시작해 **같은 제출물 안에서 모순**된다.
   <sup>(2026-07-29 최종 감사에서 이 항목의 서술을 중립화했다 — 세션 로그에서 지운 내용을 감사 보고서가 그대로 되풀어 설명하면 redaction이 무의미해진다. 결함의 성격과 조치는 그대로 남긴다.)</sup>
3. **97% 중복.** `03_실측전환과_AI안정화.md`가 `04`의 거의 완전한 중복본(공유 턴 67/69). `prompt_sessions/README.md:15`의 "사람 지시 61건 · AI 답변 814건"은 이중 계상값 — 고유값은 **56 / 752**.
4. 이메일 평문 2건 (`01:326`·`:378`, 제출자 본인 계정).

**조치** 목차를 실제 4개 파일명으로 교체 · TODO 삭제 · 1820-1868행을 "경쟁사 분석 구간 — 제출 목적과 무관하여 제외" 한 줄로 대체 · `export_sessions.py`에 턴 해시 중복 제거(약 8줄) + SECRET 정규식에 이메일 추가 후 재생성 · README 합계 갱신

---

### P0-4. 미션 5 겨울 결론이 표본 하나에 무너지고, 저장소가 같은 지점을 정반대로 분류한다 🔬✅
**비용 40분(문구) + 3시간(분류 일원화·게이트)**

`verify.js:279` 화면 문장:
> 「바다는 천천히 식기 때문에 **해안(평균 22일)은 내륙(평균 11일)보다 가장 추운 날이 훨씬 늦게 찾아옵니다**」

`verify.js:306` 자가진단 **정답 해설**:
> 「바다는 천천히 식으므로 **해안일수록 가장 추운 날이 늦게** 옵니다 — 그래서 지역마다 다릅니다.」

**🔬 독립 재계산 (원시 CSV에서 `build_solar_terms.seasonal_lag` 재실행, leave-one-year-out)**

| 표본 | 해안 | 내륙 | 격차 |
|---|---|---|---|
| 전체 5년 | 22 | 11 | **11** |
| 2021 제외 | 30 | 7 | 23 |
| 2022 제외 | 23 | 15 | 8 |
| **2023 제외** | 18 | 16 | **2** |
| 2024 제외 | 20 | 12 | 8 |
| 2025 제외 | 8 | 2 | 6 |

**5년 중 한 해만 빠지면 격차가 11일 → 2일로 사라진다.** 대조적으로 같은 검사에서 **여름 지연은 38~46일로 견고**하다(전체 39~44). 즉 여름 결론은 지켜야 할 강점이고, 무너지는 것은 겨울 서사뿐이다.

**✅ 지점별 실측** (배포 데이터 `present.coldLag`)
```
'해안' 7곳: 제주 42 · 강릉 35 · 전남 24 · 부산 15 · 충남 15 · 인천 11 · 경북 10
'내륙' 9곳: 광주 13 · 경남 13 · 전북 12 · 서울 11 · 대구 11 · 대전 11 · 강원 11 · 경기 10 · 충북 10
```
해안 평균 22일은 **제주·강릉·전남 3곳이 만든 값**이고, 인천(11)·포항(10)은 내륙 분포(10~13) 안에 완전히 들어간다. 미션 5는 지역 칩이 열려 있어(`lockCity:false`) 학습자가 **인천을 고르면 "11일 늦음"과 "해안 평균 22일 — 바다가 천천히 식기 때문" 이 같은 화면에 동시에 뜬다**(실측 확인).

**✅ 저장소 내 분류 모순**
```
verify.js:362              COASTAL = { 제주, 전남, 부산, 강릉, 인천, 충남, 경북 }   ← 경북=해안
verify_solar_terms.py:216  inland  = { 서울, 대구, 대전, 경기, 충북, 전북, 경북, 강원, 광주 }  ← 경북=내륙
verify_solar_terms.py:234  coast_lag = [제주, 강릉, 전남]                          ← 값이 가장 큰 3곳만
```
같은 지점(포항)이 정반대로 분류돼 있고, H2축 assert는 **화면이 주장하는 명제(7곳 22일 vs 9곳 11일)가 아니라 훨씬 쉬운 명제(3곳 33.7일 vs 9곳)를 검사한다.** 그래서 통과한다.

> H2축 자체는 여전히 드문 아이디어다("화면의 물리 설명문이 데이터로 무너지면 빌드가 깨진다"). 다만 **현재는 화면과 다른 명제를 검사하고 있다**는 사실을 알고 발표해야 한다.

**조치**
1. 이분법을 폐기하고 데이터가 말하는 것으로 교체:
   > 「제주·강릉·목포처럼 **바다에 크게 노출된 3곳**만 뚜렷하게 늦고(24~42일), 인천·포항 같은 해안 지점은 내륙(10~13일)과 구별되지 않습니다 — **해안/내륙 이분법으로는 설명되지 않는다는 것 자체가 이 자료의 발견입니다.**」
2. 자가진단 정답 해설에서 '해안일수록' 인과 단정을 제거 → "지점마다 다르고, 그 이유를 이 자료만으로는 확정할 수 없다"
3. 분류를 데이터 파일의 `stationType` 한 곳으로 일원화
4. 게이트에 **leave-one-year-out 안정성 assert** 추가 — `coldLag` 격차가 잭나이프에서 임계치 넘게 흔들리면 인과 문구를 못 쓰게 차단

---

### P0-5. 상태 초기화 3중 누락 — 미션 5 정답 선공개 · 표/지도 보기 잔존 ✅
**비용 40분 · `verify.js:1725`**

`startMission()`의 lagMode 분기가 `markDoy`만 비우고 `lagRevealed`·`predicts.lag`를 남긴다. 진행바 ✓ 칩으로 미션 5를 다시 열면 화면에 **「내 예상 아직 안 찍음」과 「실제 가장 더운 날 7월 31일 · 하지보다 40일 늦음」이 동시에** 뜬다.

같은 함수가 `state.view`도 초기화하지 않아, 미션 3 안내문(*"'표'와 '지도' 보기로 확인해 보세요"*)을 따른 학습자는 미션 5를 **그래프 없이** 열고 유일한 조작(SVG 가로 드래그)을 잃는다.

**조치** — `season()`(`:1360`)이 이미 정확히 이 4줄을 수행한다. 그대로 복사하면 된다.
```js
state.lagRevealed = false;
delete state.predicts['lag']; delete state.selfChecks['lag'];
state.view = 'chart';
// load() 정규화: o.lagRevealed = !!o.lagRevealed && o.markDoy != null;
// 보강: setView()에 if (mode !== 'chart' && isLagMode()) mode = 'chart';
```

> 심사위원 핸즈온에서 화면이 자기모순을 띄우는 유일한 항목이다. **리허설 전에 반드시.**

---

## 4. P1 — 명확한 감점

### P1-1. 「자료에 최저·최고기온이 없다」가 사실과 다르다 ✅
**차선 20분 / 최선 6~8시간 · `data_collectors/collect_kma_allyears.py:29`**

```
KEEP = ["tm", "avgTa", "avgRhm", "sumRn"]
```

같은 저장소의 원시 응답 파일 헤더:
```
data_collectors/output/kma_asos_daily_서울_1969_1973.csv
stnId,stnNm,date,avgTa,minTa,minTaHrmt,maxTa,maxTaHrmt,...
108,서울,1969-01-01,-10.4,-12.6,2400,-6.8,1432,...
```
**1969년부터 `minTa`·`maxTa`가 실제로 채워져 있다.** 수집기가 한 줄로 버려서 가공본(`allyears/*.csv`)에 `date,avgTa,avgRhm,sumRn`만 남았다.

반면 화면·문서:
- `verify.js:858` 「**자료에** 하루 평균값만 있어 최저·최고기온이 없습니다」
- `README.md:238` 「**자료에** 일평균만 있으므로 열대야(최저)·폭염(최고)은…」
- `KMA_REF` 표(`:923-926`) 폭염주의보·경보·열대야·한파주의보 4행 전부 '계산 불가'

주최기관이 기상청이고 **폭염일수·열대야일수는 기상청의 대표 기후변화 지표**다. 「기상청은 폭염을 일최고 33℃로 정의합니다. 여기서 폭염일수를 볼 수 있습니까」에 "자료에 없습니다"로 답하면 사실과 다른 답이 된다. 자체 레드팀이 RC-M으로 '해결'했다고 적었으나 **문구만 바꿨고 원인(수집 시 컬럼 폐기)은 그대로다.**

**조치**
- **최선:** `KEEP`에 `minTa`·`maxTa` 추가 → 재수집 → 폭염일(일최고 33℃↑)·열대야(일최저 25℃↑)를 `exceedDays`와 같은 방식으로 사전계산하고 프리셋을 실제 동작시킨다. **이것 하나로 ①의 감점이 강점으로 뒤집힌다.**
- **차선:** 「ASOS 원자료에는 최저·최고기온이 있지만, **이 버전은** 일평균 3개 지표만 가공했습니다. 폭염·열대야는 다음 버전에 추가합니다」로 정정 — 최소한 거짓 진술을 없앤다.

---

### P1-2. 고향 기후 카드가 '최근 5년'이라 인쇄하며 3년만 평균한다 ✅🔬
**비용 40분 · `verify.js:1989`**

```js
var tBirth = meanAround(temps, bi, 2), tNow = meanAround(temps, last, 2);
// meanAround는 idx 중심 ±half → idx = last 이므로 상한이 잘려 last-2..last = 3년
```
라벨(`:2002`)은 `'최근 5년(' + ys[last-4] + '–' + ys[last] + ') 평균은'` → 화면 「최근 5년(2021–2025) 평균은」

**🔬 배포 데이터로 재계산**

| 지역 | 카드 표시 | 실제 2021–2025 평균 | 오차 |
|---|---|---|---|
| 서울 | 14.40℃ | 14.06℃ | +0.34 |
| 부산 | 16.27 | 16.00 | +0.27 |
| 강원 | 12.73 | 12.50 | +0.23 |
| 제주 | 17.80 | 17.60 | +0.20 |

**2008년생 서울 카드: 표시 ΔT +1.5℃ / 올바른 값 +1.2℃ — 25% 과대.** 16지점 전부 온난화 방향으로 과대다.

이 PNG는 **앱의 유일한 대외 배포 산출물**이고 하단에 「기상청 ASOS 실측 · 5년 평균 비교」와 배포 URL을 함께 새긴다. 게다가 `레드팀_해결확인.md:42`의 '831개 조합 전수: 음수·0 = 0건' 검증이 **값을 따뜻한 쪽으로 미는 이 버그 덕에 통과했다.**

**조치** 끝단에는 중심평균을 쓰지 말고 후행 5년 평균(`meanTrailing(temps, last, 5)`). 출생연도 상한을 `ys.length-8`(=2018)로 내려 라벨 구간과 실제로 겹치지 않게 한다 — 현재 `ys.length-6`(=2020)은 2018–2022로 **2년 겹치는데 UI는 "겹치지 않는 해만 비교할 수 있어요"라고 단언한다.** 831조합 재검증 기준을 '음수 0건'에서 **'라벨 연도 구간 = 실제 평균 구간'**으로 바꿔 게이트에 박는다.

---

### P1-3. 미션 5 겨울 모드에서 화면 지시문 3곳이 여름 문구를 유지한다 ✅
**비용 30분 · `verify.js:1717`(HEADLINES) · `:233-235`(task/brief)**

겨울·동지 기준을 선택하면 차트는 동지 12/22, 예측 문항은 「밤이 가장 긴 날…」, 판독 카드는 「가장 추운 날」, 판정문도 「가장 추운 날」인데:

| 위치 | 표시 |
|---|---|
| H1 (가장 큰 글자) | 「가장 **더운** 날은 왜 **하지**가 아닐까?」 |
| 지금 할 일 | 「그래프를 좌우로 끌어 가장 **더울** 것 같은 날을 찍어 보세요」 |
| 이 미션은 무엇을 확인하나요 | 「낮이 가장 긴 날은 **하지(6/21)**입니다… 우리가 가장 **더위**를 느끼는 날도…」 |

팀은 이 결함 유형을 예측 문항에서 이미 고쳤다 — `verify.js:236` 주석: *"문항이 화면과 어긋나면 '예측 봉인'이라는 장치 자체가 무너진다."* 같은 논리가 **가장 큰 글자에는 적용되지 않았다.** `renderLiveNums`의 잠금 문구는 계절로 분기하므로 앱이 자기 안에서 불일치한다.

**조치** `HEADLINES.lag`·`task`·`brief`를 `predict`/`selfCheck`처럼 함수로 바꿔 `state.lagSeason`으로 분기. `season()`에서 `setStage` 재렌더 또는 해당 노드만 갱신.

---

### P1-4. 미션 5 차트 라벨 겹침 + 절기 6개 라벨 잘림 ✅
**비용 1시간 · `verify.js:582`(term line) · `:646`(lagOverlay)**

**겹침** — 미션 5는 `state.ti`를 하지(11)/동지(23)로 맞추므로 `drawHero`의 절기 라벨과 `lagOverlay`의 절기 라벨이 **같은 x에서 1px 간격으로 포개진다**(실측):
```
"하지(夏至) 6/21 · 낮이 가장 긴 날"   y=31, x=360.1  (vb 360→533)
"하지 6월 21일"                      y=32, x=361.1  (vb 361→438)
"낮이 가장 긴 날"                     y=47, x=361.1
```
뜻도 중복이고(「낮이 가장 긴 날」 2회), 겹친 구간은 판독 불가다. 세로선도 두 개가 겹쳐 그려진다(둘 다 `var(--sun)`, dasharray만 다름).

**잘림** — SVG `overflow:hidden`이라 viewBox(720) 우측을 넘는 라벨은 잘린다. 24절기 전수 계측:
```
추분 8px · 상강 23px · 입동 44px · 소설 66px · 대설 91px · 동지 151px 초과
```
**동지는 절반 이상이 잘린다** — 미션 5 겨울이 강제로 선택하는 절기이고, 자유탐구에서는 24개 중 6개가 같은 증상이다.

**조치** ① lagMode에서는 `drawHero`의 절기 라벨을 그리지 않는다(1줄) ② 라벨 x가 `W-R-폭` 을 넘으면 절기선 **왼쪽**에 `text-anchor="end"`로 그린다 ③ 겹친 세로선 하나 제거

---

### P1-5. 26창 '비교 기간' 슬라이더가 손으로 끌리지 않는다 ✅
**비용 1.5시간 · `verify.js:1118-1130`**

`updateWindow()`가 `input` 1회마다 `el.innerHTML = windowPanel()`로 **`#winRange` 노드를 통째로 교체**한다(실측: `oldNodeStillInDom: false`, `nodeIdentityChanged: true`). 네이티브 range의 암묵적 포인터 캡처가 끊겨 **드래그가 한 칸에서 종료된다.** 값은 바뀌면서 손을 뿌리치는 형태라 '무반응'보다 완성도가 낮아 보인다. (−/+ 버튼과 키보드는 정상.)

`데모_대본.md:114`가 *"비교 기간 슬라이더를 1996–2000 → 2021–2025로 훑는다"*를, `발표_10분_구성안.md:71-72·104`가 **Q&A 답변 동작**으로 이 조작을 지정한다. 즉 "5년은 표본이 부족하지 않나"라는 가장 확실한 질문의 답이 무대에서 끊긴다.

**조치** `updateWindow()`를 (a) 최초 1회 마크업+리스너 (b) 이후 `#winOut`·`.win-read`·`.win-verdict`·`windowSVG()`만 교체로 분리. **`#winRange`는 절대 재생성 금지**(값만 동기화), `:1127`의 `focus()` 제거. `:614` `drawHero` 경로도 (b)만 타게 한다.

---

### P1-6. 규칙 기반 감사관이 「~아님?/아니냐」 한 단어에 무력화된다 ✅🔬
**비용 1시간 · `verify.js:2180`**

`neg` 어휘에 `아니`·`아님`이 있어 `hasLimitation`(`:2181`)·`climateLimit`(`:2182`)이 **한국어 수사의문문을 '한계 진술'로 오인**한다. 이 두 불리언이 `scope`·`causal`·`one_year` 경고를 전부 게이트한다.

**🔬 배포 코드(`localAudit`)를 추출해 직접 실행**
```
"…이건 전국이 다 더워졌다는 뜻 아니냐"  →  ready / 경고 0건  ❌
"…이건 전국이 다 더워졌다는 뜻이다"      →  revise / scope    ✓
"5년인데 기후가 바뀐 거 아님?"          →  climateLimit=true → one_year 억제  ❌
```
동일한 주장인데 **어미만 바꾸면 「근거 충분」으로 통과한다.** `eval_set.json` 실패 4건 중 3건(C017·C023·C026)이 정확히 이 원인이다.

부수 확인: `아닌가요`·`아닌가`는 음절이 달라(`아닌` ≠ `아니`) 정상 검출된다. 즉 **`아니냐`·`아님` 두 형태만의 문제**라 수정 범위가 좁다.

**왜 중요한가.** 교실 40명이 IP당 10분 18회 제한(`api/ai-turn.js:16`)에 걸리면 대부분이 이 규칙 경로를 본다. 그리고 심사위원이 무대에서 「그럼 기후변화 아님?」을 입력하면 앱이 「근거 충분 ✓」로 답한다.

**조치** `neg`에서 `아니`·`아님`을 제거하고 진짜 서술형만 남긴다(`아니다|아니라|아니며|아니고|아닙니다|아니어서`). 더 나은 안: 문장이 `?`·`냐`·`나요`로 끝나면 **수사의문문으로 보고 단정문과 동일하게 취급**한다(2~3줄).

---

### P1-7. 예측 봉인이 방법론 서랍과 접근성 경로에서 샌다 ✅
**비용 1.5시간 · `verify.js:1267-1272` · `:598` · `:602`**

`updateTable`·`renderLiveNums`·`renderReadouts`·`updateMap`에는 잠금 분기가 있으나 **`updateMethod`에만 없다.** 봉인 전 서랍을 열면(실측):
- 「같은 25°C 기준으로 5년 창을 옮겨 보면 시차가 +4~+17.2일… **지금 화면의 값은 +13.2일입니다**」 ← 미션 1의 답
- `monthTable` 12행: 「7월 과거 기준초과 15일 / 현재 31일」「8월 18일 / 31일」 ← 미션 2~5의 봉인 문항이 묻는 방향·격차

또한 `:598` `aria-label = chartAlt()`와 `:602` `aria-valuetext`에 잠금 분기가 없어 **스크린리더 사용자는 봉인을 경험하지 못한다** — 접근성을 전면에 내세우는 팀에게 체험 동등성 결손이다.

> **반증된 부분(불필요한 작업)**: 서랍은 `open` 속성이 없어 **기본 닫힘**이다. "서랍이 열려 있다"는 전제의 시나리오는 재현되지 않는다. 인트로 카운터의 31→68일도 미션 1의 봉인 문항이 **개념 문항**이라 답을 유출하지 않는다.

**조치** `updateMethod()`에 동일 잠금 분기 — 잠금 중에는 자료·지점·표본·계산방법 절만 남기고 민감도·monthTable은 「예측 봉인 후 공개」로 대체. `chartAlt()`·`aria-valuetext`에도 분기.

---

### P1-8. 미션 3 결론이 창·임계값 선택의 인공물이고, '기준을 밝혀라'를 가르치는 미션에 기준이 없다 ✅🔬
**비용 1.5시간 · `verify.js:156`·`:160`·`:410-414`**

**🔬 배포 데이터 재계산**

| | 화면(5년·25℃) | 26창 중 위치 | 30년(1996–2025) | 27℃ |
|---|---|---|---|---|
| 제주 | +26.4일 | **최댓값** (min −4.6) | +6.5 | +19 |
| 강원 | +16.4일 | **최댓값** (min +2.8) | +7.7 | +21 |

두 지점 모두 **26개 창 중 최댓값**이 화면에 나오고, 30년으로 넓히면 **제주 < 강원으로 순서가 뒤집힌다.** 임계값 27℃에서도 뒤집히고(19 vs 21), 26℃에서는 격차가 15일로 벌어진다. 미션이 가르치는 「9일 차이」는 임계값 두 칸 이동으로 사라지거나 반전된다.

그런데 미션 3의 한계절(`:160`)에는 `n.sensText`가 **붙지 않는다**(미션 1·2·4는 붙는다). 그리고 `sensitivityText`(`:412`)는
```js
if (!s || state.metric !== 'temp' || state.thr !== s.thr) return '';
```
**기준이 정확히 25℃일 때만** 나온다 — 미션 1·2가 *"기준선을 여러 높이로 옮겨"*라고 시키는 바로 그 행동을 하는 순간 유일한 강건성 고지가 사라진다(27℃ 판정문에서 실측 확인).

추가로 미션 3의 근거절(`:156`)에만 **기준이 없다** — 미션 1·2·4는 모두 「'덥다 25°C' 기준으로」를 명시한다. 판정 화면에는 슬라이더가 없어 **화면 어디에도 기준이 남지 않는다.** `hasCriterion`(`:2192`)이 '더위' 단독 매칭을 통과시켜 **앱의 규칙 점검이 자기 모범문장의 결함을 못 잡는다.**

**조치**
1. `:156` 앞에 `'‘' + n.thr + '°C 이상’ 기준으로 '` 삽입 (1줄)
2. 한계절에 실측 고지: 「이 격차는 고른 기준에 따라 달라집니다 — 25°C에서 9일, 27°C에서 2일. 두 값 모두 26개 5년 창 중 가장 큰 값이고, 30년으로 넓히면 제주 +6일 · 강원 +8일로 순서가 뒤집힙니다. **지역 차이의 방향은 남지만 크기는 고른 5년에 달려 있습니다.**」
3. `sensitivityText`의 thr 게이트를 풀어 `windows.list/long`에서 현재 thr 값을 직접 읽게 한다(데이터에 20~34 전 임계값이 이미 있다)
4. `hasCriterion`에서 '더위/여름/비' 단독 매칭 제거 → **숫자+단위 요구**

> **경고**: 미션 3 근거절에서 제주·강원 중 한쪽 값을 빼서는 안 된다. 주장이 '지역마다 다르다'인데 근거에서 한쪽을 빼면 주장이 근거 없이 서게 되어 CERL 일관성이 오히려 깨진다. **게이트와 고지만 강화하라.**

---

### P1-9. 전지구 그래프가 불완결 2026년(1~5월)을 '연평균'으로 쓴다 ✅
**비용 1시간 · `processing/build_web_data.py:153` · `verify.js:2119-2120`**

```
noaa_gml_co2_monthly_FULL.csv 마지막 3행 → 2026-03, 2026-04, 2026-05  (5개월뿐)
web_data/climate_change.json 마지막 → {"year":2026, "co2_ppm":430.32, "temp_anomaly_C":1.396}
                                       (2024=1.533, 2025=1.414 보다 낮다)
```
Keeling 곡선은 5월이 연중 최대이므로 CO₂에 **상향 편의가 구조적으로** 들어간다. 그리고 화면 문장(`:2119-2120`)은 「…전지구 평균기온 이상은 … **함께** 올랐습니다」인데 **그래프의 기온선은 마지막 두 점에서 내려간다.**

같은 팀이 한국 자료에는 `MIN_DAYS = 350` 게이트를 걸고 `README.md:232`에 「**불완결 연도를 섞지 않는다**」를 원칙으로 내세웠다. `verify_solar_terms.py`는 `web_data/*.json`을 **한 건도 검사하지 않는다.**

> `relationship.r2 = 0.934`는 독립 재계산으로 검증됨(r=0.966, n=69). 값 자체는 정확하다.

**조치** `co2_y`·`temp_y`·`sea_y`에 완결성 게이트 추가(월 12개 미만 제외) → 재빌드하면 1958–2025가 되고 마지막 기온값 1.41℃로 단조 상승 서사와도 맞는다. 게이트에 `climate_change.json` 검사 5줄 추가.

---

### P1-10. 지표·기준 하드코딩 4건 — 앱이 자기 규칙을 어긴다 ✅
**비용 3시간**

같은 근본 원인: **`state.metric`을 보지 않고 기온 25℃를 가정한 문자열.**

| 건 | 현상 | 위치 |
|---|---|---|
| 예시 문장 | 미션 4(강수)의 AI 감사 예시가 **「서울에서 '덥다'를 1°C로 정하면 기준 이상 **더위일**이 연평균 과거 89.2일 → 현재 82.8일」**. `placeholder`로 **항상 노출** + '예시 문장 넣기' 버튼이 값으로 삽입. 미션 5(계절지연)도 더위일 문장 | `verify.js:1917-1923` — 지표 분기가 `m.id==='region'`에만 있다 |
| 방법론 서랍 | 가드 없이 「지금 화면의 값은 +13.2일입니다」를 무조건 출력. 미션 1의 판정 게이트가 **기준 변경을 강제**하므로 28℃(실제 +21일)에서 서랍을 여는 학습자는 **거의 항상 거짓 문장**을 본다. 강수 미션에는 시차 지표 자체가 없는데도 같은 문장 | `verify.js:857` |
| 스파크라인 | 강수 미션 판정 아래에 **기온** 장기 흐름 + "당신이 비교한 5년" 캡션 | `verify.js:1842` `sparkBlock(state.city,'temp')` → `state.metric`으로 |
| 반올림 불일치 | 방법 서랍은 **+13.2일**, 판독 카드·판정문·고향카드·교사학습지는 **+13일**. `build_solar_terms.py:194`는 반올림 전 평균, `:158`은 연도평균을 먼저 정수화 | `verify.js:857` vs `:718` |

`mc.verb`/`mc.unit`/`dayLabel()` 세 헬퍼가 `:36`·`:380`에 **이미 존재하는데 쓰이지 않는다.**

---

### P1-11. 대본에 적힌 조작이 실행되지 않는다 ✅
**비용 2.5시간**

- **고향 칩 (30분)** — 대본 [10–25초]가 지정한 지역 칩 조작이 미션 1에서 **DOM 자체가 없다**(`lockCity:true` → `heroShell({cityChips:false})`). '심사위원이 직접 만지는' 유일한 순간이 실행 불가. → **지역 칩 시연을 미션 2로 이동**(같은 대본 블록에 프리셋 25→28℃ 전환이 이미 적혀 있다).
- **고정 안내문 (2h)** — `:1798` *"지역·절기를 바꿔 보고, '표'와 '지도' 보기로…"*가 **5개 미션 전부**에 붙는다. 그런데 5미션 전부 `lockTerm:true`라 절기 스트립은 한 번도 렌더되지 않고, 미션 1은 지역 칩도 없고, 미션 5는 지도 버튼까지 숨는다(`:1288`). 지시를 따르려는 심사위원에게 **'미완성' 신호**로 읽힌다. → 미션별 조건부 생성 + 최소 1개 미션에 절기 스트립 개방.
- **네트워크 사고 대응표 (30분)** — `loadGlobal`(`:2036`)은 지구 화면 **진입 시** fetch하므로 회선이 끊기면 에러 배너가 뜬다. `발표_10분_구성안.md:138`의 「네트워크 끊김 → 이미 로드된 페이지는 계속 동작. 그대로 시연」은 **이 화면에서 거짓**이고, 데모 [75–85초]가 정확히 그 구간이다. → 두 JSON(합 31KB)을 페이지 로드 직후 프리페치.

---

### P1-12. 24절기 궤도 화면 — 물리 180° 반전 + 모바일 판독 불가 ⚠️✅
**비용 3.5시간 · `verify.js:1596`(pos) · `:1611-1618`**

`termLongitude(i)=((i*15)+285)%360`은 **태양의 황경**이다(하지 90° · 동지 270° · 춘분 0°로 데이터 확인). `pos()`는 이 값을 **지구의 궤도각으로 그대로** 쓴다. 지구의 일심경도는 태양 황경 **+180°**이므로 **지구 위치가 정확히 반대편에 그려진다** — 하지에 북극이 태양 반대쪽, 동지에 태양 쪽이다.

같은 패널 본문(`:1659-1660`)은 「북반구가 태양 쪽으로 기울었다가(**여름**) 반대로 기울었다가(**겨울**)」라고 쓴다 — **그림이 문장의 정반대다.** `교사_학습지.html:51`이 이 화면을 '오개념 교정' 사전 활동으로 지정한다.

부수: 태양을 타원 **중심**에 놓았기 때문에 근일점·원일점 라벨이 무의미하다(둘 다 태양까지 같은 거리). 모바일 375px에서 절기 이름 **5.96px** · 비선택 점 지름 **7.05px** (WCAG 2.5.8 24×24 명백 미달). 팀은 지도에서 같은 문제를 `@media(max-width:620px)`로 풀었고 `small{font-size:max(11.5px,.86em)}` 하한 정책까지 세웠는데 **궤도만 그 패스에서 빠졌다.**

**조치** `pos()` 각도를 `(termLongitude(i) + 180 - 90)`으로(절기 점·라벨이 함께 회전하므로 링 내부 정합 유지) · 타원을 `rx≈ry`로 원에 가깝게 + 캡션 「거리 차이는 3.4%뿐이라 원으로 그렸습니다」 · 라벨·점을 클래스화(`.orb-name`/`.orb-dot`)해 `@media` 보정 · `pick()`이 innerHTML을 갈아 포커스가 body로 날아가므로 재포커스.

---

### P1-13. 학습 효과 측정이 성립하지 않는다 ⚠️
**비용 5시간 + 파일럿 4시간**

`개발계획서(고도화).md:509`가 검증 지표로 「개념 문항 **사전/사후 정답률**과 오답 유형」을 명시했으므로, 이것이 성립하지 않으면 ③ 10점의 절반이 근거를 잃는다.

1. **사전=사후 동일 문항 + 정답이 같은 화면에 축자 인쇄 (3h).** `renderPost`(`:1864`)가 `PRE_QUESTION`을 그대로 재사용. 사후 문항이 열리는 위치는 미션 1 **판정 화면 내부**이고, 같은 페이지 위쪽 CERL이 「절기 날짜는 그대로인데, 관측된 더위가 13일 늦게까지 이어진 것입니다」를 출력 — 정답 보기 b와 사실상 동일 문장. **사전 문항도 오염**: 화면 하단 `.integrity`(`:1307`)와 `index.html:64`가 「절기는 태양 위치로 정한 천문 날짜라 해마다 거의 움직이지 않습니다」를 상시 표시한다. 보기 길이도 b 54자 / a 22자 / c 6자로 **최장 보기가 정답**인데 `:1875`는 「**생각이 자랐어요**」로 성장을 단정한다.
   → 사후를 **동형 문항**으로 교체(「'입동이 지났는데 안 춥다'를 가장 정확히 설명한 것은?」) · 보기 3개를 40±8자로 · 사후 응답 전에는 CERL을 `details`로 접는다
2. **자가진단 4/5의 정답이 바로 위 CERL에 있다 (2h).** `renderVerdict`(`:1841-1845`)가 CERL → sparkBlock → selfcheck를 같은 section에 순서대로 렌더. 미션 2·3·4·5에서 확인(미션 1은 예외 — 다른 축을 묻는다).
   → **최소 비용 안: 자가진단을 CERL 앞에 배치하고 응답 후 판정문 공개.** 예측 봉인과 동일 논리를 판정 단계에 적용하는 것이므로 설계 일관성도 좋아진다.
3. **완료 화면 '내 기록'에 예측·사전/사후가 한 줄도 안 남는다 (1h).** `renderComplete`가 모으는 것은 `selfChecks`·`missionDraft`뿐 — `state.pre`·`post`·`predicts`·`markDoy`는 **state에 다 있는데 렌더만 없다.**
   → 「처음 생각: (a) → 검증 뒤: (b)」 1줄, 미션별 「내 예측 ○○ → 실제 ○○」 1줄, 미션 5 「내가 찍은 날 7/15 → 실제 7/31」. 복사·인쇄가 같은 DOM을 쓰므로 추가 작업 없음
4. **학생 파일럿 0회 (4h).** 저장소 어디에도 기록이 없다. → 8/21 전 중·고생 5~10명 12분 동선 1회. 「학습 효과를 어떻게 측정했나」의 **유일한 실증 답변**이 된다.

---

### P1-14. README가 권하는 검증 명령이 zip에서 죽는다 ✅
**비용 1시간**

`제출_체크리스트.md:40` 지시대로 `git archive --format=zip HEAD`로 만든 zip을 풀고 `python processing/verify_solar_terms.py`를 실행하면 **안내 없는 `FileNotFoundError: data_collectors/output/allyears/kma_allyears_서울.csv`.** 원인은 `:28`이 `.gitignore`에 걸린 디렉터리를 참조하기 때문. `README.md:242-243`은 전제 조건 없이 "검증: python …"만 적는다. (같은 zip에서 `node eval_harness.js`는 정상 — 96/100 재현 확인.)

이 게이트는 README §7 '과학적 무결성 6가지 약속'의 **유일한 실행 증거**다.

**조치** 부재 가드 + `--no-raw` 플래그 + README에 전제 명시. **더 나은 안: 서울 1지점 축약 CSV를 git 추적해 D축 일부가 zip에서도 실제로 돌게 한다** — "심사위원이 직접 돌려보게 하는 것"이 이 팀의 최대 무기이므로 여기에 1시간 쓰는 것은 값싸다.

### P1-15. 지구 맥락 패널에 NOAA·OWID 출처가 한 글자도 없다 ✅
**비용 30분 · `verify.js:2102-2128`**

OWID는 **CC BY 4.0**이라 저작자표시가 의무이고, README §7 약속 6은 「**모든 화면에** 출처·표본 수·계산 방법·한계」다. 이 패널이 그 반례다. → `renderGlobal()` 말미에 `web_data/*.json`의 `meta.sources`를 읽어 출처 문단 렌더(하드코딩하지 말 것 — 데이터 교체에 자동 추종).

---

## 5. P2 이하 — 시간이 남으면

| # | 내용 | 비용 |
|---|---|---|
| 1 | 절기 대표날짜 2개가 자기 산출규칙 위반 🔬 — 앱 선언은 「1969~2026 **최빈** 날짜」인데 **독립 태양황경 전수계산 결과: 입하 5/5(31회) > 5/6(27회) · 입추 8/7(31회) > 8/8(27회)**. 앱은 5/6·8/8. 처서 8/23(51회)·상강 10/23(37회)은 정확. **2026 입하 5/5 20:35 · 입추 8/7 20:38 KST**(자정에서 3.5시간 여유 → 계산 오차로 뒤집히지 않음). `레드팀_실행계획.md:170`이 「입하·입추는 앱 표기가 최빈」이라 **반대 결론을 검증 기록으로 남겼다.** → `build_solar_terms.py:69` `5,6`→`5,5`, `:75` `8,8`→`8,7` + 재빌드 + 24절기 doy assert 추가 | 15분 + 1.5h |
| 2 | 「실제 가장 더운 날 7월 31일(**29.4°C**)」이 **이중 평활된 평년곡선 값**이다(`build_solar_terms.py:122-130` 날짜별 평균 → 15일 원형 이동평균). 같은 파일 `:227` 주석이 이미 「극값일은 개별 연도의 최고기온일이 아니라 평년 곡선의 최고점」이라 알고 있고, `verify.js:853`은 「일수와 날짜는 곡선에서 세지 않습니다」를 원칙으로 내건다. **미션 5만 곡선값에 '실제'를 붙인다.** → 「5년 평년 곡선에서 가장 더운 날(그날의 **일평균기온 평년값** 29.4°C)」 | 20분 |
| 3 | 26창 판정이 **동률 1개를 '결론이 갈린다'로 오판**(`:1089` 엄격 부등호). 서울 26℃: above 25 · **동률 1** · below 0 → Q&A에서 기준을 한 칸 올리면 「26개 창 전부」 대사가 화면에서 반증된다. → below 카운트 3분기 + 대사를 「**과거보다 적은 창은 없습니다**」로 조건화 | 1h |
| 4 | 미션 3·4 판정 게이트가 **미션 과제와 무관한 임계값 이동 1회**만 요구(`canJudge` = `moved && missionAsked`). 미션 3은 칩을 한 번도 안 눌러도, 미션 4는 프리셋 1개만 눌러도 열린다. → `visitedCities`/`usedThr` Set 기록, 미션3 {제주,강원}⊇ · 미션4 {1,50}⊆ 요구(약 20줄) | 1.5h |
| 5 | `↺ 처음부터`가 `location.reload()`만 해서 **URL 해시가 남고** `applyHash`가 phase를 되돌린다. 앱이 배부하는 링크는 항상 `&v=free`이므로 다음 학생이 **자유탐구(사실상 종료 화면)에서 시작**한다. → `location.replace(location.pathname + location.search)` 1줄 | 15분 |
| 6 | 다크 테마 인쇄 시 `@media print`가 색을 강제하는 줄이 `:354` 하나뿐이어서 `.stage-h`(#fff on #fff, **1.00:1**)·부제·역량 칩이 흰 종이에 흰 글자. 실질 제출 내용(`.rec-list`·`.cerl`·`.method-body`)은 정상 출력 → '백지'는 아니다. → print 블록 맨 앞에 `:root, [data-theme="dark"]`로 라이트 팔레트 재선언 | 30분 |
| 7 | `#heroSvg{touch-action:none}`이 **5미션 전부**에서 브라우저 제스처를 끄는데 JS는 기준선 ±22px 밴드에서만 반응 → 그 밖 세로 스와이프는 브라우저도 JS도 무반응. 차트가 375px에서 화면의 31.6% 차지. → `touch-action: pan-y` + 히트테스트 통과 후에만 `preventDefault`. 그립 원 `r` 8→13 | 1h |
| 8 | 과거 곡선이 기준선에 닿지 않는데 마커를 **무조건 `cy=yT`(기준선)에 찍는다**(`:594`). 서울 과거 평년 곡선 최댓값 25.90℃ → 26℃ 이상에서 **곡선 0일 vs 숫자 21.4일**. 기본값 25℃에서도 12.6px 부유. → 곡선 위에 찍고 기준선까지 점선 + '곡선 밖(실측 21일)' 태그 | 1.5h |
| 9 | 미션 5 겨울에서 「11일 늦다」 브래킷이 **플롯 폭의 97.2%**를 가로지른다(동지 356 ↔ 실측 2). '겨울은 340일쯤 늦다'로 오독 가능 → 겨울에는 브래킷 생략, 「12/22 → 1/2, 해를 넘겨 11일 뒤」 라벨 | 1.5h |
| 10 | 미션 5 날짜 조작에 슬라이더 없음. `setMark` 클램프(`:1326`)에 랩어라운드가 없어 **겨울에는 초기 동지 356에서 ＋를 눌러도 365에서 멈추고, 정답(doy 1~2)까지 −를 354회** 눌러야 한다(키보드 한정). 게이트는 아무 `markDoy`든 통과하므로 진행은 막히지 않는다. → 클램프 랩어라운드 + 프리셋 칩 3개 | 1h |
| 11 | 봉인 전 `markNext`/`markPrev`를 누르면 `state.markDoy`는 바뀌는데 `#markOut`은 **「그래프를 좌우로 끌어 날짜를 찍으세요」에 멈춘다**(`renderLiveNums`의 잠금 분기가 조기 반환). 차트에는 마커가 그려지므로 치명적이진 않으나 키보드 경로의 피드백 결손 | 15분 |
| 12 | `polite` 라이브 영역이 한 화면에 **4~5개**(`#liveNums`·`#thrOut`·`#readouts`·`#heatNote`·`#markOut`), `drawHero` 말미가 값 1칸 변화마다 전부 갱신 → 중복 낭독(장황함). 사용 불가는 아니다 → `aria-valuetext` 한 채널로 일원화 | 1h |
| 13 | 미참조 잔재 — **`mission.js`(18.4KB)는 참조 0건**, `절기_드리프트_프로토타입.html`(45.7KB)은 `<title>`이 다른 **별개 구버전 앱**인데 `vercel.json` 캐치올 rewrite로 공개 서빙된다. → 두 파일 `git rm`. (`app.js`·`style.css`는 archive.html 전용이므로 유지) | 40분 |
| 14 | 교사 학습지 `[12지구02]`만 항목번호 누락 — 레드팀 권고가 "코드 미확인이니 빈칸으로 두라"였는데 **반대로 미확인 코드를 기입**했다. 헤더 「45~50분(활동 4 포함)」인데 본문은 활동 1~5, 태그 합계 **61분** → NCIC 대조 또는 빈칸 환원 + 헤더를 2코스로 분리 | 1h |
| 15 | 「이건 시뮬레이터가 아니라 데이터 뷰어 아닌가」 준비가 3개 문서 grep **0건**. Mo1 p.28이 **④ 데이터 탐구 대시보드**를 정식 유형으로 등재하고 p.32 구성요소(필터·기간 UI / 동적 차트 / 탐구 질문 패널)를 규정하며 이 앱은 셋을 다 갖췄다 → 슬라이드 1줄 + Q&A 카드 | 1h |
| 16 | 태풍(C-04 ★) 통째 부재 + 계획서 v3 전환 논거가 README·발표자료에 없어 '못 만든 것'으로 읽힌다 → README 각주 + 발표자료 3쪽 한 줄 + 계획서 ★9건 대비 구현 매트릭스 슬라이드 | 40분 |
| 17 | `AI_활용_기록.md:149` 「(외 5건) `레드팀_해결확인.md` 부록 참조」 — 그 파일에 '부록' 0건·'기각' 0건. 실제 기각 기록은 `레드팀_카피·벤치마크보고서.md §8` | 30분 |
| 18 | 관측 결손일 미보정 ⚠️ — 강원 2024 365행+결측 2일 / 2025 364행+결측 2일 등. `exceed_stats`가 결손일을 자동으로 '기준 미달'로 취급. 영향 상한 연평균 −0.6일. → 방법 서랍 한계 목록에 「사용한 10개 연도 중 4개 지점에 1~3일 관측 결손, 보정하지 않음(영향 ≤0.6일)」 한 줄 | 20분 |
| 19 | 공용 기기 이어받기 감지 없음 — **결함이 아니라 미구현 기능**(`↺ 처음부터`가 표준적 답). → `state.savedAt` 1줄 + 20분 경과 시 '이어서 하기 / 새로 시작' 선택 | 1.5h |
| 20 | 계획서가 목표① 증거로 지목한 '개념 렌즈 **분류 게이트**'가 `verify.js` 전문 검색 0건(렌즈 자체는 `goal-chip`으로 구현됨) → 예측 봉인 직후 3문항 분류 미니 게이트 30초 | 2h |

---

## 6. 기각 — 다시 걱정하지 말 것

**감사 중 제기됐으나 검증에서 기각된 항목**

- **명도 대비 문제 없음** ✅ — 라이트·다크 양쪽에서 텍스트 요소 전수 계산, WCAG AA 위반 **0건**. `verify.css:711-721`의 실측 기반 보정이 실제로 작동한다.
- **외부 의존성 0 확인** ✅ — 첫 로드 요청 **6건**, 전부 자기 도메인. `grep 'https\?://' prototype/verify.js` 매치 0. DOMContentLoaded **132ms**.
- **API 키 노출 0** ✅ — `.env.local`이 git에 **추적된 적 없음**, 전체 히스토리에 `sk-` 패턴 0건. `submit_check.sh`가 git 추적·히스토리·배포 자산 3곳 + 실제 zip 시험 생성까지 검사. 유일한 구멍은 `.claude/` 미제외.
- **극단 임계값 정상 처리** ✅ — 강수 80mm(3→2.4일)·습도 95%(10→5.2일) 모두 문장·곡선 설명 정상. `heatNote`가 곡선이 눌린 이유까지 설명한다.
- **`archive.html`은 방치가 아니다** ✅ — `:22`에 「**개발 아카이브입니다** … 본 제품은 절기의 약속 검증소입니다」 배너 + README:210 설명. 로드 시 콘솔 에러 0. 손대지 말 것.
- **`flatSpan`의 '1°C'를 0.5로 바꾸지 마라** — `v0`는 실측 극값일의 값이라 평활 곡선의 최저점이 아니다. 조건을 만족하는 27일의 실제 값 범위는 **0.70°C** — 현재 표기가 참이고 0.5로 바꾸면 거짓이 된다.
- **미션 3의 `gap < 5` 분기는 결함이 아니다** — 데이터에 정직하게 반응한 것이고 두 문장 모두 참이다. 실제 결함은 기준 누락과 의존성 미고지 둘뿐.
- **미션 5 겨울에서 '동지보다 나중'은 표현 가능하다** — 클램프가 1~365이므로 doy 357~365를 찍을 수 있고, 오차가 순환 처리(`min(raw, 365-raw)`)라 12/31을 찍으면 실제 1/2와 **2일 차이**로 「잘 짚었습니다」가 정상 출력된다.
- **기준선 ±22px 히트테스트는 의도된 설계다** — `:1466` 주석 "곡선을 보려는 탭이 값을 바꾸지 않게(F-6)". 없애면 회귀가 난다. 고칠 대상은 `touch-action`이다.
- **AI 레이트리밋이 학습을 끊지 않는다** — `:2245`가 429를 별도 분기해 「규칙 점검으로 같은 항목을 방금 확인했어요」를 띄우고 `localAudit`이 즉시 대체한다. 코드 변경 없이 답변 카드 한 줄만. (단 P1-6의 어미 결함은 별개 문제다.)
- **README의 미사용 자료 표기는 현행이 더 정확하다** — `:118-122`가 ERA5·GPM·IBTrACS를 「현재 배포본 미사용」으로, 각주가 실제 읽는 데이터를 못 박았다.
- **'조작 3축'은 부풀린 주장이 아니다** — 축별 적용 범위를 이미 명기했다(세로 미션1~4·자유탐구 / 가로 미션5 / 기간 자유탐구). 미션 5에는 날짜 외에 **계절 토글**이 있어 판정문·데이터·전국 범위를 전부 바꾼다.
- **미션 1 자가진단은 정답 노출 예외다** — 다른 축('기준↑ → 일수↓')을 묻고 CERL에 답이 없다. 4/5이지 5/5가 아니다.
- **교사 루브릭은 자가진단에 기대지 않는다** — 6점 3행 모두 판정문만 채점한다.
- **방법론 서랍은 기본 닫힘이다** — `open` 속성 없음. "열려 있다"는 전제의 시나리오는 재현되지 않는다(누출 자체는 P1-7로 유효).
- **발표자료 PDF의 「33일 → 82일」은 정정 대상이 아니다** — 그 슬라이드는 **수정된 결함을 설명하는 'before'**다(「무엇이 틀렸었나」 섹션). 오히려 PDF의 다른 수치(서울 최한일 1/2 · 내륙 10~13일 · 제주 42일 · 30.8→68.0 · +13일 · 4,852)는 **전부 최신값으로 정확**하다. 낡은 것은 README·대본·학습지 쪽이다.
- **첫 15초 인상 판단은 검증 불가** — 검증 가능한 부분(인트로 카운터 13px)만 P2로 남긴다.

---

## 7. 본선까지 시간 배분

총 필요: **P0/P1 약 29시간 + P2 상위 약 10시간.** 「발견 확률 × 감점 폭 ÷ 비용」순.

### 1단계 · 오늘~내일 (3h) — 시연 사고 차단
1. **P0-5** 상태 초기화 (40분) ← *리허설 전에 반드시*
2. **P1-3** 겨울 문구 3곳 (30분) · **P1-4** 라벨 겹침·잘림 (1h) · **P1-15** 출처 (30분) · P2-11 markOut (15분)
3. 데모 대본에 "시연 시작 전 `↺ 처음부터`" 절차 명문화 (10분)

### 2단계 · 제출 전 (9h) — 문서 진실성
4. **P0-1** 수치 일괄 정정 24곳+ (3h)
5. **P0-3** prompt_sessions 정리 (2h)
6. **P0-2** 게이트 F축 반전 + submit_check 보강 (4h)

> **순서를 바꾸지 말 것.** P0-1만 하고 P0-2를 빼면 다음 편집에서 다시 썩는다. 작업 종료 시 `submit_check.sh` 전체 통과 + `verify_solar_terms.py` 출력과 문서 일치를 스크린샷으로 남긴다.

### 3단계 · 리허설 전 (7h) — 대본이 실제로 돌게
7. **P1-5** 26창 드래그 (1.5h) · **P1-11** 대본 조작 3건 (2.5h)
8. **P1-10** 지표 하드코딩 (3h)

> 리허설은 **7·8 완료 후 1회, 2단계 문서 확정 후 1회** 총 2회 이상.

### 4단계 · 과학 무결성 (6h)
9. **P0-4** 겨울 문구 + 분류 일원화 (3.5h)
10. **P1-8** 미션3 기준·민감도 (1.5h) · **P1-9** 전지구 완결성 (1h)

### 5단계 · 여력 (10h+)
11. **P1-2** 고향카드 (40분) · **P1-14** zip 검증 (1h) · **P1-7** 봉인 누출 (1.5h)
12. **P1-1 차선**(문구 20분) — 시간이 되면 **최선**(폭염·열대야 지표 추가, 6~8h)이 ① 회복 폭이 가장 크다
13. **P1-13** 중 3번(기록 렌더 1h) → 2번(자가진단 순서 2h) — 비용 대비 ③ 상승 최대
14. **학생 파일럿 5~10명 1회** (4h) ← *"학습 효과를 어떻게 측정했나"의 유일한 실증*
15. **P1-12** 궤도 물리 반전 (1.5h) · P2-1 절기 2개 (15분)

> **잘라도 되는 것**: P1-12 모바일 보정(라이브 데모는 데스크톱 투사), P2 7·9·10·12·19·20
> **잘라선 안 되는 것**: P0 전부, P1-2, P1-14

---

## 8. 심사위원 예상 질문 12개 · 현재 답변 가능 여부

| # | 질문 | 현재 | 근거 / 필요 작업 |
|---|---|---|---|
| 1 | **5년은 표본이 부족하지 않나? 왜 하필 그 5년인가?** | 🟡 | 말로 답하지 말고 **26창 슬라이더를 움직여라** — 36~68일에서 흔들리지만 26개 창 전부가 과거보다 많고 30년은 52일. **단 (a) P1-5로 드래그가 끊긴다, (b) P2-3으로 26℃에서는 동률 1이라 "전부" 대사가 화면에서 반증된다** |
| 2 | **이건 시뮬레이터가 아니라 데이터 뷰어 아닌가?** | 🔴 | 3개 문서 grep 0건. Mo1 p.28의 ④ 데이터 탐구 대시보드 근거가 실재하는데 **어디에도 안 적혀 있다.** 답변: *"학습자가 바꾸는 것은 세계의 상태가 아니라 **측정 기준**이며, 그게 이 앱이 가르치려는 개념이다. 미션 4에서 1mm와 50mm가 정반대 결론을 내는 것이 증명이다"* |
| 3 | **학습 효과를 어떻게 측정했나?** | 🔴 | **가장 약한 질문.** 사전=사후 동일 문항이고 정답이 두 화면 모두에 축자 인쇄돼 있어 정답률 상승은 학습이 아니라 읽기를 잰 것. 파일럿 0회. 그때까지는 **정직하게** "형성평가 장치는 판정문 작성 + 감사이고, 사전/사후는 인식 변화 표시용"으로 한정해 말할 것 |
| 4 | **절기가 안 맞는 게 기후변화 아닌가?** | 🟢 | 미션 5가 정면으로 답한다 — 가장 더운 날은 하지 +40일인데 **이 지연은 과거에도 41일**, 16지점 평균 변화 −0.8일, 잭나이프에서도 38~46일로 견고. 변한 것은 더위일 30.8→68일. **단 대사 수치를 P0-1대로 정정할 것("33일"·"+0.9일"은 틀린 값)** |
| 5 | **인천·포항도 해안인데 왜 11일·10일입니까?** | 🔴 | **현재 답변 불가.** P0-4가 실재하며 자가진단이 이 미완성 주장을 '정답'으로 채점한다. 40분 문구 교정으로 **약점이 발견으로 뒤집힌다** |
| 6 | **지금 여기서 AI가 죽으면?** | 🟢 | `:2242` catch → `localAudit` 즉시 대체, 실패 원인 3분기, 429 전용 문구. 96/100 · 오탐 0. **"96/100은 규칙 점검(반드시 동작하는 경로)의 성적"으로 귀속을 명확히 — 약화가 아니라 강화다.** 단 P1-6의 「아님?」 어미 결함은 먼저 닫을 것 |
| 7 | **4,852건 검사는 무엇을 검증하나? 직접 돌려봐도 되나?** | 🟡 | 축 자체는 자랑거리다(D축 독립 재계산 / E축 부호역전 / H2축 물리 전제). **그러나 (a) zip에서 실행하면 안내 없는 FileNotFoundError, (b) 문서 8곳이 4,810, (c) F축이 문서 오류를 하나도 못 잡고, (d) H2축이 화면과 다른 명제를 검사한다.** → P0-1·P0-2·P1-14 완료 전에는 "직접 돌려보시라"고 권하지 말 것 |
| 8 | **폭염일수를 볼 수 있습니까?** | 🔴 | 현재 답변이 **사실과 다르다** — 원자료에 `minTa`·`maxTa`가 있고 수집기가 버렸다(P1-1). 최소한 "이 버전은 일평균만 가공했다"로 정정. 재수집하면 **가장 큰 ① 회복** |
| 9 | **교실에서 40명이 공용 PC로 돌려 쓰면?** | 🟡 | 답할 재료는 있다 — 상시 `↺ 처음부터` + 확인 문구 + localStorage만 사용(서버 전송 0) + 429 폴백. **자동 이어받기는 미구현이라고 말하라**(로드맵). P2-5(15분)는 닫아 둘 것 |
| 10 | **성취기준·차시 연결은?** | 🟡 | 교사 학습지 + 6점 루브릭이 실재해 강점이다. **그러나 `[12지구02]` 형식 오류, 시간 태그 61분 vs 헤더 45~50분, 수치 5종 불일치 — 교육 전문 심사위원이 가장 먼저 보는 두 칸이 어긋나 있다.** 이 질문은 반드시 나온다 |
| 11 | **계획서의 20개 사건이 왜 5개 미션이 됐나? 태풍은?** | 🟡 | 계획서 `:442·446`에 v3 전환 논거가 실재하는데 **README·발표자료에 그 연결이 없어 '못 만든 것'으로 읽힌다** → P2-16, 40분 |
| 12 | **이 그림이 맞나요? 하지에 북극이 태양 반대쪽인데.** | 🔴 | **현재 답변 불가.** P1-12가 실재하며 같은 패널 본문이 그림의 정반대를 말한다. 1.5시간으로 닫힌다 |

---

## 9. 지켜야 할 구조적 강점 8건

레드팀 보고서가 약점만 나열하면 쓸모가 반이다. 아래는 **심사위원이 점수를 주지 않을 수 없는 구조**만 적었다.

| # | 강점 | 증명 위치 | 발표에서 |
|---|---|---|---|
| 1 | **예측 봉인** — 문구가 아니라 **렌더 분기**로 강제한다. 계절을 바꾸면 봉인을 스스로 해제해 "여름 예측으로 겨울 판정"을 막는다 | `verify.js:678-683`·`:735`·`:909`·`:1223`·`:1819`·`:1358-1361` | **라이브 3초 시연이 25점 항목 전체를 대신한다** |
| 2 | **같은 자료, 반대 결론** — 1mm는 줄고 50mm는 늘었다. 조작이 결론을 *바꾸는* 게 아니라 *뒤집는* 유일한 미션. 뒤집히지 않는 지역은 그렇게 정직하게 처리한다 | `verify.js:173-208`·`:217-224` | *"학생이 배우는 것은 기후변화가 아니라 '어떤 기준으로 셌는지 말하지 않은 결론은 결론이 아니다'입니다"* |
| 3 | **심사위원의 반문을 조작 변수로 넣었다** — 26개 5년 창. 결론이 갈리면 **앱이 먼저 "단정하면 안 된다"고 말한다.** 습도·강수는 창 통계가 없으므로 기능을 끄고 그 이유를 적는다 | `verify.js:1079-1117`·`:1080-1083` | **Q&A 예비 탄약.** 슬라이드에 넣지 말고 반문이 나오면 슬라이더로 답하라 |
| 4 | **계절 지연과 온난화의 분리** — 여름 지연은 잭나이프에서도 38~46일로 견고하다(🔬 확인). 조작축도 세로가 아닌 **가로 드래그**. 연말·연초 경계까지 순환 처리 | `verify.js:227-320`·`:265` | *"저희가 가장 경계한 오개념입니다"* — 기상청 주최 대회에서 **방어이면서 동시에 차별점** |
| 5 | **회귀 검증 게이트 4,852건** — 원시 CSV에서 **다른 코드 경로로 다시 세어 대조**하고, **화면의 물리 설명문이 데이터로 무너지면 빌드가 깨지는** assert까지 있다. 실패 시 exit 1 | `processing/verify_solar_terms.py` (직접 실행 확인) | 30점 항목의 유일한 하드 증거. 터미널 실물 1컷. **단 P0-2 완료 후에 자랑할 것** |
| 6 | **AI를 대필자가 아니라 감사관으로 좁히고 이중화** — 입력창이 비어 있고 예시문 그대로면 요청을 거부. strict JSON 스키마 + 인젝션 방어. 502 근본 원인을 수치로 규명해 수정한 기록. fallback 품질을 100케이스로 정량화 | `verify.js:2227`·`:2242` · `api/ai-turn.js` 전체 · `eval_harness.js` | *"지금 이 자리에서 AI가 죽어도 학습은 안 멈춥니다"* — 데모 리스크를 0으로 만든다 |
| 7 | **외부 의존성 0** — 라이브러리·CDN·폰트파일·지도타일 전부 0. 차트·지도·스파크라인 전부 인라인 SVG 자체 구현. 이미지 4개는 AI 자체 제작 | `index.html` · 요청 6건 실측 · `grep https://` 0건 | 한 문장으로 끝내라. **짧을 때 가장 강하다** |
| 8 | **사무국 사전교육을 페이지 단위로 역설계** — Mo1 p.31 기준표(각 행에 「이 화면에서: 계산 불가」), p.32 3층 탐구질문(지표·절기·기준에 따라 문장이 바뀐다), p.33 핵심 2분, Mo3 p.20 지점자료 통일. **근거 페이지를 코드 주석에 남겼다** | `verify.js:914`·`:974` · `verify_solar_terms.py` A축 | 반복하면 아첨으로 들린다. **⑥ 교육 적합성 파트에서 한 번만, 근거로** |

---

## 10. 한 줄 요약

**P0 5건에 9시간을 쓰면 76.5 → 86점대가 된다.** 가장 위험한 것은 코드가 아니라, **심사위원이 터미널 한 줄과 zip 목차로 3분 만에 확인할 수 있는 문서 층**이다. 그다음이 미션 5 겨울 서사(40분 문구 교정으로 약점이 발견으로 뒤집힌다)와 「자료에 최저·최고기온이 없다」(재수집하면 ① 감점이 강점이 된다)다. 학습 효과 측정과 궤도 그림은 그 뒤다.

---

*검증 방법 요약: 6축 독립 감사(14 에이전트) → 축별 반증 검증(기본값 REFUTED) → 배포본 실조작 → 독립 재계산(잭나이프·태양황경 전수·고향카드·문서 대조). `과학무결성` 축은 반증 검증관이 API 오류로 사망해 본 보고서 작성자가 주요 8건을 직접 재검증했다(✅🔬 표기).*

---
---

# 조치 결과 (2026-07-28)

지적 전량을 반영했다. **가장 큰 변화는 “데이터 뷰어 아닌가”라는 지적에 답변이 아니라 화면으로 답했다는 것**이다.

## A. 신규 — 🔬 열관성 실험실 (SCORE-6 대응)

관측을 조작하는 화면만 있던 앱에 **물리 방정식을 조작하는 화면**을 추가했다.

```
C · dT/dt  =  Q(날짜, 위도)·(1−α)  −  (A + B·T)      ← 0차원 에너지 균형 모형
             천문 계산(Spencer 1971)      선형 감쇠   ← 40년 스핀업 후 365일 적분
```

| 조작 | 반응 | 교육적 의미 |
|---|---|---|
| 열을 머금는 층 **0.5m → 50m** | 지연 **3일 → 88일**, 진폭 ±20.7 → ±2.6℃ | 계절 지연의 원인이 **열관성**임을 학습자가 만들어 본다 |
| **온실효과 0 → +12 W/m²** | 연평균 **12.4 → 14.7℃**, 지연 **17일 → 17일(불변)** | **미션 5의 결론을 모형이 증명한다** — 지연과 온난화는 다른 원인 |
| 지역(위도) 전환 | 일사량이 바뀌어 곡선 전체가 이동 | 실측과 겹쳐 **가장 잘 맞는 두께**를 찾는 활동 |

- 보정 결과 **서울·5m에서 지연 40일·최고 28.0℃** — 실측(40일·29.4℃)과 거의 일치한다.
- 화면 맨 위와 방법 서랍이 **“이 곡선만은 관측이 아니다”** 와 단순화 5가지를 먼저 밝힌다.
- 진입: 인트로 `🔬 열관성 실험실` · 미션 5 심화 링크 · 완료 화면 · 자유탐구 · 딥링크 `#v=lab`

## B. P0 — 5건 전량 반영

| # | 조치 | 확인 |
|---|---|---|
| P0-1 | 문서 수치 4계열 정정(교사학습지·데모대본·발표구성안·README·AI활용기록) | 게이트가 강제 |
| P0-2 | **게이트 F축을 블랙리스트 → 데이터-문서 대조로 반전.** 검사 대상 2 → 5개 문서, 불일치는 경고가 아니라 **실패** | 뒤집자마자 **19건 검출** → 전량 수정 후 통과 |
| P0-3 | 세션 로그 — 중복 턴 **78건 제거**, 제출 목적과 무관한 구간 제외, 이메일 마스킹, 목차를 실물과 자동 동기화, 미완 TODO 삭제 | `이메일 0건` 검사 통과 |
| P0-4 | **COASTAL 이분법 폐기.** 잭나이프(leave-one-year-out)를 데이터에 기록하고, 화면은 “제주·강릉·목포만 뚜렷하게 늦다 / 인천 11일·포항 10일은 내륙과 같다 / 한 해만 빼도 29일 흔들린다”만 말한다. 자가진단 정답 해설의 인과 단정도 제거 | 게이트가 이분법 부활을 차단 |
| P0-5 | 미션 재진입 시 `lagRevealed`·`predicts`·`view` 초기화 | 5미션 자동 통과 시험 |

## C. P1 — 15건 전량 반영 (주요 항목)

| # | 조치 | 실측 |
|---|---|---|
| P1-1 | **폭염·열대야를 실제로 계산했다.** 수집기가 버리던 `minTa`/`maxTa`를 되살려 8지점 지수를 산출 | 서울 폭염일 **4.2 → 23일**, 열대야 **3.2 → 32일**, 결빙일 25 → 14일 |
| P1-2 | 고향 카드가 ‘최근 5년’이라 인쇄하며 3년만 평균하던 것 수정 | 2008년생 서울 ΔT **+1.5 → +1.2℃**(정확) |
| P1-3 | 미션 5 겨울 모드 제목·지시문·배경설명을 계절로 분기 | “가장 추운 날은 왜 동지가 아닐까?” |
| P1-4 | 절기 라벨 겹침·잘림 | 24절기 전수 **잘림 0건** |
| P1-5 | 26창 슬라이더가 드래그 한 칸에서 끊기던 것 | 노드 보존 확인(`sameNode: true`) |
| P1-6 | 규칙 감사관이 “~아님?/아니냐”에 무력화되던 것 | 평가셋 **96 → 99/100**, 오탐 0 유지 |
| P1-7 | 봉인 누출 — 방법론 서랍·월별표·폭염표·`aria-label` 전부 잠금 | 스크린리더도 동일 체험 |
| P1-8 | 미션 3 근거에 기준 명시 + 한계에 임계값·30년 역전 고지 | “27°C에서는 2일이고 순서도 뒤집힙니다 … 30년으로 넓히면 제주 +6일 · 강원 +8일” |
| P1-9 | 전지구 자료에 완결 연도 게이트 적용 | 마지막 연도 2026 → **2025** |
| P1-10 | 지표 하드코딩 4건(강수 미션의 “덥다 1°C” 예시문 등) | `metricOf()` 기반으로 전환 |
| P1-11 | 대본이 가리키던 없는 컨트롤 — 안내문을 미션별 조건부로 | 미션 1 지역 칩 시연을 미션 2로 이동 |
| P1-12 | **24절기 궤도의 물리 180° 반전 수정** | 하지=북극이 태양 쪽(+10.3), 동지=반대쪽(−10.3) |
| P1-13 | 자가진단을 판정문 **앞**으로, 사후 문항을 **동형 문항(입동)** 으로, 예측·사전/사후를 ‘내 기록’에 저장 | 완료 화면에서 확인 |
| P1-14 | zip에서 죽던 검증 명령 | `--no-raw` 플래그 + 안내 메시지 |
| P1-15 | 지구 패널에 NOAA·OWID 출처·라이선스 렌더(CC BY 4.0 이행) | 데이터 파일의 `meta.sources`에서 자동 |

## D. P2 — 반영분

절기 대표날짜 **입하 5/6 → 5/5 · 입추 8/8 → 8/7**(태양황경 전수 재계산, 게이트 J축이 24개 전부 검사) ·
26창 동률 오판(“전부” → “적은 창은 하나도 없습니다”) · `↺ 처음부터`의 해시 잔존 ·
다크 테마 인쇄 시 흰 글자 · `touch-action: pan-y`(차트 위 세로 스크롤 복원) ·
겨울 브래킷이 플롯을 가로지르던 문제 · 미션 5 날짜 스테퍼 랩어라운드 ·
봉인 중 `#markOut` 미갱신 · 기록 복사 실패 무시 · 궤도 모바일 표적 **7px → 26.6px**(WCAG 2.5.8 충족) ·
미참조 파일 `mission.js`·`절기_드리프트_프로토타입.html` 제거 · `.claude/` 추적 해제 ·
교사 학습지 성취기준·시간 표기 정정 · 딥링크 `hashchange` 대응

## E. 제출물 최종 사양 (2026-07-28 확정)

| # | 제출물 | 현재 상태 |
|---|---|---|
| ① | `발표자료_Weather24.pdf` | **11장**(열관성 실험실 슬라이드 신설) · 16:9 · 폰트 임베드 · `scripts/build_deck.py`의 `TOTAL`로 쪽번호 일원화 |
| ② | 구동 URL | <https://weather-24solar-terms.vercel.app> · Git 연동 자동 배포 · `verify.js?v=54` |
| ③ | 소스코드 | `git archive --format=zip HEAD` — `.claude/`·`.env*`·원시 CSV·`__pycache__`·`_simulated/` 구조적 제외 |
| ④ | 프롬프트 세션 | **5개 세션** · 사람 지시 56 · AI 답변 815 · 365KB · 중복 80턴 제거 · 이메일 0건 · 목차 자동 생성 |
| ⑤ | README | 학습목표 5개 · 조작 4축 · 폭염·열대야 · 무결성 8약속 · 저장소 구조 갱신 |

관련 문서도 현재 버전으로 정리했습니다 — `제출_체크리스트`(5종 사양·배점 대응), `데모_대본`(실험실 40초 추가),
`발표_10분_구성안`(Q&A 4문항 신설), `AI_활용_기록`(4차 라운드·세션 5건), `AI_품질_평가`(96→99 개선 경위),
`교사_학습지`(활동 6 신설·2코스), `web_data/DATA_GUIDE`(완결 연도 규칙), `data_collectors/README`(KEEP 컬럼).
과거 라운드 보고서 4종에는 **“현재 값이 아님”** 배너를 달아 원문을 보존했습니다.

## F. 검증 결과

```
python processing/verify_solar_terms.py   ✓ 전체 통과 — 데이터 8,213 + 문서 대조
                                            축 A~E · G · H/H2 · I · J(절기) · K(전지구) · L(안정성) · M(폭염)
node eval_harness.js                      ✓ 99/100 · 오탐 0건 (good_scoped 13/13 · hold_valid 12/12)
bash scripts/submit_check.sh              ✓ 전 항목 통과 — 제출 가능
프로덕션 실측                              ✓ 5미션 완주 · JS 오류 0 · 375px 가로스크롤 0 · 작은 표적 0
```

**여전히 남은 약점(정직하게)** — 실제 학생 파일럿 0회. 이것만은 코드로 메울 수 없다.
발표에서는 “형성평가 장치는 갖췄고, 현장 검증은 아직”이라고 말하는 편이 정확하다.
