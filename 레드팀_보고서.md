# Weather24 레드팀 보고서

> 대상: 배포본 `prototype/index.html` + `verify.js`(657줄) + `verify.css` + `solar_terms_data.js`
> 라이브: https://weather-24solar-terms.vercel.app/ (2026-07-26 실측 · 콘솔 에러 0 · `/api/ai-turn` 200 정상)
> 기준일 2026-07-26 · **현장 데모(8/21~22 천안)까지 26일**
> 방법: 6개 차원 독립 탐색 → 반박 지향 검증 → 병합. **모든 수치는 원시 ASOS CSV(20,992행, 1969-01-01~2026-06-22)·배포 데이터 파일·배포 코드 실행으로 재검증함.**

---

## 0. 결론

| | |
|---|---|
| **한 줄 판정** | 프레임과 실행력은 상위권이나, **제품의 유일한 조작 변수가 동시에 모든 수치 오류의 발원지**다. 라벨 하나가 아니라 측정 정의 하나를 고쳐야 한다. |
| **가장 위험한 단일 원인** | `exceed()`/`lastExceed()`(verify.js:120–121)가 **15일 평활된 5년 평년곡선**에 임계값을 적용하는데, 라벨은 `'더위일'·'비 온 날'·'습한 날'`(verify.js:17–19)로 **실제 관측일수처럼** 읽힌다 |
| **데모 2분 내 노출되는 오류** | ① 기준선 기본값 25→**26℃(1칸)** → 앱이 "과거엔 26℃ 이상 더위가 **거의 없었는데**"라고 씀 (실측 **21.4일/년**) ② 강수 탭 1클릭 → "서울 비 온 날 292→**309일**" (실측 89.2→82.8, **방향 반대**) ③ 대본이 지시한 28→30℃ → 차트가 비고 시차가 사라짐 |
| **가장 아까운 자기모순** | 발표에서 "AI는 결론을 대신 쓰지 않습니다"(데모_대본.md:28) → 화면의 AI 감사 상자에는 **앱이 써 준 완성 CERL 문장이 이미 들어 있고**, 버튼만 누르면 "근거 충분"이 뜬다 (실행 검증) |
| **역설적 호재** | 이 결함을 고치면 28℃는 **4.8→34.8일(7.3배)**, 30℃는 **0.6→7.5일(12.5배)**. **지금 죽어 있는 구간이 앱 최고의 실연 장면이 된다.** 결함 수정이 곧 데모 강화다 |
| **심사 점수(내부 추정)** | 현재 **73~80 / 100** → P0 완료 시 84~88 → P0+P1 시 **89~92** |

---

## 1. 지켜야 할 강점 (수정 중 깨뜨리지 말 것)

레드팀의 절반은 무엇을 건드리면 안 되는지 아는 것이다. 아래는 과장 없이 확인된 실제 강점이다.

| # | 강점 | 근거 |
|---|---|---|
| 1 | **예측 봉인이 문구가 아니라 코드로 강제된다** — 조작 이력·예측 응답 둘 다 없으면 판정으로 못 넘어가고, 버튼을 눌러도 오버레이를 띄운다 | verify.js:427–431, 397–402 |
| 2 | **자유탐구 판정문은 진짜 학습자 산출** — 시드 없이 placeholder만, `state.freeDraft`로 보존 | verify.js:560, 565–566 |
| 3 | **AI 없이도 형성 피드백이 유지된다** — `localAudit`이 범위·인과·5년→기후·절기 오개념·인젝션 5종을 정규식으로 판별, 100케이스 96/100, **오탐 0** | verify.js:588–629, eval_result.json |
| 4 | **오개념 교정 언어가 UI·판정문·AI·교사자료에서 한 문장으로 정렬** — 교사 학습지 '되돌리는 질문' 4개가 `localAudit` 소크라테스 질문 4개와 거의 축자 일치하고 플래그와 1:1 대응 | 교사_학습지.html:73–78 ↔ verify.js:622–628 |
| 5 | **기본 데모 경로(기온 25℃)의 수치는 방어 가능** — 서울 33→82 vs 실측 30.8→**72.5**(현재값 1.13배). 과거값은 거의 정확 | 본 보고 §2.1 재계산 |
| 6 | **`annual_series`가 불완결 연도를 명시적으로 배제** — 57년 스파크라인·timeline에 2026 오염 없음 | build_solar_terms.py:102 |
| 7 | **`sumRn` 공백→0 처리가 ASOS 관례와 일치** — 결측률 50~66%는 무강수일. 과거·현재 편향 없음(차이 ≤5%p). **우려 기각** | build_solar_terms.py:87 |
| 8 | **모바일·성능 안전** — 375px 가로 오버플로 0, `#heroSvg { touch-action: none }`으로 드래그 정상, `drawHero()` 1회 0.66ms, 히어로 SVG 9.9KB | verify.css:44, 실측 |
| 9 | **AI 경로가 안전** — 응답 3필드 전부 이스케이프, 3중 실패 조건이 모두 `localAudit`로 수렴, 중복 id는 `setStage`가 subtree를 통째 교체하므로 충돌 0건 | verify.js:586, 632–645 |
| 10 | **비밀 관리 정상** — `.env*` gitignore 처리, git 히스토리에 키 커밋 없음, 키는 서버리스 환경변수만 | .gitignore, `git log` 전수 |
| 11 | **교사용 학습지가 실제로 인쇄 가능한 완성물** — 40분 배분, 오개념표 4행, 모범 판정문, 루브릭, `@media print` | 교사_학습지.html |
| 12 | **`archive.html`이 라이브에서 정상 작동** — 20사건·16지점 지도·증거보드·반증슬롯. 계획서 약속이 실제로 구현돼 있다 | 실측 |

**추가로 기각된 우려(안심 가능)**: `lastExceed` 연말 랩어라운드(temp 최대 제주 thr=20의 10/19, 11월 이후 0건 / precip·humidity는 `mc.showLast`로 화면 차단) · 순환 15일 평활의 연말·연초 불연속(없음, 최저점 doy 23 = 물리적으로 타당) · `interpolate` 미발동(96개 조합 빈 슬롯 0/96) · 중복 id 충돌(0건) · 이벤트 리스너 누수(기능 영향 없음).

---

## 2. P0 — 데모 전 반드시 고칠 것

### 2.1 [CRITICAL] 평활 평년곡선을 '관측일수'로 라벨링 — 단일 원인, 4개 증상

**원인**: `build_solar_terms.py:84–94`가 일자별 5년 평균 → 보간 → `csmooth(arr,7)`(원형 15일 평활)을 만들고, `verify.js:120–121`이 그 **평활 곡선**에 임계값을 적용한다. 평활은 진폭을 압축하므로 **고임계에서 과소, 저임계에서 과대**가 구조적으로 발생한다.

#### 증상 ① 26℃(기본값 +1칸)부터 앱이 사실이 아닌 문장을 쓴다 — 서울, 재검증

| thr | 앱 과거 | 앱 현재 | **실측 과거** | **실측 현재(완결 4년)** | 앱 화면 문구 |
|---|---|---|---|---|---|
| 25 | 33 | 82 | 30.8 | 72.5 | 정상 |
| **26** | **0** | 60 | **21.4** | 58.2 | **"과거엔 26°C 이상 더위가 거의 없었는데"** ← 허위 |
| 27 | 0 | 35 | 9.8 | 45.5 | 동일 허위 |
| 28 | 0 | 18 | **4.8** | **34.8** | 동일 허위 |
| 29 | 0 | 6 | 2.0 | 19.0 | 동일 허위 |
| **30~32** | **0** | **0** | 0.6 | **7.5** | **"과거·현재 모두 더위일이 적습니다"** ← 데이터 소거 |

서울 평활 과거 곡선 최대 **25.9℃**. 인천 25.2·충남 25.7·경기 25.9℃도 동일 — **16지역 대부분이 기본값 1칸 위에서 허위 진술 구간**에 들어간다. `verify.js:44`의 두 번째 분기가 발동한다.

#### 증상 ② 슬라이더 23%가 데이터에 없는 패딩 영역

`verify.js:18` `hi = Math.max(28, Math.min(34, Math.round(b.hi)))`이고 `bounds()`가 `pad=(hi−lo)*0.08`을 더한다. 서울: 곡선 최대 29.4 → `b.hi=32.0` → 슬라이더 **20~32**. thr 30~32(3/13=23%)는 **데이터에 존재하지 않는 구간**이며 16지역 전부에서 재현된다.

> **`데모_대본.md:40`은 "(기준선을 25→28→30도로 끌며) 기준을 바꿔도 시차의 방향은 일관됩니다"를 발표자에게 지시한다. 28·30℃에는 시차 값이 존재하지 않으므로 그 실연은 물리적으로 불가능하다.** 방향 일관성을 실제로 보일 수 있는 구간은 20~25뿐(+9,+11,+17,+18,+24,+25). `데모_대본.md:17`의 "25→28도로 올리면 숫자가 0초로 뒤집힌다"도 같은 문제.

#### 증상 ③ 강수 라벨은 방향까지 반대 — 서울, 재검증

| 기준 | 앱 | **실측(완결연도)** | 판정 |
|---|---|---|---|
| ≥1mm "비 온 날" | 292 → **309** (+17) | 89.2 → 82.8 (**−6.4**) | **방향 반대 · 3.7배 부풀림** |
| ≥3mm (기본값) | 144 → 149 (+5) | 64.8 → 59.2 (−5.6) | **방향 반대** |
| ≥10mm (집중호우) | 52 → **47** (−5) | 35.2 → **38.2** (+3.0) | **방향 반대** — 기후적으로 가장 중요한 사례에서 |

자유탐구 지표 탭 **1클릭**으로 도달(verify.js:262). **`데모_대본.md:58`이 "지표 전환(기온 → 강수 → 습도)"을 필수 변주로 지시한다.** 오염 지점 3곳: `:222`(readouts), `:184`(liveSentence), **`:573`(AI에 전달되는 증거카드 E-1)**.

#### 증상 ④ 습도도 부호 역전

| 지역 (70% 기준) | 앱 | 실측 |
|---|---|---|
| 인천 | 129 → **149** (+20) | 199.6 → 182.2 (**−17.4**) |
| 대구 | 115 → **126** (+11) | 164.4 → 134.5 (**−29.9**) |
| 서울 | 178 → 120 (−58) | 199.8 → 144.8 (−55.0) ← 방향 일치 |

#### 증상 ⑤ 규칙 감사관이 방향이 틀린 문장을 "근거 충분"으로 인증한다 (실행 검증)

배포 `localAudit`을 그대로 추출해 실행한 결과:

```
인천에서 습하다를 70%로 정하면 습한 날이 과거 129일에서 현재 149일로 늘어났다.
다만 이는 5년 관측 신호라 전국이나 원인으로 넓히기는 어렵다.
→ ready | flags: [] | "좋아요. …한계까지 밝힌 점이 특히 좋습니다."
```

`localAudit`의 검사 항목은 injection/scope/causal/one_year/misconception/missing뿐 — **증감 부호 검증이 없다.** 즉 RC-A의 오류가 감사 계층에서 **인증**된다.

**수정**: `build_solar_terms.py`에서 **연별 실측 초과일수·마지막 초과일**을 임계값 그리드로 사전 계산해 JSON에 넣고(+약 30KB), `exceed()`/`lastExceed()`가 평활 곡선 대신 그 값을 읽게 한다. 라벨 3곳(`:222, :184, :573`) 동기화. **작업량 6~8h. 이 한 작업이 5개 증상을 동시에 해소하고 28·30℃를 최고의 데모 장면으로 바꾼다.**

---

### 2.2 [CRITICAL] 5년 창 선택이 헤드라인을 2배로 부풀린다 — 직접 재검증

앱과 **완전히 동일한 방법**(15일 평활 평년곡선, 과거창 1969–73 고정)으로 현재창만 이동시켜 서울 25℃ 시차를 계산했다:

| 현재 5년 창 | 시차 | | 현재 5년 창 | 시차 |
|---|---|---|---|---|
| 1996–2000 | +6일 | | 2015–2019 | +9일 |
| 2000–2004 | +1일 | | 2018–2022 | +9일 |
| 2001–2005 | **−1일** | | 2019–2023 | +9일 |
| 2005–2009 | +5일 | | 2020–2024 | +12일 |
| 2010–2014 | +11일 | | 2021–2025 | +14일 |
| … (22개 창 전부 −1 ~ +14일) | | | **2022–2026 (앱)** | **+26일** |

**30년 창(1996–2025) = +9일. 20년 창(2006–2025) = +11일.**

앱이 고른 창만 유일하게 +26일로 튄다. 원인은 2026 포함이 아니라(2022–26 ≡ 2022–25 = +26) **2021년 제외**다. 즉 헤드라인 "+25일"은 **비교 가능한 어떤 추정치의 약 2배**이며, 5년 창을 한 해만 옮기면 절반이 된다.

**영향**: 이것은 앱이 목표②로 가르치는 **소표본 과잉일반화**를 앱 자신이 저지르는 사례다. 이 수치는 인트로·데모 대본·교사 학습지·공유용 고향카드 전부에 인쇄돼 있다. 심사위원의 "왜 하필 이 5년입니까?" 한 마디에 대표 주장이 반토막 난다.

**수정(권장)**: 창 민감도를 **숨기지 말고 학습 장치로 노출**하라. 판정문·증거카드에 "5년 창을 옮기면 시차는 −1~+14일에서 흔들리고, 30년으로 보면 +9일입니다"를 병기하면 **가장 위험한 질문이 최고급 교재로 전환된다.** 동시에 `PRESENT=(2021,2025)`로 옮겨 완결 5년을 확보한다. **작업량 3~4h + 병기 1.5h**

---

### 2.3 [HIGH] '5년 관측 신호' 표기가 사실과 다르다

원시 CSV는 **2026-06-22까지 173행**이고, `climatology()`(build_solar_terms.py:84–94)에는 완결성 필터가 **없다**. 결과: doy 1~173은 5년 평균, **doy 174~365(192개 doy)는 4년 평균**. **처서 = doy 235 → 제품 대표 수치가 4년 표본이다.**

- 같은 파일 `:102 annual_series`에는 `if len(grp) < 350: continue  # 불완전 연도 제외(2026 등)`가 **이미 있다.** 팀은 규칙을 알고 한쪽에만 적용했다.
- UI 표기는 전 화면 "5년": `verify.js:244`, `:295`(인트로 배지 "1969–2026"), `:538`(고향카드), `:573/574`(AI 증거 period).
- **개발계획서(고도화).md:39가 스스로 요구**: *"불완결 연도를 현재 평년 비교에 섞지 않고, 동일한 길이의 완결 기간·표본 수·출처를 모든 화면에 표시"* → **자기 요구사항 위반.**

**수치 영향은 미미하다**(처서 시차는 2026 포함/제외 모두 +26일). 그러나 심사위원 질문 "2026년 자료는 언제까지입니까? → 6/22. 그러면 8월 23일 값은 몇 년 평균입니까? → 4년"에 답할 수 없다. 대본이 "표본의 한계 자체가 학습 소재"라고 선제 방어하도록 설계됐는데, **그 방어의 전제인 표기가 부정확하면 방어가 역효과를 낸다.**

---

### 2.4 [CRITICAL] 미션3 판정문이 근거 없이 단정하고, 조작이 산출물에 연결되지 않는다

`verify.js:85–88`에 **null 가드가 없고 "지역마다 다릅니다"가 무조건 붙는다.** 슬라이더 13스텝 전수 검증:

| thr | 제주 | 강원 | 판정문이 말하는 것 |
|---|---|---|---|
| 21 | +18 | +15 | 차이 3일 |
| **22** | **+15** | **+16** | **순위 역전** |
| **23** | **+18** | **+21** | **순위 역전** |
| **25 (기본)** | **+21** | **+17** | 차이 **4일** → "지역마다 다릅니다" |
| 26 | +26 | +27 | **1일 차** → "지역마다 다릅니다" |
| **27** | +29 | **비교 불가** | **"제주 +29일, 강원 비교 불가로 지역마다 다릅니다"** |
| **28~32** | **비교 불가** | **비교 불가** | **"비교 불가, 비교 불가로 지역마다 다릅니다"** |

> **정정 (3라운드 재계산)**: 초판은 붕괴 구간을 "thr≥28"로 적었으나 **강원은 27부터** 과거 곡선이 기준을 넘지 않는다. 슬라이더 20~32의 13칸 중 **6칸(46%)** 에서 판정문이 깨진다 — 초판보다 나쁘다.

네 가지가 겹친다:
1. **근거 없는 단정** — 어떤 스텝도 "크게 다르다"를 지지하지 않고, 2개 스텝에서 순위가 역전된다.
2. **자기모순 문장** — 5개 스텝에서 "비교 불가, 비교 불가로 지역마다 다릅니다"를 출력.
3. **조작–산출물 단절** — 과제문(`:84`)이 "제주 칩과 강원 칩을 번갈아 눌러"라고 지시하는데, 칩을 눌러도 판정문은 **한 글자도 바뀌지 않는다**.
4. **비단조성** — 강원 시차가 thr 23→24에서 +21→+14로 역행. `lastExceed`는 거의 평평한 곡선의 단일 교차점이라 민감도가 크다.

**영향**: 이 미션의 존재 이유가 목표②(과잉일반화 금지)인데, 그것을 가르치는 화면이 **근거 없이 단정하는 시범**을 보인다. `데모_대본.md:41`의 방어 논거 3번("지역마다 크기가 다릅니다")도 함께 무효화된다.

**수정**: `|a−b| < 5`면 **실제로 데이터가 지지하는 결론으로 교체** — *"시차의 크기는 제주 +21일, 강원 +17일로 **비슷하지만**, 더위가 그치는 **절대 날짜**는 제주 9/23, 강원 8/27로 약 한 달 차이입니다"*. 절대 날짜는 이미 계산되므로 추가 데이터가 필요 없다. **작업량 2~3h (RC-A 이후)**

---

### 2.5 [HIGH] AI 감사 상자 선입력 = 앱이 학습자 대신 결론을 쓴다

`verify.js:487` `$('freeDraft').value = buildDraftSeed(m);` — **세 미션 모두** textarea에 완성된 4요소 CERL 문장을 선입력한다. 배포 `localAudit`을 실행한 결과:

| 미션 seed | 판정 | flags | 피드백 |
|---|---|---|---|
| chuseo | **ready** | `[]` | "좋아요. 지역·기간·기준이 문장에 들어 있어… 한계까지 밝힌 점이 특히 좋습니다." |
| region | **ready** | `[]` | 동일 |

**미션3 seed에는 비교 기간 토큰이 한 단어도 없다.** 그런데 `:605` cautious 예외가 `:615` missing 경고를 억제해, **존재하지 않는 요소를 "들어 있어"라고 칭찬한다.**

- **계획서 §7.5**가 *"학습자 대신 최종 브리핑을 작성하는 행동"*을 **절대 금지**로 명시.
- **`데모_대본.md:28`**이 *"AI는 결론을 대신 쓰지 않습니다"*로 선언.
- 교사 루브릭(교사_학습지.html:83–88) 기준 **만점 4/4, 학습자 산출은 클릭 1회.**

**수정**: `:487` 선입력 제거 → 자유탐구와 동일한 placeholder. `:618` 피드백을 **실제 검출된 요소만** 나열. **작업량 1.5h — 효과/비용 비율 최상.**

---

### 2.6 [HIGH] 고향카드 dT가 단년 차분 → 음수·0 발생

`verify.js:524` `dT = Math.round((tNow − tBirth)*10)/10` — **출생연도 1년 vs 2025년 1년의 차분**. 앱이 미션1 사후문항으로 가르치는 "한 해의 날씨 ≠ 기후"를 카드가 정면으로 위반한다.

**전수 계산 결과 dT ≤ 0 조합 60건**: 2024년생 **16지점 전부 음수**(−0.4~−1.1), 2025년생 **전부 0.0**, 2023년생 11곳, 그 밖에 2015·2016·2019·1998 등.

**실패 시나리오**: 2024년생 학생이 자기 지역을 넣으면 PNG에 *"내가 태어난 2024년 14.9℃ / 지금 14.2℃ **−0.7℃**"* 가 렌더되어 저장·공유된다. **앱의 유일한 외부 공유 산출물이 "내 고향은 태어난 뒤 시원해졌다"고 말한다.** 자기 출생연도를 넣는 것이 이 기능의 전체 목적이다.

부수 결함 2건: `:538` 카드 각주 "5년 관측 신호"는 단년 차분 dT에 틀린 설명 · `:501`이 `yrs`를 서울 기준으로 잡아 **경남+1969 입력 시 1970년 값을 "1969년"으로 무음 표기**.

**수정**: dT를 **출생연도 ±2년 평균 vs 최근 5년 평균**으로 교체. **작업량 2.5h**

---

### 2.7 [MEDIUM] 폭염·열대야를 화면에 꺼내놓고 검증 불가

`verify.js:173–179` `heatNote`가 thr≤24에서 *"실제 기상청은 **열대야**(밤새 최저 25℃↑)·**폭염**(낮 최고 33℃↑)으로 더위를 정의합니다 — 기준을 **조금 높이면**…"* 을 띄운다. 그러나 데이터에는 `avgTa/avgRhm/sumRn`만 있어 **최저·최고기온이 없다.** 앱이 검증할 수 없는 정의를 스스로 꺼내 심사위원 질문을 초대하고, 동시에 **상향을 유도해 §2.1의 허위 진술 구간으로 밀어 넣는다**(24→26에서 시차 소멸).

**수정**: 문구를 "일평균을 쓰는 이유 + 최저/최고는 다음 단계 로드맵"으로 교체하고 **상향 유도 문구 삭제**. **작업량 0.5h**

---

### 2.8 [MEDIUM] 재빌드 회귀 게이트 부재

`processing/verify_web_data.py`의 assert 9개가 **전부 구 자산**(`web_data/*.json`) 대상이고, **현 배포본이 유일하게 읽는 `prototype/solar_terms_data.js`에 대한 assert는 0개**다. `.github`(CI)도 `package.json`도 없다. `build_solar_terms.py:85`에 죽은 코드도 남아 있다.

**§2.1·§2.2 수정이 반드시 데이터 재빌드를 유발하므로 이 게이트를 먼저 세워야 한다.** 그러지 않으면 8/20 밤에 화면 수치·교사 학습지·데모 대본이 조용히 갈라진다. **작업량 3h — 순서상 최우선.**

---

## 3. P1 — 점수 상승 효과가 큰 것

| 항목 | 근거 | 문제 | 수정 | 시간 |
|---|---|---|---|---|
| **핵심 조작 없이 미션 통과 가능** | `verify.js:427–431`의 게이트는 `state.touched`뿐인데, `:249/:254/:259`에서 **도시·비교·절기 칩 클릭만으로도 true**가 되고 값 변화 검사가 없다 | 미션3에서 이미 켜진 '제주' 칩 1탭 → 판정 완료. **기준선을 움직인 적도, 강원 곡선을 본 적도 없다.** ②체험형의 근거 소실 | `state.thr0` 도입 → 판정 조건을 `thr !== thr0`로. 칩 플래그 분리 | 2h |
| **출처 고지 미비** | integrity 한 줄(`:244`)에 관측소 실명·지점번호·표본 수·평활 사실이 없다. '경기'·'강원' 칩은 실제로 **단일 관측소**(수원·춘천)인데 광역 이름으로 표시 | 계획서 §8.2가 요구한 *"지역이 실제 관측소인지 대표 지점인지 표시"* 위반. UHI·관측소 이전 효과가 기후 신호로 오독될 위험을 아무 곳도 고지하지 않음 | integrity 확장 + 칩·고향카드 병기 | 2h |
| **시그니처 제스처 품질 3종** | ① 예측 봉인 오버레이가 화면 밖에서 뜰 수 있음 ② y축이 겨울 최저까지 포함해 유효 드래그 밴드가 세로 높이의 일부뿐 ③ 히트영역이 좁음 | 심사위원이 가장 먼저 손대는 지점의 완성도 | `scrollIntoView` 1줄 + y축 재계산 + 히트영역 ±16px + 그립 렌더 | 3~4h |
| **사전/사후 개념측정 오염** | `:332` 인트로가 "과거 33일 → 현재 82일"을, `:299`가 "① 절기 ≠ 기후"를, `:432` `autoDemo`가 "+25일"을 **예측 봉인 전에** 노출. `:347` 튜토리얼은 정답 구분을 그대로 알려줌. `renderPre()`(`:283`)는 **호출되지 않는 죽은 코드** — "조작 전에 묻는" 경로가 실제로 없다 | `데모_대본.md:24`가 하이라이트로 내세운 "처음 생각 vs 지금 — **측정되는 학습**"이 측정하지 않는다. 계획서 498행이 이를 실패 사례로 못 박음 | `autoDemo`를 사전 응답 이후로, 사후문항을 다른 사례로 치환, 미션2 예측을 다른 축으로 | 3~4h |
| **문항 품질** | `:50` 미션1 자가진단과 `:62` 미션2 예측이 **동일 개념·동일 방향**이고, 그 사이 `:52` 해설이 정답 규칙을 이미 제공 | 미션2 '예측 봉인'이 선개념이 아니라 직전 해설의 재인. 미션2 오답지 2개("그날의 내 기분")는 기능하지 않고, 미션3은 2택(추측 50%) | 문항 교체 | (위 포함) |
| **학습 산출물 미저장** | 자가진단 응답·미션 판정문이 `state`에 저장되지 않음 | 교사가 걷을 수 있는 기록이 없다. ③학습효과 직결 | `state.selfChecks`/`missionDrafts` + 완료 화면 "내 기록 요약" 복사/인쇄 | 1.5h |
| **새로고침으로 산출물 소실** | `phase`에 `verdict`/`complete`가 없어 새로고침 시 판정 화면·고향카드가 사라짐 | 부스에서 한 대를 돌려 쓰는 시나리오에서 빈발 | phase 저장 + 부팅 분기 확장 | 1.5h |
| **이중 제품 노출** | `index.html:27`이 "데이터 보관실 →"을 상시 링크. `archive.html`은 "20개 사건 대시보드"라는 **다른 용어·다른 UI**의 완성 제품 | 발표 시간 소모 + 단일 제품 인상 희석. `:652`의 `?demo` 우회는 임시 대응 | 링크 제거 후 **완료 화면에서 "개발 아카이브"로 의도적 노출** — 계획을 좁혀 완성한 증거로 재프레이밍 | 1h |
| **세 미션의 과학적 대상이 1개** | 세 미션 전부 `metric:'temp'`, thr 25 (verify.js:37/59/78). 강수·습도는 자유탐구에만 | 계획서 §4 레드팀이 스스로 적은 *"세 사건이 같은 게임을 반복한다"*를 통과하지 못함. 단 조작 UI 자체는 칩 구성이 달라 '완전 반복'은 아님 | RC-A 완료 후 미션2를 '여름 길이 + 장마 이동' 2단으로 (본선 후 권장) | 8h+ |
| **교사 학습지 불일치** | `:46`이 "과거 33일 → 현재 82일 / +25일" 하드코딩(RC-A·RC-C 수정 시 갱신 필요), `:80`이 "활동 5"인데 활동은 3개, 루브릭 4점에 목표①·0점 열 없음 | ⑤실현가능성의 현장 교사 신뢰 지점 | 수치·문안 정정 | 0.5h |

---

## 4. P2 — 여유 시

| 항목 | 내용 | 시간 |
|---|---|---|
| 접근성 3종 | `setStage` 제목 포커스 이관 · `#stage` 전체 `aria-live` 축소(`#thrOut`로) · `prefers-reduced-motion` 게이트(인트로 애니메이션·`autoDemo`가 무시 중) · 탭 타깃 44px 미만 3건 | 2h |
| 견고성 위생 | `series()` fallback · `phase` 화이트리스트 + `renderPre` 삭제 · `pointerup` 리스너 정리 · `fetch`에 `AbortController` 12초 | 2.5h |
| 진행 표시 | `done.push`를 `revealVerdictActions`로 이동(현재 판정 진입 즉시 ✓ 점등) · 오답 시 "기준선 직접 끌어 확인" 버튼 | 1h |
| 용어 | 사용법 다이얼로그에 '기후평년' 정의 1항목(현재 학생 화면에 미정의) | 0.5h |
| API 운영 | `api/ai-turn.js`의 in-memory 레이트리밋(IP당 10분 18회)은 서버리스 인스턴스별이라 **우회 가능하면서 동시에 학교·행사장 공용 NAT에서는 한 반이 즉시 429**. 코드 수정보다 **발표 답변으로 대응** 권장 | — |

---

## 5. 심사 100점 추정 (내부 우선순위 정렬용 — 대외 사용 금지)

세부 루브릭은 비공개다. 확실한 것은 배점과 *"실현 가능성과 체험·참여형 설계에 무게를 둡니다"*이며, **이 두 항목 합계 55점 = 전체의 55%**다.

| 항목 | 배점 | 현재 추정 | 주 감점 요인 | 최효율 개입 | 개입 후 |
|---|---|---|---|---|---|
| ① 주제 적합성 | 20 | 16~17 | 지표 정의가 기상청 정의와 어긋남 · 표본 표기 · 폭염 검증 불가 | §2.7(0.5h) + 절기 라벨 정정(1h) | 19 |
| ② 체험·참여형 | **25** | 17~19 | 슬라이더 23% 죽은 구간 · 조작 우회 가능 · 제스처 품질 · 과학적 대상 1개 | **§2.1(6~8h)** + 제스처(3~4h) + 우회 차단(2h) | 22 |
| ③ 학습 효과 | 10 | 6~7 | AI 대필 · 과대주장 모델 문장 · 사전/사후 오염 · 산출물 미저장 | **§2.5(1.5h) + 산출물 저장(1.5h)** — 가성비 1위 | 9 |
| ④ 독창성 | 15 | 12~13 | 1지표 3회 · 이중 제품 · 카드 신뢰도 | 아카이브 재프레이밍(1h) + §2.6(2.5h) | 13~14 |
| ⑤ 실현 가능성 | **30** | 22~24 | 표기 무결성 · 재빌드 게이트 부재 · 계획서–구현 불일치 · 출처 고지 | **§2.3(3~4h) + 출처 고지(2h) + §2.8(3h)** | 27 |
| **합계** | 100 | **73~80** | | | **90~92** |

**시간당 점수 효율 순**: 절기 라벨(1h) → §2.5 AI 선입력(1.5h) → 출처 고지(2h) → 산출물 저장(1.5h) → **§2.1(6~8h, 절대 규모 1위 ~6점)** → §2.3(3~4h).

---

## 6. 26일 실행 순서

```
§2.8 검증 게이트 신설 (3h)   ← 반드시 먼저
   ├→ §2.3 완결 기간 정정 (3~4h)  ─┐
   └→ §2.1 실측 초과일수 교체 (6~8h) ├→ 재빌드 1회
                                    ├→ §2.4 미션3 판정문 (2~3h)  ← §2.1 후 수치 확정 필요
                                    ├→ §2.2 창 민감도 병기 (1.5h)
                                    ├→ 교사_학습지.html:46 수치 갱신
                                    └→ 데모_대본.md:12,13,17,40,41 수치·조작 지시 갱신
§2.5 / §2.6 / §2.7 / 절기 라벨 ── 독립, 병행 가능
```

| 기간 | 작업 | 누적 |
|---|---|---|
| 7/27 ~ 8/2 | **P0 파이프라인** (§2.8 → §2.3 → §2.1 → §2.4 → §2.2) | 18~22h |
| 8/3 ~ 8/8 | **P0 나머지** (§2.5, §2.6, §2.7, 절기 라벨, 문서 동기화) | 26~30h |
| 8/9 ~ 8/15 | **P1 전량** | 43~50h |
| 8/16 ~ 8/18 | P2 선별 + 버퍼 | 48~55h |
| **8/19** | **코드 동결** | — |
| 8/20 | 실기기 3대 리허설 3회 · 예상 질문 답변 암기 · **오프라인(AI 꺼짐) 시연 1회** | — |

> **코드 동결일을 8/19로 못박아라.** §2.1은 데이터 재빌드를 포함하므로 8/20 밤 수정은 검증 게이트를 건너뛰게 만든다.

**리허설 운영 필수 사항 (대본에 없음)**: 리허설을 하면 `state.pre`·`state.done`이 남아 `autoDemo`가 실행되지 않고(`verify.js:432`), `phase`가 `mission`이면 **부팅 시 인트로를 완전히 건너뛴다**(`:654–656`). 대본 [0–10초]의 "열자마자 임팩트"를 재현하려면 **매 시연 전 `↺ 처음부터`를 반드시 누를 것.** 또한 대본 12행은 인트로가 "더위 그치는 날 8/18 → 9/12 (+25일)"을 보여준다고 적었지만, 실제 인트로 카운터(`:332`)는 **더위일 수만** 표시한다.

---

## 7. 심사위원 예상 질문 8개

| # | 질문 | 현재 | P0 후 |
|---|---|---|---|
| 1 | "화면의 82일은 실제로 82일 더웠다는 뜻입니까?" | **답 못 함**(실측 72.5) | 답 가능 |
| 2 | "2026년 자료는 6/22까지인데 처서 값은 몇 년 평균입니까?" | **답 못 함**(4년인데 전 화면 '5년') | 답 가능 |
| 3 | "기준선을 28℃, 30℃로 올려 주세요"(대본이 스스로 지시) | **답 못 함**(28℃ '없음→8/11', 30℃ '없음→없음') | **최강 실연**(4.8→34.8, 0.6→7.5) |
| 4 | "강수 탭을 눌러 주세요. 서울에 비 온 날이 연 309일입니까?" | **답 못 함**(실측 82.8, 방향 반대) | 답 가능 + "비는 줄었는데 강도는?"이라는 새 학습 소재 |
| 5 | "왜 하필 이 5년입니까? 다른 5년을 쓰면?" | **답 못 함**(−1~+14일에서 흔들림, 화면에 언급 없음) | **강점으로 전환**(민감도 병기) |
| 6 | "제주 +21, 강원 +17 — 4일 차이로 지역마다 다르다고 단정할 수 있습니까? 22℃로 낮추면 강원이 더 큰데요" | **답 못 함** | 답 가능(절대 날짜 결론) |
| 7 | "기상청은 폭염을 일최고 33℃로 정의합니다. 여기서 폭염일수를 볼 수 있습니까?" | **부분** | 답 가능(문구 + 로드맵) |
| 8 | "AI 감사를 눌렀는데 규칙 기반으로 넘어갑니다. AI가 실제로 도는 겁니까?" | **부분** | 답변 준비로 대응: "규칙 점검이 1차, AI는 심화"를 **먼저** 말하고 오프라인 시연 1회를 대본에 넣을 것 |

---

## 8. 가장 냉정한 판단 — 대상을 놓친다면 그 이유

**독창성 부족이 아니라, 과학적 무결성을 제품의 대표 주장으로 내세운 팀이 자기 화면에서 과대주장을 하다 첫 2분 안에 잡히는 것이다.**

'절기의 약속을 57년 실측으로 검증한다'는 프레임은 대회가 제시한 예시 6종에 없는 진짜 신규 각도이고, 예측 봉인 게이트·CERL·AI 없이 작동하는 규칙 감사·96/100 평가세트는 본선 15팀 중 상위권 실행력이다. 그런데 이 제품의 유일한 조작 변수인 '평활된 5년 평년곡선 위의 임계선'이 동시에 모든 수치 오류의 발원지다 — 기본값에서 슬라이더를 **한 칸** 올리면 앱은 "과거엔 26℃ 이상 더위가 거의 없었다"고 **사실이 아닌 문장**을 쓰고(실측 21.4일/년), 강수 탭 **한 번**이면 "서울에 비 온 날 연 309일"(실측 82.8일, 방향도 반대)을 심사위원 눈앞에 놓고, 팀이 직접 쓴 대본이 지시하는 28→30℃ 조작은 화면을 비우며 발표자의 방금 한 말을 반박하고, "AI는 결론을 대신 쓰지 않습니다"라는 선언 직후 화면의 AI 감사 상자에는 앱이 써 준 완성 문장이 들어 있다. 여기에 헤드라인 "+25일"이 비교 가능한 어떤 5년 창(−1~+14일)이나 30년 창(+9일)의 **약 2배**라는 사실이 겹친다.

즉 **이 제품의 최대 차별점이 곧 최대 취약점**이며, "결론은 근거만큼만 쓰라"고 가르치는 앱이 자기 판정문에서 근거 4일 차이로 "지역마다 다릅니다"를 단정하는 순간 CERL·AI 감사관·교사 학습지의 정당성이 한꺼번에 무너진다.

구조적 불리함도 있다: 배점의 55%가 걸린 ②체험형·⑤실현가능성에서 기압·바람 맵이나 예보관 의사결정 시스템은 **메커니즘을 시뮬레이션**하는데 이 앱은 **두 곡선을 비교**한다. 조작 변수 1개는 "프레임 집중"으로 변호 가능하지만, **"조작하면 결과가 사라진다"는 변호 불가**다.

**다만 결론은 비관적이지 않다.** 위 오류들은 대부분 `exceed()`/`lastExceed()`를 평활 곡선에서 실측 초과일수로 바꾸는 6~8시간 작업에서 비롯되며, 그 수정 후 28℃는 4.8→34.8일(7.3배), 30℃는 0.6→7.5일(12.5배)로 **지금 죽어 있는 구간이 앱 최고의 실연 장면이 된다.** 26일은 충분하다. 위험은 시간이 아니라 **"수치 영향이 미미하니 라벨만 고치자"는 유혹**이다. 라벨만 고치면 심사위원은 정직한 앱을 보되 여전히 죽은 슬라이더와 방향이 반대인 강수를 보게 된다.

---

## 부록. 레드팀 내부에서 기각·하향한 주장 (정직 고지)

레드팀 1차에서 제기됐으나 재검증에서 **틀렸거나 과장으로 확인된** 주장들. 이것을 근거로 작업하지 말 것.

| 주장 | 재검증 결과 |
|---|---|
| "2026 포함 여부가 시차의 **부호를 바꾼다**(−0.4일)" | **틀림.** 연도별 실측 최종초과일을 6/22에서 잘린 2026에 적용한 부적절한 추정에서 나온 값. **앱 방식으로는 2022–26 ≡ 2022–25 = +26일.** 진짜 문제는 §2.2의 창 이동 민감도 |
| "절기 날짜 라벨 3개(입하 5/6, 입추 8/8, 상강 10/24)가 소수파" | **1개만 사실.** 1969–2026 태양황경 전수 계산: **상강만 명확한 소수파**(앱 10/24, 최빈 10/23, 24/58). 입하 29/58·입추 31/58은 앱 표기가 최빈. 2026년 기준으로 앱과 다른 절기는 5개(소한 1/5·경칩 3/5·입하 5/5·입추 8/7·상강 10/23). **미션에 쓰는 처서 8/23은 51/58로 정확.** 심각도 MEDIUM → **LOW**(계산에는 영향 없음, 표기·문구 문제) |
| "기온 초과일수 현재값이 +38% 과장" | **하향.** 2026년 173일을 5년 분모에 넣어 생긴 수치. **완결연도 기준 +13%**(33 vs 30.8, 82 vs 72.5)로 기온은 방어 가능. 부호 역전은 **강수·습도에서만** 발생 |
| "1969–73 강수 결측(50~66%)이 편향을 만든다" | **기각.** ASOS는 무강수일을 공란 처리하므로 `fillna(0)`이 올바른 처리. 결측률 순서가 지역별 강수일수 순서와 정확히 대응하고 과거·현재 차이 ≤5%p |
| "순환 15일 평활이 연말·연초에 불연속을 만든다" | **기각.** 12/31→1/1 값이 매끄럽게 이어지고 연중 최저점이 doy 23(1월 하순)로 물리적으로 타당 |
| "중복 id(`freeDraft` 등)가 충돌한다" | **기각.** `setStage`가 subtree를 통째 교체하므로 동시 존재 불가. 전 동선 실주행 확인 |
| "습도 관측 불균질이 신호를 오염" | **기각.** 단일 지배적 계단 변화 없고 최대 연간 변동 8.8%p가 16지점 자연 변동폭(5.2~9.6%p)과 동일 규모 |
| "`lastExceed` 연말 랩어라운드로 '12월 31일'이 표시된다" | **기각.** temp 최대는 제주 thr=20의 10/19. precip·humidity에서는 발생하나 `mc.showLast`로 화면 차단 |
| "전이(transfer) 과제 부재가 결함" | **기각.** 계획서 §5는 출시 전 관찰 프로토콜이며 기능 요구사항이 아님 |
