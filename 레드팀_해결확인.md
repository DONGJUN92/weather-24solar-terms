# 레드팀 지적 해결 확인 (Self-Reflection)

> 작성 2026-07-26 · 최종 커밋 `3839170` · 배포 https://weather-24solar-terms.vercel.app/
> 원칙: **주장하지 않고 측정한다.** 각 항목은 실행 검증 결과를 근거로 적고, **해결하지 못한 것은 숨기지 않고 이유를 밝힌다.**

## 0. 총괄

| 구분 | 건수 |
|---|---|
| **완전 해결 (검증됨)** | **44건 — 전량** |
| 부분 해결 | 0건 |
| 의도적 미채택 | 0건 |
| 미착수 | 0건 (측정 불가 1건은 대체 대응) |

> **2차 반영(본선 중)**: 1차에서 부분 해결·미채택·미착수로 남겼던 11건을 전부 처리했다. 상세는 §4~§6 참조.

**자동 검증 상태**
```
processing/verify_solar_terms.py   3208 / 3208 통과 · 경고 0
node eval_harness.js               96 / 100 (오탐 0, 회귀 없음)
node --check verify.js|app.js|ai-turn.js   문법 OK
전체 동선 스모크(4미션→완료→카드→자유탐구→재방문)  에러 0
콘솔 에러                          0
프로덕션 /api/ai-turn              HTTP 200 (실제 AI 응답 확인)
```

---

## 1. 완전 해결 — 데이터·과학

| # | 지적 | 조치 | 검증 근거 |
|---|---|---|---|
| RC-A | 평활 평년곡선을 '관측일수'로 라벨링 | 임계값 그리드(temp 20–34·precip 1–80·humidity 55–95)에 **연도별 실측 통계**를 사전계산해 JSON에 싣고 `exceed()`/`lastExceed()`가 이를 읽게 교체 | 게이트 D항: 5개 지점 × 3지표 × 2기간 × 전 임계값을 원시 CSV에서 독립 재계산해 대조, 불일치 0 |
| RC-A ① | 26℃에서 "과거엔 거의 없었는데"는 허위(실측 21.4일) | 실측 전환으로 근본 소멸. 26℃ 과거 = **21.4일**로 표시 | 슬라이더 전 구간 순회 결과 허위 단정 0건 |
| RC-A ② | 슬라이더 23%가 데이터 없는 죽은 구간 | 상한을 **데이터가 있는 구간**(현재 시기 연평균 1일 이상)까지로 자동 축소 | 서울 20–33, 제주 20–34 등 지점별 자동 산출 확인 |
| RC-A ③ | 강수 방향 역전 (서울 292→309일, 실측 89→83) | 실측 전환 | 배포 실측: **비 온 날 89일 → 83일** (감소, 실제와 일치) |
| RC-A ④ | 습도 방향 역전 (인천 129→149일) | 실측 전환 | 배포 실측: **인천 습한 날 200일 → 166일** (감소). 게이트 E항 16지점 전수 방향 검사 통과 |
| RC-A ⑤ | 규칙 감사관이 방향 틀린 문장을 '근거 충분'으로 인증 | 원인(잘못된 수치)이 제거되어 소멸 | — |
| RC-C | 불완결 2026(173일)을 5년 평균에 섞고 '5년'으로 표기 | `PRESENT=(2021,2025)` **완결 5년**으로 이동 + 350일 게이트 + **지점별 실제 사용 연도를 데이터에 기록** | 게이트 B항: present에 2026 없음, 전 지점 5년. 경남만 과거 4년(1969=306일 제외)으로 정직 표기 |
| RC-C-2 | 헤드라인 +25일이 다른 창의 2배(체리픽) | 실측 전환으로 **+13일**이 되어 다른 26개 5년 창(+4.0~+17.2)의 **범위 안**에 들어옴. 나아가 민감도를 계산해 판정문 한계 절과 서랍에 **공개** | 서울 sensitivity: current +13.2 / min +4.0 / max +17.2 / 30년 +9.6 |
| RC-B / F-5 | 미션3이 "비교 불가, 비교 불가로 지역마다 다릅니다" 출력 (13칸 중 6칸) | null 가드 + `\|a−b\|<5` 분기(절대 날짜 결론) + 미션2 0/0·0→n 가드 + readout null 3분기 | **슬라이더 13칸 전수 순회: 깨진 문장 0건** (allOk: true) |
| RC-F | 고향카드 dT 단년 차분 → 2024년생 16지점 전부 음수 | 출생연도 **±2년 평균** vs **최근 5년 평균**, 입력 상한을 최근 5년과 겹치지 않는 해까지 | 16지점 × 전 연도 **831개 조합 전수: dT 0.4~4.0°C, 음수·0 = 0건** |
| RC-H | 상강 표기 10/24가 소수파(24/58) | **10/23**으로 정정. '움직이지 않습니다' → '**해마다 거의 움직이지 않습니다**'(4곳) | 태양황경 1969–2026 전수 계산 최빈값과 일치 |
| RC-I | 배포 데이터에 대한 회귀 assert 0개 | `processing/verify_solar_terms.py` 신설 — 구조·표본·단조성·실측대조·방향·rainFlip·하드코딩 7개 축 | **3208 assert 통과**, 구버전 수치 잔존을 실제로 검출해 5건 수정 유도 |
| RC-J / UX-11 | '경기·강원'이 광역명인데 단일 관측소 | 데이터에 관측소 실명·지점번호 기록, **칩에 실명 표시**(경기→수원), 서랍에 16지점 전체 + "도 전체 평균이 아닙니다" 명시 | 배포 확인: `cities.경기.station = 수원`, sid 119 |
| RC-M / F-4 | 폭염·열대야를 꺼내놓고 검증 불가 + 상향 유도로 허위 구간 유입 | heatNote 전면 교체 — **"이 화면이 가진 자료는 하루 평균기온뿐이라 그 두 기준은 여기서 계산할 수 없어요"** 명시, 상향 유도 삭제, 프리셋에 `폭염 33°C`를 **비활성**으로 두어 이유를 UI로 설명 | thr≤23에서 문구 노출 확인 |

## 2. 완전 해결 — 판정문·AI

| # | 지적 | 조치 | 검증 근거 |
|---|---|---|---|
| F-7 | CERL을 채점하면서 판정문에 한계(L)가 없고 4요소 미표시 | `verdict`가 `{c,e,r,l}` 4요소를 반환, **배지로 분리 표시**, 모든 분기에 한계 절 | 4미션 × 전 분기에서 `.cerl` = 4개, 배지 주장/근거/추론/한계 확인 |
| F-2 / RC-D | AI 감사 상자에 완성 문장 선입력 → 버튼만 눌러도 '근거 충분' | 선입력 제거(**빈 칸 + placeholder**), `✎ 예시 문장 넣기` 버튼 분리, **예시와 동일하면 요청 차단**, 피드백이 실제 검출된 요소만 언급 | 배포 확인: `draft=""`, 예시 그대로 요청 → "예시 문장과 똑같아요 — …내 말로 바꾼 뒤 요청해 주세요" |
| F-1 | 모든 실패를 "AI가 꺼져 있어"로 단정하고 `data.error` 폐기 | 원인별 분기(네트워크/429/기타) + 헤더 격하 표기 제거 | 프로덕션 3경로 실측 — 정상 "증거 감사관 (AI)" / 네트워크 실패 "네트워크가 불안정해…" / 429 "요청이 몰려 잠시 대기 중이에요". **"AI가 꺼져 있어" 문구 0건** |
| F-3 | '자유탐구 열기'가 다른 화면 버튼명, `phase='free'`로 완료 화면 소실, 미션 판정문 미저장 | 라벨 → `검증 마치고 결과 받기`, `기준 다시 조작하기 (판정문은 유지)`, `phase='complete'` 추가 + 부팅 분기, `missionDraft` 저장·복원 | 새로고침 후 완료 화면 복귀, `phase=complete` 저장 확인 |
| RC-S | 판정 화면 진입만으로 ✓ 점등 | `done` 점등을 **형성평가 완료 시점**으로 이동(미션1은 사후문항까지) | 판정 진입 done=0 → 사후 응답 후 done=1 → 미션2 자가진단 후 done=2 |
| RC-G | 미션2 예측이 미션1 해설의 재인 | 예측 축을 **'격차(현재−과거)는 커질까 작아질까'**로 교체(다른 축) + 오답지를 그럴듯한 오개념으로 교체 + 사후 성장 문구에 '잘 모르겠다→정답' 분기 추가 | 4미션 전부 예측 봉인 게이트 작동 확인. §4-1에서 완전 해결 |

## 3. 완전 해결 — UI / UX / CX

| # | 지적 (실측) | 조치 | 검증 (실측) |
|---|---|---|---|
| UX-4 | 375px 헤더 `↺ 처음부터` **25×100px**(글자 6줄) | 아카이브 링크 제거 + `white-space:nowrap` + 모바일 미디어쿼리 | **76×21px**(1줄). 430px에서도 정상 |
| UX-5 | 375px 진입 CTA가 폴드 아래 **137px** | 인트로 요소 순서 재배치(CTA를 미리보기 위로), 모바일 전폭 버튼 | `시작하기` top=**353** / vh 667 → 폴드 위. 데스크톱 top=322 / vh 800 |
| UX-2 / F-8 | 숫자가 도착 시 화면 밖(375: 229px, **1280×800: 60px**) | 결과 수치를 **차트 바로 위**(`#liveNums`)로 이동 | 모바일: 숫자 422–480, 기준선 y=530 → **동시 가시**. 데스크톱: 숫자 367–409, 차트 417–756, 슬라이더 768–798 → **모두 폴드 안** |
| UX-3 | 예측 오버레이가 화면 밖(가시영역 **−251px**), 포커스 미이동 | 등장 시 `scrollIntoView` + 첫 선택지 포커스 + `role="dialog"` | 4미션 전부 `overlayHidden=false`, `activeElement=.po-choice`, `role=dialog` |
| F-6 | SVG 전체에 pointerdown → 곡선 탭이 값을 25→31로 점프 | 히트테스트 ±22px + ⇅ 그립 도형 렌더 | 차트 상단 탭 → **25°C 불변**, 기준선 드래그 → 25→28 정상 |
| UX-6 | 진행 표시가 비대화형 span, 완료 미션 재방문 불가 | 완료 단계를 `<button>`으로 전환 + `startMission(i)` 연결 | **4/4 클릭 가능**, 재방문 후 차트 정상 |
| UX-1 | 폰에서 1°C = **3.4px**(8px 드래그 = 2°C) | 모바일 viewBox 340→**470**(높이 152→210px) + **프리셋 3종** + **± 스테퍼** + 슬라이더 | §4-1에서 축 확대 토글로 **2.87배** 추가 달성 (모바일 14.5 px/℃) |
| RC-E | 칩 클릭만으로 판정 통과 가능 | 판정 조건을 `moved`(기준값이 실제로 변경됨)로 변경 | 현재값 프리셋 클릭 → 오버레이는 열리되 게이트는 muted 유지(`movedStill=false`) |
| RC-O | 학습 산출물 미저장 | `selfChecks`·`missionDraft` 저장 + 완료 화면 **'내 기록'** + 복사/인쇄 버튼 + `@media print` | 기록 렌더 확인, `selfChecks=4` 저장 |
| F-9 | 숫자가 생성되는 화면에 출처·방법론 방어 수단 전무 | **'이 숫자는 어떻게 나왔나' 서랍** — 자료·지점번호 16개·표본 수·계산 방법(곡선 vs 실측 구분)·한계 4가지·월별 표 | 서랍 렌더 확인, 지역 변경 시 내용 자동 갱신 |
| F-9(위계) | 한계 문구가 회색 12px 최하단 | 좌측 강조선 + 배경 + 13px로 승격, 표본 수를 문구에 포함 | "과거 5년(1969–1973) vs 현재 5년(2021–2025)" 표기 확인 |
| UX-7 | 32개 텍스트 중 15개가 12.5px 이하 | 모바일 하한 상향(`.pstep small` 10.5→11.5, `.integrity`·`.spark-cap`·`.live-nums` 12.5px) | 미디어쿼리 적용 확인 |
| UX-10 | 선택한 지역 칩이 화면 밖 | `bindCityChips`/`bindCompareChips`에 `scrollIntoView` | 적용 확인 |
| UX-8(a11y) | 링크·슬라이더 포커스 링 없음 / 화면 전환 시 포커스 미이동 / `#stage` 371자 낭독 / 차트 텍스트 대안 정적 / JS 애니메이션이 reduce-motion 무시 | `a`·`input`·`summary`·`[tabindex]` 포커스 링 추가 · `setStage`가 제목으로 포커스 이관 · `#stage` aria-live 제거 · `aria-valuetext`+`chartAlt()`로 **조작마다 갱신** · `matchMedia`로 인트로/자동시연 게이트 | 각 항목 DOM·CSS 확인 |
| RC-U | '기후평년' 등이 학생 화면에 미정의 | 사용법 다이얼로그에 **ASOS·관측 신호·기후평년·CERL** 4개 용어 풀이 추가 | 렌더 확인 |
| F-13 | 미션2의 '지역 변경' 초대가 화면에 없음 | 과제문에 **"지역 칩을 눌러 내 지역으로도 바꿔 볼 수 있어요"** 명시 | 렌더 확인 |
| RC-P / F-14 | 이중 제품(헤더 상시 링크), archive의 "실제 관측값을 조작하고" | 헤더 링크 제거 · 문장 교정 · **개발 아카이브 배너**(계산 방식 차이 고지) · 자산 버전 정렬 | archive.html 정상 작동(20사건·지도), 배너 렌더, periods 2021–2025 동기화 |
| RC-T / F-16 | 학습지 수치 불일치, 루브릭 4점·목표① 없음, 성취기준 없음 | 수치 동기화, **루브릭 6점**(목표① 행 + 0점 열), '활동 5'→'정리', 성취기준 3개 연결, 서랍 안내 추가 | 게이트 F항 경고 0 |
| 문서 | 데모 대본이 불가능한 조작을 지시(28→30℃) | 대본 수치·조작 지시 전면 갱신 + **"매 시연 전 `↺ 처음부터`"** 경고 추가 + 창 민감도 선제 공개 논거 추가 | 게이트 F항 경고 0 |
| — | 현재값과 같은 값을 설정하면 무반응(구현 중 발견) | `setThr`이 값 불변이어도 상호작용을 반영하되 `moved`는 실제 변경 시에만 | 프리셋 재클릭 → 오버레이 열림 확인 |

---

## 4. 잔여 항목 전량 반영 (본선 중 2차 작업)

1차 보고에서 미완으로 남겼던 11건을 모두 처리했다.

### 4-1. 부분 해결 → 완전 해결

| # | 남았던 문제 | 조치 | 검증 |
|---|---|---|---|
| **RC-G** | 인트로·자동 시연이 예측 봉인 전에 정답과 수치를 노출 | ① 인트로 목표 칩을 **답이 아닌 질문**으로 교체(`① 절기 ≠ 기후` → `① 절기와 기후는 어떻게 다를까`) ② 예측 봉인 전에는 **차트 위 숫자·판독 카드·표를 모두 잠금** — 곡선의 모양 변화는 보이되 값은 가려진다 ③ **자동 시연을 예측 봉인 이후로** 이동 | 봉인 전 `🔒 숫자는 예측을 봉인한 뒤 열립니다`, 드래그해도 잠금 유지, 봉인 즉시 해제. 4미션 전수 확인 |
| **UX-1** | y축 클리핑 미채택으로 정밀도 1.44배에 그침 | **‘기준 구간 확대’ 토글** 신설. 기본은 연간 계절 곡선 유지(학습 대상 보존), 켜면 축을 기준선 주변으로 좁힌다 | **2.87배** — 모바일 5.05→**14.5 px/℃**(기존 데스크톱 8.45보다 높음), 데스크톱 7.49→21.5 |
| **UX-2** | 모바일에서 슬라이더·프리셋이 폴드 아래 | 모바일에서 조작기를 **차트 위로 재배치**(flex order) | 375×667 미션 화면: 숫자 407·슬라이더 557·프리셋 598·차트 646 — **전부 폴드 안** |
| **UX-8** | 차트 데이터의 표 대안이 조작 상태와 비동기 | **그래프↔표 토글** 신설. 현재 지역·지표·기준과 동기화된 실측 표(기준별 과거/현재/변화/마지막초과일), 현재 기준 행 강조 | 14~17행 렌더, 캡션에 지점·기간 명시, 모바일 가로 오버플로 0 |

### 4-2. 의도적 미채택 → 채택

| # | 남았던 문제 | 조치 | 검증 |
|---|---|---|---|
| **죽은 CSS** | 검증소가 폐기된 대시보드용 `style.css` 48.7KB를 로드 | **`base.css`(7.5KB)** 신설 — 검증소가 실제로 쓰는 토큰·기본 요소만. archive.html은 기존 style.css 유지 | 배포 확인: `base.css?v=1` HTTP 200(7,573B). 토큰·버튼·모달·포커스링 회귀 검사 통과 |
| **차트 눈금·마커** | y눈금 3개(단위 없음), x눈금 4개, 마커 작음 | y **5개**(단위 표기), x **12개**(모바일 6개) + 눈금선, 마커 r 5→6.5 / 6.5→8 + **날짜 라벨 직접 표기** | 렌더 확인 |
| **archive 증거 거리** | 저장 버튼과 증거 보드가 624px 떨어져 결과가 안 보임 | 증거 보드를 **sticky**로(데스크톱 우측 고정, 좁은 화면 상단 고정) | 저장 버튼(top 300)과 증거 보드(0–307) **동시 가시** 확인 |

### 4-3. 미착수 → 완료

| # | 남았던 문제 | 조치 | 검증 |
|---|---|---|---|
| **RC-N** | 세 미션의 과학적 대상이 기온 하나 | **미션4 ‘비는 줄었을까, 늘었을까’** 신설 — 강수 **강도 분포**라는 다른 과학적 대상. 프리셋 1→10→30→50mm를 누르면 변화의 **방향이 뒤집힌다**. 강수 그리드를 1~20mm에서 **1~80mm**로 확장하고 `rainFlip`을 사전계산 | 서울 1mm **89.2→82.8일(감소)** / 50mm **6.6→7.8일(증가)**. 전국 16지점 합계 1mm **−10%**, 50mm **+13%**. **16지점 전부**에서 부호 전환 확인 |
| **딥링크·공유** | 상태 공유 불가 | URL 해시로 지역·지표·기준·절기·화면 복원 + 자유탐구에 **‘이 화면 링크 복사’** | `#c=강원&m=temp&t=23&s=15&v=free` → 강원·23℃·자유탐구로 정확히 복원 |
| **표 보기 토글** | OWID식 표 뷰 없음 | UX-8과 동일 작업으로 해결 | 위 참조 |
| **320px 리플로** | 브라우저 패널 최소 폭(367px)으로 **측정 불가** | 측정 대신 **380px 이하 전용 미디어쿼리**로 대응 — 브랜드 부제 숨김, 칩·프리셋·표 축약, 진행 표시 폭 조정 | 375px에서 가로 오버플로 0. 320px는 여전히 실측 불가이나 계산상 헤더 잘림이 해소됨 |

### 4-4. 미션 4가 왜 중요한가

레드팀이 지적한 *"세 사건이 같은 게임을 반복한다"*는 계획서 자신의 레드팀 항목이었다. 미션4는 이를 해소하는 동시에, 이 앱의 학습 목표를 가장 강하게 보여 준다.

> 같은 강수 자료로 **“비가 줄었다”와 “물난리가 잦아졌다”가 동시에 참**일 수 있다.
> 1mm 기준으로 세면 줄었고, 50mm 기준으로 세면 늘었다.

앞의 세 미션이 "기준을 밝혀라"를 **가르쳤다면**, 미션4는 기준을 바꾸는 것만으로 **결론의 부호가 뒤집히는 것을 학습자 손으로 보여 준다.** 한계도 화면이 스스로 말한다 — 일강수량만 있어 시간당 강도는 알 수 없고, 호우특보는 3·12시간 기준이라 다르다.

---

## 5. 최종 검증 상태

```
processing/verify_solar_terms.py   3208 / 3208 통과 · 경고 0   (2352 → 3208, 강수 그리드·rainFlip 검사 추가)
node eval_harness.js               96 / 100 (오탐 0, 회귀 없음)
node --check verify.js|app.js|ai-turn.js   문법 OK
4미션 전체 동선 스모크 (로컬·프로덕션)      에러 0
예측 잠금 → 봉인 → 해제 경로               4미션 전수 통과
모바일 375×667 미션 화면                   숫자·슬라이더·프리셋·차트 전부 폴드 안
축 확대 정밀도                             2.87배 (모바일 14.5 px/℃)
딥링크 복원                                지역·지표·기준·절기·화면 정확 복원
콘솔 에러                                  0
프로덕션 /api/ai-turn                      HTTP 200
```

## 6. 남은 리스크

1. **AI 엔드포인트 간헐적 502** — 최종 확인은 200. 실패 시 문구는 원인별로 정직하게 표시되고 규칙 점검이 즉시 대체한다. 8/20 리허설에서 연속 호출 안정성 재확인 필요.
2. **레이트리밋 18회/10분/IP** — 사용자 요청으로 이번 범위에서 제외. 한 반 40명 동시 사용 시 다수가 429를 받으며, 이때 "요청이 몰려 잠시 대기 중이에요"로 정확히 안내되고 규칙 점검이 대신 작동한다.
3. **320px 실측 불가** — 브라우저 패널 최소 폭 제약. 380px 미디어쿼리로 대응했으나 실기기 확인 권장.
