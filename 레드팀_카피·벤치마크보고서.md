# Weather24 레드팀 3라운드 통합 보고서
### 차원: 글로벌 벤치마크(bench) × 한국어 카피(copy) — 반박 검증 후 확정분만

대상: `C:\신대리 활동\공모전\완료\기상·기후 AI 해커톤` · 배포 https://weather-24solar-terms.vercel.app/
전제: 1차(데이터·과학)·2차(UI/UX) 확정 사실은 재발견하지 않고 활용했다. 본 라운드에서 파일을 직접 열어 인용·행번호를 재확인했다(verify.js 657행, verify.css 238행, index.html 49행, archive.html, api/ai-turn.js, processing/, data_collectors/).

---

## 0. 한 줄 판정

> **이번 두 차원이 찾아낸 것의 90%는 "코드가 아니라 문장"이다.** 순수 신규 P0는 합쳐 **3.5~4.5시간**이며 앞선 P0(데이터 24~28h + UX 11~13h)와 거의 겹치지 않는다. 가장 값싼 득점원은 **방법론 서랍 하나(2.5~3.5h)** — 1차 라운드가 찾아낸 과학 결함을 "숨긴 오류"에서 "공시된 한계"로 바꾸는 유일한 저비용 수단이다. 가장 위험한 신규 결함은 **AI 폴백 문구 한 줄(`verify.js:643`)** 로, AI 해커톤 무대에서 팀의 최대 방어 카드를 자기 손으로 부채로 뒤집는다. 반면 두 차원이 제기한 주장 중 **11건은 실측으로 반박되어 하향·기각**했다(§8).

---

## 1. 이번 라운드의 회계 — 신규 / 중복 / 반박

| 구분 | 건수 | 합계 시간 | 비고 |
|---|---|---|---|
| **순수 신규 P0** | 8건 (F-1~F-8) | **3.5~4.5h** | 앞선 P0와 독립 또는 스톱갭 |
| **순수 신규 P1** | 5건 (F-9·F-13·F-14·F-11·F-12) | **5.5~7h** | F-9가 절반 |
| 앞선 P0/P1에 **흡수**(이중 계상 금지) | 6건 | +0h | F-10(UX-1 C안), F-15(UX-6), F-5 일부(1차 2.4), F-2(1차 2.5), F-9 일부(1차 "출처 고지 2h"), GB-08(UX-1 C안) |
| **실측 반박·강도 하향** | 11건 | — | §8 정직 고지 |
| 취향으로 재분류(결함 아님) | 3건 | — | 은유 프레임 수, 합니다:해요 비율, '당신' 호칭 |

---

## 2. 병합 — 두 차원이 같은 뿌리를 지목한 곳

요구사항 3의 핵심. 벤치마크가 "구조가 없다"고 말한 곳과 카피가 "문장이 틀렸다"고 말한 곳이 **동일 코드 지점**에서 만난다.

| 클러스터 | 근본 원인 | bench 항목 | copy 항목 | 앞선 라운드 | 통합 처방 |
|---|---|---|---|---|---|
| **M-1 조작–결론 비공존** | 단일 수직 스택 + 초기 스크롤 프레이밍/자동 스크롤 부재 | GB-01 | — | 2차 UX-2(CRITICAL) | 스톱갭 F-8(20분) → 본수정은 UX-2(2~3h, RC-A와 묶음) |
| **M-2 임계값 픽커 4층 결핍** | `state.thr` 하나에 축 기하학·정지점·어포던스·경계 문안이 전부 미비 | GB-03·GB-05·GB-08 | H2·H3 | 2차 UX-1(C안 4~5h) / 1차 2.4·2.7 | F-6(히트테스트 40분) + F-5(가드 25분) + F-4(문안 35분) → P1 F-10(프리셋) → UX-1 C안 흡수 |
| **M-3 한계가 공시되지 않고, 공시된 한계는 12px 회색 최하단** | 방법론 서랍 부재 + 무결성 문구 위계 역전 + 용어 미정의 | GB-04·GB-09 | M1 | 1차 2.1·2.3, "출처 고지 2h" | **F-9 단일 작업**(서랍 + 무결성 승격·정정 + 용어 풀이 3곳) 2.5~3.5h |
| **M-4 AI 층의 자기채점** | 앱이 정답을 선입력 → AI/규칙이 그 문장을 칭찬 | — | H1 | 1차 2.5(HIGH 1.5h) | F-2에 통합(선입력 제거 + 예시 토글 + 시드 동일성 게이트) |
| **M-5 폴백 카피가 강점을 결함으로 보고** | catch 하나가 429/502/503/네트워크를 "AI가 꺼져 있어"로 단정, 서버 메시지 폐기 | — | H6 | 없음(신규) | F-1(25분) |
| **M-6 산출물 소실** | 미션 감사 textarea 미저장 + `phase='complete'` 부재 | GB-02 | C2 | 1차 "산출물 미저장"(P1) / 2차 UX-6 | F-3(20~40분) + P1 F-15(딥링크, UX-6 흡수) |
| **M-7 텍스트 대안** | 차트 aria-label 정적·범례 aria-hidden | GB-06 | — | 2차 UX-8 | P1 F-12(1h) |
| **M-8 세계관 분열·이중 제품** | 같은 대상 5종 명칭 + archive.html 상시 링크 | — | M2 | 1차 P1 "이중 제품 1h" | P1 F-14(20분, 1차 항목과 병합) |

---

## 3. '글로벌 최고' 최소선 5개 — 근거 URL과 현재 격차

교육용 인터랙티브에서 **URL로 확인한 5곳**(PhET, Our World in Data, En-ROADS, NOAA Climate.gov, Met Office for Schools)에서 예외 없이 지켜지는 패턴만 골랐다. (1차 인용에 등장한 NASA는 URL이 제시되지 않아 근거에서 제외했다 — §8-1)

| # | 최소선 | 레퍼런스 근거 (확인한 URL) | Weather24 현재 (파일:줄 / 실측) | 격차 | 항목 |
|---|---|---|---|---|---|
| **1** | **조작 중에는 조작기와 결론 수치가 같은 화면** | PhET Greenhouse Effect를 375×667로 로드해 `document.documentElement.scrollHeight = 667`, `body{overflow:hidden}` 실측 — https://phet.colorado.edu/sims/html/greenhouse-effect/latest/greenhouse-effect_all.html · En-ROADS는 슬라이더 19개와 100+ 그래프를 한 대시보드에 두고 "update as you move sliders" — https://docs.climateinteractive.org/projects/en-roads/en/latest/guide/tutorial.html | `#readouts`가 도착 시 폴드 밖(375·1280 모두). 첫 조작 후 자동 스크롤 없음 — `bindThreshold` `verify.js:271-277`에 `scrollIntoView` 0건(앱 내 scrollIntoView는 `:260`·`:429`뿐) | **미달** | GB-01 / UX-2 |
| **2** | **의미 있는 프리셋 또는 값 직접 입력 + 축 줌 모드** | PhET 병렬 DOM에 `radiogroup "Time Period"` = Ice Age/1750/1950/2020, 별도 `radiogroup "Experiment Mode"` = By concentration / By time period, 그리고 `Energy Flux Range Zoom In/Out` 버튼 실존(같은 URL) · En-ROADS: "directly input numeric values to set the slider level to a specific value of your choice (within range)"(같은 docs URL) | 프리셋 0개, 숫자 입력 0개, 축 줌 0개. `#thrRange` min=20 max=32 step=1. `bounds()`(`verify.js:115-119`)가 서울 y축을 −6.24~32.04(38.28°C)로 잡아 **조작 가능 구간이 축의 31%**, 375px에서 1°C = 3.41 CSS px | **미달** | GB-03·GB-08 / UX-1 |
| **3** | **표 보기 + 출처·방법론 서랍** | OWID: "view the same dataset in multiple ways: as a chart, a color-coded map, and a spreadsheet-like table", 전환 버튼은 좌상단, 다운로드는 우하단, 'Learn more about this data'가 오버레이를 연다 — https://ourworldindata.org/redesigning-our-interactive-data-visualizations · https://ourworldindata.org/easier-to-reuse-our-data · 캡션이 스키마 1급 필드(subtitle/note) — https://ourworldindata.org/grapher/monthly-average-surface-temperatures-by-year.config.json · NOAA는 관측망(GHCN-D)·품질검사·5×5km 격자·보간법·Reviewer까지 문답 서랍으로 공개 — https://www.climate.gov/maps-data/data-snapshots/data-source/temperature-us-monthly-average | `<table>` 0개. verify.js에 CSV·"방법론" 0건. 관측소 ID·15일 평활·표본 수 어디에도 없음. 차트 화면의 메타 정보는 범례(`verify.js:233`, aria-hidden)와 무결성 1줄(`verify.js:244`, 12px)뿐 | **미달** | GB-04·GB-09 |
| **4** | **조작기의 어포던스 3층: 히트영역·정성 텍스트(aria-valuetext)·키보드 안내** | PhET 슬라이더는 진짜 `input[type=range]`에 `aria-valuetext="Moderate levels of greenhouse gases."`, 컨트롤별 도움말 "Adjust amount of greenhouse gases in the atmosphere.", 내비에 `Keyboard Shortcuts` 버튼, 발견 유도 문장 "If needed, check out keyboard shortcuts under Sim Resources."(PDOM 실측, 위 sim URL) | `verify.js:273`이 **SVG 전체**에 `pointerdown` → 노란 처서 라벨을 눌러도 `#thrOut`이 25°C→31°C로 점프(재현 확인). 그립/핸들 도형 0개, `aria-valuetext` 0건, 키보드 도움말 0개. `style.css:2`의 `:focus-visible`은 `button/select/textarea`만(직접 확인) → a·range는 UA 기본 링만 받아 앱의 3px 주황 링과 불일치 | **미달** | GB-05 / UX-9 |
| **5** | **차트 데이터의 텍스트 대안이 조작에 따라 갱신** | PhET 관측창 안에 라이브 서술 목록: "Carbon dioxide 277 parts per million", "The sky is cloudy.", "The surface temperature is -28.1 degrees Celsius.", 범례도 문장("Sunlight radiation: yellow waves, wavelength shorter than infrared")(PDOM 실측, 위 sim URL) | `#heroSvg` aria-label = "과거와 현재의 하루 관측 곡선, 고정된 절기 세로선, 드래그 가능한 기준선" — 정적, 임계값 25→31 후에도 동일. 범례는 `verify.js:233`에서 `aria-hidden="true"` | **부분 미달**(핵심 수치는 `#readouts`·`#thrOut`·`#stage`의 `aria-live="polite"`로 낭독됨 — §8-8) | GB-06 / UX-8 |

**6번째 후보(준-최소선, 이번 라운드는 medium으로 하향)**: 상태 딥링크·스냅샷 공유. OWID는 쿼리에 상태를 인코딩(`?tab=table`, `?tab=chart&country=JPN~KOR~TWN~…`), En-ROADS는 "You can share your unique scenario link with others, who can open your En-ROADS scenario with all the settings you have chosen"(docs URL). Weather24는 배포된 `verify.js?v=15`에 `URLSearchParams`·상태용 `location.hash` 0건, 유일한 쿼리 사용은 `verify.js:652`의 `?demo`(보관실 링크 숨김). **단 localStorage 복원은 실제로 작동**하므로 "데모 사고 복구 불가"는 성립하지 않는다(§8-7). 남는 실질 손실은 **교사의 링크 배부**와 **모둠 간 링크 비교**뿐이다.

**Met Office for Schools**(https://weather.metoffice.gov.uk/learn-about/met-office-for-schools)는 최소선 표에 넣지 않았다. 대상이 7–14세('First explorations' 7–11 / 'Deeper discovery' 11–14)로 Weather24의 중3~고2보다 아래여서 **톤을 가져오면 유치해진다**. 가져올 것은 패키징 규범 하나('Educator guide' + 'curriculum-linked resources'를 명시적 산출물로) 뿐이다.

---

## 4. P0 — 8/21 전 반드시 (신규분, 합계 3.5~4.5h)

### F-1 [P0·25분] AI 폴백 문구가 원인을 허위로 단정하고 서버 메시지를 폐기한다 — **이번 라운드 최대 신규 결함**

**근거**
- `verify.js:637-644`의 `try` 하나가 fetch 실패·`!res.ok`·`!data.feedback`·JSON 파싱 실패를 모두 받아 `:643`에서 `'AI가 꺼져 있어 규칙 기반으로 점검했어요 — 동일한 항목(범위·과장·인과)을 확인합니다.'`를 출력한다.
- 서버는 원인을 **이미 구분해 보낸다**: `api/ai-turn.js:138` → 429 `"잠시 후 다시 시도해 주세요."`, `:139` → 503 `"AI 탐구 파트너가 아직 설정되지 않았습니다."`, `:164`·`:174` → 502 `"AI 탐구 파트너가 응답하지 않았습니다…"`. 클라이언트가 `data.error`를 버리고 자기 추측으로 덮어쓴다.
- **배포 엔드포인트는 200 정상 응답**한다(실측 POST 1회, AI feedback 반환). 즉 API 키는 설정돼 있고, 이 문구는 **화면에 뜨는 모든 순간에 거짓**이다.
- 교실 발생 확률: `api/ai-turn.js:1-3` `WINDOW_MS = 10*60*1000`, `MAX_REQUESTS = 18`, `:91-98`이 `x-forwarded-for` 기준. NAT 뒤 40명 학급이 동시에 누르면 다수가 429.
- `renderAudit`(`verify.js:580`)이 헤더를 `'증거 점검 (규칙 기반)'`(정상: `'증거 감사관 (AI)'`)으로 **격하 표시**한다.
- `데모_대본.md:28`은 이것을 능동 강점으로 발표하라고 지시한다 — 대본은 "작동해", 화면은 "꺼져 있어".

**심사 영향** ①주제적합성·⑤실현가능성. AI 해커톤에서 "AI가 꺼져 있어" 7글자는 즉시 "이 팀 AI 안 돌아가네"로 읽힌다. 발표자가 화면을 가리키며 강점을 말할 수 없다.

**붙여넣기 문안**

`verify.js:637-644` 교체:
```js
    var res = null;
    try {
      res = await fetch('/api/ai-turn', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) });
      var data = await res.json();
      if (!res.ok || !data.feedback) throw new Error('ai');
      renderAudit(data.feedback, false); status.textContent = 'AI 감사 완료 — 필요하면 판정문을 고쳐 다시 요청하세요.';
    } catch (e) {
      renderAudit(localAudit(draft), true);
      status.textContent = !res ? '네트워크가 불안정해 규칙 점검으로 확인했어요 — AI와 같은 항목(범위·과장·인과)을 봅니다.'
        : res.status === 429 ? '요청이 몰려 잠시 대기 중이에요 — 규칙 점검으로 같은 항목(범위·과장·인과)을 방금 확인했어요.'
        : '지금은 규칙 점검으로 확인했어요 — AI와 같은 항목(범위·과장·인과)을 봅니다.';
    } finally { btn.disabled = false; }
```
`verify.js:580` 헤더 통일(격하 인상 제거):
```
증거 감사관 (규칙 점검)
```
사전 상태문 2곳을 **동일 문장·동일 어체**로 (`verify.js:486`, `verify.js:561` 모두):
```
판정문을 쓰면 과장·범위·인과를 점검합니다. AI가 응답하지 않아도 같은 항목을 규칙 점검이 확인합니다.
```

---

### F-2 [P0·25분 / 1차 2.5와 동일 작업] 완성 문장 선입력 + "고쳐 써도 좋아요"가 쓰기 과제를 무력화한다

**근거**
- `verify.js:486` 라벨: `내 판정을 한 문장으로 <small>고쳐 써도 좋아요</small>` → `verify.js:487`이 `$('freeDraft').value = buildDraftSeed(m)`로 **완성 정답 문장을 선입력**.
- 대조: 자유탐구 `verify.js:559-560`은 라벨 `'내 판정문 — 지역·기간·기준을 넣어 한 문장으로'` + **빈 textarea** + placeholder 예시. **강한 카피가 선택 화면에, 약한 카피가 전원 통과 경로에** 있다.
- 양쪽 감사 경로에서 "한 글자도 쓰지 않고 통과"를 검증했다. 규칙 경로: 시드가 `hasLimitation`(`전국`+30자 내 `어렵`)에 걸려 `evidence_status='ready'` + `verify.js:618` "한계까지 밝힌 점이 특히 좋습니다". AI 경로: 배포 `/api/ai-turn`에 시드 상당 문장 POST → **HTTP 200, `ready`, "기준과 수치가 증거와 일치합니다"**.
- 즉 **AI가 정답을 먼저 쓰고 학생이 승인받는 구조 + AI가 앱 자신의 문장을 감사하는 자기채점 구조**. `데모_대본.md:53`의 "AI를 정답 생성기가 아니라 비판적 사고 코치로"와 정면 충돌한다.

**붙여넣기 문안** — `verify.js:486-487`

라벨:
```
내 판정문 — 지역·기간·기준·한계를 넣어 내 말로 한 문장
```
textarea(값은 `''`, placeholder만):
```
예: 서울에서 ‘덥다’를 25°C로 정하면, 처서 무렵 더위가 그치는 날이 과거보다 25일 늦어졌다. 다만 이는 5년 관측 신호라 전국이나 원인으로 넓혀 말하기는 어렵다.
```
예시 토글 버튼(시드는 버튼 뒤로):
```
✎ 예시 문장 보기
```
예시 삽입 직후 상태문:
```
예시를 넣었어요 — 지역·숫자·한계 중 한 부분이라도 내 말로 바꾼 뒤 감사를 요청하세요.
```
자기채점 차단 게이트(`doAudit` 앞 3줄, 권장):
```
예시 문장과 거의 같아요. 한 부분이라도 내 말로 바꾼 뒤 요청해 주세요.
```

---

### F-3 [P0·20~40분] 라벨이 다른 화면의 버튼 이름을 말하고, 완료 화면이 새로고침으로 영구 소실된다

**근거**
- `verify.js:493` 라벨 `'자유탐구 열기 ✦'`(id=toFree) → `:495`가 `renderComplete()` 실행 → 완료·고향카드 화면(`h1 = '검증을 마쳤어요.'`)이 뜨고, **자유탐구는 그 화면의 세 번째 버튼** `startFree`(`:506`). 라벨이 자기 목적지가 아니라 다음 화면 버튼명을 말한다. `데모_대본.md` [75-90초]의 전환 지점이다.
- `renderComplete`가 `verify.js:500`에서 `state.phase = 'free'` 저장 → 부팅 분기 `:654` `if (state.phase === 'free') renderFree()` → **완료·고향카드 화면은 새로고침 후 도달 불가**(renderComplete 호출 지점은 toFree 하나뿐).
- 미션 감사 textarea(`:486-487`)에는 `input` 저장 리스너가 **없다**(리스너는 `renderFree` `:566`에만). `'다시 조작'`(`:492`→`:496 renderExplore`)을 누르면 `setStage`의 innerHTML 교체로 학생 문장이 **경고·복구 없이 소실**되고, 복귀 시 `:487`이 기계 시드로 재초기화한다.

**붙여넣기 문안 + 코드**

`verify.js:493` 라벨:
```
검증 마치고 결과 받기 →
```
`verify.js:492` 라벨(무엇이 유지되는지 명시):
```
기준 다시 조작하기 (판정문은 유지)
```
`verify.js:500`:
```js
state.phase = 'complete'; save();
```
`verify.js:654` 부팅 분기 앞에 추가:
```js
if (state.phase === 'complete') renderComplete();
else if (state.phase === 'free') renderFree();
```
`verify.js:487` 뒤에 저장·복원 2줄:
```js
state.missionDraft = state.missionDraft || {};
$('freeDraft').value = state.missionDraft[m.id] || '';
$('freeDraft').addEventListener('input', function () { state.missionDraft[m.id] = $('freeDraft').value.slice(0, 400); save(); });
```

---

### F-4 [P0·35분 / 1차 2.1·2.7과 직접 충돌 — §7 참조] heatNote가 허위 분기로 유도하고, 검증 불가 사실을 숨긴다

**근거**
- `verify.js:175-177`(노출 조건 `metric==='temp' && thr<=24`) 현재 문안 끝: `기준을 <b>조금 높이면</b> 뚜렷한 더위만 골라 비교할 수 있어요.`
- 실제 데이터 계산: 서울 25°C → pd=33, pl=229(정상 A분기). **26°C → pd=0, pl=−1, cd=60** → 미션1이 B분기(`verify.js:46`)로 넘어가 `'서울에서 과거(1969–73)엔 ‘26°C 이상’ 더위가 거의 없었는데'`를 출력한다. 1차 라운드 확정 실측은 **21.4일/년**이다.
- 즉 1차의 B분기 결함이 "드물게 도달하는 엣지"가 아니라 **앱이 문구로 권하는 경로**다.
- 데이터에 `avgTa/avgRhm/sumRn`만 있어 최저·최고기온이 없다(1차 확정). 열대야(밤 최저)·폭염(낮 최고)을 정의로 인용하면서 **이 화면에서 계산할 수 없음을 말하지 않는다**.
- 서울 30~32°C에서 pd=0·cd=0 → 3분기(`:47`)가 `'기준선을 조금 낮춰'`를 출력 → 같은 화면이 **"높이세요 → 낮추세요"** 자기모순에 도달한다.

**붙여넣기 문안 — `verify.js:177` 전량 교체 (RC-A 이전 스톱갭)**
```
☀ 하루 <b>평균</b> 24℃는 더위를 넓게 잡은 기준이에요. 기상청의 <b>열대야</b>는 밤 최저기온 25℃↑, <b>폭염</b>은 낮 최고기온 33℃↑로 정의하는데, 이 화면이 가진 자료는 <b>하루 평균기온</b>뿐이라 그 두 기준은 여기서 계산할 수 없어요. 평균 <b>25℃ 부근</b>에서 비교하면 과거·현재 양쪽에 값이 나옵니다.
```
**RC-A(평활→실측 초과일수) 완료 후 마지막 문장만 교체**:
```
평균 <b>25~28℃</b> 사이에서 비교하면 과거·현재 양쪽에 값이 나옵니다.
```

**미션1 B분기 — `verify.js:46` 전량 교체 (허위 단정 제거, RC-A 이전 스톱갭)**
```
서울에서 이 기준(‘26°C 이상’)으로는 <b>과거 평년 곡선이 한 번도 기준을 넘지 않고</b>, 현재(2022–26)는 <b class="hot">60일</b> 넘습니다. 다만 이 값은 15일 평활한 평년 곡선에서 센 것이라 개별 연도의 실제 관측 일수보다 적게 나옵니다 — 두 시기를 함께 비교하려면 기준을 25°C 쪽으로 낮춰 보세요.
```
> RC-A 완료 후에는 이 분기가 거의 발생하지 않으므로 문안을 다시 정리해야 한다(30분 예산, §7).

---

### F-5 [P0·25분 / 미션3 가드는 1차 2.4에 포함] 경계값에서 앱이 침묵하거나 깨진 문장을 낸다

**근거**
- `verify.js:225` null 문구 `'두 시기 모두 나타나면 시차를 계산합니다'` — 앱 내부 동작 설명이고 **사용자가 무엇을 해야 하는지(기준을 낮춰라)를 말하지 않는다**. 실측: 서울 28°C → `더위가 그치는 날 (마지막 기준초과일) / 없음 → 8월 11일`. "없음"은 한국어로 "(과거엔) 더위가 그친 날이 없었다 = 더위가 안 끝났다"로 읽혀 **오독 방향이 정확히 최악**이다.
- 미션2 `verify.js:66-68`에 가드 분기가 **없다**(미션1 `:47`에는 있다 — 미션 간 불일치). 부산 29~31°C에서 pd=0·cd=0 → `'여름은 과거 0일 → 현재 0일(+0일)입니다'` 뒤에 기준·기간을 밝히라고 훈계한다. 27~28°C에서는 pd=0·cd>0 → **분모 0 비교**("무한배 증가" 발표를 낳는다).
- 미션3 `verify.js:85-88`에 null 가드 없음. **임계값별 재계산 결과: 강원은 thr=27부터 null, 제주는 28부터 null** → 슬라이더 13칸 중 **6칸(46%)** 에서 `'제주 비교 불가, 강원 비교 불가로 지역마다 다릅니다'`가 출력된다(1차 라운드의 "28 이상"보다 넓다). 그리고 정지점 없는 "기준을 조금 높이면"(F-4)이 정확히 이 구간으로 유도한다.

**붙여넣기 문안**

`verify.js:225`의 `ro-s` null 분기 3분기화:
```
과거에는 이 기준을 넘은 날이 없어 시차를 계산할 수 없어요 — 기준선을 조금 낮추면 두 시기를 비교할 수 있어요.
현재에는 이 기준을 넘은 날이 없어 시차를 계산할 수 없어요 — 기준선을 조금 낮춰 보세요.
이 기준을 넘은 날이 과거·현재 모두 없어요 — 기준선을 낮추면 비교가 시작됩니다.
```
미션2 `verdict` 첫 줄 앞에 가드 2개(`verify.js:66`):
```js
if (n.pd === 0 && n.cd === 0) return n.city + '에서 ‘여름 = ' + n.thr + '°C 이상’으로 정하면 과거·현재 모두 여름이 <b>0일</b>이 되어 비교할 것이 남지 않습니다. 기준을 높일수록 세어지는 날이 줄어든다는 것 자체가 이 미션의 답이에요 — 기준선을 25°C 쪽으로 내려 다시 판정해 보세요.';
if (n.pd === 0 && n.cd > 0) return n.city + '에서 ‘여름 = ' + n.thr + '°C 이상’으로 정하면 과거는 <b>0일</b>, 현재는 <b class="hot">' + n.cd + '일</b>입니다. 과거가 0일이면 <b>몇 배 늘었다</b>고는 말할 수 없어요 — 늘어난 일수(+' + n.cd + '일)로만 말하고, 기준을 낮춰 두 시기를 함께 비교해 보세요.';
```
미션3 `verdict` 가드(`verify.js:85` 뒤):
```js
if (a == null || b == null) return '이 기준(' + n.thr + '°C 이상)에서는 ' + (a == null ? '제주' : '') + (a == null && b == null ? '·' : '') + (b == null ? '강원' : '') + '의 두 시기 중 한쪽에 기준을 넘은 날이 없어, 지역 간 시차를 비교할 수 없습니다. 기준을 <b>26°C 이하</b>로 낮추면 두 지역을 나란히 비교할 수 있어요.';
```
**1차 2.4와 병합** — `|a−b| < 5`일 때의 대체 문안(절대 날짜는 이미 계산되어 추가 데이터 불필요. 날짜 수치는 RC-A 후 재확인 필요):
```
같은 처서·같은 25°C 기준에서 시차의 <b>크기</b>는 제주 +21일, 강원 +17일로 비슷하지만, 더위가 그치는 <b>날짜</b> 자체는 제주 9월 23일, 강원 8월 27일로 약 한 달 차이입니다. 같은 기준이라도 지역마다 여름이 끝나는 시점이 다르므로, 한 지역의 결과를 전국으로 넓혀 말할 수 없습니다.
```

---

### F-6 [P0·40분] 차트 아무 곳이나 누르면 기준값이 그 지점으로 점프한다 (오조작)

**근거** `verify.js:273` `svg.addEventListener('pointerdown', …)`가 **SVG 전체**에 히트테스트 없이 걸려 있다. 재현: 노란 처서 라벨 위치(viewBox x=xf(234), y≈30)에 pointerdown → `#thrOut`이 **25°C → 31°C** 점프. 학생은 자기가 무엇을 했는지 모른 채 판정문 내용이 바뀐다. 잡을 그립/핸들 도형도 없다.

**수정** `verify.js:273` 핸들러 앞에 히트테스트 — y가 임계선 ±14px 밖이면 무시, 또는 임계선 위에 `<rect class="thr-hit" height="28">`를 얹어 그 위에서만 드래그를 받는다. 슬라이더(`#thrRange`)는 그대로 두어 "아무 곳이나 눌러 값을 정하고 싶다"는 요구를 흡수한다.

**터치 힌트 문안**(오조작 대신 안내로 유도, `verify.js:418` `#touchHint`):
```
보라색 기준선을 잡아 위아래로 끌어 보세요. 아래 슬라이더로도 1°C씩 맞출 수 있어요.
```

---

### F-7 [P0·35분] CERL을 요구·채점하면서 판정문 자신에 한계(L)가 없고, 4요소 매핑 장치가 0개다

**근거**
- 판정 화면 eyebrow `verify.js:440` = `'판정 (주장·근거·추론·한계)'`. 그런데 미션1 `verify.js:45` A분기는 서울 25°C 실행값(pl=229→8월 18일, cl=254→9월 12일, drift=25)으로 **주장O 근거O 추론O 한계 0개**.
- 4요소 중 어느 부분이 무엇인지 표시하는 장치가 0개(문장은 단일 `<p class="cerl">`).
- 앱 밖 모범답안이 앱 안 시범보다 완전하다: `교사_학습지.html` 루브릭 '추론·한계' 2점 배정 + 모범 판정문에 `'다만 이는 서울의 5년 관측 신호이므로…'`. `buildDraftSeed`(`verify.js:480`)에도 한계 절이 있다.
- 학생이 배우는 실제 메시지: **"한계는 채점용 형식이지 진짜 쓰는 게 아니다."**
- (정정: 미션2 `:67`의 `'기준을 바꾸면 이 숫자도 함께 바뀝니다'`는 결과 한정 한계 진술로 성립하고, 판정 화면에는 스파크라인 캡션 `:208`이 표본 한계를 진술한다. 결함은 **"CERL 단락 내부의 L 누락 + 요소 미표시"**로 좁혀야 한다.)

**붙여넣기 문안** — `verify.js:45` A분기 문장 끝에 한계 절 추가:
```
 다만 이는 서울 한 지역의 5년(1969–73 vs 2022–26) 관측 신호이고, ‘덥다’를 다른 온도로 정하면 이 날짜도 함께 바뀝니다 — 전국이나 그 원인까지 넓혀 말할 수는 없습니다.
```
4요소 가시화(구조 변경 30분): `verdict`가 문자열 대신 `{c, e, r, l}`을 반환하고 `renderVerdict`가 배지로 조립. 미션1 A분기 분해 예시:
| 요소 | 문안 |
|---|---|
| **주장** | 처서(더위가 그침)가 지나도 더위가 이어집니다. |
| **근거** | 서울 ‘덥다 25°C’ 기준으로 더위가 그치는 날이 과거 8월 18일 → 현재 9월 12일입니다. |
| **추론** | 절기 날짜는 그대로인데, 관측된 더위가 25일 늦게까지 이어진 것입니다. |
| **한계** | 서울 한 지역의 5년 관측 신호이고, 기준을 바꾸면 날짜도 바뀝니다 — 전국·원인까지 넓힐 수 없습니다. |

CSS 3줄(`verify.css`):
```css
.cerl .t { display:inline-block; margin-right:6px; padding:1px 8px; border-radius:999px; font-size:11.5px; font-weight:800; vertical-align:2px; }
.cerl .t-c { background:rgba(255,190,88,.18); color:#ffe1a8; } .cerl .t-e { background:rgba(119,191,247,.16); color:#cce8ff; }
.cerl .t-r { background:rgba(59,208,192,.14); color:#b7f1ef; } .cerl .t-l { background:rgba(241,94,88,.14); color:#ffd5cf; }
```

---

### F-8 [P0·20분, 스톱갭] 첫 조작 후 결론 수치로 프레이밍하지 않는다

**근거** §3-1. 재확인된 사실: 375×667에서 `document.documentElement.scrollTop = 250`이면 `#heroSvg [205,357]`(드래그면 100% 가시) + `#thrRange [380,404]` + `#readouts [447,646]`이 **전부 동시에 보인다**(차트 top~readouts bottom 441px < 667). **공간 부족이 아니라 프레이밍 부재다.**

**수정** `bindThreshold`의 `setY` 안, 최초 1회만:
```js
if (!framed) { framed = true; var ro = $('readouts'); if (ro && ro.scrollIntoView) ro.scrollIntoView({ block: 'end', behavior: 'smooth' }); }
```
추가(권장): 미션 화면 차트 위에 `liveSentence`(`verify.js:181-186`) 재사용 — 자유탐구에만 있는 컴포넌트이므로 신규 개발이 거의 없다. **본수정은 2차 UX-2(2~3h, RC-A와 한 작업)** 이며 그것이 끝나면 F-8은 폐기한다.

---

## 5. P1 — 점수 상승 효과가 큰 것 (합계 5.5~7h + 흡수분)

### F-9 [P1·2.5~3.5h] **"이 숫자는 어떻게 나왔나" 서랍** — 이번 라운드 최고 가성비

M-3 병합 작업 하나로 GB-04 + GB-09 + M1 + 1차 "출처 고지 2h"를 모두 닫는다. 심사 질의가 들어오는 지점(차트 화면)에 방어 수단이 **전무**하다: `document.querySelectorAll('table').length === 0`, verify.js에 CSV·"방법론" 0건. (정정: 출처가 "0건"은 과장 — 인트로 배지 `verify.js:295`와 교사 학습지에 있다. 없는 곳은 **숫자가 생성되는 화면**이다.)

**붙여넣기 문안 — `chart-card` 하단 `<details>` 전문**

```
▾ 이 숫자는 어떻게 나왔나 (자료·계산·한계)

■ 자료
기상청 ASOS(전국 종관기상관측) 일자료. 16개 관측 지점의 하루 평균기온·하루 평균습도·일강수량.

■ 지점 번호
서울 108 · 부산 159 · 인천 112 · 대구 143 · 광주 156 · 대전 133 · 제주 184 · 강릉 105
경기=수원 119 · 충북=청주 131 · 충남=서산 129 · 전북=전주 146 · 전남=목포 165 · 경북=포항 138 · 경남=진주 192 · 강원=춘천 101
→ ‘경기’는 수원 한 지점의 관측입니다. 도 전체의 평균이 아닙니다.

■ 비교 기간
과거 1969–1973년(5년) vs 현재 2022–2026년. 단 2026년 자료는 연중 일부까지만 있어 6월 하순 이후 구간(연중 174~365일째)은 4년 평균입니다.

■ 계산 방법
① 날짜별로 5년 평균을 냅니다(2월 29일 제외) → ② 빈 날짜를 보간합니다 → ③ 15일 원형 이동평균으로 매끄럽게 다듬은 ‘평년 곡선’을 만듭니다 → ④ 그 곡선에 여러분이 정한 기준을 적용해 넘는 날수를 셉니다.

■ 이 방법의 한계 (반드시 함께 읽어 주세요)
1) 화면의 ‘더위일’은 평년 곡선이 기준을 넘는 날수입니다. 다듬는 과정에서 오르내림이 줄어들기 때문에, 개별 연도의 실제 관측 일수와는 다릅니다 — 높은 기준에서는 적게, 낮은 기준에서는 많게 나옵니다.
2) 5년 비교는 ‘관측 신호’이지 기후평년(보통 30년)이 아닙니다. 비교하는 5년 창을 다르게 잡으면 값이 달라집니다.
3) 자료에 하루 평균값만 있어 최저·최고기온이 없습니다. 그래서 열대야(밤 최저 25℃↑)와 폭염(낮 최고 33℃↑)은 이 화면에서 계산할 수 없습니다.
4) 절기 날짜는 태양의 위치(황경)로 정한 천문 날짜를 대표값으로 표기했습니다. 해에 따라 1일 정도 차이가 날 수 있습니다(예: 상강은 1969~2026년 최빈값이 10월 23일).

■ 월별 요약 (표 보기)
월 | 과거 평균 | 현재 평균 | 기준 이상 일수(과거→현재)
1~12월 12행 — 현재 화면의 지역·지표·기준으로 자동 계산
```

**무결성 문구 승격·정정** — `verify.js:244`, 위치를 차트 패널 **상단(범례 옆)** 으로, `verify.css:63`의 `font-size: 12px` → `13.5px`:
```
◈ 기상청 ASOS 실측 · 과거 1969–73 vs 현재 2022–26 · <b>5년 관측 신호</b>(일부 시기는 4년) — 30년 <b>기후평년</b>이 아닙니다 · 절기는 태양 위치로 정한 <b>천문 날짜</b>라 해마다 거의 움직이지 않습니다 · <a href="#howto">이 숫자는 어떻게 나왔나 ▾</a>
```
**용어 풀이 3곳**(M1의 실측 확인분 — 실제 렌더되는 무정의 용어는 ASOS / 자료의 범위 / 근거만큼 결론 / 기준 이상 더위일 / 5년 관측 신호 / 30년 기후평년 / 마지막 기준초과일 7개):
| 위치 | 현재 | 교체 문안 |
|---|---|---|
| `verify.js:295` | 기상청 ASOS 실측 · 1969–2026 · 16지역 × 24절기 | `기상청 ASOS(전국 종관기상관측) 실측 · 1969–2026 · 16지역 × 24절기` |
| `verify.js:299` | ① 절기 ≠ 기후 / ② 자료의 범위 / ③ 기준을 정의 / ④ 근거만큼 결론 | `① 절기 ≠ 기후` / `② 자료의 범위(지역·기간·지표)` / `③ ‘덥다’를 숫자로 정의` / `④ 근거만큼만 결론` |
| `verify.js:225` | 더위가 그치는 날 (마지막 기준초과일) | `더위가 그치는 날 (그해 마지막으로 기준을 넘은 날)` |

> **주의(§7)**: 서랍의 "한계 1"은 RC-A(평활→실측 초과일수) 완료 시 삭제·교체된다. RC-A 이전에 만들 경우 갱신 30분을 예산에 넣어라. 그래도 **먼저 만들 가치가 있다** — 8/21까지 RC-A가 끝나지 않을 경우의 유일한 방어선이다.

---

### F-10 [P1·1.5h · RC-A 후 확정 · 2차 UX-1 C안에 흡수] 기준값 프리셋 칩

`#thrRange` 실측 min=20 max=32 step=1, 프리셋 0개, 숫자 입력 0개, 축 줌 0개. 375px에서 1°C = 3.41 CSS px, 8px 드래그 = 2.35°C → **40명이 같은 기준으로 비교할 수 없다.** PhET은 같은 문제를 4개 역사 프리셋 + `Experiment Mode`(By concentration / By time period) + `Energy Flux Range Zoom In/Out`으로 푼다.

**칩 라벨(붙여넣기)** — RC-A 이전 값. `range-row` 위에 배치:
```
넓게 20°C   |   기본 25°C   |   뚜렷 26°C   |   ↺ 기준 되돌리기
```
RC-A 이후: `뚜렷 26°C` → `뚜렷 28°C`(RC-A 후 28°C가 4.8→34.8일로 최고의 실연 구간이 된다 — 1차 확정).
**비활성 칩 하나를 일부러 남겨 한계를 UI로 설명한다**(1차 RC-M 동시 해소):
```
폭염 33°C — 최고기온 자료가 없어 계산 불가
```
미션3에서는 상한 프리셋을 **26°C**로 제한(27부터 강원 null, 28부터 제주 null — F-5).

### F-11 [P1·1h] 그립 도형 + `aria-valuetext` + focus 링 통일
임계선 우측 pill을 잡히는 그립(원+두 줄)으로 그린다(30분). `#thrRange`에 값의 정성 텍스트를 넣는다(20분):
```
25도 — 기준 이상 더위일 과거 33일 대 현재 82일
```
`style.css:2`의 `:focus-visible` 셀렉터에 `a`와 `input` 추가(5분) — 현재 `button/select/textarea`만이라 a·range는 UA 기본 링을 받아 앱의 3px 주황 링과 불일치한다(부재는 아니다 — §8-5).

### F-12 [P1·1h] 차트 텍스트 대안을 상태와 함께 갱신
`drawHero` 끝에서 `svg.setAttribute('aria-label', …)`를 상태 문장으로 갱신, `verify.js:233` 범례의 `aria-hidden` 제거 + 문장형 텍스트 병기.
**붙여넣기 문안(템플릿)**:
```
서울 처서 무렵. 내가 정한 ‘덥다’ 기준 25도. 과거 곡선(1969–73)이 기준을 넘는 날 33일, 현재 곡선(2022–26)은 82일. 더위가 그치는 날은 과거 8월 18일, 현재 9월 12일. 절기선은 8월 23일에 고정.
```

### F-13 [P1·18분] 미션2의 '지역 변경' 초대가 화면에 없다
`brief` 3개(`verify.js:38/60/79`)를 참조하는 코드가 0곳(`renderExplore` `:413-420`은 `HEADLINES`와 `m.task`만 렌더). 미션2는 `lockCity:false`로 16지역 칩을 띄우는데(`:417`) 지역 변경을 권하는 문구는 **미렌더 brief**(`:60`)에만 있고 `task`(`:65`)·`picker-label`("지역")에는 없다. `데모_대본.md` [10-25초]의 "심사위원님 고향이 어디세요?"라는 최대 자산이 학생 화면에서 초대되지 않는다.

**붙여넣기 문안** — `verify.js:65` `task` 끝에 추가:
```
 지역 칩을 눌러 내 지역으로 바꿔도 좋아요 — 같은 기준에서 내 지역은 며칠인지 확인해 보세요.
```
(brief 3개는 `renderExplore:416` 앞에 `<p class="brief">`로 부활 — `verify.css`에 `.brief`가 이미 있다.)

### F-14 [P1·20분 / 1차 P1 "이중 제품 1h"와 병합] 같은 대상 5종 명칭 + 데이터 조작 지시로 읽히는 문장
| 파일:줄 | 현재 | 교체 문안 |
|---|---|---|
| `archive.html:22` | 예측하고, **실제 관측값을 조작하고**, 그것으로 판정하세요. | `예측하고, 실제 관측값을 직접 확인하고, 그것으로 판정하세요.` |
| `index.html:13`(og:description) | 기후 **레슨** | `기후 검증 미션` |
| `index.html:31`(nav aria-label) | **레슨** 진행 단계 | `미션 진행 단계` |
| `verify.js:348` | 진짜 **임무** ‘처서’로 가 볼까요? / 진짜 임무 시작 → | `진짜 미션 ‘처서’로 가 볼까요?` / `진짜 미션 시작 →` |
| `MISSIONS.title` 3개 (10.5px 진행 칩) | 처서, 약속은 유효한가 / 여름은 며칠일까 / 우리 지역만 그럴까 | `처서` / `여름 길이` / `지역 차이` (화면 h1 `HEADLINES`와 이름 이중화 해소) |

"실제 관측값을 조작하고"는 **데이터 과장을 경계시키는 앱에서 자료 조작을 지시하는 문장으로 읽히는 단일 문장**이며, 인용될 위험이 있다. 2분 수정이므로 최우선.

### F-15 [P1·1h / 2차 UX-6 2.5h에 흡수] 상태 딥링크
`?c=부산&t=13&m=temp&thr=27` 4개 파라미터를 부팅 시 읽어 주입하고 조작 시 `history.replaceState`로 되쓴다. 목적은 데모 복구가 아니라(§8-7) **교사의 링크 배부**와 **모둠 간 비교**다.

### F-16 [P1·30분 · **미확인**] 교사 학습지의 성취기준 연결
`prototype/교사_학습지.html`(8109바이트) 전문 확인 결과, 앞선 주장 5개 중 3개가 사실이 아니다 — **시간 배분(도입 5분·활동 10/8/7분·정리 10분, 헤더 "소요: 40~45분"), 모범 판정문 + 4점 루브릭 + 4행 오개념 교정표, `@media print` + `.pagebreak`, 대상 학년 표기가 모두 이미 존재한다.** 실제로 없는 것은 **성취기준 코드 하나**다.
2022 개정 교육과정 코드는 **이번 라운드에서 확인하지 않았다**. 따라서 코드 번호를 쓰지 말고 다음 문안으로 낮춰 넣어라:
```
연계 과목·영역: 통합과학 「기후변화와 환경생태」 · 지구과학Ⅰ 「대기와 해양」 · 통합사회 「기후변화와 지속가능한 삶」
성취기준 코드는 학교 진도표에 맞춰 담당 교사가 기입해 주세요. ( ______________ )
```

---

## 6. P2 — 여유 시

| 항목 | 내용 | 시간 | 비고 |
|---|---|---|---|
| GB-08 y축 줌 모드 | `bounds()`에 '연중 전체 / 더위 구간 18~34°C' 2버튼(PhET `Experiment Mode` 규범) | 2h | **2차 UX-1 C안과 중복** — C안 채택 시 별도 작업 불필요 |
| GB-07 Undo | 임계값만 되돌리는 컨트롤(F-10 칩에 포함) / 조작 1단계 Undo | 15분 / 1h | 파괴적 리셋만 있다는 전제는 반박됨(§8-6) |
| 375px 헤더 붕괴 | `verify.css:7` `.ghost-link { white-space:nowrap }` 제거 또는 `max-width:560px`에서 해당 링크 숨김 | 10분 | **2차 UX-4에서 이미 확정·실증** |
| 어체 통일 | 합니다 48 : 해요 57. 같은 요소 안 역전 2건(`:486` vs `:561` — F-1에서 이미 해소, `:177` — F-4에서 해소) | 30분 | 나머지는 취향 |

---

## 7. 앞선 라운드 P0와의 충돌·중복 (요구사항 4)

**앞선 P0 규모**: 1차 데이터 파이프라인 **24~28h**(2.8 게이트 3h → 2.3 기간 정정 3~4h → **2.1 RC-A 실측 초과일수 6~8h** → 2.4 미션3 2~3h → 2.2 창 민감도 1.5h + 2.5/2.6/2.7/절기 라벨) · 2차 UX **11~13h**(UX-1 4~5h, UX-2 2~3h, UX-3 0.5h, UX-4 15분, UX-5 1h, UX-6 2.5h).

| 이번 항목 | 앞선 P0와의 관계 | 순서 / 조치 |
|---|---|---|
| **F-1** 폴백 카피 | **없음 — 완전 독립 신규** | 7/27 즉시. 25분 |
| **F-2** 감사 선입력 | **1차 2.5(HIGH 1.5h)와 동일 대상** | 한 작업으로 합쳐 1.5h. **이중 계상 금지** |
| **F-3** 라벨/phase/draft | 1차 "산출물 미저장"(P1 1.5h)와 부분 중복. `phase='free'` 버그·라벨 오배치는 신규 | 합쳐 1.5h |
| **F-4** heatNote·B분기 | **1차 2.1(RC-A 6~8h)·2.7과 직접 충돌** — RC-A 완료 시 문안을 다시 써야 함 | 스톱갭 문안 지금(35분) + RC-A 후 재작성(30분). RC-A를 8/21까지 못 하면 이 스톱갭이 **유일한 방어선** |
| **F-5** 경계 가드 | 미션3 가드·`\|a−b\|<5` 문안은 **1차 2.4(2~3h)에 포함**. 미션2 0/0·0→n 가드와 readout null 3분기는 신규 | 미션2·readout만 별도 25분, 미션3는 2.4에서 |
| **F-6** 히트테스트 | 2차가 발견(`bindThreshold` 전역 pointerdown)했으나 **수정이 배정되지 않았던 항목** | 신규 40분. UX-1과 동일 파일이나 독립 |
| **F-7** CERL의 L | **없음 — 신규** | 35분 |
| **F-8** 스크롤 프레이밍 | **2차 UX-2(2~3h)의 스톱갭** | UX-2 완료 시 폐기. 지금 20분 |
| **F-9** 방법론 서랍 | **1차 "출처 고지 2h"(P1)와 동일 대상**, 1차 2.1·2.3의 공시 경로. 서랍 본문 "한계 1"은 RC-A 후 수정 | 통합 2.5~3.5h + RC-A 후 갱신 30분 |
| **F-10** 프리셋 | **2차 UX-1 C안의 부분집합** + RC-A가 임계값별 값을 바꿈 | **RC-A 후** 확정. C안 채택 시 흡수 |
| **F-11·F-12** a11y | 2차 UX-8·UX-9와 이웃(중복 아님 — 그립·valuetext·aria-label 갱신은 미배정) | P1 구간 2h |
| **F-13** brief | 없음 — 신규 | 18분 |
| **F-14** 용어·archive 문장 | **1차 P1 "이중 제품 노출 1h"와 병합** | 합쳐 1h |
| **F-15** 딥링크 | **2차 UX-6(2.5h)에 흡수** | 별도 계상 금지 |
| **GB-08** 축 줌 | **2차 UX-1 C안과 중복** | 별도 작업 아님 |

**권고 배치 (7/27 첫날, 데이터 P0와 완전 독립 · 합계 약 3h)**
```
F-14 archive 문장(2분) → F-1 폴백(25분) → F-3 라벨/phase(40분) → F-5 미션2·readout 가드(25분)
→ F-6 히트테스트(40분) → F-8 스크롤 프레이밍(20분) → F-7 CERL 한계 절(15분, 4요소 배지는 P1로 미뤄도 됨)
```
그 다음 날 **F-9 서랍(2.5~3.5h)**. 이 순서의 이점: 심사위원이 폰으로 열었을 때 첫 30초에 보이는 것(오조작·폴드 밖 수치·"AI가 꺼져 있어")을 데이터 파이프라인을 건드리지 않고 이틀 안에 고친다.

**예상 점수 영향(내부용, 과장 금지)**: 이번 라운드 항목만으로는 ①②⑤ 합쳐 **+2~3점** 수준이다. 다만 F-9 서랍은 점수표보다 **질의응답 방어**에서 실효가 크다 — 1차가 확정한 과학 결함(26℃ 허위 문장, 강수 방향 반대, doy 174~365의 4년 표본)이 심사장에서 "숨긴 오류"로 노출될 확률을 실질적으로 낮추는 유일한 저비용 수단이다.

---

## 8. 강도 하향·미확인 (정직 고지 · 요구사항 1)

이번 라운드의 두 차원이 제기했으나 **실측으로 반박되거나 근거가 확인되지 않은** 항목. 보고서를 밖으로 인용할 때 아래는 쓰지 마라.

| # | 주장 | 판정 | 실측 |
|---|---|---|---|
| 1 | "6개 레퍼런스 중 …" | **표현 하향** | URL로 확인한 곳은 **5곳**(PhET·OWID·En-ROADS·NOAA·Met Office). NASA는 URL이 제시되지 않아 **미확인** → 근거에서 제외했다 |
| 2 | PhET 공식 문서 "PhET follows known accessible design patterns" (phet.colorado.edu/en/accessibility, /en/inclusive-design/features) | **미확인** | 이번 라운드에서 확인한 것은 sim의 병렬 DOM 실측뿐. 해당 페이지 본문은 재확인하지 않았다 → 인용 강도를 낮춰라 |
| 3 | MDN slider role 키보드 계약(화살표/PageUp/Home/End) | **미확인(대체 확인)** | 규범 문서 본문은 재확인 안 됨. 다만 `#thrRange`가 네이티브 range이므로 브라우저가 계약을 제공하고 `verify.js:276`이 input 이벤트를 처리하는 것은 확인 |
| 4 | 375px `scrollHeight = 1155`(뷰포트 1.73배) / readouts "과거 107일 → 현재 134일 +27일" | **수치 신뢰 없음** | 같은 차원 안에서 두 번 다르게 측정됐다(1155 vs **1070**, 1280px는 1063 vs **1349**). readouts 내용도 25°C 기본값에서는 "33→82 / +25일"이다. **"폴드 밖"이라는 사실만 유지하고 픽셀·수치는 재측정하라** |
| 5 | "무결성 문구가 화면에서 가장 작은 10.5px 글자" | **반박** | `.integrity`는 **12px**(`verify.css:63` 직접 확인). 10.5px은 `.pstep small`(`verify.css:78`, 진행표시 미션명)이다. 스테이지 내부 최소는 11.04px. "리프 35개 중 20개(57%)"도 재측정 시 39개 중 25개로 재현되지 않음 → **GB-09는 medium→low** |
| 6 | "`verify.js:650`이 유일한 리셋이고 기본값 복귀가 없다" | **반박** | `verify.js:649` homeLink가 `confirm('처음(소개)으로 돌아갈까요? 진행 기록은 유지됩니다.')` 후 인트로 복귀 → `startMission(0)`(`:379`)이 city/ti/metric/thr만 기본값으로 되돌리고 done·pre·post는 보존 = PhET `Reset All`과 동일 의미. 판정 화면에 '다시 조작'(`:496`)도 있다 → **GB-07 low** |
| 7 | "데모 사고 시 복구 경로가 없어 인트로부터 다시 시작 + 자동 데모 재생" | **반박** | localStorage를 미션2 상태로 놓고 하드 리로드 → 미션2·부산·thr 27·완료표시 그대로 복원. 자동 데모는 `verify.js:432`가 `!demoPlayed && state.mi===0 && state.pre==null && !state.touched`로 게이트 → 재생 불가 → **GB-02 high→medium** |
| 8 | "화면을 못 보는 사용자에게 이 앱의 모든 데이터는 존재하지 않는다 / 동선 완주 불가" | **반박** | `#readouts`(`verify.js:242`)·`#thrOut`(`:241`)·`#stage`(`index.html:32`) 모두 `aria-live="polite"`이고 `renderReadouts`가 매 조작마다 블록을 다시 쓴다 → 핵심 수치는 낭독된다. "폴드 밖이라 접근성 문제"는 범주 오류(스크린리더에 스크롤 위치는 무관) → **GB-06 medium** |
| 9 | 교사 학습지에 시간 배분·정답 예시·인쇄 형식·연령 표기가 없다 | **3건 반박** | 모두 존재(§F-16). 실제 결함은 성취기준 코드 하나 → **GB-10 low** |
| 10 | 슬라이더 상한 34 → "폭염 33℃를 그대로 입력" | **반박** | `heatRange`(`verify.js:18`) 계산 결과 상한은 지역별 **30~33**(서울 32, 부산 31). 33은 충북뿐이고 34는 어느 지역에도 없다. 자기모순은 서울 30~32°C에서 발생(도달 온도만 다름) |
| 11 | "미션1 화면에 처서=더위가 그침 전제가 없어 조작 이후에 등장" / "학생이 제주↔강원 칩을 왜 바꾸는지 모른다" | **반박** | 직전 인트로 `verify.js:298`이 "'처서가 지나면 더위가 그친다' 같은 **절기의 약속**"을 명시하고 부팅 분기 `:655`상 신규 사용자는 반드시 통과한다. 미션3 `task`(`:83`)는 "제주 칩과 강원 칩을 번갈아 눌러…"라고 직접 지시하며 `hero-sub`로 렌더된다(`:416`) → H4는 **미션2 지역 초대 부재 한 건**으로 축소 |
| 12 | 은유 프레임 8개 / 합니다:해요 48:57 / '당신' 3회 | **취향으로 재분류** | 감사·법정·주술을 별 프레임으로 쪼갠 구성물이고 둘 다 존댓말이며 오류가 아니다. M2에서 살아남는 실질 결함은 **동일 대상 5종 명칭 · 미션 이름 이중화 · archive.html "실제 관측값을 조작하고"** 세 건 |
| 13 | "모바일에서 차트는 작아지는데 페이지는 넘친다"(최악 조합) | **반박** | 실측 `scrollHeight` 375=**1070**(뷰포트의 1.60배), 1280=**1349**(1.69배). 페이지 절대 높이는 모바일에서 오히려 279px 작고, 초과 비율은 데스크톱이 더 나쁘다. 모바일 특유 병리가 아니라 **폭 무관한 단일 수직 스택**의 결과 → GB-08 **medium** |
| 14 | heatNote의 "33℃는 max=32 밖이라 도달 불가" | **트집으로 기각** | 문안 자체가 '하루 평균 25℃' / '밤새 최저 25℃↑' / '낮 최고 33℃↑'로 변수를 구분해 쓴다. 일평균 축을 33까지 끌라는 안내가 아니다(단 §F-4의 검증 불가 미표기는 유효) |
| 15 | 미션2 판정문 "한계 0개" / "판정 화면에 한계가 없다" | **부분 반박** | `verify.js:67`의 "기준을 바꾸면 이 숫자도 함께 바뀝니다"는 결과 한정 한계 진술이고, 판정 화면에는 스파크라인 캡션(`:208`)이 표본 한계를 진술한다 → C1은 **"CERL 단락 내부의 L 누락 + 요소 미표시"**로 좁혔다 |

**반대로 앞선 주장보다 나쁘게 나온 것 1건**: 미션3 null 붕괴 구간. 1차는 "28 이상"이라 했으나 임계값별 재계산 결과 **강원은 27부터, 제주는 28부터 null** → 슬라이더 13칸 중 **6칸(46%)** 에서 판정문이 깨진다(F-5).

---

## 9. 잘 된 점 (과장 없이)

**A. 레퍼런스가 도구 밖으로 밀어낸 교수 설계를 도구 안으로 끌어들였다 — 예측 봉인**
PhET Greenhouse Effect의 상호작용 DOM 전체(3스크린)를 덤프해 확인한 결과 **"먼저 예측하고 봉인하라"는 단계가 없다.** PhET의 예측 유도는 교사가 만드는 별도 활동지에 위임되고 시뮬레이션 자체는 언제든 자유 조작이다. En-ROADS도 Share/Assumptions/Related Examples는 있으나 결과를 보기 전 추측을 확정하는 게이트는 없다. Weather24는 `state.pre`가 없으면 판정으로 넘어갈 수 없게 동선을 막고(`verify.js:427-431`) 봉인한 예측과 실측을 대조한다. ③학습효과·④독창성에서 방어 가능한 진짜 차별점이다.

**B. 조작 대상이 '모델 입력'이 아니라 '측정 범주의 정의'다**
PhET에서 학생이 끄는 것은 물리적 입력값(Greenhouse Gas Concentration)이고 프리셋도 역사적 실제 상태(Ice Age/1750/1950/2020)다. En-ROADS의 19개도 정책 입력, OWID에서 고르는 것은 국가·기간·뷰다. **확인한 5곳 전부에서 "무엇을 무엇이라고 부를지"는 사이트가 미리 정해 준다.** Weather24는 '몇 도부터 덥다고 할지'를 학습자가 정하게 하고 그 정의가 결론을 바꾸는 것을 즉시 보여준다. 데이터 리터러시의 메타 층위를 조작으로 가르치는 설계다. **단 이 강점은 F-6(오조작)·F-10(프리셋 없음)·F-5(경계 붕괴)로 절반쯤 무력화돼 있어, 그 세 수정이 곧 이 강점의 실현이다.**

**C. 최종 산출물이 그래프가 아니라 학생의 서술이다**
OWID의 산출물은 차트/표/CSV, PhET은 화면 상태, En-ROADS는 시나리오 링크와 모델 요약 — 모두 기계가 만든 것이다. Weather24는 자동 CERL 판정문 → 학생이 고쳐 쓰는 한 문장(`verify.js:486`) → 자가진단 → 고향카드 PNG로 이어져 수업이 끝났을 때 **교사 손에 학생별 서술 증거**가 남는다. ⑤실현가능성의 실질 우위다(단 F-2·F-3이 지금 이 사슬을 끊고 있다).

**D. 피드백 카피의 질이 실제로 높다 — 벤치마크와 무관하게**
- 자가진단 해설 3개(`verify.js:52/72/93`)가 정답 확인이 아니라 **일반화 규칙**을 가르친다: "기준이 높을수록 그 기준을 넘는 날은 줄어듭니다. 그래서 '덥다'를 몇 도로 정하는지에 따라 결론이 달라지고, 우리는 늘 그 기준을 밝혀야 합니다." 오답 피드백도 "틀렸습니다"가 아니라 "다시 볼까요."(`:455`)
- 사후문항이 3분기(`:469-471`)로 갈리고, **미도달자에게 점수를 매기지 않고 핵심 명제를 다시 준다**. 드물고 옳은 설계다.
- `localAudit` 경고문(`:607-611`)이 오개념 유형별로 행동지시적이고 소크라테스 질문(`:622-628`)이 경고와 1:1로 짝지어져 있다. "보완이 필요합니다" 같은 공허한 피드백이 한 줄도 없다.
- `spark-cap`(`:208`) **"당신의 5년 비교는 이 긴 흐름의 양 끝입니다."** — 표본 한계라는 추상 개념을 바로 옆 57년 그래프와 묶어 한 문장으로 설명한다. 이 앱 최고의 정직성 카피이며, **무결성 문구(F-9)가 도달해야 할 수준을 이미 보여준다.**
- `goal-chip`(`:35/57/76`)이 학습목표를 도입부가 아니라 **조작 화면에 상주**시킨다.

**E. 레퍼런스를 그대로 따라가면 안 되는 지점 3개 (반-권고)**
1. **En-ROADS식 다중 슬라이더·다중 그래프**: Climate Interactive는 이를 성인 대상 진행자 워크숍('facilitated group learning experiences')으로 운용한다. 중3~고2, 교사 1명이 40~45분에 40~45명, 부스 접촉 30초~2분이라는 제약에서는 선택지가 늘면 완주율이 떨어진다. **'슬라이더 1개'라는 결정은 옳다** — 늘려야 할 것은 개수가 아니라 그 하나의 어포던스·프리셋·키보드·텍스트 대안이다.
2. **PhET식 전면 무스크롤(`body{overflow:hidden}`)**: PhET이 375×667에서 `scrollHeight=667`을 달성하는 것은 고정 종횡비 캔버스 씬 하나만 스케일하기 때문이다. Weather24는 예측→조작→판정→자가진단→사후→AI 감사의 선형 텍스트 레슨이라 한 화면 압축은 오히려 읽히지 않는다. **원리만 가져와라**: "조작 중에는 조작기와 헤드라인 수치가 같은 화면"이라는 좁은 규칙(F-8/UX-2)만 지키고 단계 간 스크롤은 유지한다. 전면 무스크롤을 목표로 잡으면 8/21까지 레이아웃 전면 재작성이 되고 데모 안정성을 잃는다.
3. **OWID식 데이터 재사용 스택(CSV/SVG/PNG/메타 JSON/zip)**: 이 대회 배점에 데이터 재사용 항목이 없다. 필요한 것은 다운로드 파이프라인이 아니라 **'표 보기 + 방법론 서랍' 최소선(F-9)** 이며 그것으로 ①⑤ 방어가 끝난다. **CSV는 만들지 않는 것이 옳다.**

---

## 10. 가장 냉정한 판단

이 앱의 교수 설계와 피드백 카피는 이미 레퍼런스 상위권이다. 문제는 그 설계가 **자기 문장으로 스스로를 부정하는 세 지점**에 몰려 있다는 것이다.

1. **CERL을 채점하면서 CERL을 못 쓰는 판정문**(F-7)
2. **정답을 먼저 써 주고 그 정답을 AI가 칭찬하는 감사 상자**(F-2)
3. **"AI가 꺼져 있어"로 자기 최대 방어 카드를 결함으로 보고하는 폴백**(F-1)

이 세 개는 코드가 아니라 문장이고, 합쳐 **1시간 25분**이다. 여기에 F-9 서랍(2.5~3.5h)을 더하면, 1차가 찾아낸 과학 결함이 심사장에서 **"숨긴 오류"에서 "공시된 한계"로** 프레임이 바뀐다.

**가장 큰 리스크는 시간이 아니라 순서다.** RC-A(6~8h)를 기다리며 카피 수정을 미루면, 8/21에 RC-A가 끝나지 않았을 때 방어선이 하나도 없다. 반대로 카피를 먼저 고치면 RC-A가 늦어도 **정직한 앱**을 보여줄 수 있고, RC-A가 끝나면 문안 갱신 1시간으로 최종형에 도달한다. **F-4의 "한계 1" 문장과 F-9 서랍은 일부러 RC-A 이전 문안으로 먼저 넣고, RC-A 후 30분을 예산에 잡아라.**

---

### 부록. 붙여넣기 패치 색인

| 우선 | 파일:줄 | 대상 | 시간 |
|---|---|---|---|
| P0 | `archive.html:22` | "실제 관측값을 조작하고" → "직접 확인하고" | 2분 |
| P0 | `verify.js:637-644` / `:580` / `:486` / `:561` | 폴백 원인 분기 + 헤더 통일 + 사전 상태문 통일 | 25분 |
| P0 | `verify.js:486-487` | 감사 라벨·placeholder·예시 토글·동일성 게이트 | 25분 |
| P0 | `verify.js:492/493/500/654` | 라벨 2개 + `phase='complete'` + draft 저장 | 40분 |
| P0 | `verify.js:177` / `:46` | heatNote 전량 교체 + 미션1 B분기 | 35분 |
| P0 | `verify.js:225` / `:66` / `:85` | readout null 3분기 + 미션2 가드 2개 + 미션3 가드 | 25분 |
| P0 | `verify.js:273` / `:418` | pointerdown 히트테스트 + 터치 힌트 | 40분 |
| P0 | `verify.js:45` (+ `verify.css` 4줄) | CERL 한계 절 + 4요소 배지 | 35분 |
| P0 | `verify.js:271-277` | 첫 조작 후 `#readouts` 프레이밍 | 20분 |
| P1 | `chart-card` 하단 / `verify.js:244` / `verify.css:63` / `:295` / `:299` / `:225` | 방법론 서랍 + 무결성 승격·정정 + 용어 풀이 3곳 | 2.5~3.5h |
| P1 | `range-row` 위 | 프리셋 칩 3개 + 비활성 폭염 칩 + 되돌리기 (**RC-A 후**) | 1.5h |
| P1 | `drawHero` 끝 / `verify.js:233` | aria-label 상태 갱신 + 범례 aria-hidden 제거 | 1h |
| P1 | 임계선 pill / `#thrRange` / `style.css:2` | 그립 도형 + aria-valuetext + focus 링 통일 | 1h |
| P1 | `verify.js:65` / `:416` | 미션2 지역 초대 + brief 부활 | 18분 |
| P1 | `index.html:13/31` / `verify.js:348` / `MISSIONS.title` | 용어 5종 → 1종, 미션명 이중화 해소 | 20분 |
| P1 | `교사_학습지.html` | 성취기준 연계 문안(**코드는 미확인 — 빈 칸**) | 30분 |