# Weather24 레드팀 통합 실행계획 (3라운드 종합)

> 작성 2026-07-26 · **현장 데모(8/21~22 천안)까지 26일**
> 근거 보고서 3종: [데이터·과학·제품](레드팀_보고서.md) · [UI/UX/CX](레드팀_UIUX보고서.md) · [카피·벤치마크](레드팀_카피·벤치마크보고서.md)
> 본 문서는 **무엇을 어떤 순서로 언제** 할지만 담는다. 근거와 상세는 위 3종을 보라.

---

## 0. 3라운드 종합 판정

| | |
|---|---|
| **결함의 성격** | 라운드마다 다른 층에서 **같은 병리**가 나왔다. ① 데이터층: 앱이 사실이 아닌 문장을 쓴다 ② UX층: 조작과 결과를 같은 화면에 두지 않았다 ③ 카피층: 자기 강점을 자기 문장으로 부정한다 |
| **총 작업량** | P0 **31~37h** · P1 **31~37h** · P2 약 10h. 26일 기준 하루 2.5~3h면 P0+P1 완주 |
| **가장 값싼 득점** | 카피 3건(F-1·F-2·F-7) 합쳐 **1시간 25분**. 코드가 아니라 문장이다 |
| **가장 규모 큰 득점** | RC-A 평활→실측 초과일수 **6~8h**. ②+①+⑤ 합쳐 약 6점, 그리고 죽어 있던 28·30℃ 구간이 최고의 실연 장면으로 바뀐다 |
| **가장 큰 리스크** | **시간이 아니라 순서.** RC-A를 기다리며 카피를 미루면 8/21에 RC-A가 안 끝났을 때 방어선이 0이다 |
| **심사 점수(내부 추정)** | 현재 73~80 → P0 후 84~88 → P0+P1 후 **89~92** |

---

## 1. 절대 순서 규칙 (어기면 8/20 밤에 수치가 갈라진다)

```
[게이트 먼저]
 RC-I 검증 게이트 신설 (3h)
   │  verify_web_data.py의 assert 9개는 전부 구 자산 대상이고
   │  배포본이 읽는 solar_terms_data.js에 대한 assert가 0개다.
   ↓
[재빌드 1회로 묶어라 — 따로 하면 라벨과 값이 갈라진다]
 RC-C 완결 기간 정정 (3~4h) ─┐
 RC-A 실측 초과일수 (6~8h) ──┤ 같은 재빌드
 UX-2 숫자 co-visibility (2~3h) ┤ ← #readouts를 RC-A와 같은 손으로
   ↓                            │
 RC-B 미션3 판정문 (2~3h) ←──────┘ RC-A 후 수치 확정 필요
 RC-C-2 창 민감도 병기 (1.5h)
   ↓
[문서 동기화 — RC-A/RC-C가 바꾼 수치를 따라가야 함]
 교사_학습지.html:46 · 데모_대본.md:12,13,17,40,41 · F-4 문안 재작성(30분) · F-9 서랍 본문(30분)

[완전 독립 — 위와 병행 가능]
 F-1 폴백 카피 · F-2 감사 선입력 · F-3 라벨/phase · F-7 CERL 한계
 UX-3 오버레이 · UX-4 헤더 · UX-5 인트로 CTA · UX-6 내비게이션
 F-6 히트테스트 · UX-11/RC-J 관측소명 · 절기 라벨(상강)
```

**이중 계상 금지 목록** — 아래는 같은 작업이다. 따로 잡지 마라.
| 합칠 것 | 실제 작업 |
|---|---|
| 1차 2.5(AI 선입력) = 3라운드 F-2 | 1.5h |
| 1차 "출처 고지" = UX-11 = RC-J = 3라운드 F-9 서랍 | 2.5~3.5h |
| 1차 "이중 제품" = UX-4 헤더 = F-14 용어 | 1h |
| UX-1 C안 = F-10 프리셋 = GB-08 축 줌 | 4~5h |
| UX-6 내비게이션 = F-15 딥링크 = F-3 phase 일부 | 2.5h |
| 1차 2.4(미션3) = F-5 미션3 가드 | 2~3h |

---

## 2. 26일 일정

### 7/27 (일) — 문장만 고친다. 데이터 파이프라인 무접촉. **합계 약 3h**

심사위원이 폰으로 열었을 때 첫 30초에 보이는 것을 하루에 고친다.

| 순서 | 항목 | 파일:줄 | 시간 |
|---|---|---|---|
| 1 | **UX-4 헤더 깨짐** — 아카이브 링크 제거 (+ `.text-btn{white-space:nowrap}`) | `index.html:27`, `verify.css` | **15분** |
| 2 | **F-1 AI 폴백 카피** — 원인별 분기 + `data.error` 사용 + 헤더 격하 제거 | `verify.js:637-644`, `:580`, `:486`, `:561` | **25분** |
| 3 | **UX-3 예측 봉인 오버레이** — 등장 시 `scrollIntoView` + 첫 선택지 포커스 | `verify.js:397-402` | **30분** |
| 4 | **F-3 라벨·phase·draft 저장** | `verify.js:492/493/500/654/487` | **40분** |
| 5 | **F-6 히트테스트** — pointerdown을 임계선 ±16px로 제한 | `verify.js:271-277` | **40분** |
| 6 | **F-8 스크롤 프레이밍**(UX-2 스톱갭) — 첫 조작 후 `#readouts`로 프레이밍 | `verify.js:271-277` | **20분** |
| 7 | **F-14 archive 문장** — "실제 관측값을 조작하고" → "직접 확인하고" | `archive.html:22` | **2분** |

### 7/28 (월) — 나머지 카피·구조. **합계 약 4h**

| 항목 | 파일:줄 | 시간 |
|---|---|---|
| **F-2 감사 선입력 제거** (= 1차 2.5) — placeholder 전환 + 예시 토글 + 동일성 게이트 + `:618` 피드백 정정 | `verify.js:486-487`, `:618` | 1.5h |
| **F-7 CERL 한계 절** — 판정문에 L 추가 + 4요소 배지 | `verify.js:45`, `verify.css` | 35분 |
| **F-4 heatNote + 미션1 B분기 스톱갭** — 검증 불가 명시 + 상향 유도 삭제 + 허위 단정 제거 | `verify.js:177`, `:46` | 35분 |
| **F-5 경계 가드** — readout null 3분기 + 미션2 0/0 가드 | `verify.js:225`, `:66` | 25분 |
| **UX-5 인트로 CTA** — 모바일에서 폴드 위로 | `verify.js:294-303` | 1h |

### 7/29~7/30 — 내비게이션·정직성. **합계 약 6h**

| 항목 | 시간 |
|---|---|
| **UX-6 내비게이션**(= F-15) — 완료 미션 재방문 `<button>` 전환 + `phase` 확장 | 2.5h |
| **F-9 방법론 서랍**(= 1차 출처고지 = UX-11 = RC-J) — "이 숫자는 어떻게 나왔나" + 무결성 승격·정정 + 관측소 실명(경기→수원, 강원→춘천, 경북→포항…) + 용어 풀이 3곳 | 3.5h |

> **F-9는 이번 3라운드가 지목한 최고 가성비 항목이다.** 1차가 찾아낸 과학 결함(26℃ 허위 문장, 강수 방향 반대, doy 174~365의 4년 표본)을 심사장에서 **"숨긴 오류"에서 "공시된 한계"로** 프레임을 바꾸는 유일한 저비용 수단이다.

### 7/31 — 절기 라벨 + 검증 게이트. **합계 약 4h**

| 항목 | 시간 |
|---|---|
| **RC-H 절기 라벨** — 상강 10/24 → **10/23**(앱 적중 24/58, 유일한 명확 오표기) + "움직이지 않습니다" → "태양 황경은 고정, 양력 날짜는 ±1일 진동" 4곳 | 1h |
| **RC-I 검증 게이트 신설** — `verify_solar_terms.py`(곡선 유효값·표본 수·하드코딩 일치·실측 괴리 4종 assert) + `build_solar_terms.py:85` 죽은 코드 제거 | 3h |

### 8/1~8/7 — 데이터 파이프라인. **합계 약 16~20h. 가장 위험한 구간**

| 순서 | 항목 | 시간 |
|---|---|---|
| 1 | **RC-C 완결 기간 정정** — `PRESENT=(2021,2025)` 완결 5년 (차선: 350일 게이트 + "4년" 표기) + 화면 표기 4곳 | 3~4h |
| 2 | **RC-A 실측 초과일수** — 임계값 그리드 사전계산(+30KB), `exceed()`/`lastExceed()` 교체, 라벨 3곳 | **6~8h** |
| 3 | **UX-2 co-visibility** — `#readouts`를 차트 위/안으로. **RC-A와 같은 손으로** | 2~3h |
| 4 | **RC-B 미션3 판정문** — null 가드 + `\|a−b\|<5` 분기 + 절대 날짜 결론 | 2~3h |
| 5 | **RC-C-2 창 민감도 병기** — "5년 창을 옮기면 −1~+14일, 30년은 +9일" | 1.5h |
| 6 | **문서 동기화** — 교사 학습지 + 데모 대본 + F-4 재작성 + F-9 서랍 본문 | 2h |
| 7 | **RC-F 고향카드 dT** — 출생연도 ±2년 평균 vs 최근 5년 평균 (dT≤0 60건 제거) | 2.5h |

**검증 기준**: 16지역 × 3지표 × 전 임계값을 원시 CSV와 대조하는 스크립트로 **부호 역전 0건** 확인. 슬라이더 13칸 전수 클릭 → **자기모순 문장 0건**.

### 8/8~8/15 — P1. **합계 약 15h**

| 항목 | 시간 |
|---|---|
| **UX-1 C안** — 모바일 viewBox `720×560` + y축 `[8,32]` 클립 + 프리셋 칩(= F-10, RC-A 후 확정) | 4~5h |
| **UX-9/F-11 어포던스** — 그립 도형 + `aria-valuetext` + focus 링 통일(`a`·`range` 추가) | 1.5h |
| **RC-E 조작 우회 차단** — `state.thr0` 도입, 판정 조건 `thr !== thr0`, 칩 플래그 분리 | 2h |
| **RC-O 산출물 저장** — `selfChecks`/`missionDrafts` + "내 기록 요약" 복사·인쇄 | 1.5h |
| **RC-G 사전/사후 오염** — `autoDemo`를 사전 응답 이후로, 사후문항 다른 사례 치환, 미션2 예측 축 교체 | 3~4h |
| **UX-7 타이포** — 모바일 최소 12→13px, `.pstep small` 10.5→11.5px, `.integrity` 12→13px | 1.5h |
| **F-12 텍스트 대안** — `drawHero` 끝에서 aria-label 상태 갱신 + 범례 `aria-hidden` 제거 | 1h |
| **UX-10 칩 scrollIntoView** + **F-13 미션2 지역 초대** | 1h |

### 8/16~8/18 — P2 선별 + 버퍼

접근성 3건(포커스 이관·aria-live 축소·`matchMedia` 게이트, 1h) · 죽은 CSS 정리(1.5h) · 차트 눈금(1.5h) · UX-12 자유탐구 프리셋(2.5h)

### **8/19 — 코드 동결** (엄수)

RC-A는 데이터 재빌드를 포함한다. 8/20 밤 수정은 RC-I 검증을 건너뛰게 만들고, 그것이 이 프로젝트의 알려진 실패 모드다.

### 8/20 — 리허설

- 실기기 3대(iPhone SE 375×667 / 안드로이드 / 1280×800)로 전 동선 2회
- **데모 대본 그대로 실연** — 특히 28→30℃ 조작(RC-A 후 최강 장면이 됨)
- **오프라인(AI 꺼짐) 시연 1회** — F-1 수정 후 문구가 무대에서 어떻게 들리는지 확인
- 예상 질문 8개 답변 암기
- **매 시연 전 `↺ 처음부터` 필수** — `state.pre`가 남으면 `autoDemo` 미실행, `phase='mission'`이면 인트로를 건너뛴다(`verify.js:432`, `:654-656`)

---

## 3. P0 체크리스트 (데모 전 이것만은)

- [ ] 헤더가 375px에서 깨지지 않는다
- [ ] "AI가 꺼져 있어"가 화면에 뜨지 않는다
- [ ] 기준선을 어느 값으로 옮겨도 앱이 사실이 아닌 문장을 쓰지 않는다 (26~32℃ 전수 확인)
- [ ] 강수·습도 지표의 증감 **방향이 실측과 일치**한다 (16지역 전수 스크립트)
- [ ] 미션3 판정문이 슬라이더 13칸 어디서도 깨지지 않는다
- [ ] AI 감사 상자가 비어 있고, 학생이 쓴 문장만 감사받는다
- [ ] 예측 봉인 질문이 등장할 때 화면에 보인다
- [ ] 조작 중 숫자가 조작기와 같은 화면에 있다
- [ ] 고향카드 dT가 어느 지역·어느 출생연도에서도 음수가 아니다
- [ ] 표본 수·기간 표기가 실제 데이터와 일치한다 (2026 부분연도 처리)
- [ ] 새로고침해도 판정·고향카드가 남는다
- [ ] `node eval_harness.js`와 새 `verify_solar_terms.py`가 모두 통과한다

---

## 4. 3라운드에서 정정된 것 (외부 인용 금지 목록)

레드팀 스스로 반박·하향한 항목. **이것을 근거로 작업하거나 발표하지 마라.**

| 초판 주장 | 정정 |
|---|---|
| "2026 포함 여부가 시차의 **부호를 바꾼다**(−0.4일)" | **틀림.** 앱 방식으로는 2022–26 ≡ 2022–25 = +26일. 진짜 문제는 창 이동 민감도(−1~+14 vs 앱 +26) |
| "절기 라벨 3개가 소수파" | **1개만.** 태양황경 전수 계산 결과 **상강**(24/58)만. 입하·입추는 앱 표기가 최빈. 처서 8/23은 51/58로 정확 |
| "기온 초과일수 현재값 +38% 과장" | **하향.** 완결연도 기준 **+13%**. 부호 역전은 강수·습도에서만 |
| "무결성 문구가 화면에서 가장 작은 10.5px" | **틀림.** `.integrity`는 **12px**(verify.css:63). 10.5px은 진행 표시 미션명뿐 |
| "미션3은 thr≥28에서 깨진다" | **더 나쁘다.** **강원은 27부터** → 13칸 중 **6칸(46%)** |
| "기본값 복귀 경로가 없다" | **반박.** `homeLink`(verify.js:649) → 인트로 → `startMission(0)`이 city/ti/metric/thr를 기본값으로 되돌린다 |
| "데모 사고 시 복구 불가" | **반박.** localStorage 복원이 실제로 작동. 하드 리로드 후 미션2·부산·thr 27 그대로 복귀 |
| "스크린리더로 데이터 접근 불가" | **반박.** `#readouts`·`#thrOut`·`#stage`가 모두 `aria-live="polite"`. 핵심 수치는 낭독된다. 남는 결함은 **차트 aria-label이 정적**이라는 것 |
| "강수 결측(50~66%)이 편향" | **기각.** ASOS는 무강수일을 공란 처리 → `fillna(0)`이 올바름 |
| "순환 15일 평활이 연말 불연속" | **기각.** 12/31→1/1 연속, 최저점 doy 23(1월 하순)로 물리적 타당 |
| "교사 학습지에 시간 배분·정답 예시·인쇄 형식 없음" | **기각.** 모두 존재. 실제 결함은 성취기준 코드 하나 |
| 320px 리플로 실패 | **측정 실패.** 브라우저 패널 최소 폭 367px 때문에 값이 어긋남 |
| PhET 접근성 공식 문서 인용 / NASA 레퍼런스 | **미확인.** URL로 확인한 곳은 5곳(PhET sim DOM·OWID·En-ROADS·NOAA·Met Office) |

---

## 5. 지켜야 할 것 (수정 중 깨뜨리지 말 것)

3라운드 전체에서 과장 없이 확인된 강점. 여러 개가 P0 수정으로 손상될 위험 구간에 있다.

| 강점 | 손상 위험 |
|---|---|
| **예측 봉인이 코드로 강제된다**(verify.js:427-431). PhET·En-ROADS 상호작용 DOM 전수 확인 결과 **이 게이트가 없다** — 진짜 차별점 | **RC-E 수정 시 게이트 로직을 건드린다. 최우선 회귀 검사** |
| **조작 대상이 '모델 입력'이 아니라 '측정 범주의 정의'다.** 확인한 5곳 전부에서 "무엇을 무엇이라 부를지"는 사이트가 정해 준다 | F-6·F-10·F-5 수정이 곧 이 강점의 실현 |
| **최종 산출물이 학생의 서술이다** — 교사 손에 학생별 증거가 남는다 | F-2·F-3이 지금 이 사슬을 끊고 있다 |
| **피드백 카피의 질이 높다** — 자가진단 해설이 정답 확인이 아니라 일반화 규칙을 가르치고, 오답에 "다시 볼까요"(verify.js:455), 사후 미도달자에게 점수 대신 핵심 명제를 다시 준다 | 문안 교체 시 이 톤을 유지 |
| `spark-cap`의 **"당신의 5년 비교는 이 긴 흐름의 양 끝입니다."**(verify.js:208) — 이 앱 최고의 정직성 카피 | **F-9 무결성 문구가 도달해야 할 수준의 기준선.** 건드리지 말 것 |
| **`localAudit` 오탐 0** — 잘 쓴 결론을 나무라지 않는다(96/100, 실패 4건 전부 miss) | F-2에서 `:605` cautious 예외를 손대되 **인식 자체를 없애면 오탐이 생긴다.** missing 경고만 살려라 |
| **성능·모바일 기본기** — 92.7KB(gzip)·DCL 135ms·`drawHero` 0.66ms·`touch-action:none`·가로 오버플로 0·명암비 전 구간 AA | UX-1 C안으로 차트가 커지고 데이터가 +30KB → 재측정 필요(여유 큼) |
| **`annual_series`가 불완결 연도를 배제**(build_solar_terms.py:102) | RC-C는 이 게이트를 `climatology()`에 **복제**하는 것. 기존 것을 건드리지 말 것 |
| **교사 학습지가 인쇄 가능한 완성물** | RC-A/RC-C 후 `:46` 수치만 갱신 |
| **캐시버스팅 규율이 지켜진다** — 최근 6커밋 모두 `?v` 상승 | P0 배포마다 유지 |

---

## 6. 하나만 기억한다면

**RC-A(6~8h)를 기다리며 카피 수정을 미루지 마라.** 7/27~7/28 이틀 동안 문장만 고쳐도 심사위원이 폰으로 열었을 때의 첫 30초가 완전히 달라지고, 8/21에 RC-A가 끝나지 않아도 **정직한 앱**을 보여줄 수 있다. RC-A가 끝나면 문안 갱신 1시간으로 최종형에 도달한다.

반대 순서로 가면, RC-A가 늦어졌을 때 방어선이 하나도 없다.
