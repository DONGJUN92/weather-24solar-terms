# 대용량·재생성 가능한 원시 수집 데이터 (스크립트로 다시 생성 가능)
data_collectors/output/

# 로그·중간 산출물
*.log
_raw_page.html
_page_text.txt

# 비밀/자격증명 (혹시라도 — 절대 커밋 금지)
.cdsapirc
.edl_token
.env
.env.*
*.key
secrets*.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / Vercel
node_modules/
.vercel

# OS / 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/

# 로컬 개발 편의 설정 — 제출물이 아니다
.claude/
_raw_page.html

# 제출용 산출물 — scripts/pack_submission.py 로 언제든 다시 만든다(저장소에 담지 않는다)
제출물/

# 발표자료 템플릿 작업용 임시 폴더 (덤프·렌더·캡처) — 제출물에 들어갈 이유가 없다
work/
